---
phase: 37-independent-delete
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt
autonomous: true
requirements: [IND-01, IND-02, IND-03]

must_haves:
  truths:
    - "재생성 다이얼로그가 '새 회의록이 추가됩니다' 텍스트를 표시한다"
    - "전사를 삭제해도 연결된 회의록 파일은 삭제되지 않는다 (FK SET_NULL)"
    - "회의록을 삭제해도 전사 파일과 Meeting 상태는 변경되지 않는다"
    - "전사에서 회의록을 재생성하면 기존 회의록은 유지되고 새 회의록이 추가된다"
    - "모든 회의록을 삭제한 전사에서 다시 회의록을 생성할 수 있다"
  artifacts:
    - path: "app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt"
      provides: "수정된 재생성 다이얼로그 텍스트"
      contains: "새 회의록이 추가됩니다"
  key_links:
    - from: "TranscriptsScreen.kt (재생성 다이얼로그)"
      to: "TranscriptsViewModel.regenerateMinutes()"
      via: "다이얼로그 확인 버튼 클릭"
      pattern: "viewModel\\.regenerateMinutes"
    - from: "MeetingRepositoryImpl.deleteMeeting()"
      to: "Room FK SET_NULL"
      via: "meetingDao.delete() 트리거"
      pattern: "meetingDao\\.delete"
    - from: "MinutesDataRepositoryImpl.deleteMinutes()"
      to: "minutesDao.delete()"
      via: "Minutes Row만 삭제, Meeting 무관"
      pattern: "minutesDao\\.delete"
---

<objective>
Phase 37의 전사-회의록 독립 삭제/재생성 요구사항을 완료한다.

Purpose: Phase 36에서 데이터 레이어(FK SET_NULL, Minutes 독립 테이블)는 이미 구현 완료. 남은 작업은 UI 텍스트 보정과 빌드 검증이다.
Output: 재생성 다이얼로그 텍스트 수정 + assembleDebug 빌드 성공
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-independent-delete/37-RESEARCH.md

@app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt
@app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsViewModel.kt
@app/src/main/java/com/autominuting/data/repository/MeetingRepositoryImpl.kt
@app/src/main/java/com/autominuting/data/repository/MinutesDataRepositoryImpl.kt

<interfaces>
<!-- 데이터 레이어는 Phase 36에서 이미 올바르게 구현됨. 변경 불필요. -->

From MeetingRepositoryImpl.kt:
```kotlin
// deleteMeeting: audio/transcript 파일만 삭제, meetingDao.delete() -> FK SET_NULL -> Minutes Row 보존
override suspend fun deleteMeeting(id: Long)
```

From MinutesDataRepositoryImpl.kt:
```kotlin
// deleteMinutes: Minutes Row + 회의록 파일만 삭제, Meeting에 영향 없음
override suspend fun deleteMinutes(id: Long)
```

From TranscriptsViewModel.kt:
```kotlin
// regenerateMinutes: transcriptPath 확인 후 enqueueMinutesWorker() -> 새 Minutes Row INSERT (기존 유지)
fun regenerateMinutes(meetingId: Long)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 재생성 다이얼로그 텍스트 수정 + 전사 삭제 다이얼로그 회의록 수 표시</name>
  <files>app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt</files>
  <action>
TranscriptsScreen.kt의 재생성 확인 다이얼로그(L237-255)를 수정한다.

1. 재생성 다이얼로그 텍스트 변경 (IND-03):
   - 현재 (L241): `text = { Text("기존 회의록을 삭제하고 새로 생성할까요?") }`
   - 변경: `text = { Text("새 회의록이 추가됩니다.\n기존 회의록은 유지됩니다.") }`
   - 다이얼로그 title은 "회의록 재생성" 유지
   - 확인 버튼 텍스트를 "재생성"에서 "추가 생성"으로 변경 (동작을 더 정확히 반영)

2. 재생성 다이얼로그의 주석도 수정 (L236, L244):
   - L236: "회의록 재생성 확인 대화상자 (기본 템플릿 설정 시)" 유지
   - L244: "regenerateMinutes: 기존 회의록 파일 삭제 후 새로 생성" → "regenerateMinutes: 기존 회의록을 유지하고 새 회의록을 추가 생성"

주의사항:
- 데이터 레이어(ViewModel, Repository)는 수정하지 않는다. 이미 올바르게 동작 중.
- TranscriptsScreen.kt 외의 파일은 수정하지 않는다.
  </action>
  <verify>
    <automated>cd D:/workspace/temp/Auto_Minuting && grep -n "새 회의록이 추가됩니다" app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt && grep -n "기존 회의록을 삭제하고" app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt | wc -l</automated>
  </verify>
  <done>"새 회의록이 추가됩니다" 텍스트가 존재하고, "기존 회의록을 삭제하고" 텍스트가 0건이다</done>
</task>

<task type="auto">
  <name>Task 2: assembleDebug 빌드 검증</name>
  <files>app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt</files>
  <action>
assembleDebug Gradle 태스크를 실행하여 빌드가 성공하는지 검증한다.

1. `./gradlew assembleDebug` 실행
2. BUILD SUCCESSFUL 확인
3. 빌드 실패 시 오류를 분석하고 TranscriptsScreen.kt의 수정 사항만 원인인지 확인하여 수정

이 태스크는 IND-01, IND-02, IND-03 모두의 데이터 레이어가 Phase 36에서 올바르게 구현되었고,
Task 1의 UI 텍스트 수정이 빌드를 깨뜨리지 않았음을 검증하는 최종 게이트이다.
  </action>
  <verify>
    <automated>cd D:/workspace/temp/Auto_Minuting && ./gradlew assembleDebug --no-daemon 2>&1 | tail -5</automated>
  </verify>
  <done>assembleDebug가 BUILD SUCCESSFUL로 완료된다</done>
</task>

</tasks>

<verification>
1. TranscriptsScreen.kt의 재생성 다이얼로그에 "새 회의록이 추가됩니다" 텍스트가 표시된다
2. "기존 회의록을 삭제하고" 문구가 코드에서 완전히 제거되었다
3. assembleDebug 빌드가 성공한다
4. 데이터 레이어 코드(MeetingRepositoryImpl, MinutesDataRepositoryImpl, TranscriptsViewModel)는 변경되지 않았다
</verification>

<success_criteria>
- 재생성 다이얼로그가 "새 회의록이 추가됩니다. 기존 회의록은 유지됩니다." 텍스트를 표시한다
- assembleDebug BUILD SUCCESSFUL
- IND-01/IND-02/IND-03 데이터 동작은 Phase 36에서 이미 구현 완료 (이 플랜에서 변경 없음)
</success_criteria>

<output>
After completion, create `.planning/phases/37-independent-delete/37-01-SUMMARY.md`
</output>
