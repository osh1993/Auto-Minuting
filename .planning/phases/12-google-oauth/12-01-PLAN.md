---
phase: 12-google-oauth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gradle/libs.versions.toml
  - app/build.gradle.kts
  - app/src/main/java/com/autominuting/data/minutes/MinutesEngine.kt
  - app/src/main/java/com/autominuting/data/minutes/GeminiEngine.kt
  - app/src/main/java/com/autominuting/data/minutes/GeminiRestApiService.kt
  - app/src/main/java/com/autominuting/data/minutes/GeminiRestModels.kt
  - app/src/main/java/com/autominuting/di/RepositoryModule.kt
autonomous: true
requirements: [AUTH-02]

must_haves:
  truths:
    - "GeminiEngine이 MinutesEngine 인터페이스를 구현하여 기존 동작이 그대로 유지된다"
    - "MinutesRepositoryImpl이 MinutesEngine 인터페이스에 의존하여 엔진 교체가 가능하다"
    - "Gemini REST API 호출용 Retrofit 서비스와 데이터 모델이 정의되어 있다"
    - "Credential Manager 및 play-services-auth 의존성이 빌드에 포함된다"
  artifacts:
    - path: "app/src/main/java/com/autominuting/data/minutes/MinutesEngine.kt"
      provides: "회의록 엔진 공통 인터페이스"
      exports: ["MinutesEngine"]
    - path: "app/src/main/java/com/autominuting/data/minutes/GeminiRestApiService.kt"
      provides: "Retrofit 기반 Gemini REST API 호출 인터페이스"
      exports: ["GeminiRestApiService"]
    - path: "app/src/main/java/com/autominuting/data/minutes/GeminiRestModels.kt"
      provides: "Gemini REST API 요청/응답 데이터 모델"
      exports: ["GenerateContentRequest", "GenerateContentResponse", "Content", "Part", "Candidate"]
  key_links:
    - from: "app/src/main/java/com/autominuting/data/minutes/GeminiEngine.kt"
      to: "MinutesEngine interface"
      via: "implements interface"
      pattern: "class GeminiEngine.*: MinutesEngine"
    - from: "app/src/main/java/com/autominuting/data/repository/MinutesRepositoryImpl.kt"
      to: "MinutesEngine"
      via: "constructor injection"
      pattern: "private val.*minutesEngine.*MinutesEngine"
---

<objective>
MinutesEngine 인터페이스를 도입하여 API 키 모드와 OAuth 모드를 추상화하고, Gemini REST API 호출 인프라를 구축한다.

Purpose: Phase 8 D-09에서 예고한 인터페이스 도입으로, OAuth 엔진이 기존 API 키 엔진과 교체 가능한 구조를 만든다. 또한 Credential Manager/play-services-auth 의존성을 추가하여 Plan 02에서 Google 인증 구현이 바로 가능하게 한다.
Output: MinutesEngine.kt 인터페이스, GeminiEngine 리팩토링, GeminiRestApiService + 데이터 모델, 빌드 의존성 추가
</objective>

<execution_context>
@D:\Users\ADMIN\.claude\get-shit-done\workflows\execute-plan.md
@D:\Users\ADMIN\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-google-oauth/12-RESEARCH.md

@app/src/main/java/com/autominuting/data/minutes/GeminiEngine.kt
@app/src/main/java/com/autominuting/data/repository/MinutesRepositoryImpl.kt
@app/src/main/java/com/autominuting/domain/repository/MinutesRepository.kt
@app/src/main/java/com/autominuting/di/RepositoryModule.kt
@app/src/main/java/com/autominuting/domain/model/MinutesFormat.kt
@app/src/main/java/com/autominuting/data/security/SecureApiKeyRepository.kt
@gradle/libs.versions.toml
@app/build.gradle.kts

<interfaces>
<!-- 현재 GeminiEngine 시그니처 — MinutesEngine 인터페이스 추출 기반 -->
From app/src/main/java/com/autominuting/data/minutes/GeminiEngine.kt:
```kotlin
@Singleton
class GeminiEngine @Inject constructor(
    private val secureApiKeyRepository: SecureApiKeyRepository
) {
    suspend fun generate(transcriptText: String, format: MinutesFormat = MinutesFormat.STRUCTURED): Result<String>
    fun engineName(): String
}
```

From app/src/main/java/com/autominuting/data/repository/MinutesRepositoryImpl.kt:
```kotlin
@Singleton
class MinutesRepositoryImpl @Inject constructor(
    private val geminiEngine: GeminiEngine,  // -> MinutesEngine으로 변경 대상
    @ApplicationContext private val context: Context
) : MinutesRepository
```

From app/src/main/java/com/autominuting/di/RepositoryModule.kt:
```kotlin
@Module @InstallIn(SingletonComponent::class)
abstract class RepositoryModule {
    @Binds abstract fun bindMinutesRepository(impl: MinutesRepositoryImpl): MinutesRepository
    // MinutesEngine 바인딩 추가 필요
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MinutesEngine 인터페이스 + GeminiEngine 리팩토링 + Gemini REST API 모델</name>
  <files>
    app/src/main/java/com/autominuting/data/minutes/MinutesEngine.kt
    app/src/main/java/com/autominuting/data/minutes/GeminiEngine.kt
    app/src/main/java/com/autominuting/data/minutes/GeminiRestApiService.kt
    app/src/main/java/com/autominuting/data/minutes/GeminiRestModels.kt
    app/src/main/java/com/autominuting/data/repository/MinutesRepositoryImpl.kt
    app/src/main/java/com/autominuting/di/RepositoryModule.kt
  </files>
  <read_first>
    app/src/main/java/com/autominuting/data/minutes/GeminiEngine.kt
    app/src/main/java/com/autominuting/data/repository/MinutesRepositoryImpl.kt
    app/src/main/java/com/autominuting/di/RepositoryModule.kt
    app/src/main/java/com/autominuting/domain/model/MinutesFormat.kt
  </read_first>
  <action>
Phase 8 D-09 결정에 따라 MinutesEngine 인터페이스를 도입하고 GeminiEngine이 이를 구현하도록 리팩토링한다.

1. **MinutesEngine.kt 생성** (`data/minutes/MinutesEngine.kt`):
   - `suspend fun generate(transcriptText: String, format: MinutesFormat = MinutesFormat.STRUCTURED): Result<String>` — 회의록 생성
   - `fun engineName(): String` — 엔진 이름 (로깅용)
   - `fun isAvailable(): Boolean` — 현재 엔진 사용 가능 여부
   - package: `com.autominuting.data.minutes`

2. **GeminiEngine.kt 수정**:
   - `class GeminiEngine ... : MinutesEngine` 으로 인터페이스 구현 추가
   - `override` 키워드를 generate(), engineName()에 추가
   - `isAvailable()` 구현: `secureApiKeyRepository.getGeminiApiKey() ?: BuildConfig.GEMINI_API_KEY`가 비어있지 않으면 true
   - 기존 로직은 변경하지 않는다 (API 키 우선 폴백 패턴 유지)

3. **GeminiRestModels.kt 생성** (`data/minutes/GeminiRestModels.kt`):
   - `GenerateContentRequest(contents: List<Content>)`
   - `Content(parts: List<Part>, role: String = "user")`
   - `Part(text: String)`
   - `GenerateContentResponse(candidates: List<Candidate>?)`
   - `Candidate(content: Content?)`
   - 모두 data class, Gson 직렬화 호환 (필드명 = JSON 키)

4. **GeminiRestApiService.kt 생성** (`data/minutes/GeminiRestApiService.kt`):
   - Retrofit interface
   - `@POST("v1beta/models/{model}:generateContent") suspend fun generateContent(@Path("model") model: String = "gemini-2.5-flash", @Body request: GenerateContentRequest): GenerateContentResponse`
   - baseUrl은 Hilt Module에서 설정: `https://generativelanguage.googleapis.com/`

5. **MinutesRepositoryImpl.kt 수정**:
   - 생성자 파라미터 `geminiEngine: GeminiEngine` -> `minutesEngine: MinutesEngine`로 변경
   - 내부 참조도 `geminiEngine` -> `minutesEngine`으로 변경
   - 로직은 동일 (generate, engineName 호출)

6. **RepositoryModule.kt 수정**:
   - `@Binds abstract fun bindMinutesEngine(impl: GeminiEngine): MinutesEngine` 추가 (기본 바인딩은 API 키 모드)
   - Plan 02에서 `@Named` 또는 동적 전환으로 OAuth 엔진 추가 예정
  </action>
  <verify>
    <automated>cd D:/workspace/temp/Auto_Minuting && ./gradlew assembleDebug 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - MinutesEngine.kt 파일이 존재하고 `interface MinutesEngine` 선언 포함
    - GeminiEngine.kt에 `: MinutesEngine` 구현 선언 포함
    - GeminiRestApiService.kt에 `interface GeminiRestApiService` 선언 포함
    - GeminiRestModels.kt에 `data class GenerateContentRequest` 포함
    - MinutesRepositoryImpl.kt에서 `MinutesEngine` 타입으로 주입받음
    - RepositoryModule.kt에 `bindMinutesEngine` 바인딩 존재
    - `./gradlew assembleDebug` 빌드 성공
  </acceptance_criteria>
  <done>MinutesEngine 인터페이스가 도입되고 GeminiEngine이 이를 구현하며, Gemini REST API 모델/서비스가 정의되고, MinutesRepositoryImpl이 인터페이스에 의존하며, 빌드가 성공한다</done>
</task>

<task type="auto">
  <name>Task 2: Credential Manager + play-services-auth 빌드 의존성 추가</name>
  <files>
    gradle/libs.versions.toml
    app/build.gradle.kts
  </files>
  <read_first>
    gradle/libs.versions.toml
    app/build.gradle.kts
  </read_first>
  <action>
Google OAuth 인증에 필요한 라이브러리 의존성을 버전 카탈로그에 추가한다.

1. **gradle/libs.versions.toml에 추가:**

   [versions] 섹션에 추가:
   ```
   credentialManager = "1.5.0"
   googleid = "1.1.1"
   playServicesAuth = "21.5.1"
   ```

   [libraries] 섹션에 추가:
   ```
   # Google 인증 (OAuth)
   credentials = { group = "androidx.credentials", name = "credentials", version.ref = "credentialManager" }
   credentials-play-services = { group = "androidx.credentials", name = "credentials-play-services-auth", version.ref = "credentialManager" }
   googleid = { group = "com.google.android.libraries.identity.googleid", name = "googleid", version.ref = "googleid" }
   play-services-auth = { group = "com.google.android.gms", name = "play-services-auth", version.ref = "playServicesAuth" }
   ```

2. **app/build.gradle.kts의 dependencies 블록에 추가:**
   ```
   // Google 인증 (OAuth) -- Phase 12
   implementation(libs.credentials)
   implementation(libs.credentials.play.services)
   implementation(libs.googleid)
   implementation(libs.play.services.auth)
   ```
   기존 `// AI (Gemini)` 섹션 바로 아래에 배치한다.

3. **빌드 확인:** 의존성 추가 후 assembleDebug가 성공하는지 확인한다.
  </action>
  <verify>
    <automated>cd D:/workspace/temp/Auto_Minuting && ./gradlew assembleDebug 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - libs.versions.toml에 `credentialManager = "1.5.0"` 버전 선언 존재
    - libs.versions.toml에 `credentials`, `credentials-play-services`, `googleid`, `play-services-auth` 라이브러리 선언 존재
    - build.gradle.kts에 `implementation(libs.credentials)` 등 4개 의존성 추가됨
    - `./gradlew assembleDebug` 빌드 성공
  </acceptance_criteria>
  <done>Credential Manager, googleid, play-services-auth 의존성이 버전 카탈로그와 빌드 스크립트에 추가되어 빌드가 성공한다</done>
</task>

</tasks>

<verification>
- `./gradlew assembleDebug` 빌드 성공
- MinutesEngine 인터페이스가 GeminiEngine에 의해 구현됨
- MinutesRepositoryImpl이 MinutesEngine 인터페이스에 의존함
- Credential Manager 의존성이 빌드에 포함됨
</verification>

<success_criteria>
- 기존 API 키 모드의 회의록 생성 동작이 인터페이스 도입 후에도 동일하게 유지된다
- GeminiRestApiService와 데이터 모델이 Plan 02에서 바로 사용 가능하다
- Google 인증 라이브러리가 빌드에 포함되어 Plan 02에서 import 가능하다
</success_criteria>

<output>
After completion, create `.planning/phases/12-google-oauth/12-01-SUMMARY.md`
</output>
