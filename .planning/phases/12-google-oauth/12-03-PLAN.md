---
phase: 12-google-oauth
plan: 03
type: execute
wave: 3
depends_on: ["12-02"]
files_modified: []
autonomous: false
requirements: [AUTH-02]

must_haves:
  truths:
    - "실기기에서 Google 로그인 플로우가 정상 동작한다"
    - "OAuth 모드에서 Gemini API 호출이 성공한다"
    - "API 키 모드와 OAuth 모드 전환이 정상 동작한다"
  artifacts: []
  key_links: []
---

<objective>
Google OAuth 인증 + Gemini API 호출의 E2E 동작을 실기기에서 검증한다.

Purpose: OAuth 스코프, 토큰 획득, REST API 호출이 실기기 환경에서 정상 동작하는지 확인하여 AUTH-02 완료를 확정한다.
Output: 실기기 검증 결과 확인
</objective>

<execution_context>
@D:\Users\ADMIN\.claude\get-shit-done\workflows\execute-plan.md
@D:\Users\ADMIN\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/12-google-oauth/12-RESEARCH.md
@.planning/phases/12-google-oauth/12-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Google Cloud Console 설정 확인</name>
  <files>local.properties</files>
  <read_first>
    .planning/phases/12-google-oauth/12-RESEARCH.md
  </read_first>
  <action>
    사용자에게 Google Cloud Console 사전 설정 완료 여부를 확인한다. 12-RESEARCH.md의 "Google Cloud Console 설정 가이드" 섹션 참조.
    확인 항목: (1) Generative Language API 활성화, (2) OAuth 동의 화면 구성, (3) 테스트 사용자 등록, (4) Android/Web 클라이언트 ID 생성, (5) local.properties에 GOOGLE_OAUTH_WEB_CLIENT_ID 설정.
  </action>
  <what-built>Phase 12 Plan 01-02에서 구현한 Google OAuth 인증 코드. 동작하려면 Google Cloud Console 사전 설정 필요.</what-built>
  <how-to-verify>
    다음 항목을 Google Cloud Console에서 완료했는지 확인:
    1. Google Cloud 프로젝트 생성/선택 완료
    2. Generative Language API 활성화 완료 (APIs and Services > Library)
    3. OAuth 동의 화면 구성 완료 (External, 테스트 모드)
    4. 테스트 사용자에 본인 Google 계정 등록 완료
    5. Android 클라이언트 ID 생성 완료 (패키지명: com.autominuting, SHA-1 등록)
    6. Web 클라이언트 ID 생성 완료
    7. local.properties에 GOOGLE_OAUTH_WEB_CLIENT_ID 설정 완료
  </how-to-verify>
  <verify>사용자가 "설정 완료"라고 응답</verify>
  <acceptance_criteria>
    - 사용자가 Google Cloud Console 설정을 완료했다고 확인
    - local.properties에 GOOGLE_OAUTH_WEB_CLIENT_ID가 설정됨
  </acceptance_criteria>
  <done>Google Cloud Console 사전 설정이 완료되어 OAuth 인증 테스트가 가능하다</done>
  <resume-signal>"설정 완료" 또는 미완료 항목 설명</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 실기기 E2E 검증 — Google 로그인 + Gemini OAuth 호출</name>
  <files>app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt</files>
  <read_first>
    .planning/phases/12-google-oauth/12-02-SUMMARY.md
  </read_first>
  <action>
    실기기에 앱을 설치하고 Google OAuth 인증 E2E 플로우를 검증한다.
    테스트 시나리오: (1) OAuth 모드 전환, (2) Google 로그인, (3) Gemini 회의록 생성, (4) 모드 전환 후 API 키 모드 동작 확인.
  </action>
  <what-built>Google OAuth 인증 플로우 + GeminiOAuthEngine REST API 호출 전체 구현</what-built>
  <how-to-verify>
    1. 앱을 실기기에 설치하여 실행
    2. 설정 화면 열기
    3. 인증 모드를 "Google 계정"으로 전환
    4. "Google 계정으로 로그인" 버튼 탭
    5. Google 계정 선택 바텀시트 표시 확인
    6. 계정 선택 후 로그인 성공 확인 (이름, 이메일 표시)
    7. 메인 화면에서 전사 텍스트로 회의록 생성 시도
    8. OAuth 모드에서 Gemini API 호출 성공 확인 (회의록 생성됨)
    9. 설정에서 "로그아웃" 후 다시 "API 키" 모드로 전환
    10. API 키 모드에서도 회의록 생성이 정상 동작하는지 확인
  </how-to-verify>
  <verify>사용자가 "검증 완료"라고 응답하고 모든 시나리오 통과 확인</verify>
  <acceptance_criteria>
    - Google 계정 선택 바텀시트가 표시됨
    - 로그인 성공 후 사용자 정보가 설정 화면에 표시됨
    - OAuth 모드에서 회의록 생성이 성공함
    - API 키 모드로 전환 후에도 회의록 생성이 성공함
  </acceptance_criteria>
  <done>실기기에서 Google OAuth 로그인과 Gemini API 호출이 모두 정상 동작하고, 인증 모드 전환도 정상 동작한다</done>
  <resume-signal>"검증 완료" 또는 발견된 이슈 설명</resume-signal>
</task>

</tasks>

<verification>
- Google Cloud Console 설정 확인
- 실기기에서 Google 로그인 플로우 동작 확인
- OAuth 모드 Gemini API 호출 성공 확인
- API 키 / OAuth 모드 전환 동작 확인
</verification>

<success_criteria>
- 실기기에서 Google 로그인이 성공하고 OAuth 모드로 Gemini 회의록 생성이 동작한다
- API 키 모드와 OAuth 모드 전환이 정상 동작한다
</success_criteria>

<output>
After completion, create `.planning/phases/12-google-oauth/12-03-SUMMARY.md`
</output>
