---
phase: 35
plan: 3
type: execute
gap_closure: true
wave: 1
depends_on: ["35-01", "35-02"]
title: "MinutesFormat 참조 제거 및 자동모드 Switch 이동"
goal: "잔존하는 MinutesFormat 참조 4개 파일에서 전면 제거, 자동모드 Switch를 회의록 설정 섹션으로 이동, 빌드 성공 확인"
autonomous: true
requirements:
  - SET-01
  - SET-02
files_modified:
  - app/src/main/java/com/autominuting/worker/TranscriptionTriggerWorker.kt
  - app/src/main/java/com/autominuting/service/PipelineNotificationHelper.kt
  - app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsViewModel.kt
  - app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt
must_haves:
  truths:
    - "MinutesFormat 참조가 코드베이스에서 0건이다"
    - "설정 화면에서 '완전 자동 모드' Switch가 '회의록 설정' 섹션 상단에 위치한다"
    - "앱이 컴파일 에러 없이 빌드된다"
  artifacts:
    - path: "app/src/main/java/com/autominuting/worker/TranscriptionTriggerWorker.kt"
      provides: "MinutesFormat import, KEY_MINUTES_FORMAT 상수, minutesFormat 읽기/전달 코드가 제거된 Worker"
    - path: "app/src/main/java/com/autominuting/service/PipelineNotificationHelper.kt"
      provides: "minutesFormat 파라미터가 제거된 notifyTranscriptionComplete()"
    - path: "app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsViewModel.kt"
      provides: "getMinutesFormatOnce() 및 KEY_MINUTES_FORMAT 참조가 제거된 enqueueMinutesWorker()"
    - path: "app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt"
      provides: "MinutesFormat 드롭다운 삭제, 자동모드 Switch가 회의록 설정 섹션 상단에 위치"
  key_links:
    - from: "TranscriptionTriggerWorker.kt"
      to: "PipelineNotificationHelper.notifyTranscriptionComplete()"
      via: "minutesFormat 인자 없이 호출"
      pattern: "notifyTranscriptionComplete\\(.*meetingId.*transcriptPath"
    - from: "SettingsScreen.kt"
      to: "회의록 설정 섹션"
      via: "완전 자동 모드 Row가 섹션 상단에 위치"
      pattern: "회의록 설정.*완전 자동 모드"
---

<objective>
VERIFICATION.md에서 발견된 4개 파일의 MinutesFormat 잔존 참조를 전면 제거하고, 자동모드 Switch를 올바른 섹션으로 이동하여 빌드가 성공하도록 수정한다.

Purpose: Plan 01과 Plan 02 실행 후 MinutesFormat.kt 파일은 삭제되었으나 4개 파일(TranscriptionTriggerWorker.kt, PipelineNotificationHelper.kt, TranscriptsViewModel.kt, SettingsScreen.kt)에 11개의 참조가 잔존하여 빌드가 실패하는 상태다. 이를 제거하면 Phase 35의 전체 목표(SET-01, SET-02)가 완성된다.
Output: MinutesFormat 참조 0건, 자동모드 Switch 회의록 설정 이동 완료, assembleDebug 빌드 성공
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-/35-01-SUMMARY.md
@.planning/phases/35-/35-02-SUMMARY.md

<interfaces>
<!-- VERIFICATION.md에서 확인된 잔존 참조 목록 (파일별) -->

TranscriptionTriggerWorker.kt 잔존 참조:
- line 14: `import MinutesFormat` — 제거 대상
- line 59-60: `minutesFormat` 읽기 코드 — 제거 대상
- line 143: `KEY_MINUTES_FORMAT to minutesFormat` — 제거 대상 (CUSTOM_PROMPT_MODE_ID 분기는 이미 추가됨, line 131)
- line 155: `notifyTranscriptionComplete`에 `minutesFormat` 전달 인자 — 제거 대상
- line 205: `KEY_MINUTES_FORMAT` 상수 — 제거 대상

PipelineNotificationHelper.kt 잔존 참조:
- line 10: `import MinutesFormat` — 제거 대상
- line 122: `minutesFormat: String = MinutesFormat.STRUCTURED.name` 파라미터 — 제거 대상
- line 130: `putExtra(minutesFormat, ...)` — 제거 대상 (customPrompt putExtra는 Plan 02에서 추가됨, 유지)

TranscriptsViewModel.kt enqueueMinutesWorker() 잔존 참조:
- line 148: `getMinutesFormatOnce()` 호출 — 제거 대상 (메서드 이미 삭제됨, 컴파일 오류 원인)
- line 155: `KEY_MINUTES_FORMAT` 전달 — 제거 대상

SettingsScreen.kt 잔존 참조:
- line 58: `import MinutesFormat` — 제거 대상
- line 96: `viewModel.minutesFormat.collectAsStateWithLifecycle()` — 제거 대상 (SettingsViewModel에 해당 StateFlow 없음)
- lines 251-253: `formatLabels mapOf(MinutesFormat...)` — 제거 대상
- lines 256-285 (약): `ExposedDropdownMenuBox with MinutesFormat.entries` — 제거 대상
- lines 291-317 (약): 완전 자동 모드 Row(Switch) + HYBRID 조건부 텍스트 — '전사 설정' 섹션에서 제거 후 '회의록 설정' 섹션 상단으로 이동
- line 235-285 (약): "회의록 형식" Text + formatLabels + 드롭다운 블록 전체 — 제거 대상

<!-- Plan 02에서 이미 구현된 것 (건드리지 말 것) -->
<!-- PipelineNotificationHelper.kt line 131: customPrompt?.let { putExtra("customPrompt", it) } — 유지 -->
<!-- TranscriptionTriggerWorker.kt line 131: CUSTOM_PROMPT_MODE_ID 분기 — 유지 -->
<!-- SettingsScreen.kt: defaultCustomPrompt TextField, CUSTOM_PROMPT_MODE_ID 드롭다운 옵션 — 유지 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TranscriptionTriggerWorker.kt — MinutesFormat 전면 제거</name>
  <files>app/src/main/java/com/autominuting/worker/TranscriptionTriggerWorker.kt</files>
  <action>
    파일을 먼저 읽어 실제 라인 번호를 확인한 후 아래 항목을 제거한다. VERIFICATION.md 기준 라인은 참고용이며, 파일 읽기 후 실제 위치로 조정한다.

    **제거 항목 (4종):**

    1. `import MinutesFormat` 라인 — 해당 import 구문 전체 삭제
       (VERIFICATION: line 14 부근)

    2. `minutesFormat` 읽기 코드 — DataStore에서 minutesFormat을 읽는 2라인 블록 삭제
       형태: `val minutesFormat = userPreferencesRepository.getMinutesFormatOnce()` 또는 유사 코드
       (VERIFICATION: line 59-60 부근)

    3. `KEY_MINUTES_FORMAT to minutesFormat` — workDataOf() 블록 내 해당 항목만 삭제
       주의: 같은 workDataOf() 블록에 있는 CUSTOM_PROMPT_MODE_ID 분기(line 131 부근)와 KEY_CUSTOM_PROMPT 관련 코드는 유지
       (VERIFICATION: line 143 부근)

    4. `notifyTranscriptionComplete()` 호출에서 minutesFormat 인자 제거
       형태: `notifyTranscriptionComplete(context, meetingId, transcriptPath, minutesFormat)` → `notifyTranscriptionComplete(context, meetingId, transcriptPath)`
       (VERIFICATION: line 155 부근)

    5. `KEY_MINUTES_FORMAT` 상수 선언 삭제
       형태: `const val KEY_MINUTES_FORMAT = "minutesFormat"` 또는 유사
       (VERIFICATION: line 205 부근, companion object 내부)

    **확인 방법:** 제거 후 `grep -n "MinutesFormat\|KEY_MINUTES_FORMAT\|minutesFormat" TranscriptionTriggerWorker.kt` 결과가 0건이어야 한다.
    단, `customPrompt`나 `CUSTOM_PROMPT_MODE_ID` 관련 코드는 건드리지 않는다.
  </action>
  <verify>
    <automated>grep -n "MinutesFormat\|KEY_MINUTES_FORMAT\|minutesFormat" "D:/workspace/temp/Auto_Minuting/app/src/main/java/com/autominuting/worker/TranscriptionTriggerWorker.kt" | wc -l</automated>
  </verify>
  <done>TranscriptionTriggerWorker.kt에서 MinutesFormat import, KEY_MINUTES_FORMAT 상수, minutesFormat 읽기/전달 코드가 전부 제거되어 grep 결과 0건이다</done>
</task>

<task type="auto">
  <name>Task 2: PipelineNotificationHelper.kt — minutesFormat 파라미터 제거</name>
  <files>app/src/main/java/com/autominuting/service/PipelineNotificationHelper.kt</files>
  <action>
    파일을 먼저 읽어 실제 라인 번호를 확인한 후 아래 항목을 제거한다.

    **제거 항목 (3종):**

    1. `import MinutesFormat` 라인 삭제
       (VERIFICATION: line 10 부근)

    2. `notifyTranscriptionComplete()` 함수 시그니처에서 `minutesFormat: String = MinutesFormat.STRUCTURED.name` 파라미터 제거
       형태 변경: `fun notifyTranscriptionComplete(context: Context, meetingId: Long, transcriptPath: String, minutesFormat: String = MinutesFormat.STRUCTURED.name)` → `fun notifyTranscriptionComplete(context: Context, meetingId: Long, transcriptPath: String, customPrompt: String? = null)`
       주의: Plan 02에서 이미 customPrompt 파라미터가 추가되었을 수 있음. 파일을 읽어 현재 시그니처를 확인하고, minutesFormat 파라미터만 제거하되 customPrompt는 유지
       (VERIFICATION: line 122 부근)

    3. Intent에 minutesFormat을 putExtra하는 코드 제거
       형태: `putExtra("minutesFormat", minutesFormat)` 또는 유사 코드 삭제
       주의: Plan 02에서 추가된 `customPrompt?.let { putExtra("customPrompt", it) }` 라인은 유지
       (VERIFICATION: line 130 부근)

    **확인 방법:** 제거 후 `grep -n "MinutesFormat\|minutesFormat" PipelineNotificationHelper.kt` 결과가 0건이어야 한다.
    `customPrompt` 관련 코드는 보존되어야 한다.
  </action>
  <verify>
    <automated>grep -n "MinutesFormat\|minutesFormat" "D:/workspace/temp/Auto_Minuting/app/src/main/java/com/autominuting/service/PipelineNotificationHelper.kt" | wc -l</automated>
  </verify>
  <done>PipelineNotificationHelper.kt에서 MinutesFormat import, minutesFormat 파라미터, putExtra minutesFormat이 제거되고, customPrompt 파라미터와 putExtra는 그대로 유지된다</done>
</task>

<task type="auto">
  <name>Task 3: TranscriptsViewModel.kt — enqueueMinutesWorker() 잔존 참조 제거</name>
  <files>app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsViewModel.kt</files>
  <action>
    파일을 먼저 읽어 실제 라인 번호를 확인한 후 아래 항목을 제거한다.

    **제거 항목 (2종, enqueueMinutesWorker() 함수 내부):**

    1. `getMinutesFormatOnce()` 호출 라인 삭제
       형태: `val minutesFormat = userPreferencesRepository.getMinutesFormatOnce()` 또는 유사 코드
       이 메서드는 UserPreferencesRepository에서 이미 삭제된 상태 — 컴파일 오류의 직접 원인
       (VERIFICATION: line 148 부근)

    2. workDataOf() 내 `KEY_MINUTES_FORMAT to minutesFormat.name` 항목 삭제
       해당 항목만 삭제하고 나머지 workData 항목(KEY_CUSTOM_PROMPT 등)은 유지
       (VERIFICATION: line 155 부근)

    **주의:** 같은 함수에 있는 CUSTOM_PROMPT_MODE_ID 분기(Plan 02에서 추가됨)는 건드리지 않는다.
    함수 전체를 재작성하지 말고 해당 2라인만 정확히 제거한다.

    **확인 방법:** 제거 후 `grep -n "getMinutesFormatOnce\|KEY_MINUTES_FORMAT\|MinutesFormat" TranscriptsViewModel.kt` 결과가 0건이어야 한다.
  </action>
  <verify>
    <automated>grep -n "getMinutesFormatOnce\|KEY_MINUTES_FORMAT\|MinutesFormat" "D:/workspace/temp/Auto_Minuting/app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsViewModel.kt" | wc -l</automated>
  </verify>
  <done>TranscriptsViewModel.kt의 enqueueMinutesWorker()에서 getMinutesFormatOnce() 호출과 KEY_MINUTES_FORMAT 전달이 제거되어 컴파일 오류가 해소된다</done>
</task>

<task type="auto">
  <name>Task 4: SettingsScreen.kt — MinutesFormat 드롭다운 삭제 + 자동모드 Switch 이동 + 빌드 검증</name>
  <files>app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt</files>
  <action>
    파일을 먼저 읽어 전체 구조를 파악한 후 세 가지 수정을 수행한다.

    **수정 1: MinutesFormat 관련 코드 제거 (3곳)**

    1a. `import MinutesFormat` 라인 삭제
        (VERIFICATION: line 58 부근)

    1b. `val selectedFormat by viewModel.minutesFormat.collectAsStateWithLifecycle()` 라인 삭제
        SettingsViewModel에 minutesFormat StateFlow가 더 이상 존재하지 않음 — 컴파일 오류 원인
        (VERIFICATION: line 96 부근)

    1c. "회의록 형식" 드롭다운 블록 전체 삭제
        삭제 범위: `Text("회의록 형식")` 또는 `// 회의록 형식` 주석부터 해당 ExposedDropdownMenuBox 닫는 괄호까지 전체
        포함 항목:
        - "회의록 형식" 레이블 Text 컴포저블
        - `formatLabels = mapOf(MinutesFormat.STRUCTURED to "...", ...)` 변수 선언
        - `ExposedDropdownMenuBox` + `OutlinedTextField` + `DropdownMenu` + `MinutesFormat.entries.forEach` 블록
        - 관련 Spacer
        (VERIFICATION: lines 235-285 부근)

    **수정 2: 완전 자동 모드 Switch를 '회의록 설정' 섹션으로 이동**

    2a. '전사 설정' 섹션(SettingsSection(title = "전사 설정") 블록 내부)에서 다음 블록을 찾아 잘라낸다:
        ```
        // 자동화 모드 Row (Switch 포함)
        Row(verticalAlignment = Alignment.CenterVertically, ...) {
            Text("완전 자동 모드" 또는 유사 텍스트)
            Switch(checked = automationMode == AutomationMode.FULL_AUTO, ...)
        }
        if (automationMode == AutomationMode.HYBRID) {
            Text("하이브리드 모드..." 또는 유사 설명 텍스트)
        }
        Spacer(modifier = Modifier.height(16.dp))
        ```
        (VERIFICATION: lines 291-317 부근)

    2b. 잘라낸 블록을 '회의록 설정' 섹션(SettingsSection(title = "회의록 설정") 블록 내부) 맨 첫 번째 위치에 붙여넣는다.
        — 프롬프트 템플릿 관리 OutlinedButton이나 기본 프롬프트 드롭다운보다 위에 위치해야 한다.

    **수정 후 기대 구조:**
    ```
    SettingsSection(title = "회의록 설정") {
        // 1. 완전 자동 모드 Switch (이동됨)
        Row { ... Switch ... }
        if (automationMode == AutomationMode.HYBRID) { ... }
        Spacer(16.dp)
        // 2. 프롬프트 템플릿 관리 버튼 (기존 유지)
        OutlinedButton { ... }
        // 3. 기본 프롬프트 드롭다운 (기존 유지, CUSTOM_PROMPT_MODE_ID 포함)
        // 4. 직접 입력 TextField (Plan 02에서 추가됨, 유지)
        // 회의록 형식 드롭다운은 삭제됨
    }

    SettingsSection(title = "전사 설정") {
        // 자동화 모드는 위로 이동됨
        // STT 엔진 설정 (기존 유지)
        // Whisper 모델 관리 (기존 유지)
    }
    ```

    **수정 3: 빌드 검증**
    모든 수정 완료 후 `./gradlew assembleDebug` 실행.
    빌드 실패 시 오류 메시지를 분석하여 누락된 import 제거 또는 파라미터 불일치를 수정한다.
    selectedFormat 변수 등 MinutesFormat 드롭다운 삭제로 인해 dangling reference가 생겼다면 함께 제거한다.
  </action>
  <verify>
    <automated>cd "D:/workspace/temp/Auto_Minuting" && grep -n "MinutesFormat\|minutesFormat\|formatLabels\|selectedFormat" app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt | wc -l && ./gradlew assembleDebug 2>&1 | tail -10</automated>
  </verify>
  <done>SettingsScreen.kt에서 MinutesFormat 참조가 0건이고, "완전 자동 모드" Switch가 "회의록 설정" 섹션 상단에 위치하며, assembleDebug 빌드가 성공한다</done>
</task>

</tasks>

<verification>
1. MinutesFormat 전체 잔존 참조 0건 확인:
   `grep -rn "MinutesFormat\|KEY_MINUTES_FORMAT\|getMinutesFormatOnce\|MINUTES_FORMAT_KEY" app/src/main/java/com/autominuting/`
2. SettingsScreen.kt에서 "완전 자동 모드" 텍스트가 "회의록 설정" 섹션 내부에 위치하는지 확인
3. SettingsScreen.kt에서 "전사 설정" 섹션에 "완전 자동 모드" 텍스트가 없는지 확인
4. `./gradlew assembleDebug` 빌드 성공 확인
5. Plan 02에서 구현된 customPrompt 관련 코드(CUSTOM_PROMPT_MODE_ID 드롭다운, defaultCustomPrompt TextField, PipelineNotificationHelper customPrompt putExtra)가 그대로 유지되는지 확인
</verification>

<success_criteria>
- 4개 파일 모두에서 MinutesFormat 참조 0건
- 자동모드 Switch가 회의록 설정 섹션 상단에 위치
- Plan 02에서 구현된 customPrompt 파이프라인 코드 온전히 보존
- `./gradlew assembleDebug` 빌드 성공
- Phase 35 전체 목표(SET-01 MinutesFormat 전면 제거, SET-02 자동모드 이동) 완성
</success_criteria>

<output>
After completion, create `.planning/phases/35-/35-03-SUMMARY.md`
</output>
