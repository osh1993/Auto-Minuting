---
phase: "35"
plan: 2
type: execute
wave: 2
depends_on: ["35-01"]
files_modified:
  - app/src/main/java/com/autominuting/data/preferences/UserPreferencesRepository.kt
  - app/src/main/java/com/autominuting/presentation/settings/SettingsViewModel.kt
  - app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt
  - app/src/main/java/com/autominuting/worker/MinutesGenerationWorker.kt
  - app/src/main/java/com/autominuting/presentation/dashboard/DashboardViewModel.kt
  - app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsViewModel.kt
  - app/src/main/java/com/autominuting/presentation/share/ShareReceiverActivity.kt
  - app/src/main/java/com/autominuting/receiver/PipelineActionReceiver.kt
  - app/src/main/java/com/autominuting/service/PipelineNotificationHelper.kt
autonomous: true
requirements:
  - SET-03
must_haves:
  truths:
    - "기본 프롬프트 드롭다운에서 '직접 입력' 옵션을 선택할 수 있다"
    - "'직접 입력' 선택 시 프롬프트 텍스트를 입력하는 TextField가 나타난다"
    - "입력한 커스텀 프롬프트가 DataStore에 저장되어 앱 재시작 후에도 유지된다"
    - "자동 모드에서 직접 입력 프롬프트로 회의록이 생성된다"
    - "직접 입력 프롬프트가 비어있으면 기본 STRUCTURED 프롬프트로 폴백한다"
    - "공유 수신(ShareReceiverActivity) 경로에서도 직접 입력 프롬프트가 적용된다"
    - "알림 액션(PipelineActionReceiver) 경로에서도 직접 입력 프롬프트가 적용된다"
  artifacts:
    - path: "app/src/main/java/com/autominuting/data/preferences/UserPreferencesRepository.kt"
      provides: "CUSTOM_PROMPT_MODE_ID, DEFAULT_CUSTOM_PROMPT_KEY, defaultCustomPrompt Flow, setDefaultCustomPrompt()"
      contains: "CUSTOM_PROMPT_MODE_ID"
    - path: "app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt"
      provides: "직접 입력 드롭다운 옵션 + 조건부 TextField"
      contains: "직접 입력"
  key_links:
    - from: "SettingsScreen.kt"
      to: "SettingsViewModel"
      via: "setDefaultCustomPrompt() 호출"
      pattern: "setDefaultCustomPrompt"
    - from: "MinutesGenerationWorker.kt"
      to: "UserPreferencesRepository"
      via: "defaultTemplateId == CUSTOM_PROMPT_MODE_ID일 때 customPrompt 읽기"
      pattern: "CUSTOM_PROMPT_MODE_ID"
    - from: "DashboardViewModel.kt"
      to: "MinutesGenerationWorker"
      via: "Worker enqueue 시 customPrompt 전달"
      pattern: "KEY_CUSTOM_PROMPT"
    - from: "ShareReceiverActivity.kt"
      to: "MinutesGenerationWorker"
      via: "CUSTOM_PROMPT_MODE_ID 분기 후 customPrompt로 Worker enqueue"
      pattern: "CUSTOM_PROMPT_MODE_ID"
    - from: "PipelineActionReceiver.kt"
      to: "MinutesGenerationWorker"
      via: "intent에서 customPrompt 읽어 Worker enqueue"
      pattern: "KEY_CUSTOM_PROMPT"
    - from: "PipelineNotificationHelper.kt"
      to: "PipelineActionReceiver"
      via: "알림 Intent에 customPrompt putExtra"
      pattern: "customPrompt"
---

<objective>
설정 화면의 기본 프롬프트 드롭다운에 "직접 입력" 옵션을 추가하여, 사용자가 커스텀 프롬프트를 기본으로 설정할 수 있게 한다.

Purpose: 기존에는 프리셋 템플릿만 기본으로 설정할 수 있었으나, 사용자가 자유롭게 작성한 프롬프트를 기본으로 지정하면 매번 다이얼로그 없이 해당 프롬프트로 자동 생성이 가능해진다.
Output: 직접 입력 기본 프롬프트 기능 완성, 모든 enqueue 지점(4개) 파이프라인 연동 완료
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-/35-RESEARCH.md
@.planning/phases/35-/35-01-SUMMARY.md

<interfaces>
<!-- Plan 01 완료 후 상태: MinutesFormat 제거됨, format 파라미터 없음 -->

From app/src/main/java/com/autominuting/data/preferences/UserPreferencesRepository.kt (Plan 01 후):
```kotlin
companion object {
    val AUTOMATION_MODE_KEY = stringPreferencesKey("automation_mode")
    val AUTH_MODE_KEY = stringPreferencesKey("auth_mode")
    val DEFAULT_TEMPLATE_ID_KEY = longPreferencesKey("default_template_id")
    // ... 기타 키
}
val defaultTemplateId: Flow<Long>  // 0L = 매번 선택, >0 = 템플릿 ID
suspend fun setDefaultTemplateId(id: Long)
suspend fun getDefaultTemplateIdOnce(): Long
```

From app/src/main/java/com/autominuting/presentation/settings/SettingsViewModel.kt (Plan 01 후):
```kotlin
val defaultTemplateId: StateFlow<Long>
val templates: StateFlow<List<PromptTemplate>>
fun setDefaultTemplateId(id: Long)
```

From app/src/main/java/com/autominuting/worker/MinutesGenerationWorker.kt:
```kotlin
companion object {
    const val KEY_MEETING_ID = "meetingId"
    const val KEY_TRANSCRIPT_PATH = "transcriptPath"
    const val KEY_TEMPLATE_ID = "templateId"
    const val KEY_CUSTOM_PROMPT = "customPrompt"
}
```

기존 프롬프트 해결 로직 (MinutesGenerationWorker.doWork()):
1. templateId > 0 → 해당 템플릿의 promptText 사용
2. customPrompt != null → customPrompt 사용
3. 둘 다 없음 → null (GeminiEngine에서 STRUCTURED_PROMPT 폴백)

<!-- Plan 01 완료 후 ShareReceiverActivity.kt 상태: minutesFormat 관련 코드 제거됨 -->
<!-- processSharedText() 메서드에서 Worker enqueue 시 KEY_MEETING_ID, KEY_TRANSCRIPT_PATH만 전달 -->

<!-- Plan 01 완료 후 PipelineActionReceiver.kt 상태: minutesFormat/MinutesFormat 제거됨 -->
<!-- ACTION_GENERATE_MINUTES 핸들러에서 intent로부터 meetingId, transcriptPath만 읽어 Worker enqueue -->

<!-- Plan 01 완료 후 PipelineNotificationHelper.kt 상태: minutesFormat 파라미터 제거됨 -->
<!-- notifyTranscriptionComplete()에서 meetingId, transcriptPath만 Intent에 putExtra -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 직접 입력 프롬프트 DataStore + ViewModel + 전체 파이프라인 연동 (4개 enqueue 지점)</name>
  <files>
    app/src/main/java/com/autominuting/data/preferences/UserPreferencesRepository.kt,
    app/src/main/java/com/autominuting/presentation/settings/SettingsViewModel.kt,
    app/src/main/java/com/autominuting/presentation/dashboard/DashboardViewModel.kt,
    app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsViewModel.kt,
    app/src/main/java/com/autominuting/presentation/share/ShareReceiverActivity.kt,
    app/src/main/java/com/autominuting/receiver/PipelineActionReceiver.kt,
    app/src/main/java/com/autominuting/service/PipelineNotificationHelper.kt,
    app/src/main/java/com/autominuting/worker/MinutesGenerationWorker.kt
  </files>
  <read_first>
    app/src/main/java/com/autominuting/data/preferences/UserPreferencesRepository.kt,
    app/src/main/java/com/autominuting/presentation/settings/SettingsViewModel.kt,
    app/src/main/java/com/autominuting/presentation/dashboard/DashboardViewModel.kt,
    app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsViewModel.kt,
    app/src/main/java/com/autominuting/presentation/share/ShareReceiverActivity.kt,
    app/src/main/java/com/autominuting/receiver/PipelineActionReceiver.kt,
    app/src/main/java/com/autominuting/service/PipelineNotificationHelper.kt,
    app/src/main/java/com/autominuting/worker/MinutesGenerationWorker.kt
  </read_first>
  <action>
    **1. UserPreferencesRepository에 직접 입력 프롬프트 저장 기능 추가**

    companion object에 추가:
    ```kotlin
    /** 직접 입력 모드를 나타내는 특수 템플릿 ID */
    const val CUSTOM_PROMPT_MODE_ID = -1L

    /** 직접 입력 기본 프롬프트 키 */
    val DEFAULT_CUSTOM_PROMPT_KEY = stringPreferencesKey("default_custom_prompt")
    ```

    클래스 본문에 추가:
    ```kotlin
    /** 기본 커스텀 프롬프트 텍스트를 관찰한다. 기본값: 빈 문자열 */
    val defaultCustomPrompt: Flow<String> = dataStore.data.map { prefs ->
        prefs[DEFAULT_CUSTOM_PROMPT_KEY] ?: ""
    }

    /** 기본 커스텀 프롬프트 텍스트를 저장한다. */
    suspend fun setDefaultCustomPrompt(prompt: String) {
        dataStore.edit { prefs ->
            prefs[DEFAULT_CUSTOM_PROMPT_KEY] = prompt
        }
    }

    /** 현재 기본 커스텀 프롬프트를 즉시 조회한다. */
    suspend fun getDefaultCustomPromptOnce(): String =
        dataStore.data.first()[DEFAULT_CUSTOM_PROMPT_KEY] ?: ""
    ```

    **2. SettingsViewModel에 직접 입력 프롬프트 상태/액션 추가**

    새 StateFlow 추가:
    ```kotlin
    /** 기본 커스텀 프롬프트 텍스트 */
    val defaultCustomPrompt: StateFlow<String> = userPreferencesRepository.defaultCustomPrompt
        .stateIn(
            scope = viewModelScope,
            started = SharingStarted.WhileSubscribed(5_000),
            initialValue = ""
        )
    ```

    새 메서드 추가:
    ```kotlin
    /** 기본 커스텀 프롬프트를 저장한다. */
    fun setDefaultCustomPrompt(prompt: String) {
        viewModelScope.launch {
            userPreferencesRepository.setDefaultCustomPrompt(prompt)
        }
    }
    ```

    **3. DashboardViewModel의 enqueueMinutesWorker 수정**
    - `generateMinutesForPipeline()` 메서드에서 defaultTemplateId가 `UserPreferencesRepository.CUSTOM_PROMPT_MODE_ID`(-1L)인 경우를 처리:
      ```kotlin
      fun generateMinutesForPipeline(meetingId: Long) {
          viewModelScope.launch {
              val templateId = userPreferencesRepository.getDefaultTemplateIdOnce()
              if (templateId == UserPreferencesRepository.CUSTOM_PROMPT_MODE_ID) {
                  // 직접 입력 모드: 저장된 커스텀 프롬프트로 생성
                  val customPrompt = userPreferencesRepository.getDefaultCustomPromptOnce()
                  enqueueMinutesWorker(
                      meetingId,
                      customPrompt = customPrompt.ifBlank { null }
                  )
              } else if (templateId > 0) {
                  enqueueMinutesWorker(meetingId, templateId = templateId)
              } else {
                  // 매번 선택 모드 → 기존 다이얼로그 표시 로직 (변경 없음)
                  // ... 기존 코드 유지
              }
          }
      }
      ```
    - `enqueueMinutesWorker()` 호출부에서 기존 `minutesFormat.name` 전달 코드는 Plan 01에서 이미 제거됨

    **4. TranscriptsViewModel의 enqueueMinutesWorker 수정**
    - `generateMinutes()` 메서드에서 동일하게 CUSTOM_PROMPT_MODE_ID 분기 추가:
      ```kotlin
      fun generateMinutes(meetingId: Long) {
          viewModelScope.launch {
              val templateId = userPreferencesRepository.getDefaultTemplateIdOnce()
              if (templateId == UserPreferencesRepository.CUSTOM_PROMPT_MODE_ID) {
                  val customPrompt = userPreferencesRepository.getDefaultCustomPromptOnce()
                  enqueueMinutesWorker(
                      meetingId,
                      customPrompt = customPrompt.ifBlank { null }
                  )
              } else if (templateId > 0) {
                  enqueueMinutesWorker(meetingId, templateId = templateId)
              } else {
                  // 매번 선택 → 다이얼로그 표시 (기존 로직 유지)
              }
          }
      }
      ```

    **5. ShareReceiverActivity.kt의 processSharedText() Worker enqueue에 CUSTOM_PROMPT_MODE_ID 분기 추가**
    - Plan 01에서 `getMinutesFormatOnce()` / `KEY_MINUTES_FORMAT` 코드가 제거된 상태.
    - `processSharedText()` 메서드의 Worker enqueue 부분(line 261-273 부근)을 다음으로 교체:
      ```kotlin
      // 4. 회의록 자동 생성 시도 (실패해도 전사 데이터는 이미 DB에 보존됨)
      try {
          val templateId = userPreferencesRepository.getDefaultTemplateIdOnce()
          val workData = when {
              templateId == UserPreferencesRepository.CUSTOM_PROMPT_MODE_ID -> {
                  // 직접 입력 모드: 저장된 커스텀 프롬프트로 생성
                  val customPrompt = userPreferencesRepository.getDefaultCustomPromptOnce()
                  workDataOf(
                      MinutesGenerationWorker.KEY_MEETING_ID to meetingId,
                      MinutesGenerationWorker.KEY_TRANSCRIPT_PATH to transcriptPath,
                      MinutesGenerationWorker.KEY_CUSTOM_PROMPT to customPrompt.ifBlank { null }
                  )
              }
              templateId > 0 -> {
                  workDataOf(
                      MinutesGenerationWorker.KEY_MEETING_ID to meetingId,
                      MinutesGenerationWorker.KEY_TRANSCRIPT_PATH to transcriptPath,
                      MinutesGenerationWorker.KEY_TEMPLATE_ID to templateId
                  )
              }
              else -> {
                  // 매번 선택 또는 기본 → templateId/customPrompt 없이 enqueue (STRUCTURED 폴백)
                  workDataOf(
                      MinutesGenerationWorker.KEY_MEETING_ID to meetingId,
                      MinutesGenerationWorker.KEY_TRANSCRIPT_PATH to transcriptPath
                  )
              }
          }

          val workRequest = OneTimeWorkRequestBuilder<MinutesGenerationWorker>()
              .setInputData(workData)
              .build()
          WorkManager.getInstance(this@ShareReceiverActivity).enqueue(workRequest)
      ```
    - `import com.autominuting.data.preferences.UserPreferencesRepository` 추가 (이미 존재하면 유지)

    **6. PipelineNotificationHelper.kt의 notifyTranscriptionComplete()에 customPrompt 전달 추가**
    - Plan 01에서 `minutesFormat` 파라미터가 제거된 상태. 새로 `customPrompt: String? = null` 파라미터를 추가:
      ```kotlin
      fun notifyTranscriptionComplete(
          context: Context,
          meetingId: Long,
          transcriptPath: String,
          customPrompt: String? = null
      ) {
      ```
    - generateIntent에 customPrompt putExtra 추가:
      ```kotlin
      val generateIntent = Intent().apply {
          action = "com.autominuting.action.GENERATE_MINUTES"
          putExtra("meetingId", meetingId)
          putExtra("transcriptPath", transcriptPath)
          customPrompt?.let { putExtra("customPrompt", it) }
      }
      ```
    - 이 함수를 호출하는 TranscriptionTriggerWorker도 확인 필요 — Plan 01에서 minutesFormat 인자를 제거했으므로, 현재 customPrompt 없이 호출 중. TranscriptionTriggerWorker에서도 CUSTOM_PROMPT_MODE_ID 분기를 추가하여 customPrompt를 전달해야 함:
      - TranscriptionTriggerWorker.kt를 read_first에서 확인
      - notifyTranscriptionComplete() 호출 지점에서 defaultTemplateId를 조회하고 CUSTOM_PROMPT_MODE_ID이면 customPrompt를 함께 전달

    **7. PipelineActionReceiver.kt의 ACTION_GENERATE_MINUTES 핸들러에 customPrompt 분기 추가**
    - Plan 01에서 `minutesFormat` / `MinutesFormat` 코드가 제거된 상태.
    - `ACTION_GENERATE_MINUTES` 분기를 다음으로 교체:
      ```kotlin
      ACTION_GENERATE_MINUTES -> {
          val meetingId = intent.getLongExtra("meetingId", -1L)
          val transcriptPath = intent.getStringExtra("transcriptPath") ?: return
          val customPrompt = intent.getStringExtra("customPrompt")

          Log.d(TAG, "회의록 생성 요청: meetingId=$meetingId")

          val workRequest = OneTimeWorkRequestBuilder<MinutesGenerationWorker>()
              .setInputData(
                  workDataOf(
                      MinutesGenerationWorker.KEY_MEETING_ID to meetingId,
                      MinutesGenerationWorker.KEY_TRANSCRIPT_PATH to transcriptPath,
                      MinutesGenerationWorker.KEY_CUSTOM_PROMPT to customPrompt
                  )
              )
              .build()
          WorkManager.getInstance(context).enqueue(workRequest)
      }
      ```
    - `import com.autominuting.domain.model.MinutesFormat` 삭제 확인 (Plan 01에서 제거되어야 하지만 잔존 가능)
    - `import com.autominuting.worker.MinutesGenerationWorker` 는 유지

    **8. MinutesGenerationWorker 프롬프트 해결 로직 확인**
    - 기존 로직에서 customPrompt가 inputData로 전달되면 이미 올바르게 사용됨
    - 추가 수정 불필요 — ViewModel/Activity/Receiver에서 `customPrompt = customPrompt` 로 전달하면 Worker가 수신함
    - 단, Worker의 주석/로그에서 "minutesFormat 폴백" 등 레거시 설명이 남아있으면 정리
  </action>
  <acceptance_criteria>
    - UserPreferencesRepository.kt에 `CUSTOM_PROMPT_MODE_ID = -1L` 상수가 존재한다
    - UserPreferencesRepository.kt에 `DEFAULT_CUSTOM_PROMPT_KEY` 키가 존재한다
    - UserPreferencesRepository.kt에 `defaultCustomPrompt: Flow<String>` 필드가 존재한다
    - UserPreferencesRepository.kt에 `setDefaultCustomPrompt(prompt: String)` 메서드가 존재한다
    - UserPreferencesRepository.kt에 `getDefaultCustomPromptOnce(): String` 메서드가 존재한다
    - SettingsViewModel.kt에 `defaultCustomPrompt: StateFlow<String>` 필드가 존재한다
    - SettingsViewModel.kt에 `fun setDefaultCustomPrompt(prompt: String)` 메서드가 존재한다
    - DashboardViewModel.kt에 `CUSTOM_PROMPT_MODE_ID` 분기가 존재한다
    - TranscriptsViewModel.kt에 `CUSTOM_PROMPT_MODE_ID` 분기가 존재한다
    - ShareReceiverActivity.kt에 `CUSTOM_PROMPT_MODE_ID` 분기가 존재한다
    - ShareReceiverActivity.kt에 `getDefaultCustomPromptOnce` 호출이 존재한다
    - PipelineActionReceiver.kt에 `KEY_CUSTOM_PROMPT` 참조가 존재한다
    - PipelineActionReceiver.kt에 `MinutesFormat` import가 존재하지 않는다
    - PipelineNotificationHelper.kt의 notifyTranscriptionComplete에 `customPrompt` 파라미터가 존재한다
  </acceptance_criteria>
  <verify>
    <automated>cd "D:/workspace/temp/Auto_Minuting" && grep -n "CUSTOM_PROMPT_MODE_ID\|DEFAULT_CUSTOM_PROMPT_KEY\|defaultCustomPrompt\|setDefaultCustomPrompt\|getDefaultCustomPromptOnce" app/src/main/java/com/autominuting/data/preferences/UserPreferencesRepository.kt app/src/main/java/com/autominuting/presentation/settings/SettingsViewModel.kt app/src/main/java/com/autominuting/presentation/dashboard/DashboardViewModel.kt app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsViewModel.kt && grep -n "CUSTOM_PROMPT_MODE_ID\|KEY_CUSTOM_PROMPT\|getDefaultCustomPromptOnce\|customPrompt" app/src/main/java/com/autominuting/presentation/share/ShareReceiverActivity.kt app/src/main/java/com/autominuting/receiver/PipelineActionReceiver.kt app/src/main/java/com/autominuting/service/PipelineNotificationHelper.kt</automated>
  </verify>
  <done>직접 입력 프롬프트의 DataStore 저장, ViewModel 노출, 전체 파이프라인 연동(DashboardViewModel, TranscriptsViewModel, ShareReceiverActivity, PipelineActionReceiver 4개 enqueue 지점 + PipelineNotificationHelper 전달 경로)이 완료되었다</done>
</task>

<task type="auto">
  <name>Task 2: 설정 화면 직접 입력 UI + 빌드 검증</name>
  <files>
    app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt
  </files>
  <read_first>
    app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt,
    app/src/main/java/com/autominuting/presentation/settings/SettingsViewModel.kt,
    app/src/main/java/com/autominuting/data/preferences/UserPreferencesRepository.kt
  </read_first>
  <action>
    SettingsScreen.kt에서 기본 프롬프트 드롭다운을 확장하여 "직접 입력" 옵션과 조건부 TextField를 추가한다.

    **1. 새 state 수집 추가** (SettingsScreen 함수 상단):
    ```kotlin
    val defaultCustomPrompt by viewModel.defaultCustomPrompt.collectAsStateWithLifecycle()
    ```

    **2. 기본 프롬프트 드롭다운의 선택 라벨 수정**
    기존:
    ```kotlin
    val selectedTemplateName = if (defaultTemplateId == 0L) {
        "매번 선택"
    } else {
        templates.find { it.id == defaultTemplateId }?.name ?: "매번 선택"
    }
    ```
    변경:
    ```kotlin
    val selectedTemplateName = when (defaultTemplateId) {
        0L -> "매번 선택"
        UserPreferencesRepository.CUSTOM_PROMPT_MODE_ID -> "직접 입력"
        else -> templates.find { it.id == defaultTemplateId }?.name ?: "매번 선택"
    }
    ```
    - `import com.autominuting.data.preferences.UserPreferencesRepository` 추가

    **3. 드롭다운 메뉴에 "직접 입력" 옵션 추가**
    "매번 선택" DropdownMenuItem 바로 아래에:
    ```kotlin
    // "직접 입력" 옵션
    DropdownMenuItem(
        text = { Text("직접 입력") },
        onClick = {
            viewModel.setDefaultTemplateId(UserPreferencesRepository.CUSTOM_PROMPT_MODE_ID)
            templateDropdownExpanded = false
        }
    )
    ```

    **4. 조건부 직접 입력 TextField 추가**
    기본 프롬프트 드롭다운 ExposedDropdownMenuBox 바로 아래에:
    ```kotlin
    // 직접 입력 모드일 때 프롬프트 입력 TextField 표시
    if (defaultTemplateId == UserPreferencesRepository.CUSTOM_PROMPT_MODE_ID) {
        Spacer(modifier = Modifier.height(12.dp))
        Text(
            text = "기본 프롬프트",
            style = MaterialTheme.typography.titleMedium
        )
        Text(
            text = "회의록 생성 시 사용할 프롬프트를 직접 입력하세요",
            style = MaterialTheme.typography.bodySmall,
            color = MaterialTheme.colorScheme.onSurfaceVariant
        )
        Spacer(modifier = Modifier.height(8.dp))
        OutlinedTextField(
            value = defaultCustomPrompt,
            onValueChange = { viewModel.setDefaultCustomPrompt(it) },
            placeholder = { Text("예: 회의 내용을 요약하고 액션 아이템을 정리해주세요") },
            minLines = 3,
            maxLines = 8,
            modifier = Modifier.fillMaxWidth()
        )
        if (defaultCustomPrompt.isBlank()) {
            Spacer(modifier = Modifier.height(4.dp))
            Text(
                text = "프롬프트가 비어있으면 기본 구조화된 회의록 형식이 사용됩니다",
                style = MaterialTheme.typography.bodySmall,
                color = MaterialTheme.colorScheme.onSurfaceVariant
            )
        }
    }
    ```

    **5. 빌드 검증**
    - `./gradlew assembleDebug` 실행하여 전체 컴파일 성공 확인
  </action>
  <acceptance_criteria>
    - SettingsScreen.kt에 "직접 입력" 텍스트가 드롭다운 옵션으로 존재한다
    - SettingsScreen.kt에 `CUSTOM_PROMPT_MODE_ID` 참조가 존재한다
    - SettingsScreen.kt에 `defaultCustomPrompt` collectAsStateWithLifecycle 수집이 존재한다
    - SettingsScreen.kt에 `setDefaultCustomPrompt` 호출이 존재한다
    - SettingsScreen.kt에 OutlinedTextField가 minLines = 3으로 존재한다
    - SettingsScreen.kt에 비어있을 때 폴백 안내 텍스트 "기본 구조화된 회의록 형식이 사용됩니다"가 존재한다
    - `./gradlew assembleDebug` 빌드가 성공한다
  </acceptance_criteria>
  <verify>
    <automated>cd "D:/workspace/temp/Auto_Minuting" && ./gradlew assembleDebug 2>&1 | tail -5</automated>
  </verify>
  <done>기본 프롬프트 드롭다운에 "직접 입력" 옵션이 추가되고, 선택 시 프롬프트 입력 TextField가 표시되며, 전체 빌드가 성공한다</done>
</task>

</tasks>

<verification>
1. 기본 프롬프트 드롭다운에서 "매번 선택", "직접 입력", 템플릿 목록 3가지 카테고리가 존재하는지 확인
2. "직접 입력" 선택 시 프롬프트 TextField가 나타나는지 확인
3. 커스텀 프롬프트가 DataStore에 저장되는지 확인 (CUSTOM_PROMPT_MODE_ID + DEFAULT_CUSTOM_PROMPT_KEY)
4. 4개 enqueue 지점 모두에서 직접 입력 프롬프트로 Worker enqueue가 되는지 확인:
   - DashboardViewModel (대시보드에서 수동 생성)
   - TranscriptsViewModel (전사 목록에서 수동 생성)
   - ShareReceiverActivity (외부 앱 공유 수신)
   - PipelineActionReceiver (알림 액션 버튼)
5. `./gradlew assembleDebug` 빌드 성공 확인
</verification>

<success_criteria>
- 기본 프롬프트 드롭다운에 "직접 입력" 옵션이 추가됨
- "직접 입력" 선택 시 프롬프트 입력 TextField가 조건부로 표시됨
- 입력한 프롬프트가 DataStore에 저장되어 앱 재시작 후에도 유지됨
- 모든 enqueue 경로(DashboardViewModel, TranscriptsViewModel, ShareReceiverActivity, PipelineActionReceiver)에서 직접 입력 프롬프트로 회의록 생성이 가능함
- PipelineNotificationHelper가 customPrompt를 PipelineActionReceiver로 전달함
- 프롬프트가 비어있으면 기본 STRUCTURED 프롬프트로 안전하게 폴백됨
- 전체 빌드 성공
</success_criteria>

<output>
After completion, create `.planning/phases/35-/35-02-SUMMARY.md`
</output>
