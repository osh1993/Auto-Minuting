---
phase: "35"
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/com/autominuting/domain/model/MinutesFormat.kt
  - app/src/main/java/com/autominuting/data/minutes/MinutesEngine.kt
  - app/src/main/java/com/autominuting/data/minutes/GeminiEngine.kt
  - app/src/main/java/com/autominuting/data/minutes/GeminiOAuthEngine.kt
  - app/src/main/java/com/autominuting/data/minutes/MinutesEngineSelector.kt
  - app/src/main/java/com/autominuting/domain/repository/MinutesRepository.kt
  - app/src/main/java/com/autominuting/data/repository/MinutesRepositoryImpl.kt
  - app/src/main/java/com/autominuting/data/preferences/UserPreferencesRepository.kt
  - app/src/main/java/com/autominuting/worker/MinutesGenerationWorker.kt
  - app/src/main/java/com/autominuting/worker/TranscriptionTriggerWorker.kt
  - app/src/main/java/com/autominuting/presentation/dashboard/DashboardViewModel.kt
  - app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsViewModel.kt
  - app/src/main/java/com/autominuting/presentation/share/ShareReceiverActivity.kt
  - app/src/main/java/com/autominuting/receiver/PipelineActionReceiver.kt
  - app/src/main/java/com/autominuting/service/PipelineNotificationHelper.kt
  - app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt
  - app/src/main/java/com/autominuting/presentation/settings/SettingsViewModel.kt
autonomous: true
requirements:
  - SET-01
  - SET-02
must_haves:
  truths:
    - "설정 화면에서 '회의록 형식' 드롭다운이 완전히 제거되었다"
    - "MinutesFormat enum 파일이 삭제되어 코드베이스에 존재하지 않는다"
    - "customPrompt가 null일 때 기본 STRUCTURED 프롬프트가 사용된다"
    - "설정 화면에서 '완전 자동 모드' Switch가 '회의록 설정' 섹션에 위치한다"
    - "앱이 컴파일 에러 없이 빌드된다"
  artifacts:
    - path: "app/src/main/java/com/autominuting/data/minutes/MinutesEngine.kt"
      provides: "format 파라미터 제거된 generate() interface"
      contains: "suspend fun generate(transcriptText: String, customPrompt: String? = null)"
    - path: "app/src/main/java/com/autominuting/data/minutes/GeminiEngine.kt"
      provides: "customPrompt null일 때 STRUCTURED_PROMPT 폴백"
      contains: "STRUCTURED_PROMPT"
  key_links:
    - from: "MinutesGenerationWorker.kt"
      to: "MinutesRepository.generateMinutes()"
      via: "format 파라미터 제거된 호출"
      pattern: "generateMinutes\\(transcriptText.*resolvedPrompt\\)"
    - from: "SettingsScreen.kt"
      to: "SettingsViewModel"
      via: "automationMode가 회의록 설정 섹션에서 렌더링"
      pattern: "회의록 설정.*자동 모드"
---

<objective>
minutesFormat enum 및 관련 코드를 코드베이스에서 전면 제거하고, 자동모드 Switch를 전사 설정에서 회의록 설정 섹션으로 이동한다.

Purpose: Phase 30에서 PromptTemplate 시스템이 도입되면서 MinutesFormat의 역할이 소멸했다. 죽은 코드를 정리하고, 자동모드를 의미적으로 올바른 섹션에 배치하여 설정 화면의 논리적 구조를 개선한다.
Output: MinutesFormat 제거 완료, 자동모드 회의록 설정 이동 완료, 빌드 성공
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-/35-RESEARCH.md

<interfaces>
<!-- MinutesEngine interface (현재 상태 — format 파라미터 제거 대상) -->
From app/src/main/java/com/autominuting/data/minutes/MinutesEngine.kt:
```kotlin
interface MinutesEngine {
    suspend fun generate(
        transcriptText: String,
        format: MinutesFormat = MinutesFormat.STRUCTURED,
        customPrompt: String? = null
    ): Result<String>
    fun engineName(): String
    fun isAvailable(): Boolean
}
```

From app/src/main/java/com/autominuting/domain/repository/MinutesRepository.kt:
```kotlin
interface MinutesRepository {
    suspend fun generateMinutes(
        transcriptText: String,
        format: MinutesFormat = MinutesFormat.STRUCTURED,
        customPrompt: String? = null
    ): Result<String>
}
```

From app/src/main/java/com/autominuting/data/preferences/UserPreferencesRepository.kt:
```kotlin
companion object {
    val MINUTES_FORMAT_KEY = stringPreferencesKey("minutes_format")
    val DEFAULT_TEMPLATE_ID_KEY = longPreferencesKey("default_template_id")
}
val minutesFormat: Flow<MinutesFormat>
suspend fun setMinutesFormat(format: MinutesFormat)
suspend fun getMinutesFormatOnce(): MinutesFormat
```

<!-- minutesFormat 사용 위치 (모두 제거 대상) -->
Worker enqueue 지점 (KEY_MINUTES_FORMAT 전달하는 곳):
- DashboardViewModel.kt: line 144-149
- TranscriptsViewModel.kt: line 141-148
- ShareReceiverActivity.kt: line 262-269
- PipelineActionReceiver.kt: line 27-37
- PipelineNotificationHelper.kt: line 130-137
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MinutesFormat enum 및 데이터 레이어 전면 제거</name>
  <files>
    app/src/main/java/com/autominuting/domain/model/MinutesFormat.kt,
    app/src/main/java/com/autominuting/data/minutes/MinutesEngine.kt,
    app/src/main/java/com/autominuting/data/minutes/GeminiEngine.kt,
    app/src/main/java/com/autominuting/data/minutes/GeminiOAuthEngine.kt,
    app/src/main/java/com/autominuting/data/minutes/MinutesEngineSelector.kt,
    app/src/main/java/com/autominuting/domain/repository/MinutesRepository.kt,
    app/src/main/java/com/autominuting/data/repository/MinutesRepositoryImpl.kt,
    app/src/main/java/com/autominuting/data/preferences/UserPreferencesRepository.kt,
    app/src/main/java/com/autominuting/worker/MinutesGenerationWorker.kt,
    app/src/main/java/com/autominuting/worker/TranscriptionTriggerWorker.kt,
    app/src/main/java/com/autominuting/presentation/dashboard/DashboardViewModel.kt,
    app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsViewModel.kt,
    app/src/main/java/com/autominuting/presentation/share/ShareReceiverActivity.kt,
    app/src/main/java/com/autominuting/receiver/PipelineActionReceiver.kt,
    app/src/main/java/com/autominuting/service/PipelineNotificationHelper.kt
  </files>
  <read_first>
    app/src/main/java/com/autominuting/domain/model/MinutesFormat.kt,
    app/src/main/java/com/autominuting/data/minutes/MinutesEngine.kt,
    app/src/main/java/com/autominuting/data/minutes/GeminiEngine.kt,
    app/src/main/java/com/autominuting/data/minutes/GeminiOAuthEngine.kt,
    app/src/main/java/com/autominuting/data/minutes/MinutesEngineSelector.kt,
    app/src/main/java/com/autominuting/domain/repository/MinutesRepository.kt,
    app/src/main/java/com/autominuting/data/repository/MinutesRepositoryImpl.kt,
    app/src/main/java/com/autominuting/data/preferences/UserPreferencesRepository.kt,
    app/src/main/java/com/autominuting/worker/MinutesGenerationWorker.kt,
    app/src/main/java/com/autominuting/worker/TranscriptionTriggerWorker.kt,
    app/src/main/java/com/autominuting/presentation/dashboard/DashboardViewModel.kt,
    app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsViewModel.kt,
    app/src/main/java/com/autominuting/presentation/share/ShareReceiverActivity.kt,
    app/src/main/java/com/autominuting/receiver/PipelineActionReceiver.kt,
    app/src/main/java/com/autominuting/service/PipelineNotificationHelper.kt
  </read_first>
  <action>
    MinutesFormat enum과 모든 참조를 코드베이스에서 일괄 제거한다. 작업 순서:

    **1단계: MinutesFormat.kt 파일 삭제**
    - `app/src/main/java/com/autominuting/domain/model/MinutesFormat.kt` 파일 전체 삭제

    **2단계: MinutesEngine interface에서 format 파라미터 제거**
    - `MinutesEngine.kt`: `generate()` 시그니처를 다음으로 변경:
      ```kotlin
      suspend fun generate(
          transcriptText: String,
          customPrompt: String? = null
      ): Result<String>
      ```
    - `import com.autominuting.domain.model.MinutesFormat` 제거

    **3단계: MinutesEngine 구현체 3개 수정**
    - `GeminiEngine.kt`:
      - `generate()` 시그니처에서 `format: MinutesFormat` 파라미터 제거
      - `import com.autominuting.domain.model.MinutesFormat` 제거
      - `generate()` 본문에서 `when (format)` 분기 제거, customPrompt == null일 때 항상 `STRUCTURED_PROMPT + transcriptText` 사용
      - `SUMMARY_PROMPT`와 `ACTION_ITEMS_PROMPT` companion object에서 삭제 (기본 제공 PromptTemplate이 동일 프롬프트 포함)
      - `STRUCTURED_PROMPT`는 기본 폴백으로 유지
    - `GeminiOAuthEngine.kt`: 동일하게 format 파라미터 제거, MinutesFormat import 제거, customPrompt null시 STRUCTURED 프롬프트 기본 사용
    - `MinutesEngineSelector.kt`: generate() delegate에서 format 파라미터 제거, MinutesFormat import 제거

    **4단계: MinutesRepository interface + 구현체 수정**
    - `MinutesRepository.kt`: `generateMinutes()` 시그니처에서 `format: MinutesFormat = MinutesFormat.STRUCTURED` 파라미터 제거, import 제거
    - `MinutesRepositoryImpl.kt`: 동일하게 format 파라미터 제거, engine.generate() 호출에서 format 인자 제거, import 제거

    **5단계: UserPreferencesRepository에서 minutesFormat 관련 코드 제거**
    - `MINUTES_FORMAT_KEY` companion 필드 삭제
    - `minutesFormat: Flow<MinutesFormat>` 필드 삭제
    - `setMinutesFormat()` 메서드 삭제
    - `getMinutesFormatOnce()` 메서드 삭제
    - `import com.autominuting.domain.model.MinutesFormat` 제거
    - KDoc에서 "회의록 형식(MinutesFormat)," 부분 제거

    **6단계: Worker에서 minutesFormat 관련 코드 제거**
    - `MinutesGenerationWorker.kt`:
      - `KEY_MINUTES_FORMAT` 상수 삭제
      - inputData에서 minutesFormat 읽는 코드 삭제 (line 78-83 부근)
      - `minutesRepository.generateMinutes()` 호출에서 format 인자 제거 → `generateMinutes(transcriptText, resolvedPrompt)` 형태로 변경. 주의: customPrompt 파라미터명은 유지해야 함 (named parameter로 `customPrompt = resolvedPrompt`)
      - MinutesFormat import 제거
      - 관련 KDoc/주석 정리 ("minutesFormat 폴백" 등)
    - `TranscriptionTriggerWorker.kt`:
      - `KEY_MINUTES_FORMAT` 상수 삭제
      - inputData에서 minutesFormat 읽는 코드 삭제 (line 57-58 부근)
      - MinutesGenerationWorker enqueue 시 `KEY_MINUTES_FORMAT to minutesFormat` 제거 (line 145 부근)
      - PipelineNotificationHelper 호출 시 minutesFormat 인자 제거 (line 156 부근)
      - MinutesFormat import 제거

    **7단계: ViewModel/Activity에서 minutesFormat 관련 코드 제거**
    - `DashboardViewModel.kt`:
      - `val minutesFormat = userPreferencesRepository.getMinutesFormatOnce()` 삭제 (line 144)
      - Worker enqueue 시 `MinutesGenerationWorker.KEY_MINUTES_FORMAT to minutesFormat.name` 제거 (line 149)
      - MinutesFormat import 제거 (있다면)
    - `TranscriptsViewModel.kt`:
      - `val minutesFormat = userPreferencesRepository.getMinutesFormatOnce()` 삭제 (line 141)
      - Worker enqueue 시 `KEY_MINUTES_FORMAT to minutesFormat.name` 제거 (line 148)
      - 주석 "기존 minutesFormat 폴백으로 바로 생성" 수정 → "기본 프롬프트로 바로 생성"
      - MinutesFormat import 제거
    - `ShareReceiverActivity.kt`:
      - `val minutesFormat = userPreferencesRepository.getMinutesFormatOnce()` 삭제 (line 262)
      - Worker enqueue 시 `MinutesGenerationWorker.KEY_MINUTES_FORMAT to minutesFormat.name` 제거 (line 269)
      - MinutesFormat import 제거

    **8단계: Receiver/Service에서 minutesFormat 관련 코드 제거**
    - `PipelineActionReceiver.kt`:
      - `intent.getStringExtra("minutesFormat")` 읽는 코드 삭제 (line 27-28)
      - Worker enqueue 시 `MinutesGenerationWorker.KEY_MINUTES_FORMAT to minutesFormat` 제거 (line 37)
      - MinutesFormat import 제거
    - `PipelineNotificationHelper.kt`:
      - `minutesFormat` 파라미터를 받는 함수에서 해당 파라미터 제거 (line 130)
      - `putExtra("minutesFormat", minutesFormat)` 제거 (line 137)
      - MinutesFormat import 제거

    **9단계: SettingsViewModel에서 minutesFormat 관련 코드 제거**
    - `minutesFormat: StateFlow<MinutesFormat>` 필드 삭제 (line 56-61)
    - `setMinutesFormat()` 메서드 삭제 (line 138-141)
    - MinutesFormat import 제거

    중요: 모든 수정 후 `grep -r "MinutesFormat\|minutesFormat\|MINUTES_FORMAT\|getMinutesFormatOnce\|setMinutesFormat" app/src/main/java/` 으로 잔여 참조가 없는지 확인한다.
  </action>
  <acceptance_criteria>
    - `app/src/main/java/com/autominuting/domain/model/MinutesFormat.kt` 파일이 존재하지 않는다
    - `grep -r "MinutesFormat" app/src/main/java/com/autominuting/` 결과가 0건이다
    - `grep -r "KEY_MINUTES_FORMAT" app/src/main/java/com/autominuting/` 결과가 0건이다
    - `grep -r "getMinutesFormatOnce" app/src/main/java/com/autominuting/` 결과가 0건이다
    - `grep -r "MINUTES_FORMAT_KEY" app/src/main/java/com/autominuting/` 결과가 0건이다
    - `grep -r "SUMMARY_PROMPT\|ACTION_ITEMS_PROMPT" app/src/main/java/com/autominuting/data/minutes/GeminiEngine.kt` 결과가 0건이다
    - GeminiEngine.kt에 `STRUCTURED_PROMPT`가 여전히 존재한다
    - MinutesEngine.kt의 generate() 시그니처가 `suspend fun generate(transcriptText: String, customPrompt: String? = null): Result<String>`이다
  </acceptance_criteria>
  <verify>
    <automated>cd app && grep -rn "MinutesFormat\|KEY_MINUTES_FORMAT\|getMinutesFormatOnce\|MINUTES_FORMAT_KEY" src/main/java/com/autominuting/ | wc -l</automated>
  </verify>
  <done>MinutesFormat enum과 모든 참조가 코드베이스에서 완전히 제거되었고, customPrompt null 시 STRUCTURED_PROMPT가 기본으로 사용된다</done>
</task>

<task type="auto">
  <name>Task 2: 자동모드 Switch를 회의록 설정 섹션으로 이동 + minutesFormat UI 제거 + 빌드 검증</name>
  <files>
    app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt,
    app/src/main/java/com/autominuting/presentation/settings/SettingsViewModel.kt
  </files>
  <read_first>
    app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt,
    app/src/main/java/com/autominuting/presentation/settings/SettingsViewModel.kt
  </read_first>
  <action>
    SettingsScreen.kt에서 두 가지 UI 변경을 수행한다:

    **1. 회의록 형식 드롭다운 제거 (line 194-244 부근)**
    - "회의록 형식" Text, 설명 Text, Spacer, ExposedDropdownMenuBox 전체 블록 삭제
    - `val selectedFormat by viewModel.minutesFormat.collectAsStateWithLifecycle()` 삭제 (line 95)
    - `import com.autominuting.domain.model.MinutesFormat` 삭제 (line 57)
    - `formatLabels` mapOf 블록 삭제

    **2. 자동모드 Switch를 전사 설정 → 회의록 설정으로 이동**
    - "전사 설정" 섹션(SettingsSection(title = "전사 설정"))에서 자동화 모드 관련 코드 블록을 잘라낸다:
      ```kotlin
      // 자동화 모드
      Row(verticalAlignment = ...) { ... Switch ... }
      if (automationMode == AutomationMode.HYBRID) { ... }
      Spacer(modifier = Modifier.height(16.dp))
      ```
    - "회의록 설정" 섹션(SettingsSection(title = "회의록 설정"))의 맨 처음에 붙여넣는다 (프롬프트 템플릿 관리 OutlinedButton 바로 위).

    변경 후 "회의록 설정" 섹션 구조:
    ```
    SettingsSection(title = "회의록 설정") {
        // 1. 완전 자동 모드 Switch (여기로 이동)
        Row { ... Switch ... }
        if (automationMode == AutomationMode.HYBRID) { ... }
        Spacer(16.dp)
        // 2. 프롬프트 템플릿 관리 (기존 유지)
        OutlinedButton(onClick = onNavigateToTemplates) { ... }
        Spacer(16.dp)
        // 3. 기본 프롬프트 템플릿 드롭다운 (기존 유지)
        Text("기본 프롬프트 템플릿")
        ...
        // 회의록 형식 드롭다운은 삭제됨
    }
    ```

    변경 후 "전사 설정" 섹션 구조:
    ```
    SettingsSection(title = "전사 설정") {
        // 자동화 모드는 회의록 설정으로 이동됨
        // 1. STT 엔진 (기존 유지)
        Text("STT 엔진")
        ...
        // 2. Whisper 모델 관리 (기존 유지)
    }
    ```

    **3. 빌드 검증**
    - `./gradlew assembleDebug` 실행하여 컴파일 성공 확인
    - 빌드 실패 시 누락된 import 제거 또는 파라미터 불일치 수정
  </action>
  <acceptance_criteria>
    - SettingsScreen.kt에서 `MinutesFormat` 문자열이 존재하지 않는다
    - SettingsScreen.kt에서 "회의록 형식" 텍스트가 존재하지 않는다
    - SettingsScreen.kt에서 "회의록 설정" 섹션 안에 "완전 자동 모드" 텍스트가 존재한다
    - SettingsScreen.kt에서 "전사 설정" 섹션 안에 "완전 자동 모드" 텍스트가 존재하지 않는다
    - SettingsViewModel.kt에서 `MinutesFormat` 문자열이 존재하지 않는다
    - `./gradlew assembleDebug` 빌드가 성공한다
  </acceptance_criteria>
  <verify>
    <automated>cd "D:/workspace/temp/Auto_Minuting" && ./gradlew assembleDebug 2>&1 | tail -5</automated>
  </verify>
  <done>설정 화면에서 회의록 형식 드롭다운이 제거되고, 자동모드가 회의록 설정 섹션으로 이동되었으며, 전체 빌드가 성공한다</done>
</task>

</tasks>

<verification>
1. MinutesFormat.kt 파일이 삭제되었는지 확인
2. 코드베이스에서 MinutesFormat 참조가 0건인지 grep으로 확인
3. GeminiEngine에서 customPrompt null시 STRUCTURED_PROMPT 폴백 동작 확인
4. 설정 화면에서 자동모드가 회의록 설정 섹션에 위치하는지 확인
5. `./gradlew assembleDebug` 빌드 성공 확인
</verification>

<success_criteria>
- MinutesFormat enum과 모든 관련 코드가 코드베이스에서 완전히 제거됨
- 프롬프트 해결 우선순위가 customPrompt > templateId > STRUCTURED_PROMPT 폴백으로 단순화됨
- 자동모드 Switch가 회의록 설정 섹션에 위치함
- 앱이 컴파일 에러 없이 빌드됨
</success_criteria>

<output>
After completion, create `.planning/phases/35-/35-01-SUMMARY.md`
</output>
