---
phase: 30-prompt-template-selection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/com/autominuting/data/preferences/UserPreferencesRepository.kt
  - app/src/main/java/com/autominuting/worker/MinutesGenerationWorker.kt
  - app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsViewModel.kt
  - app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt
  - app/src/main/java/com/autominuting/presentation/dashboard/DashboardViewModel.kt
  - app/src/main/java/com/autominuting/presentation/dashboard/DashboardScreen.kt
  - app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt
  - app/src/main/java/com/autominuting/presentation/settings/SettingsViewModel.kt
  - app/src/main/java/com/autominuting/worker/TranscriptionTriggerWorker.kt
autonomous: true
requirements:
  - PIPE-03
  - PIPE-04

must_haves:
  truths:
    - "회의록 생성 시 기본 템플릿 미설정이면 프롬프트 템플릿 선택 다이얼로그가 표시된다"
    - "기본 템플릿이 설정되어 있으면 다이얼로그 없이 해당 템플릿으로 회의록이 생성된다"
    - "설정에서 기본 프롬프트 템플릿을 선택할 수 있다"
    - "자동 모드에서도 기본 템플릿 설정에 따라 회의록이 자동 생성된다"
  artifacts:
    - path: "app/src/main/java/com/autominuting/data/preferences/UserPreferencesRepository.kt"
      provides: "기본 템플릿 ID DataStore 저장/조회"
      contains: "DEFAULT_TEMPLATE_ID_KEY"
    - path: "app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt"
      provides: "템플릿 선택 다이얼로그 연동"
      contains: "ManualMinutesDialog"
    - path: "app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt"
      provides: "기본 프롬프트 템플릿 선택 드롭다운"
      contains: "기본 프롬프트 템플릿"
  key_links:
    - from: "TranscriptsViewModel"
      to: "UserPreferencesRepository"
      via: "getDefaultTemplateIdOnce()"
      pattern: "getDefaultTemplateIdOnce"
    - from: "DashboardViewModel"
      to: "UserPreferencesRepository"
      via: "getDefaultTemplateIdOnce()"
      pattern: "getDefaultTemplateIdOnce"
    - from: "MinutesGenerationWorker"
      to: "PromptTemplateRepository"
      via: "getById(templateId)"
      pattern: "getById.*templateId"
---

<objective>
회의록 생성 시 프롬프트 템플릿을 선택하는 기능을 추가하고, 설정에서 기본 템플릿을 지정하면 선택 없이 자동 생성되도록 한다.

Purpose: 사용자가 회의 성격에 맞는 프롬프트 템플릿을 선택하여 최적의 회의록을 생성할 수 있다. 기본 템플릿 설정으로 반복 선택 부담을 줄인다.
Output: 템플릿 선택 다이얼로그 연동, 기본 템플릿 DataStore 설정, 자동/수동 파이프라인 템플릿 적용
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- 기존 코드베이스에서 executor가 필요한 핵심 인터페이스 -->

From domain/model/PromptTemplate.kt:
```kotlin
data class PromptTemplate(
    val id: Long = 0,
    val name: String,
    val promptText: String,
    val isBuiltIn: Boolean = false,
    val createdAt: Instant = Instant.now(),
    val updatedAt: Instant = Instant.now()
)
```

From domain/repository/PromptTemplateRepository.kt:
```kotlin
interface PromptTemplateRepository {
    fun getAll(): Flow<List<PromptTemplate>>
    suspend fun getById(id: Long): PromptTemplate?
    suspend fun insert(template: PromptTemplate): Long
    suspend fun update(template: PromptTemplate)
    suspend fun delete(id: Long)
    suspend fun ensureDefaultTemplates()
}
```

From domain/repository/MinutesRepository.kt:
```kotlin
interface MinutesRepository {
    suspend fun generateMinutes(
        transcriptText: String,
        format: MinutesFormat = MinutesFormat.STRUCTURED,
        customPrompt: String? = null
    ): Result<String>
    fun isGenerating(): Flow<Boolean>
}
```

From domain/model/MinutesFormat.kt:
```kotlin
enum class MinutesFormat { STRUCTURED, SUMMARY, ACTION_ITEMS }
```

From worker/MinutesGenerationWorker.kt companion:
```kotlin
const val KEY_MEETING_ID = "meetingId"
const val KEY_TRANSCRIPT_PATH = "transcriptPath"
const val KEY_MINUTES_PATH = "minutesPath"
const val KEY_MINUTES_FORMAT = "minutesFormat"
```

From presentation/transcripts/ManualMinutesDialog.kt:
```kotlin
@Composable
fun ManualMinutesDialog(
    meetingTitle: String,
    templates: List<PromptTemplate>,
    isGenerating: Boolean,
    onGenerate: (templateId: Long?, customPrompt: String?) -> Unit,
    onDismiss: () -> Unit
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 데이터 레이어 — 기본 템플릿 ID DataStore + Worker 템플릿 적용</name>
  <read_first>
    - app/src/main/java/com/autominuting/data/preferences/UserPreferencesRepository.kt
    - app/src/main/java/com/autominuting/worker/MinutesGenerationWorker.kt
    - app/src/main/java/com/autominuting/worker/TranscriptionTriggerWorker.kt
    - app/src/main/java/com/autominuting/di/RepositoryModule.kt
  </read_first>
  <files>
    app/src/main/java/com/autominuting/data/preferences/UserPreferencesRepository.kt,
    app/src/main/java/com/autominuting/worker/MinutesGenerationWorker.kt,
    app/src/main/java/com/autominuting/worker/TranscriptionTriggerWorker.kt
  </files>
  <action>
    1. **UserPreferencesRepository** — 기본 템플릿 ID 설정 추가 (PIPE-04):
       - `DEFAULT_TEMPLATE_ID_KEY = longPreferencesKey("default_template_id")` 추가
       - `val defaultTemplateId: Flow<Long>` 추가 (기본값 0L = 미설정, 매번 선택)
       - `suspend fun setDefaultTemplateId(id: Long)` 추가
       - `suspend fun getDefaultTemplateIdOnce(): Long` 추가

    2. **MinutesGenerationWorker** — 템플릿 ID 기반 프롬프트 로딩 (PIPE-03):
       - `KEY_TEMPLATE_ID = "templateId"` 상수 추가 (Long)
       - `KEY_CUSTOM_PROMPT = "customPrompt"` 상수 추가 (String?)
       - `PromptTemplateRepository`를 생성자에 주입
       - doWork()에서:
         - inputData에서 templateId(Long, 기본 0)와 customPrompt(String?)를 읽음
         - templateId > 0이면 `promptTemplateRepository.getById(templateId)`로 promptText 조회
         - customPrompt가 있으면 그것 사용
         - 둘 다 없으면 기존 minutesFormat 폴백 유지
         - `minutesRepository.generateMinutes(transcriptText, format, resolvedPrompt)` 호출
       - **기존 KEY_MINUTES_FORMAT은 하위 호환을 위해 유지** (templateId/customPrompt 우선)

    3. **TranscriptionTriggerWorker** — FULL_AUTO 체이닝 시 templateId 전달:
       - 기존 minutesFormat 대신 `KEY_TEMPLATE_ID`를 inputData에 추가
       - inputData에서 templateId를 읽거나, 기본값 0이면 UserPreferencesRepository에서 조회하지 않고 그대로 0 전달 (Worker에서 해결)
       - 자동 모드에서 MinutesGenerationWorker enqueue 시 templateId를 함께 전달
       - templateId가 0이면 MinutesGenerationWorker가 minutesFormat 폴백 사용
  </action>
  <verify>
    <automated>cd /d/workspace/temp/Auto_Minuting && ./gradlew :app:compileDebugKotlin 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -r "DEFAULT_TEMPLATE_ID_KEY" app/src/main/java/ 결과 1건 이상
    - grep -r "KEY_TEMPLATE_ID" app/src/main/java/com/autominuting/worker/MinutesGenerationWorker.kt 결과 1건 이상
    - grep -r "KEY_CUSTOM_PROMPT" app/src/main/java/com/autominuting/worker/MinutesGenerationWorker.kt 결과 1건 이상
    - grep -r "getDefaultTemplateIdOnce" app/src/main/java/com/autominuting/data/preferences/ 결과 1건 이상
  </acceptance_criteria>
  <done>
    UserPreferencesRepository에 defaultTemplateId 설정이 추가되고,
    MinutesGenerationWorker가 templateId로 프롬프트 템플릿을 조회하여 회의록을 생성하며,
    TranscriptionTriggerWorker FULL_AUTO 체이닝에서 templateId를 전달한다.
    컴파일 성공.
  </done>
</task>

<task type="auto">
  <name>Task 2: UI 레이어 — 템플릿 선택 다이얼로그 + 설정 기본 템플릿 + Dashboard 연동</name>
  <read_first>
    - app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt
    - app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsViewModel.kt
    - app/src/main/java/com/autominuting/presentation/transcripts/ManualMinutesDialog.kt
    - app/src/main/java/com/autominuting/presentation/dashboard/DashboardScreen.kt
    - app/src/main/java/com/autominuting/presentation/dashboard/DashboardViewModel.kt
    - app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt
    - app/src/main/java/com/autominuting/presentation/settings/SettingsViewModel.kt
  </read_first>
  <files>
    app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt,
    app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsViewModel.kt,
    app/src/main/java/com/autominuting/presentation/dashboard/DashboardScreen.kt,
    app/src/main/java/com/autominuting/presentation/dashboard/DashboardViewModel.kt,
    app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt,
    app/src/main/java/com/autominuting/presentation/settings/SettingsViewModel.kt
  </files>
  <action>
    **A. TranscriptsViewModel** (PIPE-03, PIPE-04):
    - `PromptTemplateRepository`를 생성자에 주입
    - `val templates: StateFlow<List<PromptTemplate>>` 추가 (promptTemplateRepository.getAll().stateIn)
    - `val defaultTemplateId: StateFlow<Long>` 추가 (userPreferencesRepository.defaultTemplateId.stateIn, 초기값 0L)
    - `generateMinutes(meetingId: Long)` 수정:
      - defaultTemplateId가 0보다 크면 → 바로 Worker enqueue (templateId 전달)
      - defaultTemplateId가 0이면 → 다이얼로그 표시를 위한 상태만 세팅 (showTemplateDialog)
    - `generateMinutesWithTemplate(meetingId: Long, templateId: Long?, customPrompt: String?)` 신규 추가:
      - ManualMinutesDialog에서 선택 후 호출
      - MinutesGenerationWorker enqueue 시 templateId와 customPrompt를 workDataOf에 추가

    **B. TranscriptsScreen** (PIPE-03):
    - `templates`와 `defaultTemplateId`를 collectAsState로 수집
    - 현재 `onGenerateMinutes = { id -> viewModel.generateMinutes(id) }` 동작 변경:
      - defaultTemplateId > 0이면 직접 generateMinutes 호출 (기존과 동일, 내부에서 Worker enqueue)
      - defaultTemplateId == 0이면 ManualMinutesDialog를 표시
    - `var meetingForTemplateSelect by remember { mutableStateOf<Meeting?>(null) }` 상태 추가
    - onGenerateMinutes 콜백에서: defaultTemplateId > 0이면 직접 호출, 아니면 meetingForTemplateSelect에 meeting 세팅
    - meetingForTemplateSelect가 null이 아니면 ManualMinutesDialog 표시:
      - templates 전달, onGenerate에서 viewModel.generateMinutesWithTemplate 호출, onDismiss에서 null로 리셋
    - 회의록 재생성(meetingToRegenerate)도 동일 로직 적용: 기본 템플릿 있으면 바로, 없으면 다이얼로그

    **C. DashboardViewModel** (PIPE-03, PIPE-04):
    - `PromptTemplateRepository`를 생성자에 주입
    - `val templates: StateFlow<List<PromptTemplate>>` 추가
    - `val defaultTemplateId: StateFlow<Long>` 추가 (userPreferencesRepository.defaultTemplateId.stateIn)
    - `generateMinutesForPipeline(meetingId: Long)` 수정:
      - defaultTemplateId > 0이면 templateId로 바로 Worker enqueue
      - defaultTemplateId == 0이면 상태를 세팅하여 다이얼로그 표시 유도
    - `generateMinutesForPipelineWithTemplate(meetingId: Long, templateId: Long?, customPrompt: String?)` 추가:
      - 다이얼로그 선택 후 호출

    **D. DashboardScreen** (PIPE-03):
    - 하이브리드 모드 "회의록 생성" 버튼 클릭 시:
      - defaultTemplateId > 0이면 기존처럼 바로 generateMinutesForPipeline
      - defaultTemplateId == 0이면 ManualMinutesDialog 표시
    - `var showPipelineTemplateDialog by remember { mutableStateOf(false) }` 상태 추가
    - "회의록 생성" 버튼 onClick에서 defaultTemplateId 체크 후 분기

    **E. SettingsViewModel** (PIPE-04):
    - `PromptTemplateRepository`를 생성자에 주입
    - `val templates: StateFlow<List<PromptTemplate>>` 추가
    - `val defaultTemplateId: StateFlow<Long>` 추가 (userPreferencesRepository.defaultTemplateId.stateIn, 초기값 0L)
    - `fun setDefaultTemplateId(id: Long)` 추가

    **F. SettingsScreen** (PIPE-04):
    - 기존 "회의록 형식" 드롭다운(MinutesFormat enum 기반)을 **기본 프롬프트 템플릿** 드롭다운으로 교체:
      - 옵션: "매번 선택" (id=0) + 템플릿 목록 (id=template.id, label=template.name)
      - ExposedDropdownMenuBox 패턴 유지, 선택 시 viewModel.setDefaultTemplateId(id) 호출
    - 기존 MinutesFormat 드롭다운 코드를 제거하고 새 드롭다운으로 대체
    - 라벨: "기본 프롬프트 템플릿", 설명: "기본 템플릿을 설정하면 선택 없이 자동으로 해당 템플릿으로 생성됩니다"
  </action>
  <verify>
    <automated>cd /d/workspace/temp/Auto_Minuting && ./gradlew :app:compileDebugKotlin 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -r "meetingForTemplateSelect" app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt 결과 1건 이상
    - grep -r "generateMinutesWithTemplate" app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsViewModel.kt 결과 1건 이상
    - grep -r "defaultTemplateId" app/src/main/java/com/autominuting/presentation/settings/SettingsViewModel.kt 결과 1건 이상
    - grep -r "기본 프롬프트 템플릿" app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt 결과 1건 이상
    - grep -r "ManualMinutesDialog" app/src/main/java/com/autominuting/presentation/dashboard/DashboardScreen.kt 결과 1건 이상
  </acceptance_criteria>
  <done>
    전사 카드에서 "회의록 작성" 클릭 시 기본 템플릿 없으면 ManualMinutesDialog가 표시되고,
    기본 템플릿이 있으면 다이얼로그 없이 즉시 생성된다.
    대시보드 하이브리드 "회의록 생성" 버튼도 동일하게 동작한다.
    설정에서 기본 프롬프트 템플릿을 선택할 수 있다.
    컴파일 성공.
  </done>
</task>

</tasks>

<verification>
1. `./gradlew :app:compileDebugKotlin` 성공
2. grep으로 핵심 패턴 확인:
   - DEFAULT_TEMPLATE_ID_KEY 존재
   - KEY_TEMPLATE_ID 존재
   - generateMinutesWithTemplate 존재
   - ManualMinutesDialog가 DashboardScreen에서 사용됨
   - 설정에 "기본 프롬프트 템플릿" 표시
</verification>

<success_criteria>
- 회의록 생성 시 기본 템플릿 미설정(0)이면 프롬프트 템플릿 선택 다이얼로그가 표시된다 (PIPE-03)
- 기본 템플릿이 설정되어 있으면 다이얼로그 없이 즉시 생성된다 (PIPE-04)
- 설정에서 기본 프롬프트 템플릿을 드롭다운으로 선택할 수 있다 (PIPE-04)
- 자동 모드(FULL_AUTO)에서도 templateId가 Worker에 전달되어 적용된다 (PIPE-04)
- 기존 3종 프리셋(구조화/요약/액션아이템)이 ManualMinutesDialog 목록에 포함된다 (PIPE-03)
- 전체 빌드 컴파일 성공
</success_criteria>

<output>
After completion, create `.planning/phases/30-/30-01-SUMMARY.md`
</output>
