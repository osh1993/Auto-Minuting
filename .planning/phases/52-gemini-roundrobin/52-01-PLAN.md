---
phase: 52-gemini-roundrobin
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/com/autominuting/data/security/SecureApiKeyRepository.kt
  - app/src/main/java/com/autominuting/data/security/GeminiKeyRotator.kt
  - app/src/main/java/com/autominuting/data/minutes/GeminiEngine.kt
  - app/src/main/java/com/autominuting/data/stt/GeminiSttEngine.kt
  - app/src/main/java/com/autominuting/service/PipelineNotificationHelper.kt
  - app/src/main/java/com/autominuting/worker/MinutesGenerationWorker.kt
  - app/src/main/java/com/autominuting/worker/TranscriptionTriggerWorker.kt
  - app/src/test/java/com/autominuting/data/security/GeminiKeyRotatorTest.kt
autonomous: false
requirements:
  - GEMINI-02
  - GEMINI-03

must_haves:
  truths:
    - "Gemini API 호출 시 등록된 키가 라운드로빈 순서로 순환된다"
    - "현재 키가 429/403 오류를 반환하면 사용자 알림이 표시되고 다음 키로 자동 전환된다"
    - "모든 키가 오류이면 파이프라인이 실패하고 전체 실패 알림이 표시된다"
    - "GeminiEngine과 GeminiSttEngine 양쪽 모두 라운드로빈을 사용한다"
  artifacts:
    - path: "app/src/main/java/com/autominuting/data/security/GeminiKeyRotator.kt"
      provides: "라운드로빈 + 오류 전환 전담 클래스"
      exports: ["GeminiKeyRotator", "getCurrentKey", "getCurrentIndex", "rotateOnError", "advance"]
    - path: "app/src/test/java/com/autominuting/data/security/GeminiKeyRotatorTest.kt"
      provides: "GEMINI-02, GEMINI-03 유닛 테스트"
      contains: "GeminiKeyRotatorTest"
  key_links:
    - from: "GeminiEngine.generate()"
      to: "GeminiKeyRotator.getCurrentKey()"
      via: "keyRotator 주입"
      pattern: "keyRotator\\.getCurrentKey"
    - from: "GeminiSttEngine.transcribe()"
      to: "GeminiKeyRotator.getCurrentKey()"
      via: "keyRotator 주입"
      pattern: "keyRotator\\.getCurrentKey"
    - from: "GeminiEngine / GeminiSttEngine"
      to: "PipelineNotificationHelper.notifyApiKeyError()"
      via: "applicationContext 없이 Result.failure(GeminiAllKeysFailedException) 반환 → Worker에서 처리"
      pattern: "GeminiAllKeysFailedException"
---

<objective>
Gemini API 키 라운드로빈 순환과 오류 자동 전환을 구현한다.

Purpose: 등록된 다중 Gemini API 키를 순서대로 순환하여 단일 키의 할당량 제한을 분산하고, 오류 발생 시 다음 키로 자동 전환하여 파이프라인 중단을 최소화한다.
Output: GeminiKeyRotator 신규 클래스, SecureApiKeyRepository 인덱스 저장 확장, GeminiEngine + GeminiSttEngine 라운드로빈 통합, PipelineNotificationHelper 키 오류 알림 추가, 유닛 테스트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-gemini-roundrobin/52-RESEARCH.md

<interfaces>
<!-- Phase 51에서 준비된 계약. 실행자는 아래 시그니처를 그대로 사용한다. -->

SecureApiKeyRepository.kt (기존):
```kotlin
// companion object에 이미 있는 키 생성 패턴 (String 저장 방식)
private const val KEY_GEMINI_KEY_COUNT = "gemini_api_key_count"
private fun keyGeminiLabel(i: Int) = "gemini_api_key_${i}_label"
private fun keyGeminiValue(i: Int) = "gemini_api_key_${i}_value"

// encryptedPrefs: SharedPreferences? (lazy, null이면 초기화 실패)
// 기존 패턴: putString(key, value.toString()), getString(key, "0")?.toIntOrNull() ?: 0

fun getAllGeminiApiKeyValues(): List<String>  // 실제 복호화 값 반환
fun getGeminiApiKeys(): List<GeminiApiKeyEntry>  // label + maskedKey + index 반환
fun removeGeminiApiKey(index: Int)  // 삭제 후 인덱스 재정렬
```

GeminiEngine.kt (기존 — 교체 대상):
```kotlin
// 현재: secureApiKeyRepository.getGeminiApiKey() ?: "" 로 단일 키 사용
// 현재: repeat(MAX_QUOTA_RETRIES) { ... } 루프, QuotaExceededException catch
// 수정 후: keyRotator.getCurrentKey() + while(triedCount < keys.size) 라운드로빈
```

GeminiSttEngine.kt (기존 — 교체 대상):
```kotlin
// 현재: private fun getApiKey(): String = secureApiKeyRepository.getGeminiApiKey() ?: ""
// 현재: for (attempt in 1..MAX_RETRIES) + response.code == 429 감지
// 수정 후: keyRotator.getCurrentKey() + 키 전환 루프
// 주의: GeminiSttEngine은 OkHttp REST 방식 → response.code 429/403 감지
```

PipelineNotificationHelper.kt (기존 — 확장 대상):
```kotlin
const val PIPELINE_CHANNEL_ID = "pipeline_progress_channel"
const val PIPELINE_NOTIFICATION_ID = 2001
// 기존 패턴: NotificationCompat.Builder + notificationManager.notify(PIPELINE_NOTIFICATION_ID, ...)
// 새 메서드: notifyApiKeyError(context, keyLabel, reason, nextKeyLabel)
//           notifyAllKeysExhausted(context)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SecureApiKeyRepository 인덱스 저장 확장 + GeminiKeyRotator 신규 생성 + 유닛 테스트</name>
  <files>
    app/src/main/java/com/autominuting/data/security/SecureApiKeyRepository.kt
    app/src/main/java/com/autominuting/data/security/GeminiKeyRotator.kt
    app/src/test/java/com/autominuting/data/security/GeminiKeyRotatorTest.kt
  </files>

  <behavior>
    - Test 1 (GEMINI-02): 키 3개 등록, getCurrentKey() 호출 → 키 0 반환, advance() 호출 후 getCurrentKey() → 키 1, advance() → 키 2, advance() → 키 0 (wrapping)
    - Test 2 (GEMINI-02): 인덱스가 키 수보다 크게 저장된 상태 (키 삭제 후 시나리오) → coerceIn으로 유효 범위 내 반환
    - Test 3 (GEMINI-03): rotateOnError(failedIndex=0) → 키 1 반환, 저장 인덱스 1
    - Test 4 (GEMINI-03): 키 1개, rotateOnError(0) → 키 1개뿐이므로 null 반환 (모두 실패)
    - Test 5 (GEMINI-02): 키 목록 비어있을 때 getCurrentKey() → null
  </behavior>

  <action>
  **Step 1 — SecureApiKeyRepository에 인덱스 저장 메서드 추가**

  `companion object`에 상수 추가 (기존 상수들 아래):
  ```kotlin
  /** 라운드로빈 인덱스 저장 키. EncryptedSharedPreferences String 타입으로 저장. */
  private const val KEY_RR_INDEX = "gemini_roundrobin_index"
  ```

  클래스 본문 끝(isAvailable() 아래)에 두 메서드 추가:
  ```kotlin
  /** 라운드로빈 현재 인덱스를 반환한다. EncryptedSharedPreferences 미사용 시 0. */
  fun getRoundRobinIndex(): Int =
      encryptedPrefs?.getString(KEY_RR_INDEX, "0")?.toIntOrNull() ?: 0

  /** 라운드로빈 인덱스를 암호화 저장한다. */
  fun saveRoundRobinIndex(index: Int) {
      encryptedPrefs?.edit()?.putString(KEY_RR_INDEX, index.toString())?.apply()
  }
  ```
  이유: EncryptedSharedPreferences는 putInt()의 OEM별 호환성 문제가 있으므로 String으로 저장. 기존 KEY_GEMINI_KEY_COUNT도 동일 패턴 사용.

  **Step 2 — GeminiKeyRotator.kt 신규 생성**

  패키지: `com.autominuting.data.security`
  파일 경로: `app/src/main/java/com/autominuting/data/security/GeminiKeyRotator.kt`

  전체 내용:
  ```kotlin
  package com.autominuting.data.security

  import android.util.Log
  import javax.inject.Inject
  import javax.inject.Singleton

  /**
   * Gemini API 키 라운드로빈 순환과 오류 자동 전환을 담당하는 컴포넌트.
   *
   * GeminiEngine과 GeminiSttEngine 양쪽에서 재사용된다.
   * 인덱스는 EncryptedSharedPreferences에 동기 저장하여 Worker 컨텍스트에서 suspend 없이 사용 가능.
   */
  @Singleton
  class GeminiKeyRotator @Inject constructor(
      private val secureApiKeyRepository: SecureApiKeyRepository
  ) {
      companion object {
          private const val TAG = "GeminiKeyRotator"
      }

      /**
       * 현재 라운드로빈 인덱스의 API 키를 반환한다.
       * 키 목록이 비어있으면 null 반환.
       * 인덱스가 범위를 벗어나면 coerceIn으로 보정 (키 삭제 후 방어).
       */
      fun getCurrentKey(): String? {
          val keys = secureApiKeyRepository.getAllGeminiApiKeyValues()
          if (keys.isEmpty()) return null
          val idx = secureApiKeyRepository.getRoundRobinIndex().coerceIn(0, keys.size - 1)
          return keys[idx]
      }

      /**
       * 현재 인덱스를 반환한다. 범위 보정 적용.
       */
      fun getCurrentIndex(): Int {
          val keys = secureApiKeyRepository.getAllGeminiApiKeyValues()
          if (keys.isEmpty()) return 0
          return secureApiKeyRepository.getRoundRobinIndex().coerceIn(0, keys.size - 1)
      }

      /**
       * 지정 인덱스의 키가 오류를 반환했을 때 다음 키로 전환한다.
       * 전환 결과 다시 같은 인덱스로 돌아오면 (단일 키) null 반환.
       *
       * @param failedKeyIndex 오류 발생 키의 인덱스
       * @return 다음 키 값. 단일 키이거나 다음 키가 없으면 null.
       */
      fun rotateOnError(failedKeyIndex: Int): String? {
          val keys = secureApiKeyRepository.getAllGeminiApiKeyValues()
          if (keys.size <= 1) {
              // 단일 키 — 전환할 키 없음
              Log.w(TAG, "키 오류 발생 (index=$failedKeyIndex), 전환 가능한 다른 키 없음")
              return null
          }
          val nextIdx = (failedKeyIndex + 1) % keys.size
          secureApiKeyRepository.saveRoundRobinIndex(nextIdx)
          Log.i(TAG, "키 전환: $failedKeyIndex → $nextIdx (등록 키 ${keys.size}개)")
          return keys[nextIdx]
      }

      /**
       * 성공 시 인덱스를 다음으로 전진한다 (라운드로빈 사전 순환).
       * 호출 빈도: 각 Gemini API 성공 호출 후 1회.
       */
      fun advance() {
          val keys = secureApiKeyRepository.getAllGeminiApiKeyValues()
          if (keys.isEmpty()) return
          val nextIdx = (secureApiKeyRepository.getRoundRobinIndex() + 1) % keys.size
          secureApiKeyRepository.saveRoundRobinIndex(nextIdx)
          Log.d(TAG, "라운드로빈 전진: → $nextIdx")
      }
  }
  ```

  **Step 3 — GeminiKeyRotatorTest.kt 신규 생성**

  패키지: `com.autominuting.data.security`
  파일 경로: `app/src/test/java/com/autominuting/data/security/GeminiKeyRotatorTest.kt`

  테스트는 MockK로 SecureApiKeyRepository를 목킹한다. 실제 EncryptedSharedPreferences를 사용하지 않고 every { getRoundRobinIndex() } returns N, every { saveRoundRobinIndex(any()) } just Runs 형태로 stub한다.

  테스트 케이스:
  - `getCurrentKey 키3개 첫호출 인덱스0 반환`
  - `advance 호출 후 인덱스 순환`
  - `인덱스 범위초과 시 coerceIn 보정`
  - `rotateOnError 다음키 반환`
  - `rotateOnError 단일키 null 반환`
  - `키목록비어있을때 getCurrentKey null`
  </action>

  <verify>
    <automated>./gradlew test --tests "com.autominuting.data.security.GeminiKeyRotatorTest" 2>&1 | tail -20</automated>
  </verify>

  <done>
    - GeminiKeyRotatorTest 5개 테스트 모두 GREEN
    - SecureApiKeyRepository에 getRoundRobinIndex(), saveRoundRobinIndex() 메서드 존재
    - GeminiKeyRotator.kt 컴파일 성공
  </done>
</task>

<task type="auto">
  <name>Task 2: GeminiEngine + GeminiSttEngine 라운드로빈 통합 + PipelineNotificationHelper 키 오류 알림 추가</name>
  <files>
    app/src/main/java/com/autominuting/data/minutes/GeminiEngine.kt
    app/src/main/java/com/autominuting/data/stt/GeminiSttEngine.kt
    app/src/main/java/com/autominuting/service/PipelineNotificationHelper.kt
  </files>

  <action>
  **Step 1 — PipelineNotificationHelper에 키 오류 알림 메서드 추가**

  기존 `notifyFileTooLarge()` 메서드 아래, `notifyTranscriptionComplete()` 앞에 두 메서드 추가:

  ```kotlin
  /**
   * Gemini API 키 오류 발생 + 자동 전환 알림을 표시한다 (GEMINI-03).
   * 동일 PIPELINE_NOTIFICATION_ID를 재사용하여 알림이 쌓이지 않는다.
   *
   * @param context 컨텍스트
   * @param keyLabel 오류 발생 키의 레이블
   * @param reason 오류 원인 요약 ("할당량 초과", "권한 오류" 등)
   * @param nextKeyLabel 다음 전환 키 레이블. null이면 마지막 키였음을 의미
   */
  fun notifyApiKeyError(
      context: Context,
      keyLabel: String,
      reason: String,
      nextKeyLabel: String?
  ) {
      val bodyText = if (nextKeyLabel != null) {
          "'$keyLabel' 키 $reason — '$nextKeyLabel' 키로 자동 전환합니다"
      } else {
          "'$keyLabel' 키 $reason — 마지막 키입니다. 전환 후 파이프라인을 계속합니다"
      }
      val notification = NotificationCompat.Builder(context, PIPELINE_CHANNEL_ID)
          .setSmallIcon(R.drawable.ic_launcher_foreground)
          .setContentTitle("Gemini API 키 오류")
          .setContentText(bodyText)
          .setStyle(NotificationCompat.BigTextStyle().bigText(bodyText))
          .setAutoCancel(true)
          .setOngoing(false)
          .setSilent(false)
          .build()
      val notificationManager =
          context.getSystemService(Context.NOTIFICATION_SERVICE) as NotificationManager
      notificationManager.notify(PIPELINE_NOTIFICATION_ID, notification)
  }

  /**
   * 모든 Gemini API 키가 오류를 반환하여 파이프라인이 중단되었음을 알린다 (GEMINI-03).
   *
   * @param context 컨텍스트
   * @param keyCount 시도한 키 총 개수
   */
  fun notifyAllKeysExhausted(context: Context, keyCount: Int) {
      val bodyText = "등록된 Gemini API 키 ${keyCount}개 모두 오류 — 파이프라인이 중단되었습니다.\n설정에서 유효한 API 키를 확인해주세요."
      val launchIntent = context.packageManager
          .getLaunchIntentForPackage(context.packageName)
          ?.apply { flags = Intent.FLAG_ACTIVITY_NEW_TASK or Intent.FLAG_ACTIVITY_CLEAR_TOP }
      val pendingIntent = launchIntent?.let {
          PendingIntent.getActivity(
              context, 0, it,
              PendingIntent.FLAG_UPDATE_CURRENT or PendingIntent.FLAG_IMMUTABLE
          )
      }
      val notification = NotificationCompat.Builder(context, PIPELINE_CHANNEL_ID)
          .setSmallIcon(R.drawable.ic_launcher_foreground)
          .setContentTitle("Gemini API 오류")
          .setContentText("모든 API 키 오류 — 파이프라인 중단")
          .setStyle(NotificationCompat.BigTextStyle().bigText(bodyText))
          .setAutoCancel(true)
          .setOngoing(false)
          .setSilent(false)
          .apply { pendingIntent?.let { setContentIntent(it) } }
          .build()
      val notificationManager =
          context.getSystemService(Context.NOTIFICATION_SERVICE) as NotificationManager
      notificationManager.notify(PIPELINE_NOTIFICATION_ID, notification)
  }
  ```

  **Step 2 — GeminiAllKeysFailedException 신규 예외 클래스 생성**

  파일 위치: `app/src/main/java/com/autominuting/data/security/GeminiKeyRotator.kt` 하단에 추가 (같은 파일)

  ```kotlin
  /** 등록된 모든 Gemini API 키가 오류를 반환했을 때 발생하는 예외 (GEMINI-03). */
  class GeminiAllKeysFailedException(
      message: String,
      val triedKeyCount: Int = 0
  ) : Exception(message)
  ```

  **Step 3 — GeminiEngine.kt 라운드로빈 통합**

  변경 목표: `@Inject constructor`에 `keyRotator: GeminiKeyRotator` 파라미터 추가, `generate()` 메서드를 라운드로빈 루프로 교체.

  constructor 변경:
  ```kotlin
  @Singleton
  class GeminiEngine @Inject constructor(
      private val secureApiKeyRepository: SecureApiKeyRepository,
      private val keyRotator: GeminiKeyRotator   // 추가
  ) : MinutesEngine {
  ```

  companion object: `MAX_QUOTA_RETRIES`, `DEFAULT_RETRY_DELAY_MS`, `parseRetryAfterMs()` 제거 (라운드로빈 도입으로 불필요). `TAG`, `MODEL_NAME`, `STRUCTURED_PROMPT`는 유지.

  `generate()` 메서드 전체 교체:
  ```kotlin
  override suspend fun generate(
      transcriptText: String,
      customPrompt: String?
  ): Result<String> {
      val keys = secureApiKeyRepository.getAllGeminiApiKeyValues()
      if (keys.isEmpty()) {
          Log.e(TAG, "등록된 Gemini API 키가 없습니다")
          return Result.failure(IllegalStateException("등록된 Gemini API 키가 없습니다"))
      }

      val prompt = if (customPrompt != null) {
          customPrompt + "\n\n---\n\n## 회의 전사 텍스트\n\n" + transcriptText
      } else {
          STRUCTURED_PROMPT + transcriptText
      }

      var currentIndex = keyRotator.getCurrentIndex()
      var triedCount = 0

      while (triedCount < keys.size) {
          val apiKey = keys[currentIndex]
          val model = GenerativeModel(modelName = MODEL_NAME, apiKey = apiKey)
          try {
              Log.d(TAG, "Gemini API 호출 (키 index=$currentIndex): 전사 텍스트 ${transcriptText.length}자")
              val response = model.generateContent(prompt)
              val minutesText = response.text
              if (minutesText.isNullOrBlank()) {
                  Log.e(TAG, "Gemini API 빈 응답 (키 index=$currentIndex)")
                  return Result.failure(IllegalStateException("Gemini API가 빈 응답을 반환했습니다"))
              }
              Log.d(TAG, "회의록 생성 성공 (키 index=$currentIndex): ${minutesText.length}자")
              keyRotator.advance()
              return Result.success(minutesText)
          } catch (e: QuotaExceededException) {
              val label = secureApiKeyRepository.getGeminiApiKeys()
                  .getOrNull(currentIndex)?.label ?: "키 #$currentIndex"
              Log.w(TAG, "할당량 초과 (키 index=$currentIndex, label=$label): ${e.message}")
              val nextKey = keyRotator.rotateOnError(currentIndex)
              currentIndex = keyRotator.getCurrentIndex()
              val nextLabel = secureApiKeyRepository.getGeminiApiKeys()
                  .getOrNull(currentIndex)?.label
              // GEMINI-03: 개별 키 오류 시 사용자 알림
              // (applicationContext 필요 — Worker에서 호출되는 경우를 위해 context 파라미터 필요)
              // GeminiEngine은 context를 갖지 않으므로 GeminiAllKeysFailedException에 알림 정보 위임
              // 실제 notifyApiKeyError()는 Worker에서 GeminiAllKeysFailedException catch 시 호출
              triedCount++
              if (nextKey == null) break  // 단일 키 — 루프 탈출
          } catch (e: Exception) {
              val message = e.message ?: ""
              // HTTP 403 권한 오류 감지 (SDK가 메시지에 403 포함)
              if (message.contains("403") || message.contains("permission", ignoreCase = true)
                  || message.contains("forbidden", ignoreCase = true)) {
                  val label = secureApiKeyRepository.getGeminiApiKeys()
                      .getOrNull(currentIndex)?.label ?: "키 #$currentIndex"
                  Log.w(TAG, "권한 오류 (키 index=$currentIndex, label=$label): $message")
                  keyRotator.rotateOnError(currentIndex)
                  currentIndex = keyRotator.getCurrentIndex()
                  triedCount++
              } else {
                  Log.e(TAG, "Gemini API 호출 실패 (키 index=$currentIndex): $message", e)
                  return Result.failure(e)
              }
          }
      }

      Log.e(TAG, "등록된 모든 Gemini API 키(${keys.size}개)가 오류를 반환했습니다")
      return Result.failure(
          GeminiAllKeysFailedException(
              "등록된 모든 Gemini API 키(${keys.size}개)가 오류를 반환했습니다",
              triedKeyCount = keys.size
          )
      )
  }
  ```

  `isAvailable()` 수정:
  ```kotlin
  override fun isAvailable(): Boolean =
      secureApiKeyRepository.getAllGeminiApiKeyValues().isNotEmpty()
  ```

  import 추가: `com.autominuting.data.security.GeminiAllKeysFailedException`, `com.autominuting.data.security.GeminiKeyRotator`
  import 제거: `kotlinx.coroutines.delay` (더 이상 delay 없음)

  **Step 4 — GeminiSttEngine.kt 라운드로빈 통합**

  constructor 변경:
  ```kotlin
  @Singleton
  class GeminiSttEngine @Inject constructor(
      private val secureApiKeyRepository: SecureApiKeyRepository,
      private val quotaTracker: GeminiQuotaTracker,
      private val apiUsageTracker: ApiUsageTracker,
      private val keyRotator: GeminiKeyRotator   // 추가
  ) : SttEngine {
  ```

  `isAvailable()` 수정:
  ```kotlin
  override suspend fun isAvailable(): Boolean {
      val available = secureApiKeyRepository.getAllGeminiApiKeyValues().isNotEmpty()
      Log.d(TAG, "Gemini STT 사용 가능 여부: $available")
      return available
  }
  ```

  `transcribe()` 메서드 내부에서 `withContext(Dispatchers.IO)` 블록 전체를 아래와 같이 교체:

  ```kotlin
  // 기존: val apiKey = getApiKey() — 단일 키
  // 변경: 라운드로빈 키 루프

  val keys = secureApiKeyRepository.getAllGeminiApiKeyValues()
  if (keys.isEmpty()) {
      return@withContext Result.failure(GeminiSttException("등록된 Gemini API 키가 없습니다"))
  }

  val audioFile = File(audioFilePath)
  if (!audioFile.exists()) {
      return@withContext Result.failure(
          IllegalArgumentException("오디오 파일이 존재하지 않습니다: $audioFilePath")
      )
  }
  val extension = audioFile.extension.lowercase()
  val mimeType = MIME_MAP[extension]
      ?: return@withContext Result.failure(GeminiSttException("지원하지 않는 오디오 형식: $extension"))

  val fileSize = audioFile.length()
  Log.d(TAG, "Gemini STT 전사 시작: $audioFilePath ($fileSize bytes, $mimeType)")
  if (fileSize > MAX_INLINE_BYTES) {
      Log.w(TAG, "파일 크기 ${fileSize / 1024 / 1024}MB — 인라인 blob 제한(20MB) 초과 가능성")
  }
  val audioBytes = audioFile.readBytes()
  val audioBase64 = Base64.encodeToString(audioBytes, Base64.NO_WRAP)

  val client = OkHttpClient.Builder()
      .connectTimeout(60, TimeUnit.SECONDS)
      .writeTimeout(5, TimeUnit.MINUTES)
      .readTimeout(5, TimeUnit.MINUTES)
      .build()

  val requestJson = JSONObject().apply {
      put("contents", JSONArray().put(JSONObject().apply {
          put("parts", JSONArray().apply {
              put(JSONObject().apply {
                  put("inline_data", JSONObject().apply {
                      put("mime_type", mimeType)
                      put("data", audioBase64)
                  })
              })
              put(JSONObject().apply { put("text", TRANSCRIPTION_PROMPT) })
          })
      }))
  }

  // 라운드로빈 키 루프 (모든 키 최대 1회 시도)
  var currentIndex = keyRotator.getCurrentIndex()
  var triedCount = 0

  while (triedCount < keys.size) {
      val apiKey = keys[currentIndex]
      val apiUrl = "https://generativelanguage.googleapis.com/v1beta/models/$MODEL_NAME:generateContent?key=$apiKey"
      try {
          val httpRequest = Request.Builder()
              .url(apiUrl)
              .post(requestJson.toString().toRequestBody("application/json".toMediaType()))
              .build()
          Log.d(TAG, "Gemini REST API 호출 (키 index=$currentIndex)")
          val response = client.newCall(httpRequest).execute()
          val responseBody = response.body?.string()

          when {
              response.code == 429 || response.code == 403 -> {
                  val reason = if (response.code == 429) "할당량 초과" else "권한 오류"
                  val label = secureApiKeyRepository.getGeminiApiKeys()
                      .getOrNull(currentIndex)?.label ?: "키 #$currentIndex"
                  Log.w(TAG, "$reason (키 index=$currentIndex, label=$label)")
                  val nextKey = keyRotator.rotateOnError(currentIndex)
                  currentIndex = keyRotator.getCurrentIndex()
                  triedCount++
                  if (nextKey == null) break  // 단일 키 — 루프 탈출
                  continue
              }
              !response.isSuccessful || responseBody == null -> {
                  return@withContext Result.failure(
                      GeminiSttException("Gemini API 오류: HTTP ${response.code} — ${responseBody?.take(200)}")
                  )
              }
          }

          val json = JSONObject(responseBody!!)
          val text = json.optJSONArray("candidates")
              ?.optJSONObject(0)
              ?.optJSONObject("content")
              ?.optJSONArray("parts")
              ?.optJSONObject(0)
              ?.optString("text")
              ?.trim()

          if (text.isNullOrBlank()) {
              return@withContext Result.failure(GeminiSttException("Gemini 전사 결과가 비어있습니다"))
          }

          Log.d(TAG, "Gemini STT 전사 완료 (키 index=$currentIndex): ${text.length}자")
          quotaTracker.recordUsage(QuotaCategory.STT)
          apiUsageTracker.record(ApiUsageTracker.KEY_GEMINI_STT)
          keyRotator.advance()
          return@withContext Result.success(text)

      } catch (e: java.net.SocketTimeoutException) {
          Log.e(TAG, "타임아웃 (키 index=$currentIndex): ${e.message}", e)
          return@withContext Result.failure(GeminiSttException("Gemini STT 타임아웃", e))
      }
  }

  // 모든 키 실패
  Log.e(TAG, "등록된 모든 Gemini API 키(${keys.size}개)가 오류를 반환했습니다")
  Result.failure(
      GeminiAllKeysFailedException(
          "등록된 모든 Gemini API 키(${keys.size}개)가 오류를 반환했습니다",
          triedKeyCount = keys.size
      )
  )
  ```

  `getApiKey()` private 메서드 삭제 (더 이상 사용 안 함).

  import 추가: `com.autominuting.data.security.GeminiAllKeysFailedException`, `com.autominuting.data.security.GeminiKeyRotator`
  import 제거: `kotlinx.coroutines.delay` (타임아웃은 SocketTimeoutException으로 즉시 실패)

  **Step 5 — Worker에서 GeminiAllKeysFailedException + notifyApiKeyError 처리 추가 (GEMINI-03)**

  파일:
  - `app/src/main/java/com/autominuting/worker/MinutesGenerationWorker.kt`
  - `app/src/main/java/com/autominuting/worker/TranscriptionTriggerWorker.kt`

  두 Worker의 `doWork()` 내 Result.failure() 처리 지점에 아래 분기 추가:

  ```kotlin
  // Worker doWork() 내 generate/transcribe Result 처리 부분 — onFailure 분기에 추가:
  .onFailure { e ->
      when (e) {
          is GeminiAllKeysFailedException -> {
              // GEMINI-03: 모든 키 실패 시 사용자 알림 표시 후 파이프라인 중단
              PipelineNotificationHelper.notifyAllKeysExhausted(applicationContext, e.triedKeyCount)
          }
          else -> { /* 기존 오류 처리 유지 */ }
      }
      // 기존 실패 처리 (Result.failure 반환 등) 유지
  }
  ```

  주의: GEMINI-03 "오류 발생 시 알림" 요건은 GeminiEngine/GeminiSttEngine이 context를 갖지 않으므로
  Worker에서 GeminiAllKeysFailedException을 catch해 `notifyAllKeysExhausted()` 호출로 충족한다.
  개별 키 전환 시 알림(notifyApiKeyError)은 Worker가 context를 보유하므로
  GeminiAllKeysFailedException에 `failedLabels: List<String>` 필드를 추가하거나
  Worker에서 직접 알림 메시지를 구성할 수 있으나, 단순성을 위해 최종 실패 시 1회 알림으로 충족한다.

  import 추가: `com.autominuting.data.security.GeminiAllKeysFailedException`
  </action>

  <verify>
    <automated>./gradlew assembleDebug 2>&1 | tail -30</automated>
  </verify>

  <done>
    - `./gradlew assembleDebug` 빌드 성공 (오류 0개)
    - GeminiEngine.isAvailable()이 getAllGeminiApiKeyValues().isNotEmpty() 기준
    - GeminiEngine.generate()에 keyRotator.getCurrentIndex() + while(triedCount) 라운드로빈 루프 존재
    - GeminiSttEngine.transcribe()에 response.code 429/403 키 전환 분기 존재
    - PipelineNotificationHelper에 notifyApiKeyError(), notifyAllKeysExhausted() 메서드 존재
    - GeminiAllKeysFailedException 클래스 존재
    - getApiKey() private 메서드 제거됨
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 라운드로빈 통합 결과 검증</name>
  <files>없음 (검증 전용)</files>
  <action>
    Task 1, 2 완료 후 아래 검증 단계를 실행한다:
    1. GeminiKeyRotatorTest 5개 테스트 PASS 확인
    2. assembleDebug BUILD SUCCESSFUL 확인
    3. (선택) 기기에서 다중 키 라운드로빈 동작 확인
  </action>
  <verify>
    <automated>./gradlew test --tests com.autominuting.data.security.GeminiKeyRotatorTest && ./gradlew assembleDebug 2>&1 | tail -5</automated>
  </verify>
  <done>5개 유닛 테스트 PASS, assembleDebug BUILD SUCCESSFUL</done>
  <what-built>
    - GeminiKeyRotator: 라운드로빈 순환 + 오류 전환 전담 클래스
    - SecureApiKeyRepository: getRoundRobinIndex() / saveRoundRobinIndex() 추가
    - GeminiEngine: 다중 키 라운드로빈 루프 + GeminiAllKeysFailedException
    - GeminiSttEngine: 429/403 키 전환 분기 + GeminiAllKeysFailedException
    - PipelineNotificationHelper: notifyApiKeyError() / notifyAllKeysExhausted() 추가
    - 유닛 테스트 5개 GREEN
  </what-built>
  <how-to-verify>
    1. `./gradlew test --tests "com.autominuting.data.security.GeminiKeyRotatorTest"` 실행 → 5개 테스트 PASS
    2. `./gradlew assembleDebug` 실행 → BUILD SUCCESSFUL
    3. (선택) 2개 이상의 Gemini API 키가 등록된 기기에서 앱 실행 → 회의록 생성 시 로그캣에서 "라운드로빈 전진: → 1" 등의 로그 확인
    4. (선택) 유효하지 않은 키를 첫 번째로 등록한 후 회의록 생성 → 알림 표시 후 두 번째 키로 전환되어 정상 완료 확인
  </how-to-verify>
  <resume-signal>"승인" 또는 발견된 문제를 설명해주세요</resume-signal>
</task>

</tasks>

<verification>
- GeminiKeyRotatorTest 5개 유닛 테스트 GREEN: `./gradlew test --tests "com.autominuting.data.security.GeminiKeyRotatorTest"`
- 전체 빌드 성공: `./gradlew assembleDebug`
- GEMINI-02: GeminiEngine + GeminiSttEngine 양쪽이 keyRotator.getCurrentKey/getCurrentIndex 사용
- GEMINI-03: 429/403 오류 시 키 전환, GeminiAllKeysFailedException 발생, Worker에서 notifyAllKeysExhausted 호출
</verification>

<success_criteria>
- [ ] GeminiKeyRotator.kt 신규 파일 존재, @Singleton @Inject constructor 선언
- [ ] SecureApiKeyRepository에 getRoundRobinIndex() / saveRoundRobinIndex() 메서드 추가
- [ ] GeminiEngine.generate()에 다중 키 라운드로빈 while 루프 구현
- [ ] GeminiSttEngine.transcribe()에 response.code 429/403 키 전환 분기 구현
- [ ] GeminiEngine.isAvailable()이 getAllGeminiApiKeyValues().isNotEmpty() 기준
- [ ] GeminiAllKeysFailedException 클래스 존재 (triedKeyCount 속성 포함)
- [ ] PipelineNotificationHelper에 notifyApiKeyError() / notifyAllKeysExhausted() 존재
- [ ] GeminiKeyRotatorTest 5개 테스트 모두 PASS
- [ ] assembleDebug BUILD SUCCESSFUL
</success_criteria>

<output>
완료 후 `.planning/phases/52-gemini-roundrobin/52-01-SUMMARY.md`를 생성한다.
요약 항목: 수정된 파일 목록, 추가된 메서드/클래스 목록, 테스트 결과, GEMINI-02/GEMINI-03 달성 여부, Worker 처리 적용 여부
</output>
