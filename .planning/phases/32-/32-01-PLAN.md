---
phase: "32"
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/com/autominuting/presentation/share/ShareReceiverActivity.kt
autonomous: true
requirements:
  - SHARE-01

must_haves:
  truths:
    - "다른 앱에서 web.plaud.ai 링크를 텍스트로 공유하면 앱이 수신하여 처리한다"
    - "공유받은 Plaud 링크에서 WebView를 통해 S3 오디오 URL이 자동 추출된다"
    - "추출된 오디오가 다운로드되어 MeetingEntity(AUDIO_RECEIVED)로 저장되고 TranscriptionTriggerWorker가 시작된다"
    - "Plaud 링크가 아닌 일반 텍스트는 기존 텍스트 공유 경로로 정상 처리된다"
  artifacts:
    - path: "app/src/main/java/com/autominuting/presentation/share/ShareReceiverActivity.kt"
      provides: "Plaud URL 감지 + WebView 추출 + OkHttp 다운로드 + 전사 파이프라인 진입"
      contains: "isPlaudShareUrl"
  key_links:
    - from: "ShareReceiverActivity"
      to: "WebView S3 인터셉트"
      via: "shouldInterceptRequest에서 amazonaws.com/audiofiles/ 패턴 감지"
      pattern: "amazonaws.com.*audiofiles"
    - from: "ShareReceiverActivity"
      to: "TranscriptionTriggerWorker"
      via: "OkHttp 다운로드 후 WorkManager enqueue"
      pattern: "TranscriptionTriggerWorker"
---

<objective>
ShareReceiverActivity에서 Plaud 공유 링크(web.plaud.ai)를 감지하면 WebView로 S3 오디오 URL을 추출하고, OkHttp로 다운로드하여 전사 파이프라인에 자동 진입시킨다.

Purpose: 사용자가 다른 앱(카카오톡, 메신저 등)에서 Plaud 공유 링크를 받으면 Auto Minuting 앱으로 공유만 하면 자동으로 회의록이 생성되는 원클릭 파이프라인 확장.
Output: ShareReceiverActivity에 Plaud URL 처리 경로 추가
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

@app/src/main/java/com/autominuting/presentation/share/ShareReceiverActivity.kt
@app/src/main/java/com/autominuting/presentation/dashboard/DashboardViewModel.kt
@app/src/main/java/com/autominuting/presentation/dashboard/DashboardScreen.kt
@app/src/main/AndroidManifest.xml

<interfaces>
<!-- ShareReceiverActivity가 이미 사용하는 핵심 의존성 -->

From DashboardViewModel.kt (참조용 패턴 — 직접 호출하지 않음):
```kotlin
// Plaud URL 판별 패턴
private fun isPlaudShareUrl(url: String): Boolean =
    url.contains("web.plaud.ai/s/") || url.contains("web.plaud.ai/nshare/")

// WebView에서 오디오 URL이 추출되면 downloadDirectUrl() 호출하여 OkHttp 다운로드
fun onPlaudAudioUrlExtracted(audioUrl: String)
```

From DashboardScreen.kt (WebView 패턴 — ShareReceiverActivity에서 동일 패턴 사용):
```kotlin
// S3 presigned URL 인터셉트 패턴
if (requestUrl.contains("amazonaws.com") && requestUrl.contains("audiofiles/") && !audioFound)

// 페이지 로드 완료 후 재생 버튼 자동 클릭 JS
view?.evaluateJavascript("""
    (function() {
        var audios = document.querySelectorAll('audio');
        for (var i = 0; i < audios.length; i++) { audios[i].play().catch(function(){}); }
        var buttons = document.querySelectorAll('button, [role="button"], .play-btn, svg');
        for (var i = 0; i < buttons.length; i++) { buttons[i].click(); }
    })();
""", null)
```

From ShareReceiverActivity.kt (기존 파이프라인 진입 패턴):
```kotlin
// 음성 파일 저장 후 전사 파이프라인 진입
val workRequest = OneTimeWorkRequestBuilder<TranscriptionTriggerWorker>()
    .setInputData(workDataOf(
        TranscriptionTriggerWorker.KEY_AUDIO_FILE_PATH to audioFile.absolutePath,
        TranscriptionTriggerWorker.KEY_MEETING_ID to meetingId
    ))
    .build()
WorkManager.getInstance(this).enqueue(workRequest)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ShareReceiverActivity에 Plaud URL 감지 + WebView 추출 + 다운로드 파이프라인 추가</name>
  <files>app/src/main/java/com/autominuting/presentation/share/ShareReceiverActivity.kt</files>
  <read_first>
    - app/src/main/java/com/autominuting/presentation/share/ShareReceiverActivity.kt (전체)
    - app/src/main/java/com/autominuting/presentation/dashboard/DashboardScreen.kt (430-514행: PlaudAudioExtractorWebView)
    - app/src/main/java/com/autominuting/presentation/dashboard/DashboardViewModel.kt (184-220행: isPlaudShareUrl, extractPlaudAudio, downloadDirectUrl 패턴)
    - app/src/main/java/com/autominuting/presentation/dashboard/DashboardViewModel.kt (245-344행: downloadDirectUrl OkHttp 다운로드 + 파이프라인 진입)
  </read_first>
  <action>
ShareReceiverActivity.kt의 onCreate()에서 텍스트 공유 처리 이전에 Plaud URL 감지 분기를 추가한다. SHARE-01 구현.

**1단계: Plaud URL 판별 함수 추가 (companion object 또는 private fun)**

```kotlin
private fun isPlaudShareUrl(text: String): Boolean =
    text.contains("web.plaud.ai/s/") || text.contains("web.plaud.ai/nshare/")

private fun extractPlaudUrl(text: String): String? {
    // 텍스트 내에서 web.plaud.ai URL을 추출 (텍스트에 URL 외 다른 내용이 포함될 수 있음)
    val regex = Regex("""https?://web\.plaud\.ai/(s|nshare)/[^\s]+""")
    return regex.find(text)?.value
}
```

**2단계: onCreate()에서 isAudioShare 분기 이후, 텍스트 공유 처리 이전에 Plaud URL 감지 분기 추가**

```kotlin
// 텍스트에서 Plaud 공유 링크 감지
val sharedTextRaw = intent.getStringExtra(Intent.EXTRA_TEXT)
if (!sharedTextRaw.isNullOrBlank() && isPlaudShareUrl(sharedTextRaw)) {
    val plaudUrl = extractPlaudUrl(sharedTextRaw)
    if (plaudUrl != null) {
        Log.d(TAG, "Plaud 공유 링크 감지: $plaudUrl")
        processPlaudShareLink(plaudUrl)
        return
    }
}
// ... 기존 텍스트 공유 처리 계속
```

**3단계: processPlaudShareLink() 메서드 구현**

이 메서드는:
1. WebView를 Activity에 addContentView()로 추가 (0dp x 0dp 크기, 투명)
2. DashboardScreen의 PlaudAudioExtractorWebView와 동일한 WebViewClient 패턴 사용:
   - settings: javaScriptEnabled=true, domStorageEnabled=true, mediaPlaybackRequiresUserGesture=false
   - shouldInterceptRequest에서 `amazonaws.com` + `audiofiles/` 패턴 감지
   - onPageFinished에서 audio.play() + 버튼 클릭 JS 실행
   - onReceivedError에서 에러 처리
3. S3 URL 추출 성공 시 → downloadAndStartPipeline(audioUrl) 호출
4. 30초 타임아웃 → Handler.postDelayed로 타임아웃 처리 후 finish()

```kotlin
private fun processPlaudShareLink(plaudUrl: String) {
    Toast.makeText(this, "Plaud 오디오 추출 중...", Toast.LENGTH_SHORT).show()

    var audioFound = false
    val webView = WebView(this).apply {
        settings.javaScriptEnabled = true
        settings.domStorageEnabled = true
        settings.mediaPlaybackRequiresUserGesture = false
    }

    // 0dp 크기로 숨겨서 추가
    val params = android.widget.FrameLayout.LayoutParams(0, 0)
    addContentView(webView, params)

    // 30초 타임아웃
    val timeoutHandler = android.os.Handler(android.os.Looper.getMainLooper())
    val timeoutRunnable = Runnable {
        if (!audioFound) {
            Log.w(TAG, "Plaud 오디오 URL 추출 타임아웃")
            Toast.makeText(this, "오디오 추출 타임아웃", Toast.LENGTH_SHORT).show()
            webView.destroy()
            finish()
        }
    }
    timeoutHandler.postDelayed(timeoutRunnable, 30_000)

    webView.webViewClient = object : WebViewClient() {
        override fun shouldInterceptRequest(view: WebView?, request: WebResourceRequest?): WebResourceResponse? {
            val requestUrl = request?.url?.toString() ?: return null
            if (requestUrl.contains("amazonaws.com") && requestUrl.contains("audiofiles/") && !audioFound) {
                audioFound = true
                timeoutHandler.removeCallbacks(timeoutRunnable)
                android.os.Handler(android.os.Looper.getMainLooper()).post {
                    Log.d(TAG, "Plaud S3 오디오 URL 추출: ${requestUrl.take(100)}...")
                    webView.destroy()
                    lifecycleScope.launch { downloadAndStartPipeline(requestUrl) }
                }
            }
            return null
        }
        override fun onPageFinished(view: WebView?, pageUrl: String?) {
            super.onPageFinished(view, pageUrl)
            // 재생 버튼 자동 클릭 (DashboardScreen과 동일 JS)
            view?.evaluateJavascript("""
                (function() {
                    var audios = document.querySelectorAll('audio');
                    for (var i = 0; i < audios.length; i++) { audios[i].play().catch(function(){}); }
                    var buttons = document.querySelectorAll('button, [role="button"], .play-btn, svg');
                    for (var i = 0; i < buttons.length; i++) { buttons[i].click(); }
                })();
            """.trimIndent(), null)
        }
        override fun onReceivedError(view: WebView?, request: WebResourceRequest?, error: WebResourceError?) {
            if (request?.isForMainFrame == true && !audioFound) {
                audioFound = true
                timeoutHandler.removeCallbacks(timeoutRunnable)
                Log.e(TAG, "Plaud 페이지 로드 실패: ${error?.description}")
                Toast.makeText(this@ShareReceiverActivity, "Plaud 페이지 로드 실패", Toast.LENGTH_SHORT).show()
                webView.destroy()
                finish()
            }
        }
    }
    webView.loadUrl(plaudUrl)
}
```

**4단계: downloadAndStartPipeline() 메서드 구현**

DashboardViewModel.downloadDirectUrl()과 동일 패턴으로 OkHttp 다운로드 + MeetingEntity 생성 + TranscriptionTriggerWorker enqueue.

```kotlin
private suspend fun downloadAndStartPipeline(audioUrl: String) {
    try {
        val now = System.currentTimeMillis()
        val audioDir = File(filesDir, "audio")
        audioDir.mkdirs()

        // OkHttp로 S3 URL 직접 다운로드
        val client = OkHttpClient.Builder()
            .connectTimeout(30, java.util.concurrent.TimeUnit.SECONDS)
            .readTimeout(120, java.util.concurrent.TimeUnit.SECONDS)
            .build()

        val request = okhttp3.Request.Builder().url(audioUrl).build()

        withContext(Dispatchers.IO) {
            val response = client.newCall(request).execute()
            if (!response.isSuccessful) {
                throw IOException("다운로드 실패: HTTP ${response.code}")
            }
            val body = response.body ?: throw IOException("응답 본문이 비어있습니다")

            // 확장자 추출 (URL 또는 Content-Type 기반)
            val rawFileName = audioUrl.substringAfterLast("/").substringBefore("?")
            val extractedExt = rawFileName.substringAfterLast(".", "m4a")
            val extension = if (extractedExt in listOf("m4a", "mp4", "wav", "mp3", "ogg", "flac")) {
                extractedExt
            } else {
                val contentType = response.header("Content-Type") ?: ""
                when {
                    contentType.contains("m4a") || contentType.contains("mp4") -> "m4a"
                    contentType.contains("wav") -> "wav"
                    contentType.contains("mp3") || contentType.contains("mpeg") -> "mp3"
                    else -> "m4a"
                }
            }

            val audioFile = File(audioDir, "plaud_${now}.$extension")
            body.byteStream().use { input ->
                audioFile.outputStream().use { output -> input.copyTo(output) }
            }
            response.close()

            Log.d(TAG, "Plaud 오디오 다운로드 완료: ${audioFile.absolutePath} (${audioFile.length()} bytes)")

            // MeetingEntity 생성 (AUDIO_RECEIVED)
            val instantNow = Instant.ofEpochMilli(now)
            val meeting = Meeting(
                title = "Plaud 공유 회의",
                recordedAt = instantNow,
                audioFilePath = audioFile.absolutePath,
                pipelineStatus = PipelineStatus.AUDIO_RECEIVED,
                source = "PLAUD_SHARE",
                createdAt = instantNow,
                updatedAt = instantNow
            )
            val meetingId = meetingRepository.insertMeeting(meeting)

            // TranscriptionTriggerWorker enqueue
            val workRequest = OneTimeWorkRequestBuilder<TranscriptionTriggerWorker>()
                .setInputData(workDataOf(
                    TranscriptionTriggerWorker.KEY_AUDIO_FILE_PATH to audioFile.absolutePath,
                    TranscriptionTriggerWorker.KEY_MEETING_ID to meetingId
                ))
                .build()
            WorkManager.getInstance(this@ShareReceiverActivity).enqueue(workRequest)

            Log.d(TAG, "Plaud 전사 파이프라인 진입: meetingId=$meetingId")

            withContext(Dispatchers.Main) {
                PipelineNotificationHelper.updateProgress(this@ShareReceiverActivity, "Plaud 음성 전사 중...")
                Toast.makeText(this@ShareReceiverActivity, "Plaud 음성 전사 중...", Toast.LENGTH_SHORT).show()
            }
        }
    } catch (e: Exception) {
        Log.e(TAG, "Plaud 오디오 다운로드/파이프라인 진입 실패", e)
        Toast.makeText(this, "Plaud 오디오 처리 실패: ${e.message}", Toast.LENGTH_LONG).show()
    } finally {
        finish()
    }
}
```

**5단계: import 추가**

```kotlin
import android.annotation.SuppressLint
import android.webkit.WebResourceError
import android.webkit.WebResourceRequest
import android.webkit.WebResourceResponse
import android.webkit.WebView
import android.webkit.WebViewClient
import kotlinx.coroutines.Dispatchers
import kotlinx.coroutines.withContext
import okhttp3.OkHttpClient
import java.io.IOException
```

**주의 사항:**
- WebView를 Activity에서 직접 사용하므로 Compose 불필요 (ShareReceiverActivity는 ComponentActivity)
- `@SuppressLint("SetJavaScriptEnabled")` 어노테이션을 processPlaudShareLink()에 추가
- finish()는 반드시 모든 경로(성공/실패/타임아웃)에서 호출하여 투명 Activity가 남지 않도록 한다
- source 값은 "PLAUD_SHARE"로 설정하여 SAMSUNG_SHARE와 구분
  </action>
  <verify>
    <automated>cd D:/workspace/temp/Auto_Minuting && ./gradlew assembleDebug 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "isPlaudShareUrl" app/src/main/java/com/autominuting/presentation/share/ShareReceiverActivity.kt
    - grep -q "extractPlaudUrl" app/src/main/java/com/autominuting/presentation/share/ShareReceiverActivity.kt
    - grep -q "processPlaudShareLink" app/src/main/java/com/autominuting/presentation/share/ShareReceiverActivity.kt
    - grep -q "downloadAndStartPipeline" app/src/main/java/com/autominuting/presentation/share/ShareReceiverActivity.kt
    - grep -q "amazonaws.com" app/src/main/java/com/autominuting/presentation/share/ShareReceiverActivity.kt
    - grep -q "PLAUD_SHARE" app/src/main/java/com/autominuting/presentation/share/ShareReceiverActivity.kt
    - grep -q "TranscriptionTriggerWorker" app/src/main/java/com/autominuting/presentation/share/ShareReceiverActivity.kt
  </acceptance_criteria>
  <done>
    - ShareReceiverActivity가 text/plain 공유에서 web.plaud.ai URL을 감지한다
    - WebView로 S3 presigned URL을 추출한다
    - OkHttp로 오디오를 다운로드하여 filesDir/audio/에 저장한다
    - MeetingEntity(source=PLAUD_SHARE, status=AUDIO_RECEIVED)를 생성한다
    - TranscriptionTriggerWorker를 enqueue하여 전사 파이프라인에 진입한다
    - Plaud URL이 아닌 일반 텍스트는 기존 경로로 정상 처리된다
    - assembleDebug 빌드 성공
  </done>
</task>

</tasks>

<verification>
1. `./gradlew assembleDebug` 빌드 성공
2. ShareReceiverActivity.kt에 isPlaudShareUrl, processPlaudShareLink, downloadAndStartPipeline 메서드 존재
3. WebView S3 인터셉트 패턴(amazonaws.com + audiofiles/) 존재
4. 기존 텍스트/음성 공유 경로가 변경되지 않음 (isAudioShare 분기 유지)
</verification>

<success_criteria>
- web.plaud.ai 링크가 포함된 text/plain 공유 시 Plaud 추출 경로로 분기
- S3 오디오 URL 추출 + 다운로드 + 전사 파이프라인 진입 코드 완성
- 기존 텍스트/음성 공유 경로 정상 동작 유지
- assembleDebug 성공
</success_criteria>

<output>
완료 후 `.planning/phases/32-/32-01-SUMMARY.md` 생성
</output>
