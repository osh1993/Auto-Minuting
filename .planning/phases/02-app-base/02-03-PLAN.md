---
phase: 02-app-base
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - app/src/main/java/com/autominuting/presentation/navigation/AppNavigation.kt
  - app/src/main/java/com/autominuting/presentation/navigation/Screen.kt
  - app/src/main/java/com/autominuting/presentation/dashboard/DashboardScreen.kt
  - app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt
  - app/src/main/java/com/autominuting/presentation/minutes/MinutesScreen.kt
  - app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt
  - app/src/main/java/com/autominuting/presentation/theme/Theme.kt
  - app/src/main/java/com/autominuting/presentation/theme/Color.kt
  - app/src/main/java/com/autominuting/presentation/theme/Type.kt
  - app/src/main/java/com/autominuting/MainActivity.kt
  - app/src/main/java/com/autominuting/worker/TestWorker.kt
  - app/src/main/java/com/autominuting/di/WorkerModule.kt
autonomous: true
requirements:
  - APP-01
  - APP-04

must_haves:
  truths:
    - "4개 빈 화면(대시보드/전사목록/회의록목록/설정)이 Bottom Navigation으로 탐색 가능하다"
    - "WorkManager가 초기화되어 테스트 Worker가 백그라운드에서 실행 가능하다"
    - "Material 3 테마가 적용되어 있다"
  artifacts:
    - path: "app/src/main/java/com/autominuting/presentation/navigation/Screen.kt"
      provides: "화면 경로 정의"
      contains: "sealed class Screen"
    - path: "app/src/main/java/com/autominuting/presentation/navigation/AppNavigation.kt"
      provides: "Bottom Navigation + NavHost"
      contains: "NavigationBar"
    - path: "app/src/main/java/com/autominuting/presentation/dashboard/DashboardScreen.kt"
      provides: "대시보드 화면"
      contains: "@Composable"
    - path: "app/src/main/java/com/autominuting/worker/TestWorker.kt"
      provides: "WorkManager 테스트 Worker"
      contains: "class TestWorker"
  key_links:
    - from: "app/src/main/java/com/autominuting/MainActivity.kt"
      to: "AppNavigation"
      via: "setContent"
      pattern: "AppNavigation"
    - from: "app/src/main/java/com/autominuting/presentation/navigation/AppNavigation.kt"
      to: "DashboardScreen, TranscriptsScreen, MinutesScreen, SettingsScreen"
      via: "NavHost composable routes"
      pattern: "composable.*Screen"
---

<objective>
Jetpack Compose Navigation으로 4개 빈 화면을 연결하고, Material 3 테마를 적용하고, WorkManager를 초기화하여 테스트 Worker를 실행한다.

Purpose: 사용자가 설치 후 4개 화면을 탐색할 수 있는 앱 쉘을 완성하고, 백그라운드 작업 인프라를 준비한다.
Output: Bottom Navigation으로 4개 화면이 전환되고, WorkManager 테스트 Worker가 실행되는 앱
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/STACK.md
@.planning/phases/02-app-base/02-CONTEXT.md
@.planning/phases/02-app-base/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Material 3 테마 + Compose Navigation + 4개 빈 화면</name>
  <files>
    app/src/main/java/com/autominuting/presentation/theme/Color.kt,
    app/src/main/java/com/autominuting/presentation/theme/Type.kt,
    app/src/main/java/com/autominuting/presentation/theme/Theme.kt,
    app/src/main/java/com/autominuting/presentation/navigation/Screen.kt,
    app/src/main/java/com/autominuting/presentation/navigation/AppNavigation.kt,
    app/src/main/java/com/autominuting/presentation/dashboard/DashboardScreen.kt,
    app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt,
    app/src/main/java/com/autominuting/presentation/minutes/MinutesScreen.kt,
    app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt,
    app/src/main/java/com/autominuting/MainActivity.kt
  </files>
  <read_first>
    app/src/main/java/com/autominuting/MainActivity.kt,
    app/build.gradle.kts,
    .planning/research/STACK.md
  </read_first>
  <action>
1. presentation/theme/Color.kt:
   - Material 3 기본 컬러 팔레트 정의
   - md_theme_light_primary, md_theme_dark_primary 등 라이트/다크 모드 색상
   - 블루 계열 기본 색상 (회의록 앱에 적합한 전문적 느낌)

2. presentation/theme/Type.kt:
   - Material 3 Typography 정의
   - bodyLarge, titleLarge 등 기본 타이포그래피

3. presentation/theme/Theme.kt:
   - @Composable fun AutoMinutingTheme(darkTheme: Boolean = isSystemInDarkTheme(), content: @Composable () -> Unit)
   - MaterialTheme(colorScheme = if (darkTheme) darkColorScheme else lightColorScheme, typography = Typography, content = content)
   - dynamicColor 지원 (Android 12+이므로 항상 가능): dynamicDarkColorScheme/dynamicLightColorScheme 사용

4. presentation/navigation/Screen.kt:
   - sealed class Screen(val route: String, val title: String, val icon: ImageVector)
   - object Dashboard : Screen("dashboard", "대시보드", Icons.Default.Home)
   - object Transcripts : Screen("transcripts", "전사 목록", Icons.Default.TextSnippet 또는 Description)
   - object Minutes : Screen("minutes", "회의록", Icons.Default.Article 또는 NoteAlt)
   - object Settings : Screen("settings", "설정", Icons.Default.Settings)
   - companion object { val bottomNavItems = listOf(Dashboard, Transcripts, Minutes, Settings) }

5. presentation/navigation/AppNavigation.kt:
   - @Composable fun AppNavigation()
   - val navController = rememberNavController()
   - Scaffold with NavigationBar (Material 3 Bottom Navigation):
     - Screen.bottomNavItems.forEach { screen -> NavigationBarItem(selected, onClick navigate, icon, label) }
   - NavHost(navController, startDestination = Screen.Dashboard.route):
     - composable(Screen.Dashboard.route) { DashboardScreen() }
     - composable(Screen.Transcripts.route) { TranscriptsScreen() }
     - composable(Screen.Minutes.route) { MinutesScreen() }
     - composable(Screen.Settings.route) { SettingsScreen() }

6. 4개 빈 화면 (@Composable, 각각 별도 파일):
   - DashboardScreen.kt: Box(Modifier.fillMaxSize(), contentAlignment = Center) { Text("대시보드") }
   - TranscriptsScreen.kt: Box(Modifier.fillMaxSize(), contentAlignment = Center) { Text("전사 목록") }
   - MinutesScreen.kt: Box(Modifier.fillMaxSize(), contentAlignment = Center) { Text("회의록 목록") }
   - SettingsScreen.kt: Box(Modifier.fillMaxSize(), contentAlignment = Center) { Text("설정") }
   - 각 화면에 @Composable fun XxxScreen() 시그니처

7. MainActivity.kt 수정:
   - setContent { AutoMinutingTheme { AppNavigation() } }
   - 기존 임시 Text 제거
  </action>
  <verify>
    <automated>cd D:/workspace/temp/Auto_Minuting && ./gradlew assembleDebug --no-daemon 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Screen.kt에 "sealed class Screen"과 4개 object(Dashboard, Transcripts, Minutes, Settings) 존재
    - AppNavigation.kt에 "NavigationBar"와 "NavHost" 존재
    - AppNavigation.kt에 "composable(Screen.Dashboard.route)" 등 4개 경로 존재
    - DashboardScreen.kt에 "@Composable" 및 "fun DashboardScreen" 존재
    - TranscriptsScreen.kt에 "@Composable" 및 "fun TranscriptsScreen" 존재
    - MinutesScreen.kt에 "@Composable" 및 "fun MinutesScreen" 존재
    - SettingsScreen.kt에 "@Composable" 및 "fun SettingsScreen" 존재
    - Theme.kt에 "@Composable" 및 "fun AutoMinutingTheme" 존재
    - MainActivity.kt에 "AutoMinutingTheme" 및 "AppNavigation" 존재
    - ./gradlew assembleDebug BUILD SUCCESSFUL
  </acceptance_criteria>
  <done>Material 3 테마가 적용되고, 4개 빈 화면이 Bottom Navigation으로 전환되는 앱이 빌드 성공</done>
</task>

<task type="auto">
  <name>Task 2: WorkManager 초기화 + 테스트 Worker</name>
  <files>
    app/src/main/java/com/autominuting/worker/TestWorker.kt,
    app/src/main/java/com/autominuting/di/WorkerModule.kt,
    app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt
  </files>
  <read_first>
    app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt,
    app/build.gradle.kts,
    app/src/main/java/com/autominuting/di/AppModule.kt
  </read_first>
  <action>
1. worker/TestWorker.kt:
   - class TestWorker(context: Context, params: WorkerParameters) : CoroutineWorker(context, params)
   - override suspend fun doWork(): Result 구현
   - Log.d("TestWorker", "백그라운드 작업 실행 완료") 출력
   - 간단한 delay(1000) 후 Result.success() 반환
   - 한국어 주석: "Phase 6에서 실제 파이프라인 Worker로 교체 예정. WorkManager 초기화 검증용."

2. di/WorkerModule.kt:
   - @Module @InstallIn(SingletonComponent::class) object WorkerModule
   - @Provides @Singleton fun provideWorkManager(@ApplicationContext context: Context): WorkManager = WorkManager.getInstance(context)

3. presentation/settings/SettingsScreen.kt 수정:
   - 기존 빈 화면에 "WorkManager 테스트" 버튼 추가
   - 버튼 클릭 시 OneTimeWorkRequestBuilder<TestWorker>().build()로 작업 enqueue
   - WorkManager.getInstance(context).enqueue(workRequest)
   - 버튼 아래에 Text("설정 화면 - Phase 6에서 완성 예정")
   - hiltViewModel() 없이 LocalContext.current로 WorkManager 접근 (간단한 테스트 목적)

주의: Hilt Worker injection (HiltWorker)은 Phase 6 파이프라인 통합 시 도입. 현재는 기본 CoroutineWorker로 WorkManager 초기화만 검증.
  </action>
  <verify>
    <automated>cd D:/workspace/temp/Auto_Minuting && ./gradlew assembleDebug --no-daemon 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - TestWorker.kt에 "class TestWorker" 및 "CoroutineWorker" 존재
    - TestWorker.kt에 "override suspend fun doWork(): Result" 존재
    - WorkerModule.kt에 "@Provides" 및 "WorkManager.getInstance" 존재
    - SettingsScreen.kt에 "OneTimeWorkRequestBuilder" 또는 "WorkManager" 참조 존재
    - SettingsScreen.kt에 "WorkManager 테스트" 텍스트 존재
    - ./gradlew assembleDebug BUILD SUCCESSFUL
  </acceptance_criteria>
  <done>WorkManager가 DI로 제공되고, 설정 화면에서 테스트 Worker를 실행할 수 있으며, 빌드 성공</done>
</task>

</tasks>

<verification>
- ./gradlew assembleDebug가 BUILD SUCCESSFUL
- 4개 화면 Composable이 각각 존재하고 NavHost에 연결됨
- Bottom Navigation에 4개 항목 존재
- WorkManager 테스트 Worker가 정의되고 설정 화면에서 트리거 가능
</verification>

<success_criteria>
- 앱이 빌드되어 기기에 설치되고 4개 빈 화면이 Bottom Navigation으로 탐색 가능하다
- Material 3 테마(Dynamic Color 포함)가 적용되어 있다
- WorkManager가 초기화되어 테스트 Worker가 설정 화면 버튼으로 실행 가능하다
</success_criteria>

<output>
After completion, create `.planning/phases/02-app-base/02-03-SUMMARY.md`
</output>
