---
phase: 02-app-base
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - settings.gradle.kts
  - build.gradle.kts
  - app/build.gradle.kts
  - gradle/libs.versions.toml
  - app/src/main/AndroidManifest.xml
  - app/src/main/java/com/autominuting/AutoMinutingApplication.kt
  - app/src/main/java/com/autominuting/di/AppModule.kt
  - app/src/main/java/com/autominuting/MainActivity.kt
autonomous: true
requirements:
  - APP-01
  - APP-02

must_haves:
  truths:
    - "Android 프로젝트가 ./gradlew assembleDebug로 빌드 성공한다"
    - "Hilt DI 그래프가 컴파일 타임 오류 없이 구성된다"
    - "Clean Architecture 패키지 구조(domain/data/presentation)가 존재한다"
  artifacts:
    - path: "app/build.gradle.kts"
      provides: "Kotlin, Compose, Hilt, Room, WorkManager 의존성"
      contains: "compose-bom"
    - path: "gradle/libs.versions.toml"
      provides: "버전 카탈로그"
      contains: "hilt"
    - path: "app/src/main/java/com/autominuting/AutoMinutingApplication.kt"
      provides: "Hilt Application 클래스"
      contains: "@HiltAndroidApp"
    - path: "app/src/main/java/com/autominuting/di/AppModule.kt"
      provides: "Hilt DI 모듈"
      contains: "@Module"
  key_links:
    - from: "app/src/main/AndroidManifest.xml"
      to: "AutoMinutingApplication"
      via: "android:name"
      pattern: "android:name=\".AutoMinutingApplication\""
    - from: "app/src/main/java/com/autominuting/MainActivity.kt"
      to: "Hilt"
      via: "@AndroidEntryPoint"
      pattern: "@AndroidEntryPoint"
---

<objective>
Android 프로젝트를 처음부터 생성하고, Gradle 빌드 시스템을 구성하고, Clean Architecture 패키지 구조를 잡고, Hilt DI를 초기화한다.

Purpose: 이후 모든 플랜(DB, UI, WorkManager)이 올라갈 수 있는 빌드 가능한 Android 프로젝트 뼈대를 만든다.
Output: ./gradlew assembleDebug가 성공하는 Kotlin + Compose + Hilt Android 프로젝트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md
@.planning/phases/02-app-base/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gradle 프로젝트 초기화 및 버전 카탈로그 구성</name>
  <files>
    settings.gradle.kts,
    build.gradle.kts,
    app/build.gradle.kts,
    gradle/libs.versions.toml,
    gradle.properties,
    gradle/wrapper/gradle-wrapper.properties
  </files>
  <read_first>
    .planning/research/STACK.md
  </read_first>
  <action>
1. gradle/wrapper/gradle-wrapper.properties 생성: distributionUrl에 gradle-9.3.1-bin.zip 설정

2. gradle/libs.versions.toml 생성 (버전 카탈로그):
   - [versions] 섹션: kotlin = "2.3.20", agp = "9.1.0", composeBom = "2026.03.00", room = "2.8.4", hilt = "2.56", workmanager = "2.11.1", navigation = "2.9.0", datastore = "1.2.1", coroutines = "1.10.1", ksp = "2.3.20-1.1.1"
   - [libraries] 섹션: compose-bom, material3, navigation-compose, room-runtime, room-compiler, room-ktx, hilt-android, hilt-compiler, hilt-navigation-compose, work-runtime-ktx, datastore-preferences, coroutines-android, activity-compose, lifecycle-runtime-compose, lifecycle-viewmodel-compose, core-ktx
   - [plugins] 섹션: android-application, kotlin-android, kotlin-compose, ksp, hilt

3. build.gradle.kts (루트):
   - plugins 블록에 alias(libs.plugins.android.application) apply false, alias(libs.plugins.kotlin.android) apply false, alias(libs.plugins.kotlin.compose) apply false, alias(libs.plugins.ksp) apply false, alias(libs.plugins.hilt) apply false

4. settings.gradle.kts:
   - pluginManagement { repositories { google(); mavenCentral(); gradlePluginPortal() } }
   - dependencyResolution { repositories { google(); mavenCentral() } }
   - rootProject.name = "AutoMinuting"
   - include(":app")

5. app/build.gradle.kts:
   - plugins: android.application, kotlin.android, kotlin.compose, ksp, hilt
   - android { namespace = "com.autominuting", compileSdk = 36, defaultConfig { applicationId = "com.autominuting", minSdk = 31, targetSdk = 36, versionCode = 1, versionName = "1.0" }, compileOptions { sourceCompatibility = JavaVersion.VERSION_17, targetCompatibility = JavaVersion.VERSION_17 }, kotlinOptions { jvmTarget = "17" }, buildFeatures { compose = true } }
   - dependencies: implementation(platform(libs.compose.bom)), implementation(libs.material3), implementation(libs.navigation.compose), implementation(libs.room.runtime), implementation(libs.room.ktx), ksp(libs.room.compiler), implementation(libs.hilt.android), ksp(libs.hilt.compiler), implementation(libs.hilt.navigation.compose), implementation(libs.work.runtime.ktx), implementation(libs.datastore.preferences), implementation(libs.coroutines.android), implementation(libs.activity.compose), implementation(libs.lifecycle.runtime.compose), implementation(libs.lifecycle.viewmodel.compose), implementation(libs.core.ktx)

6. gradle.properties: org.gradle.jvmargs=-Xmx2048m, android.useAndroidX=true, kotlin.code.style=official
  </action>
  <verify>
    <automated>cd D:/workspace/temp/Auto_Minuting && ./gradlew tasks --no-daemon 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - gradle/libs.versions.toml에 "hilt", "composeBom", "room", "workmanager" 키가 존재
    - app/build.gradle.kts에 "com.autominuting" namespace 존재
    - app/build.gradle.kts에 "libs.hilt.android" 의존성 존재
    - settings.gradle.kts에 "AutoMinuting" rootProject.name 존재
  </acceptance_criteria>
  <done>Gradle 빌드 시스템이 구성되고 ./gradlew tasks가 에러 없이 실행된다</done>
</task>

<task type="auto">
  <name>Task 2: Clean Architecture 패키지 구조 + Hilt 초기화 + MainActivity</name>
  <files>
    app/src/main/AndroidManifest.xml,
    app/src/main/java/com/autominuting/AutoMinutingApplication.kt,
    app/src/main/java/com/autominuting/MainActivity.kt,
    app/src/main/java/com/autominuting/di/AppModule.kt,
    app/src/main/java/com/autominuting/domain/model/.gitkeep,
    app/src/main/java/com/autominuting/domain/repository/.gitkeep,
    app/src/main/java/com/autominuting/domain/usecase/.gitkeep,
    app/src/main/java/com/autominuting/data/local/.gitkeep,
    app/src/main/java/com/autominuting/data/repository/.gitkeep,
    app/src/main/java/com/autominuting/presentation/dashboard/.gitkeep,
    app/src/main/java/com/autominuting/presentation/transcripts/.gitkeep,
    app/src/main/java/com/autominuting/presentation/minutes/.gitkeep,
    app/src/main/java/com/autominuting/presentation/settings/.gitkeep,
    app/src/main/java/com/autominuting/presentation/navigation/.gitkeep,
    app/src/main/res/values/strings.xml,
    app/src/main/res/values/themes.xml
  </files>
  <read_first>
    app/build.gradle.kts,
    .planning/research/ARCHITECTURE.md
  </read_first>
  <action>
1. Clean Architecture 패키지 구조 생성 (per APP-02):
   - com.autominuting.domain.model — 도메인 엔티티 (Phase 2 Plan 02에서 구현)
   - com.autominuting.domain.repository — Repository 인터페이스 (Phase 2 Plan 02에서 구현)
   - com.autominuting.domain.usecase — Use Case (Phase 3+에서 구현)
   - com.autominuting.data.local — Room DB, DAO (Phase 2 Plan 02에서 구현)
   - com.autominuting.data.repository — Repository 구현체 (Phase 2 Plan 02에서 구현)
   - com.autominuting.presentation.dashboard — 대시보드 화면
   - com.autominuting.presentation.transcripts — 전사 목록 화면
   - com.autominuting.presentation.minutes — 회의록 목록 화면
   - com.autominuting.presentation.settings — 설정 화면
   - com.autominuting.presentation.navigation — Compose Navigation

2. AutoMinutingApplication.kt:
   - @HiltAndroidApp 어노테이션
   - class AutoMinutingApplication : Application()

3. MainActivity.kt:
   - @AndroidEntryPoint 어노테이션
   - ComponentActivity 상속
   - setContent { AutoMinutingTheme { Text("Auto Minuting") } } (임시 - Plan 03에서 Navigation으로 교체)

4. di/AppModule.kt:
   - @Module, @InstallIn(SingletonComponent::class)
   - @Provides @Singleton으로 Application Context 제공 (@ApplicationContext)

5. AndroidManifest.xml:
   - application android:name=".AutoMinutingApplication"
   - activity android:name=".MainActivity" exported=true, MAIN/LAUNCHER intent-filter
   - 향후 필요한 권한은 아직 추가하지 않음

6. res/values/strings.xml: app_name = "Auto Minuting"
7. res/values/themes.xml: Material 3 DayNight 테마 (Theme.AutoMinuting), parent="Theme.Material3.DayNight.NoActionBar"
  </action>
  <verify>
    <automated>cd D:/workspace/temp/Auto_Minuting && ./gradlew assembleDebug --no-daemon 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - AutoMinutingApplication.kt에 "@HiltAndroidApp" 어노테이션 존재
    - MainActivity.kt에 "@AndroidEntryPoint" 어노테이션 존재
    - AppModule.kt에 "@Module"과 "@InstallIn(SingletonComponent::class)" 존재
    - AndroidManifest.xml에 "android:name=\".AutoMinutingApplication\"" 존재
    - domain/model, domain/repository, domain/usecase 디렉토리 존재
    - data/local, data/repository 디렉토리 존재
    - presentation/dashboard, presentation/transcripts, presentation/minutes, presentation/settings 디렉토리 존재
    - ./gradlew assembleDebug가 BUILD SUCCESSFUL 출력
  </acceptance_criteria>
  <done>Clean Architecture 패키지 구조가 생성되고, Hilt DI가 초기화되고, 앱이 빌드 성공한다</done>
</task>

</tasks>

<verification>
- ./gradlew assembleDebug가 BUILD SUCCESSFUL
- @HiltAndroidApp, @AndroidEntryPoint, @Module 어노테이션이 올바른 파일에 존재
- Clean Architecture 3개 레이어(domain/data/presentation) 패키지가 모두 존재
</verification>

<success_criteria>
- Android 프로젝트가 빌드되어 APK가 생성된다
- Hilt DI 그래프가 컴파일 타임 오류 없이 구성된다
- Clean Architecture 패키지 구조가 잡혀 있다
</success_criteria>

<output>
After completion, create `.planning/phases/02-app-base/02-01-SUMMARY.md`
</output>
