---
phase: 43-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt
  - app/src/main/java/com/autominuting/presentation/minutes/MinutesScreen.kt
autonomous: true
requirements: [UX-01, UX-02]

must_haves:
  truths:
    - "전사목록 탭에서 카드를 터치하면 전사 편집 화면(TranscriptEdit)으로 이동한다"
    - "회의록 탭에서 카드를 터치하면 회의록 상세 화면(MinutesDetail)으로 이동한다"
    - "전사목록 카드의 제목 텍스트를 터치해도 이름 변경 다이얼로그가 뜨지 않고, 카드 전체 클릭과 동일하게 상세 화면으로 이동한다"
    - "회의록 카드의 제목 텍스트를 터치해도 이름 변경 다이얼로그가 뜨지 않고, 카드 전체 클릭과 동일하게 상세 화면으로 이동한다"
    - "전사목록 카드의 점3개(MoreVert) 메뉴에 이름 변경 항목이 존재한다"
    - "회의록 카드의 점3개(MoreVert) 메뉴에 이름 변경 항목이 존재한다"
    - "overflow 메뉴에서 이름 변경을 선택하면 기존 이름 편집 다이얼로그가 표시되고 저장이 정상 동작한다"
  artifacts:
    - path: "app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt"
      provides: "전사 카드 터치 네비게이션 + overflow 이름 변경"
      contains: "이름 변경"
    - path: "app/src/main/java/com/autominuting/presentation/minutes/MinutesScreen.kt"
      provides: "회의록 카드 터치 네비게이션 + overflow 이름 변경"
      contains: "이름 변경"
  key_links:
    - from: "TranscriptMeetingCard title Text"
      to: "Card onClick (navigate)"
      via: "title에서 clickable 제거 -> 카드 레벨 onClick이 동작"
      pattern: "Text.*meeting\\.title.*(?!clickable)"
    - from: "MinutesCard title Text"
      to: "Card onClick (navigate)"
      via: "title에서 clickable 제거 -> 카드 레벨 onClick이 동작"
      pattern: "Text.*minutes\\.minutesTitle.*(?!clickable)"
    - from: "DropdownMenuItem 이름 변경"
      to: "onRenameRequest callback"
      via: "overflow 메뉴 아이템 클릭 -> onRenameRequest 호출 -> AlertDialog 표시"
      pattern: "DropdownMenuItem.*이름 변경"
---

<objective>
전사목록/회의록 탭의 카드 터치 동작을 개선하고 이름 변경을 overflow 메뉴로 이동한다.

Purpose: 현재 카드 제목 텍스트를 터치하면 이름 변경 다이얼로그가 뜨는데, 이는 사용자가 기대하는 "상세 화면 열기" 동작과 충돌한다. 제목 터치 시 이름 변경을 제거하고, 이름 변경은 점3개(overflow) 메뉴로 이동하여 UX 일관성을 확보한다.
Output: 수정된 TranscriptsScreen.kt, MinutesScreen.kt
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt
@app/src/main/java/com/autominuting/presentation/minutes/MinutesScreen.kt
@app/src/main/java/com/autominuting/presentation/navigation/AppNavigation.kt

<interfaces>
<!-- 현재 코드 구조 요약 -->

TranscriptsScreen.kt — TranscriptMeetingCard:
- Line 317: 제목 Text에 `.clickable { onRenameRequest(meeting) }` 가 있음 -> 제거 대상
- Line 326-402: DropdownMenu에 재전사/회의록 작성/회의록 재생성/공유/삭제 메뉴 항목 존재 -> 이름 변경 항목 추가 필요
- Line 294-298: Card에 `.clickable { if (isEditable) onClick() }` 가 있음 -> 이미 상세 화면 네비게이션 동작 중 (UX-01 이미 충족)

MinutesScreen.kt — MinutesCard:
- Line 344: 제목 Text에 `.clickable { onRenameRequest(minutes) }` 가 있음 -> 제거 대상
- Line 384-418: DropdownMenu에 공유/삭제 메뉴 항목 존재 -> 이름 변경 항목 추가 필요
- Line 330-337: Column에 `combinedClickable(onClick, onLongClick)` 있음 -> 이미 상세 화면 네비게이션 동작 중 (UX-01 이미 충족)

import 추가 필요:
- TranscriptsScreen.kt: `import androidx.compose.material.icons.filled.Edit` (이름 변경 아이콘)
- MinutesScreen.kt: `import androidx.compose.material.icons.filled.Edit` (이름 변경 아이콘)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 전사목록 카드 — 제목 클릭 이름변경 제거 + overflow 메뉴에 이름 변경 추가</name>
  <files>app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt</files>
  <action>
TranscriptsScreen.kt의 TranscriptMeetingCard 컴포저블을 수정한다 (UX-01, UX-02).

1. import 추가: `import androidx.compose.material.icons.filled.Edit`

2. 제목 Text에서 `.clickable { onRenameRequest(meeting) }` modifier 제거 (Line 318 부근):
   변경 전:
   ```kotlin
   Text(
       text = meeting.title,
       ...
       modifier = Modifier
           .weight(1f)
           .clickable { onRenameRequest(meeting) }
   )
   ```
   변경 후:
   ```kotlin
   Text(
       text = meeting.title,
       ...
       modifier = Modifier.weight(1f)
   )
   ```

3. DropdownMenu 내부에 "이름 변경" 메뉴 항목 추가. 삭제 항목 바로 위에 배치:
   ```kotlin
   // 이름 변경: 항상 표시
   DropdownMenuItem(
       text = { Text("이름 변경") },
       onClick = {
           showMenu = false
           onRenameRequest(meeting)
       },
       leadingIcon = {
           Icon(Icons.Default.Edit, contentDescription = "이름 변경")
       }
   )
   ```

기존 이름 편집 다이얼로그(meetingToRename, AlertDialog)는 이미 TranscriptsScreen에 존재하므로 수정 불필요.
  </action>
  <verify>
    <automated>cd D:/workspace/temp/Auto_Minuting && ./gradlew :app:compileDebugKotlin 2>&1 | tail -5</automated>
  </verify>
  <done>전사목록 카드 제목 터치 시 이름 변경 다이얼로그가 뜨지 않고, overflow 메뉴에 "이름 변경" 항목이 존재하며 선택 시 기존 이름 편집 다이얼로그가 표시된다</done>
</task>

<task type="auto">
  <name>Task 2: 회의록 카드 — 제목 클릭 이름변경 제거 + overflow 메뉴에 이름 변경 추가</name>
  <files>app/src/main/java/com/autominuting/presentation/minutes/MinutesScreen.kt</files>
  <action>
MinutesScreen.kt의 MinutesCard 컴포저블을 수정한다 (UX-01, UX-02).

1. import 추가: `import androidx.compose.material.icons.filled.Edit`

2. 회의록 제목 Text에서 `.clickable { onRenameRequest(minutes) }` modifier 제거 (Line 344 부근):
   변경 전:
   ```kotlin
   Text(
       text = minutes.minutesTitle ?: "회의록",
       ...
       modifier = Modifier.clickable { onRenameRequest(minutes) }
   )
   ```
   변경 후:
   ```kotlin
   Text(
       text = minutes.minutesTitle ?: "회의록",
       ...
   )
   ```
   Text에서 modifier 파라미터 자체를 제거한다 (clickable 외 다른 modifier가 없으므로).

3. DropdownMenu 내부에 "이름 변경" 메뉴 항목 추가. 공유 항목과 삭제 항목 사이에 배치:
   ```kotlin
   // 이름 변경: 항상 표시
   DropdownMenuItem(
       text = { Text("이름 변경") },
       onClick = {
           showMenu = false
           onRenameRequest(minutes)
       },
       leadingIcon = {
           Icon(Icons.Default.Edit, contentDescription = "이름 변경")
       }
   )
   ```

기존 이름 편집 다이얼로그(minutesToRename, AlertDialog)는 이미 MinutesScreen에 존재하므로 수정 불필요.
  </action>
  <verify>
    <automated>cd D:/workspace/temp/Auto_Minuting && ./gradlew :app:compileDebugKotlin 2>&1 | tail -5</automated>
  </verify>
  <done>회의록 카드 제목 터치 시 이름 변경 다이얼로그가 뜨지 않고, overflow 메뉴에 "이름 변경" 항목이 존재하며 선택 시 기존 이름 편집 다이얼로그가 표시된다</done>
</task>

</tasks>

<verification>
1. `./gradlew :app:compileDebugKotlin` 빌드 성공
2. TranscriptsScreen.kt에서 제목 Text의 `.clickable { onRenameRequest` 패턴이 없음을 확인
3. MinutesScreen.kt에서 제목 Text의 `.clickable { onRenameRequest` 패턴이 없음을 확인
4. TranscriptsScreen.kt의 DropdownMenu에 "이름 변경" 텍스트가 존재함을 확인
5. MinutesScreen.kt의 DropdownMenu에 "이름 변경" 텍스트가 존재함을 확인
</verification>

<success_criteria>
- 전사목록 탭: 카드 터치 -> 전사 편집 화면 이동 (기존 동작 유지, 제목 클릭 시 이름 변경 간섭 제거)
- 회의록 탭: 카드 터치 -> 회의록 상세 화면 이동 (기존 동작 유지, 제목 클릭 시 이름 변경 간섭 제거)
- 전사목록/회의록 카드의 점3개 메뉴에 "이름 변경" 항목 존재
- overflow 메뉴 "이름 변경" 선택 시 기존 이름 편집 다이얼로그가 정상 동작
- Kotlin 컴파일 성공
</success_criteria>

<output>
After completion, create `.planning/phases/43-ux/43-01-SUMMARY.md`
</output>
