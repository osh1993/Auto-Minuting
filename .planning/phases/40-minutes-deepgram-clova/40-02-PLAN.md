---
phase: 40-minutes-deepgram-clova
plan: 02
type: execute
wave: 2
depends_on: [40-01]
files_modified:
  - app/src/main/java/com/autominuting/data/minutes/NaverClovaMinutesEngine.kt
  - app/src/main/java/com/autominuting/domain/model/MinutesEngineType.kt
  - app/src/main/java/com/autominuting/data/security/SecureApiKeyRepository.kt
  - app/src/main/java/com/autominuting/data/preferences/UserPreferencesRepository.kt
  - app/src/main/java/com/autominuting/data/minutes/MinutesEngineSelector.kt
autonomous: true
requirements: [MIN6-02, MIN6-03]

must_haves:
  truths:
    - "NaverClovaMinutesEngine이 MinutesEngine 인터페이스를 구현한다"
    - "2000자 이상 텍스트가 분할되어 각각 API 호출 후 결과가 합쳐진다"
    - "MinutesEngineType enum으로 GEMINI, DEEPGRAM, NAVER_CLOVA 선택이 가능하다"
    - "MinutesEngineSelector가 MinutesEngineType에 따라 올바른 엔진을 선택한다"
    - "기존 Gemini 회의록 엔진이 그대로 동작한다"
  artifacts:
    - path: "app/src/main/java/com/autominuting/data/minutes/NaverClovaMinutesEngine.kt"
      provides: "Naver CLOVA Summary 회의록 엔진"
      min_lines: 100
    - path: "app/src/main/java/com/autominuting/domain/model/MinutesEngineType.kt"
      provides: "회의록 엔진 유형 enum"
      contains: "enum class MinutesEngineType"
    - path: "app/src/main/java/com/autominuting/data/minutes/MinutesEngineSelector.kt"
      provides: "MinutesEngineType 기반 엔진 선택 로직"
      contains: "MinutesEngineType"
  key_links:
    - from: "NaverClovaMinutesEngine.generate()"
      to: "https://naveropenapi.apigw.ntruss.com/text-summary/v1/summarize"
      via: "OkHttp POST"
    - from: "MinutesEngineSelector.selectEngine()"
      to: "UserPreferencesRepository.getMinutesEngineTypeOnce()"
      via: "DataStore 조회"
    - from: "MinutesEngineSelector"
      to: "DeepgramMinutesEngine, NaverClovaMinutesEngine"
      via: "Provider<T>.get()"
---

<objective>
Naver CLOVA Summary Legacy API를 사용하는 NaverClovaMinutesEngine을 구현하고, MinutesEngineType enum 신설 및 MinutesEngineSelector 확장으로 3종 회의록 엔진 선택 체계를 완성한다.

Purpose: MIN6-02/MIN6-03 요구사항 — CLOVA Summary 엔진 구현 + 엔진 선택 인프라 통합
Output: NaverClovaMinutesEngine.kt, MinutesEngineType.kt, 수정된 SecureApiKeyRepository/UserPreferencesRepository/MinutesEngineSelector
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-minutes-deepgram-clova/40-RESEARCH.md
@.planning/phases/40-minutes-deepgram-clova/40-01-SUMMARY.md

<interfaces>
<!-- MinutesEngine 인터페이스 -->
From app/src/main/java/com/autominuting/data/minutes/MinutesEngine.kt:
```kotlin
interface MinutesEngine {
    suspend fun generate(transcriptText: String, customPrompt: String? = null): Result<String>
    fun engineName(): String
    fun isAvailable(): Boolean
}
```

<!-- SecureApiKeyRepository 현재 상태 — CLOVA Summary 전용 키 추가 필요 -->
From app/src/main/java/com/autominuting/data/security/SecureApiKeyRepository.kt:
```kotlin
@Singleton
class SecureApiKeyRepository @Inject constructor(@ApplicationContext context: Context) {
    companion object {
        private const val KEY_GEMINI_API = "gemini_api_key"
        private const val KEY_GROQ_API = "groq_api_key"
        private const val KEY_DEEPGRAM_API = "deepgram_api_key"
        private const val KEY_CLOVA_INVOKE_URL = "clova_invoke_url"      // CLOVA Speech용
        private const val KEY_CLOVA_SECRET_KEY = "clova_secret_key"      // CLOVA Speech용
        // KEY_CLOVA_SUMMARY_CLIENT_ID, KEY_CLOVA_SUMMARY_CLIENT_SECRET 추가 필요
    }
    fun getDeepgramApiKey(): String?
    fun getClovaInvokeUrl(): String?
    fun getClovaSecretKey(): String?
    // getClovaSummaryClientId(), getClovaSummaryClientSecret() 추가 필요
}
```

<!-- UserPreferencesRepository 현재 상태 — minutesEngineType 추가 필요 -->
From app/src/main/java/com/autominuting/data/preferences/UserPreferencesRepository.kt:
```kotlin
@Singleton
class UserPreferencesRepository @Inject constructor(dataStore: DataStore<Preferences>) {
    companion object {
        val STT_ENGINE_KEY = stringPreferencesKey("stt_engine")
        // MINUTES_ENGINE_KEY 추가 필요
    }
    val sttEngineType: Flow<SttEngineType>
    suspend fun setSttEngineType(type: SttEngineType)
    suspend fun getSttEngineTypeOnce(): SttEngineType
    // minutesEngineType, setMinutesEngineType, getMinutesEngineTypeOnce 추가 필요
}
```

<!-- MinutesEngineSelector 현재 상태 — MinutesEngineType 기반 확장 필요 -->
From app/src/main/java/com/autominuting/data/minutes/MinutesEngineSelector.kt:
```kotlin
@Singleton
class MinutesEngineSelector @Inject constructor(
    private val geminiEngineProvider: Provider<GeminiEngine>,
    private val geminiOAuthEngineProvider: Provider<GeminiOAuthEngine>,
    private val userPreferencesRepository: UserPreferencesRepository
) : MinutesEngine {
    private suspend fun selectEngine(): MinutesEngine {
        val authMode = userPreferencesRepository.authMode.first()
        return when (authMode) {
            AuthMode.OAUTH -> ...
            AuthMode.API_KEY -> geminiEngineProvider.get()
        }
    }
}
```

<!-- SttEngineType 패턴 참고 (MinutesEngineType도 동일 패턴으로 생성) -->
From app/src/main/java/com/autominuting/domain/model/SttEngineType.kt:
```kotlin
enum class SttEngineType {
    GEMINI, WHISPER, GROQ, DEEPGRAM, NAVER_CLOVA
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MinutesEngineType enum + SecureApiKeyRepository CLOVA Summary 키 + UserPreferencesRepository minutesEngineType</name>
  <files>
    app/src/main/java/com/autominuting/domain/model/MinutesEngineType.kt,
    app/src/main/java/com/autominuting/data/security/SecureApiKeyRepository.kt,
    app/src/main/java/com/autominuting/data/preferences/UserPreferencesRepository.kt
  </files>
  <action>
**1. MinutesEngineType enum 신설**

`app/src/main/java/com/autominuting/domain/model/MinutesEngineType.kt` 파일을 새로 생성한다:

```kotlin
package com.autominuting.domain.model

/**
 * 회의록 생성 엔진 유형.
 * SttEngineType과 동일한 패턴으로, UserPreferencesRepository DataStore에 저장된다.
 */
enum class MinutesEngineType {
    /** Gemini 2.5 Flash (기본값) */
    GEMINI,
    /** Deepgram Text Intelligence (영어만 지원) */
    DEEPGRAM,
    /** Naver CLOVA Summary (한국어 네이티브) */
    NAVER_CLOVA
}
```

**2. SecureApiKeyRepository에 CLOVA Summary 전용 키 추가**

기존 `SecureApiKeyRepository.kt`를 수정한다. companion object에 2개 상수 추가:
```kotlin
private const val KEY_CLOVA_SUMMARY_CLIENT_ID = "clova_summary_client_id"
private const val KEY_CLOVA_SUMMARY_CLIENT_SECRET = "clova_summary_client_secret"
```

6개 메서드 추가 (기존 getDeepgramApiKey/saveDeepgramApiKey/clearDeepgramApiKey 패턴과 동일):
- `fun getClovaSummaryClientId(): String?` — `encryptedPrefs?.getString(KEY_CLOVA_SUMMARY_CLIENT_ID, null)`
- `fun saveClovaSummaryClientId(clientId: String)` — `encryptedPrefs?.edit()?.putString(...)?.apply()`
- `fun clearClovaSummaryClientId()` — `encryptedPrefs?.edit()?.remove(...)?.apply()`
- `fun getClovaSummaryClientSecret(): String?` — `encryptedPrefs?.getString(KEY_CLOVA_SUMMARY_CLIENT_SECRET, null)`
- `fun saveClovaSummaryClientSecret(secret: String)` — `encryptedPrefs?.edit()?.putString(...)?.apply()`
- `fun clearClovaSummaryClientSecret()` — `encryptedPrefs?.edit()?.remove(...)?.apply()`

각 save/clear 메서드에 null 방어 로그: `Log.w(TAG, "CLOVA Summary ... 저장 실패: EncryptedSharedPreferences 사용 불가")` — 기존 패턴 동일.
한국어 KDoc 주석 추가.

**3. UserPreferencesRepository에 minutesEngineType 추가**

기존 `UserPreferencesRepository.kt`를 수정한다.

import 추가: `import com.autominuting.domain.model.MinutesEngineType`

companion object에 추가:
```kotlin
/** 회의록 엔진 유형 설정 키 */
val MINUTES_ENGINE_KEY = stringPreferencesKey("minutes_engine")
```

Flow 프로퍼티 추가 (sttEngineType과 동일 패턴):
```kotlin
/** 현재 회의록 엔진 유형을 관찰한다. 기본값: GEMINI */
val minutesEngineType: Flow<MinutesEngineType> = dataStore.data.map { prefs ->
    val name = prefs[MINUTES_ENGINE_KEY] ?: MinutesEngineType.GEMINI.name
    try {
        MinutesEngineType.valueOf(name)
    } catch (e: IllegalArgumentException) {
        MinutesEngineType.GEMINI  // 알 수 없는 값 -> 기본값 폴백
    }
}
```

setter 메서드 추가:
```kotlin
/** 회의록 엔진 유형을 변경한다. */
suspend fun setMinutesEngineType(type: MinutesEngineType) {
    dataStore.edit { prefs ->
        prefs[MINUTES_ENGINE_KEY] = type.name
    }
}
```

즉시 조회 메서드 추가:
```kotlin
/** 현재 회의록 엔진 유형을 즉시 조회한다. */
suspend fun getMinutesEngineTypeOnce(): MinutesEngineType =
    dataStore.data.first().let { prefs ->
        val name = prefs[MINUTES_ENGINE_KEY] ?: MinutesEngineType.GEMINI.name
        try {
            MinutesEngineType.valueOf(name)
        } catch (e: IllegalArgumentException) {
            MinutesEngineType.GEMINI
        }
    }
```
  </action>
  <verify>
    <automated>cd d:/workspace/temp/Auto_Minuting && ./gradlew assembleDebug 2>&1 | tail -5</automated>
  </verify>
  <done>MinutesEngineType enum이 3개 값(GEMINI, DEEPGRAM, NAVER_CLOVA)을 가지고, SecureApiKeyRepository에 CLOVA Summary 키 6개 메서드가 추가되고, UserPreferencesRepository에 minutesEngineType Flow/setter/getter가 추가된다</done>
</task>

<task type="auto">
  <name>Task 2: NaverClovaMinutesEngine 구현 + MinutesEngineSelector 확장</name>
  <files>
    app/src/main/java/com/autominuting/data/minutes/NaverClovaMinutesEngine.kt,
    app/src/main/java/com/autominuting/data/minutes/MinutesEngineSelector.kt
  </files>
  <action>
**1. NaverClovaMinutesEngine 구현**

`app/src/main/java/com/autominuting/data/minutes/NaverClovaMinutesEngine.kt` 파일을 새로 생성한다.

**클래스 구조:**
- package: `com.autominuting.data.minutes`
- `@Singleton class NaverClovaMinutesEngine @Inject constructor(private val secureApiKeyRepository: SecureApiKeyRepository) : MinutesEngine`
- companion object 상수:
  - `TAG = "NaverClovaMinutesEngine"`
  - `ENDPOINT = "https://naveropenapi.apigw.ntruss.com/text-summary/v1/summarize"`
  - `MAX_CHUNK_SIZE = 2000` — CLOVA Summary Legacy 2000자 제한
  - `MAX_RETRIES = 3`
  - `RETRY_DELAY_MS = 20_000L`

**engineName():**
- 반환값: `"Naver CLOVA Summary"`

**isAvailable():**
- `secureApiKeyRepository.getClovaSummaryClientId()?.isNotBlank() == true && secureApiKeyRepository.getClovaSummaryClientSecret()?.isNotBlank() == true`

**generate(transcriptText, customPrompt):**
- `customPrompt` 파라미터는 무시. 로그 경고: `Log.d(TAG, "CLOVA Summary는 커스텀 프롬프트를 지원하지 않습니다 — 무시합니다")`
- API 키 조회: `getClovaSummaryClientId()`, `getClovaSummaryClientSecret()` -> null이면 `Result.failure(IllegalStateException("CLOVA Summary API 키가 설정되지 않았습니다"))` 반환
- `withContext(Dispatchers.IO)` 블록 안에서 실행

**텍스트 분할 로직 (`splitIntoChunks` private 메서드):**
- 텍스트가 MAX_CHUNK_SIZE 이하이면 리스트 하나로 반환
- 초과 시 문장 경계(마침표+공백, 느낌표+공백, 물음표+공백, 줄바꿈)를 기준으로 분할
- 문장 경계를 찾지 못하면 MAX_CHUNK_SIZE 위치에서 강제 분할
- 알고리즘:
  ```
  fun splitIntoChunks(text: String): List<String> {
      if (text.length <= MAX_CHUNK_SIZE) return listOf(text)
      val chunks = mutableListOf<String>()
      var remaining = text
      while (remaining.isNotEmpty()) {
          if (remaining.length <= MAX_CHUNK_SIZE) {
              chunks.add(remaining)
              break
          }
          // MAX_CHUNK_SIZE 이내에서 마지막 문장 경계 찾기
          val searchRange = remaining.substring(0, MAX_CHUNK_SIZE)
          val sentenceEnd = maxOf(
              searchRange.lastIndexOf("。"),   // 일본어 마침표도 대비
              searchRange.lastIndexOf(". "),
              searchRange.lastIndexOf(".\n"),
              searchRange.lastIndexOf("! "),
              searchRange.lastIndexOf("? "),
              searchRange.lastIndexOf("\n")
          )
          val splitAt = if (sentenceEnd > MAX_CHUNK_SIZE / 2) sentenceEnd + 1 else MAX_CHUNK_SIZE
          chunks.add(remaining.substring(0, splitAt).trim())
          remaining = remaining.substring(splitAt).trim()
      }
      return chunks
  }
  ```

**API 호출 로직 (`summarizeChunk` private suspend 메서드):**
- OkHttpClient 생성: connectTimeout 30초, readTimeout 60초, writeTimeout 30초
- Request 구성:
  - POST 메서드
  - URL: ENDPOINT
  - Header: `"X-NCP-APIGW-API-KEY-ID"` to `clientId`
  - Header: `"X-NCP-APIGW-API-KEY"` to `clientSecret`
  - Header: `"Content-Type"` to `"application/json"`
  - Body JSON:
    ```json
    {
      "document": {
        "content": "<chunk text>"
      },
      "option": {
        "language": "ko",
        "model": "general",
        "tone": 2,
        "summaryCount": 3
      }
    }
    ```
  - `JSONObject` 사용하여 직접 구성 (org.json.JSONObject)

- 재시도 로직 (DeepgramSttEngine 패턴과 동일):
  - HTTP 429 -> delay 후 재시도
  - SocketTimeoutException -> delay 후 재시도
  - 그 외 HTTP 에러 -> 즉시 Result.failure

- 응답 파싱: `JSONObject(responseBody).getString("summary")`
- 빈 결과 시 Result.failure

**generate() 메인 로직:**
```
val chunks = splitIntoChunks(transcriptText)
Log.d(TAG, "전사 텍스트 ${transcriptText.length}자 → ${chunks.size}개 청크로 분할")
val summaries = mutableListOf<String>()
for ((index, chunk) in chunks.withIndex()) {
    Log.d(TAG, "청크 ${index + 1}/${chunks.size} 요약 중 (${chunk.length}자)")
    val result = summarizeChunk(chunk, clientId, clientSecret)
    result.onSuccess { summaries.add(it) }
    result.onFailure { return@withContext Result.failure(it) }
}
val combined = summaries.joinToString("\n\n")
Result.success(combined)
```

**import 목록:**
```kotlin
import android.util.Log
import com.autominuting.data.security.SecureApiKeyRepository
import kotlinx.coroutines.Dispatchers
import kotlinx.coroutines.delay
import kotlinx.coroutines.withContext
import okhttp3.MediaType.Companion.toMediaType
import okhttp3.OkHttpClient
import okhttp3.Request
import okhttp3.RequestBody.Companion.toRequestBody
import org.json.JSONObject
import java.util.concurrent.TimeUnit
import javax.inject.Inject
import javax.inject.Singleton
```

**2. MinutesEngineSelector 확장**

기존 `MinutesEngineSelector.kt`를 수정한다.

**생성자 파라미터 추가:**
```kotlin
@Singleton
class MinutesEngineSelector @Inject constructor(
    private val geminiEngineProvider: Provider<GeminiEngine>,
    private val geminiOAuthEngineProvider: Provider<GeminiOAuthEngine>,
    private val deepgramEngineProvider: Provider<DeepgramMinutesEngine>,      // 신규
    private val clovaEngineProvider: Provider<NaverClovaMinutesEngine>,       // 신규
    private val userPreferencesRepository: UserPreferencesRepository
) : MinutesEngine
```

**import 추가:**
```kotlin
import com.autominuting.domain.model.MinutesEngineType
```

**selectEngine() 메서드 변경:**
기존 authMode만 보던 로직을 MinutesEngineType 기반으로 확장한다:
```kotlin
private suspend fun selectEngine(): MinutesEngine {
    val engineType = userPreferencesRepository.getMinutesEngineTypeOnce()

    return when (engineType) {
        MinutesEngineType.GEMINI -> {
            // 기존 authMode 기반 Gemini 엔진 선택 로직 그대로 유지
            val authMode = userPreferencesRepository.authMode.first()
            when (authMode) {
                AuthMode.OAUTH -> {
                    val oauthEngine = geminiOAuthEngineProvider.get()
                    if (oauthEngine.isAvailable()) {
                        oauthEngine
                    } else {
                        Log.w(TAG, "OAuth 엔진 사용 불가 — API 키 엔진으로 폴백")
                        geminiEngineProvider.get()
                    }
                }
                AuthMode.API_KEY -> geminiEngineProvider.get()
            }
        }
        MinutesEngineType.DEEPGRAM -> deepgramEngineProvider.get()
        MinutesEngineType.NAVER_CLOVA -> clovaEngineProvider.get()
    }
}
```

**isAvailable() 메서드 확장:**
```kotlin
override fun isAvailable(): Boolean {
    // Gemini 엔진 중 하나라도, 또는 Deepgram, 또는 CLOVA가 사용 가능하면 true
    return geminiEngineProvider.get().isAvailable() ||
            geminiOAuthEngineProvider.get().isAvailable() ||
            deepgramEngineProvider.get().isAvailable() ||
            clovaEngineProvider.get().isAvailable()
}
```

**클래스/메서드 KDoc 업데이트:**
- 클래스 KDoc: "인증 모드(API 키/OAuth) 및 회의록 엔진 유형(MinutesEngineType)에 따라 적절한 MinutesEngine을 선택하는 위임 클래스."
- selectEngine() KDoc: "현재 설정된 회의록 엔진 유형에 따라 엔진을 선택한다. GEMINI 타입일 때만 기존 authMode 기반 서브셀렉션을 적용한다."
  </action>
  <verify>
    <automated>cd d:/workspace/temp/Auto_Minuting && ./gradlew assembleDebug 2>&1 | tail -5</automated>
  </verify>
  <done>NaverClovaMinutesEngine이 2000자 분할 로직을 포함하여 구현되고, MinutesEngineSelector가 MinutesEngineType 기반으로 3종 엔진(GEMINI, DEEPGRAM, NAVER_CLOVA) 중 올바른 엔진을 선택하며, assembleDebug 빌드가 성공한다</done>
</task>

</tasks>

<verification>
- `assembleDebug` 빌드 성공
- NaverClovaMinutesEngine.kt가 MinutesEngine 인터페이스 3개 메서드를 구현
- NaverClovaMinutesEngine의 splitIntoChunks가 2000자 이하 텍스트는 분할하지 않고, 초과 텍스트는 문장 경계 기준 분할
- MinutesEngineSelector가 GEMINI 선택 시 기존 authMode 로직을 유지하고, DEEPGRAM/NAVER_CLOVA 선택 시 해당 엔진을 직접 반환
- MinutesEngineType enum에 3개 값 존재
- SecureApiKeyRepository에 getClovaSummaryClientId/getClovaSummaryClientSecret 메서드 존재
- UserPreferencesRepository에 minutesEngineType Flow + setter + getter 존재
</verification>

<success_criteria>
NaverClovaMinutesEngine이 CLOVA Summary Legacy API를 호출하고 2000자 분할 처리를 지원하며, MinutesEngineSelector가 MinutesEngineType 기반으로 3종 엔진을 올바르게 선택하고, 전체 빌드가 성공한다.
</success_criteria>

<output>
After completion, create `.planning/phases/40-minutes-deepgram-clova/40-02-SUMMARY.md`
</output>
