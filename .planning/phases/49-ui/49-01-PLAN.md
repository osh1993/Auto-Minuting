---
phase: 49-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/49-ui/49-SETTINGS-PROPOSAL.md
autonomous: true
requirements:
  - SET7-01
must_haves:
  truths:
    - "현재 SettingsScreen.kt의 섹션 구조가 완전히 분석되어 문서화된다"
    - "권장 재구성안과 보수적 대안이 구체적으로 제시된다"
    - "각 안의 장단점과 코드 변경 범위가 명시된다"
  artifacts:
    - path: ".planning/phases/49-ui/49-SETTINGS-PROPOSAL.md"
      provides: "설정 화면 재구성 수정안 문서"
      min_lines: 80
  key_links: []
---

<objective>
SettingsScreen.kt의 현재 섹션 구조를 분석하고, 두 가지 재구성 수정안(권장안/보수적안)을 구체적인 코드 변경 명세와 함께 49-SETTINGS-PROPOSAL.md로 작성한다.

Purpose: SET7-01 요구사항의 "수정안 제시" 단계 — 실제 코드 변경 없이 분석과 제안만 수행
Output: .planning/phases/49-ui/49-SETTINGS-PROPOSAL.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-ui/49-RESEARCH.md
@app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: SettingsScreen.kt 현재 구조 분석 및 재구성 수정안 작성</name>
  <files>.planning/phases/49-ui/49-SETTINGS-PROPOSAL.md</files>
  <read_first>
    - app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt (전체 1558줄 — 현재 섹션 구조, 조건부 로직, state 변수 파악)
    - app/src/main/java/com/autominuting/presentation/settings/SettingsViewModel.kt (ViewModel API 확인 — 변경 불필요 확인용)
    - .planning/phases/49-ui/49-RESEARCH.md (권장 재구성안과 보수적 대안 참조)
  </read_first>
  <action>
SettingsScreen.kt를 전체 읽고 다음을 49-SETTINGS-PROPOSAL.md에 작성한다:

## 1. 현재 구조 분석
- 각 SettingsSection의 이름, 줄 범위, 포함 항목 나열
- 각 섹션이 참조하는 state 변수 목록 (collectAsStateWithLifecycle 호출 포함)
- 조건부 표시 로직 (if/when 분기) 매핑
- LaunchedEffect / rememberLauncherForActivityResult 위치와 이동 불가 이유

## 2. 권장 재구성안 (Option A: 적극적 재구성)
RESEARCH.md의 권장안을 기반으로:
- 섹션 5개: "파이프라인", "Google 계정", "Google Drive", "API 키", "모델 관리"
- 각 섹션에 포함될 항목을 현재 줄 번호 참조로 명시
- 이동해야 할 코드 블록 목록 (from L{X}-L{Y} to 새 섹션)
- 조건부 가드 변경 사항 (Drive 섹션 독립 시 필요한 if 조건)
- 변경되지 않는 코드 (LaunchedEffect, launcher 등)

## 3. 보수적 대안 (Option B: 최소 변경)
RESEARCH.md의 대안을 기반으로:
- "회의록 설정" + "전사 설정" 합병 -> "엔진 설정"
- "Gemini 인증" + "Google 계정 & Drive" 합병 -> "Google 계정 & 인증"
- 구체적 변경 범위와 이동 블록

## 4. 비교표
- 변경 규모 (이동할 줄 수), 위험도, 사용자 경험 개선 정도
- 권장 사항과 이유

문서 형식: Markdown, 한국어, 코드 블록으로 변경 전/후 섹션 구조 시각화.
ViewModel 변경은 불필요함을 명시. 파일 분리는 스코프 외임을 명시.
  </action>
  <verify>
    <automated>wc -l ".planning/phases/49-ui/49-SETTINGS-PROPOSAL.md" | awk '{if ($1 >= 80) print "PASS: "$1" lines"; else print "FAIL: only "$1" lines"}'</automated>
  </verify>
  <acceptance_criteria>
    - 파일 .planning/phases/49-ui/49-SETTINGS-PROPOSAL.md 가 존재한다
    - "## 현재 구조 분석" 헤더가 포함된다
    - "## Option A" 또는 "## 권장 재구성안" 헤더가 포함된다
    - "## Option B" 또는 "## 보수적 대안" 헤더가 포함된다
    - "## 비교" 또는 "## 비교표" 헤더가 포함된다
    - "파이프라인" 섹션명이 Option A에 명시된다
    - "LaunchedEffect" 관련 이동 불가 설명이 포함된다
    - "ViewModel 변경 불필요" 또는 동등한 표현이 포함된다
    - 80줄 이상
  </acceptance_criteria>
  <done>SettingsScreen.kt의 현재 구조가 완전히 분석되고, 두 가지 재구성안이 구체적 코드 변경 범위와 함께 문서화됨</done>
</task>

</tasks>

<verification>
- 49-SETTINGS-PROPOSAL.md가 존재하고 80줄 이상
- Option A (적극적)와 Option B (보수적) 두 안이 모두 포함
- 각 안에 구체적 코드 이동 범위가 줄 번호로 명시
</verification>

<success_criteria>
사용자가 49-SETTINGS-PROPOSAL.md를 읽고 Option A 또는 Option B를 선택할 수 있을 만큼 구체적인 수정안이 제시됨
</success_criteria>

<output>
After completion, create `.planning/phases/49-ui/49-01-SUMMARY.md`
</output>
