---
phase: 49-ui
plan: 03
type: execute
wave: 3
depends_on:
  - 49-02
files_modified:
  - app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt
autonomous: false
requirements:
  - SET7-01
must_haves:
  truths:
    - "설정 화면의 섹션이 승인된 재구성안에 따라 재배치된다"
    - "모든 기존 기능(엔진 선택, API 키 입력, Google 로그인, Drive 연결, 폴더 선택 등)이 정상 동작한다"
    - "LaunchedEffect와 rememberLauncherForActivityResult 위치가 변경되지 않는다"
    - "ViewModel API가 변경되지 않는다"
  artifacts:
    - path: "app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt"
      provides: "재구성된 설정 화면"
      contains: "SettingsSection"
  key_links:
    - from: "SettingsScreen.kt"
      to: "SettingsViewModel"
      via: "hiltViewModel + collectAsStateWithLifecycle"
      pattern: "collectAsStateWithLifecycle"
    - from: "SettingsScreen.kt (Drive 섹션)"
      to: "authState 조건부 가드"
      via: "if (authState is AuthState.SignedIn)"
      pattern: "AuthState.SignedIn"
---

<objective>
사용자가 49-02에서 승인한 재구성안에 따라 SettingsScreen.kt의 섹션을 실제로 재배치한다.

Purpose: SET7-01 요구사항의 "적용" 단계 — 승인된 수정안대로 코드 변경
Output: 재구성된 SettingsScreen.kt
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-ui/49-SETTINGS-PROPOSAL.md
@.planning/phases/49-ui/49-01-SUMMARY.md
@.planning/phases/49-ui/49-02-SUMMARY.md
@app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt
@app/src/main/java/com/autominuting/presentation/settings/SettingsViewModel.kt
</context>

<interfaces>
<!-- SettingsScreen.kt 핵심 패턴 — executor가 유지해야 할 구조 -->

SettingsSection composable 패턴:
```kotlin
@Composable
private fun SettingsSection(
    title: String,
    content: @Composable ColumnScope.() -> Unit
)
```

조건부 가드 패턴 (Drive 섹션):
```kotlin
if (authState is AuthState.SignedIn) {
    // Google Drive 관련 UI
    if (driveAuthState is DriveAuthState.Authorized) {
        // Drive 폴더 설정 UI
    }
}
```

이동 불가 코드 (L132-165 근처):
```kotlin
val driveAuthLauncher = rememberLauncherForActivityResult(...)
LaunchedEffect(driveAuthState) { ... }
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 승인된 재구성안에 따라 SettingsScreen.kt 섹션 재배치</name>
  <files>app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt</files>
  <read_first>
    - .planning/phases/49-ui/49-02-SUMMARY.md (사용자가 선택한 옵션 확인 — option-a 또는 option-b)
    - .planning/phases/49-ui/49-SETTINGS-PROPOSAL.md (선택된 옵션의 구체적 변경 명세)
    - app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt (현재 코드 전체)
    - .planning/phases/49-ui/49-RESEARCH.md (Pitfall 1-4 확인 — state 수집 누락, Drive 가드, LaunchedEffect 위치, 승인 프로세스)
  </read_first>
  <action>
49-02-SUMMARY.md에서 사용자가 선택한 옵션(option-a 또는 option-b)을 확인하고, 49-SETTINGS-PROPOSAL.md에서 해당 옵션의 구체적 변경 명세를 읽는다.

**공통 변경 불가 규칙 (양 옵션 모두 적용):**
1. L132-165 근처의 `rememberLauncherForActivityResult`와 `LaunchedEffect(driveAuthState)`는 절대 이동하지 않는다
2. ViewModel의 함수 호출 시그니처를 변경하지 않는다
3. `collectAsStateWithLifecycle()` 호출은 참조하는 섹션보다 앞에 위치해야 한다
4. Drive 관련 UI는 반드시 `authState is AuthState.SignedIn` 조건부 블록 내에 유지한다

**Option A 선택 시 (적극적 재구성):**
SettingsSection 호출 순서를 다음으로 변경:
1. "파이프라인" — 완전 자동 모드, STT 엔진, 회의록 엔진, 프롬프트 템플릿
2. "Google 계정" — 로그인/로그아웃, Gemini 인증 모드(API키/OAuth), OAuth Client ID, Gemini API 키
3. "Google Drive" — Drive 연결/해제, 자동 업로드 토글, 폴더 선택 (authState 가드 유지)
4. "API 키" — Groq, Deepgram, CLOVA Speech, CLOVA Summary (각각 엔진 선택 조건부)
5. "모델 관리" — Whisper 모델 다운로드/삭제

**Option B 선택 시 (보수적 재구성):**
- "회의록 설정" + "전사 설정" -> "엔진 설정"으로 합병 (섹션 타이틀만 변경, 내부 순서 유지)
- "Gemini 인증" + "Google 계정 & Drive" -> "Google 계정 & 인증"으로 합병

**검증:** 변경 후 `./gradlew assembleDebug` 빌드 성공 확인.
  </action>
  <verify>
    <automated>cd "D:/workspace/temp/Auto_Minuting" && ./gradlew assembleDebug 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - ./gradlew assembleDebug 빌드가 BUILD SUCCESSFUL로 완료된다
    - SettingsScreen.kt에 SettingsSection 호출이 선택된 옵션에 맞는 타이틀로 존재한다
    - rememberLauncherForActivityResult 호출이 조건부 블록 외부에 유지된다
    - LaunchedEffect(driveAuthState) 호출이 조건부 블록 외부에 유지된다
    - "AuthState.SignedIn" 가드가 Drive 관련 UI를 감싸고 있다
    - SettingsViewModel.kt 파일이 변경되지 않는다 (git diff로 확인)
  </acceptance_criteria>
  <done>SettingsScreen.kt가 승인된 재구성안에 따라 섹션 재배치 완료, 빌드 성공, 기존 기능 동작 유지</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 재구성된 설정 화면 시각 확인</name>
  <files>app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt</files>
  <action>
앱을 기기에 설치하고 설정 화면을 열어 재구성 결과를 사용자에게 확인받는다.
./gradlew installDebug 실행 후 사용자에게 검증 항목을 안내한다.
  </action>
  <what-built>SettingsScreen.kt의 섹션이 승인된 재구성안에 따라 재배치됨</what-built>
  <how-to-verify>
    1. 앱을 기기에 설치: ./gradlew installDebug
    2. 앱 실행 후 설정 화면 진입
    3. 섹션 순서가 승인한 안과 일치하는지 확인
    4. 각 섹션의 항목이 정상 표시되는지 확인
    5. Google 로그인/Drive 연결 상태에 따른 조건부 표시가 정상 동작하는지 확인
    6. STT/회의록 엔진 변경 시 조건부 API 키 입력 필드가 정상 표시/숨김되는지 확인
  </how-to-verify>
  <verify>사용자가 설정 화면을 확인하고 "approved" 응답</verify>
  <done>사용자가 재구성된 설정 화면을 시각적으로 확인하고 승인</done>
  <resume-signal>"approved" 또는 수정 사항을 기술해 주세요</resume-signal>
</task>

</tasks>

<verification>
- SettingsScreen.kt 빌드 성공
- 섹션 순서가 승인안과 일치
- ViewModel 변경 없음
- 조건부 표시 로직 정상 동작
</verification>

<success_criteria>
설정 화면이 승인된 재구성안대로 표시되고, 모든 기존 기능이 정상 동작하며, 사용자가 시각적으로 확인 완료
</success_criteria>

<output>
After completion, create `.planning/phases/49-ui/49-03-SUMMARY.md`
</output>
