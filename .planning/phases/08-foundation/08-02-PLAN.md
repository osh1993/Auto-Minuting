---
phase: 08-foundation
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - app/src/main/java/com/autominuting/data/security/SecureApiKeyRepository.kt
  - app/src/main/java/com/autominuting/di/SecurityModule.kt
  - app/src/main/java/com/autominuting/data/minutes/GeminiEngine.kt
  - app/src/main/java/com/autominuting/presentation/settings/SettingsViewModel.kt
  - app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt
autonomous: true
requirements:
  - AUTH-01

must_haves:
  truths:
    - "사용자가 설정 화면에서 Gemini API 키를 입력할 수 있다"
    - "API 키가 마스킹 토글(눈 아이콘)로 표시/숨김 가능하다"
    - "저장 시 Gemini API 테스트 호출로 유효성이 검증된다"
    - "검증 성공한 API 키가 EncryptedSharedPreferences로 암호화 저장된다"
    - "GeminiEngine이 사용자 설정 API 키를 우선 사용하고, 없으면 BuildConfig 폴백한다"
  artifacts:
    - path: "app/src/main/java/com/autominuting/data/security/SecureApiKeyRepository.kt"
      provides: "API 키 암호화 저장/조회 Repository"
      exports: ["getGeminiApiKey", "saveGeminiApiKey", "clearGeminiApiKey"]
    - path: "app/src/main/java/com/autominuting/di/SecurityModule.kt"
      provides: "Hilt DI 모듈 — 불필요 시 생략 가능 (@Inject constructor로 충분)"
    - path: "app/src/main/java/com/autominuting/data/minutes/GeminiEngine.kt"
      provides: "SecureApiKeyRepository 주입받아 동적 API 키 사용"
      contains: "secureApiKeyRepository.getGeminiApiKey()"
    - path: "app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt"
      provides: "Gemini API 키 입력 섹션 (OutlinedTextField + 마스킹 토글 + 저장 버튼)"
      contains: "PasswordVisualTransformation"
    - path: "app/src/main/java/com/autominuting/presentation/settings/SettingsViewModel.kt"
      provides: "API 키 검증 및 저장 로직"
      contains: "validateAndSaveApiKey"
  key_links:
    - from: "SettingsScreen.kt"
      to: "SettingsViewModel.validateAndSaveApiKey"
      via: "저장 버튼 onClick"
      pattern: "validateAndSaveApiKey"
    - from: "SettingsViewModel.kt"
      to: "SecureApiKeyRepository.saveGeminiApiKey"
      via: "검증 성공 후 저장"
      pattern: "secureApiKeyRepository.saveGeminiApiKey"
    - from: "GeminiEngine.kt"
      to: "SecureApiKeyRepository.getGeminiApiKey"
      via: "생성자 주입으로 API 키 조회"
      pattern: "secureApiKeyRepository.getGeminiApiKey"
    - from: "GeminiEngine.kt"
      to: "BuildConfig.GEMINI_API_KEY"
      via: "폴백 (사용자 키 없을 때)"
      pattern: "BuildConfig.GEMINI_API_KEY"
---

<objective>
Gemini API 키를 설정 UI에서 입력/검증/암호화 저장하고, GeminiEngine이 사용자 키를 우선 사용하도록 인증 추상화를 구현한다.

Purpose: BuildConfig 하드코딩에서 벗어나 사용자가 자신의 API 키로 앱을 독립적으로 사용할 수 있게 한다. Phase 12 OAuth 추가의 기반이 된다.
Output: SecureApiKeyRepository, GeminiEngine 키 주입, Settings UI API 키 섹션
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-foundation/08-CONTEXT.md
@.planning/phases/08-foundation/08-RESEARCH.md
@.planning/phases/08-foundation/08-01-SUMMARY.md

@app/src/main/java/com/autominuting/data/minutes/GeminiEngine.kt
@app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt
@app/src/main/java/com/autominuting/presentation/settings/SettingsViewModel.kt
@app/src/main/java/com/autominuting/data/preferences/UserPreferencesRepository.kt
@gradle/libs.versions.toml
@app/build.gradle.kts

<interfaces>
<!-- Plan 01에서 추가된 security-crypto 의존성이 이미 빌드에 포함됨 -->

From GeminiEngine.kt (변경 전):
```kotlin
@Singleton
class GeminiEngine @Inject constructor() {
    suspend fun generate(transcriptText: String, format: MinutesFormat = MinutesFormat.STRUCTURED): Result<String> {
        val apiKey = BuildConfig.GEMINI_API_KEY  // line 110 — 이것을 동적으로 변경
        // ...
    }
}
```

From SettingsViewModel.kt (변경 전):
```kotlin
@HiltViewModel
class SettingsViewModel @Inject constructor(
    private val userPreferencesRepository: UserPreferencesRepository
) : ViewModel() {
    val minutesFormat: StateFlow<MinutesFormat>
    val automationMode: StateFlow<AutomationMode>
    fun setMinutesFormat(format: MinutesFormat)
    fun setAutomationMode(mode: AutomationMode)
}
```

From SettingsScreen.kt (변경 전):
```kotlin
@OptIn(ExperimentalMaterial3Api::class)
@Composable
fun SettingsScreen(viewModel: SettingsViewModel = hiltViewModel()) {
    // Column 내부에 회의록 형식 + 자동화 모드 섹션
    // 여기에 Gemini API 키 섹션을 추가
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SecureApiKeyRepository + GeminiEngine 인증 추상화</name>
  <files>
    app/src/main/java/com/autominuting/data/security/SecureApiKeyRepository.kt,
    app/src/main/java/com/autominuting/data/minutes/GeminiEngine.kt
  </files>
  <read_first>
    app/src/main/java/com/autominuting/data/minutes/GeminiEngine.kt,
    app/build.gradle.kts,
    gradle/libs.versions.toml
  </read_first>
  <action>
    **1. SecureApiKeyRepository.kt 신규 생성** (per D-07):
    - 경로: `app/src/main/java/com/autominuting/data/security/SecureApiKeyRepository.kt`
    - 패키지: `com.autominuting.data.security`
    - `@Singleton` + `@Inject constructor(@ApplicationContext context: Context)`로 Hilt가 자동 주입 (별도 Module 불필요)
    - 구현:
      ```kotlin
      package com.autominuting.data.security

      import android.content.Context
      import android.content.SharedPreferences
      import android.util.Log
      import androidx.security.crypto.EncryptedSharedPreferences
      import androidx.security.crypto.MasterKeys
      import dagger.hilt.android.qualifiers.ApplicationContext
      import javax.inject.Inject
      import javax.inject.Singleton

      /**
       * Gemini API 키를 EncryptedSharedPreferences로 암호화하여 저장/조회하는 Repository.
       * 기존 DataStore와 별도 저장소로 분리하여 민감 데이터의 관심사를 분리한다.
       */
      @Singleton
      class SecureApiKeyRepository @Inject constructor(
          @ApplicationContext private val context: Context
      ) {
          companion object {
              private const val TAG = "SecureApiKeyRepository"
              private const val PREFS_FILE = "secure_api_keys"
              private const val KEY_GEMINI_API = "gemini_api_key"
          }

          @Suppress("DEPRECATION")
          private val encryptedPrefs: SharedPreferences? by lazy {
              try {
                  EncryptedSharedPreferences.create(
                      PREFS_FILE,
                      MasterKeys.getOrCreate(MasterKeys.AES256_GCM_SPEC),
                      context,
                      EncryptedSharedPreferences.PrefKeyEncryptionScheme.AES256_SIV,
                      EncryptedSharedPreferences.PrefValueEncryptionScheme.AES256_GCM
                  )
              } catch (e: Exception) {
                  // OEM별 KeyStore 초기화 실패 방어 (Samsung/Xiaomi 일부 기기)
                  Log.e(TAG, "EncryptedSharedPreferences 초기화 실패", e)
                  null
              }
          }

          /** 저장된 Gemini API 키를 반환한다. 없거나 초기화 실패 시 null. */
          fun getGeminiApiKey(): String? =
              encryptedPrefs?.getString(KEY_GEMINI_API, null)

          /** Gemini API 키를 암호화하여 저장한다. */
          fun saveGeminiApiKey(apiKey: String) {
              encryptedPrefs?.edit()?.putString(KEY_GEMINI_API, apiKey)?.apply()
                  ?: Log.w(TAG, "API 키 저장 실패: EncryptedSharedPreferences 사용 불가")
          }

          /** 저장된 Gemini API 키를 삭제한다. */
          fun clearGeminiApiKey() {
              encryptedPrefs?.edit()?.remove(KEY_GEMINI_API)?.apply()
          }

          /** EncryptedSharedPreferences가 정상 초기화되었는지 반환한다. */
          fun isAvailable(): Boolean = encryptedPrefs != null
      }
      ```

    **2. GeminiEngine.kt 변경** (per D-08, D-09, D-10):
    - 생성자에 `SecureApiKeyRepository` 주입 추가:
      `class GeminiEngine @Inject constructor(private val secureApiKeyRepository: SecureApiKeyRepository)`
    - `generate()` 함수 내부의 `val apiKey = BuildConfig.GEMINI_API_KEY` (line 110)를 다음으로 교체:
      ```kotlin
      // 사용자 설정 API 키 우선, 없으면 BuildConfig 폴백 (per D-08, D-10)
      val apiKey = secureApiKeyRepository.getGeminiApiKey()
          ?: BuildConfig.GEMINI_API_KEY
      ```
    - import 추가: `import com.autominuting.data.security.SecureApiKeyRepository`
  </action>
  <verify>
    <automated>cd D:/workspace/temp/Auto_Minuting && ./gradlew :app:compileDebugKotlin 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - SecureApiKeyRepository.kt 파일이 `app/src/main/java/com/autominuting/data/security/` 경로에 존재
    - SecureApiKeyRepository.kt에 `@Singleton` 존재
    - SecureApiKeyRepository.kt에 `EncryptedSharedPreferences.create` 존재
    - SecureApiKeyRepository.kt에 `fun getGeminiApiKey(): String?` 존재
    - SecureApiKeyRepository.kt에 `fun saveGeminiApiKey(apiKey: String)` 존재
    - SecureApiKeyRepository.kt에 `fun clearGeminiApiKey()` 존재
    - SecureApiKeyRepository.kt에 `@Suppress("DEPRECATION")` 존재
    - SecureApiKeyRepository.kt에 `catch (e: Exception)` 존재 (OEM 방어)
    - GeminiEngine.kt에 `secureApiKeyRepository: SecureApiKeyRepository` 존재
    - GeminiEngine.kt에 `secureApiKeyRepository.getGeminiApiKey()` 존재
    - GeminiEngine.kt에 `?: BuildConfig.GEMINI_API_KEY` 존재 (폴백)
    - `./gradlew :app:compileDebugKotlin` 성공 (BUILD SUCCESSFUL)
  </acceptance_criteria>
  <done>SecureApiKeyRepository가 EncryptedSharedPreferences로 API 키를 암호화 저장하고, GeminiEngine이 사용자 키 우선 + BuildConfig 폴백 구조로 동작한다. 컴파일 성공.</done>
</task>

<task type="auto">
  <name>Task 2: 설정 화면 API 키 입력 UI + 검증 로직</name>
  <files>
    app/src/main/java/com/autominuting/presentation/settings/SettingsViewModel.kt,
    app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt
  </files>
  <read_first>
    app/src/main/java/com/autominuting/presentation/settings/SettingsViewModel.kt,
    app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt,
    app/src/main/java/com/autominuting/data/security/SecureApiKeyRepository.kt
  </read_first>
  <action>
    **1. SettingsViewModel.kt 변경** (per D-06):

    **1a. 생성자에 SecureApiKeyRepository 주입 추가:**
    ```kotlin
    @HiltViewModel
    class SettingsViewModel @Inject constructor(
        private val userPreferencesRepository: UserPreferencesRepository,
        private val secureApiKeyRepository: SecureApiKeyRepository
    ) : ViewModel() {
    ```
    - import 추가: `import com.autominuting.data.security.SecureApiKeyRepository`

    **1b. API 키 검증 상태 sealed interface 추가** (클래스 내부 또는 파일 하단):
    ```kotlin
    /** API 키 검증 상태 */
    sealed interface ApiKeyValidationState {
        data object Idle : ApiKeyValidationState
        data object Validating : ApiKeyValidationState
        data object Success : ApiKeyValidationState
        data class Error(val message: String) : ApiKeyValidationState
    }
    ```

    **1c. API 키 관련 StateFlow 추가** (기존 minutesFormat, automationMode 아래):
    ```kotlin
    /** API 키 검증 상태 */
    private val _apiKeyValidationState = MutableStateFlow<ApiKeyValidationState>(ApiKeyValidationState.Idle)
    val apiKeyValidationState: StateFlow<ApiKeyValidationState> = _apiKeyValidationState.asStateFlow()

    /** 저장된 API 키 존재 여부 */
    private val _hasApiKey = MutableStateFlow(false)
    val hasApiKey: StateFlow<Boolean> = _hasApiKey.asStateFlow()

    init {
        // 초기 로드: 저장된 API 키 존재 여부 확인
        _hasApiKey.value = secureApiKeyRepository.getGeminiApiKey() != null
    }
    ```
    - import 추가: `import kotlinx.coroutines.flow.MutableStateFlow`, `import kotlinx.coroutines.flow.asStateFlow` (이미 있을 수 있음)

    **1d. validateAndSaveApiKey 함수 추가:**
    ```kotlin
    /** API 키를 Gemini API 테스트 호출로 검증 후 암호화 저장한다. */
    fun validateAndSaveApiKey(apiKey: String) {
        viewModelScope.launch {
            _apiKeyValidationState.value = ApiKeyValidationState.Validating
            try {
                val model = GenerativeModel(
                    modelName = "gemini-2.5-flash",
                    apiKey = apiKey
                )
                // 최소 비용 테스트 호출
                val response = withTimeout(10_000) {
                    model.generateContent("Hello")
                }
                if (response.text != null) {
                    secureApiKeyRepository.saveGeminiApiKey(apiKey)
                    _hasApiKey.value = true
                    _apiKeyValidationState.value = ApiKeyValidationState.Success
                } else {
                    _apiKeyValidationState.value = ApiKeyValidationState.Error("빈 응답이 반환되었습니다")
                }
            } catch (e: Exception) {
                _apiKeyValidationState.value = ApiKeyValidationState.Error(
                    e.message ?: "API 키 검증에 실패했습니다"
                )
            }
        }
    }

    /** API 키 검증 상태를 초기화한다. */
    fun resetApiKeyValidationState() {
        _apiKeyValidationState.value = ApiKeyValidationState.Idle
    }

    /** 저장된 API 키를 삭제한다. */
    fun clearApiKey() {
        secureApiKeyRepository.clearGeminiApiKey()
        _hasApiKey.value = false
    }
    ```
    - import 추가: `import com.google.ai.client.generativeai.GenerativeModel`, `import kotlinx.coroutines.withTimeout`

    **2. SettingsScreen.kt — Gemini API 키 섹션 추가** (per D-04, D-05):

    **2a. Column 내부 자동화 모드 섹션 아래에 Gemini API 섹션 추가:**
    ```kotlin
    // --- Gemini API 키 섹션 --- (per D-04)
    Spacer(modifier = Modifier.height(24.dp))

    Text(
        text = "Gemini API",
        style = MaterialTheme.typography.titleMedium
    )
    Text(
        text = "Gemini API 키를 입력하면 내장 키 대신 사용합니다",
        style = MaterialTheme.typography.bodySmall,
        color = MaterialTheme.colorScheme.onSurfaceVariant
    )

    Spacer(modifier = Modifier.height(8.dp))

    val apiKeyValidationState by viewModel.apiKeyValidationState.collectAsStateWithLifecycle()
    val hasApiKey by viewModel.hasApiKey.collectAsStateWithLifecycle()
    var apiKeyInput by remember { mutableStateOf("") }
    var isKeyVisible by remember { mutableStateOf(false) }

    // API 키 입력 필드 (per D-05)
    OutlinedTextField(
        value = apiKeyInput,
        onValueChange = {
            apiKeyInput = it
            viewModel.resetApiKeyValidationState()
        },
        label = { Text("Gemini API 키") },
        visualTransformation = if (isKeyVisible)
            VisualTransformation.None
        else
            PasswordVisualTransformation(),
        trailingIcon = {
            IconButton(onClick = { isKeyVisible = !isKeyVisible }) {
                Icon(
                    imageVector = if (isKeyVisible)
                        Icons.Default.VisibilityOff
                    else
                        Icons.Default.Visibility,
                    contentDescription = if (isKeyVisible) "숨기기" else "보기"
                )
            }
        },
        singleLine = true,
        enabled = apiKeyValidationState !is ApiKeyValidationState.Validating,
        modifier = Modifier.fillMaxWidth()
    )

    Spacer(modifier = Modifier.height(8.dp))

    // 저장 버튼 + 상태 표시
    Row(
        verticalAlignment = Alignment.CenterVertically,
        horizontalArrangement = Arrangement.spacedBy(8.dp)
    ) {
        Button(
            onClick = { viewModel.validateAndSaveApiKey(apiKeyInput.trim()) },
            enabled = apiKeyInput.isNotBlank() && apiKeyValidationState !is ApiKeyValidationState.Validating
        ) {
            if (apiKeyValidationState is ApiKeyValidationState.Validating) {
                CircularProgressIndicator(
                    modifier = Modifier.size(16.dp),
                    strokeWidth = 2.dp,
                    color = MaterialTheme.colorScheme.onPrimary
                )
                Spacer(modifier = Modifier.width(8.dp))
            }
            Text("검증 후 저장")
        }

        if (hasApiKey) {
            OutlinedButton(onClick = {
                viewModel.clearApiKey()
                apiKeyInput = ""
            }) {
                Text("키 삭제")
            }
        }
    }

    // 검증 결과 메시지
    when (val state = apiKeyValidationState) {
        is ApiKeyValidationState.Success -> {
            Spacer(modifier = Modifier.height(4.dp))
            Text(
                text = "API 키가 저장되었습니다",
                style = MaterialTheme.typography.bodySmall,
                color = MaterialTheme.colorScheme.primary
            )
        }
        is ApiKeyValidationState.Error -> {
            Spacer(modifier = Modifier.height(4.dp))
            Text(
                text = state.message,
                style = MaterialTheme.typography.bodySmall,
                color = MaterialTheme.colorScheme.error
            )
        }
        else -> {}
    }

    if (hasApiKey) {
        Spacer(modifier = Modifier.height(4.dp))
        Text(
            text = "사용자 API 키 사용 중",
            style = MaterialTheme.typography.bodySmall,
            color = MaterialTheme.colorScheme.tertiary
        )
    }
    ```

    **2b. import 추가:**
    ```kotlin
    import androidx.compose.material.icons.filled.Visibility
    import androidx.compose.material.icons.filled.VisibilityOff
    import androidx.compose.material3.AlertDialog
    import androidx.compose.material3.Button
    import androidx.compose.material3.CircularProgressIndicator
    import androidx.compose.material3.IconButton
    import androidx.compose.material3.OutlinedButton
    import androidx.compose.material3.TextButton
    import androidx.compose.foundation.layout.Arrangement
    import androidx.compose.foundation.layout.size
    import androidx.compose.foundation.layout.width
    import androidx.compose.ui.text.input.PasswordVisualTransformation
    import androidx.compose.ui.text.input.VisualTransformation
    import com.autominuting.presentation.settings.ApiKeyValidationState
    ```
    (이미 있는 import는 중복 추가하지 않을 것)
  </action>
  <verify>
    <automated>cd D:/workspace/temp/Auto_Minuting && ./gradlew :app:compileDebugKotlin 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - SettingsViewModel.kt에 `secureApiKeyRepository: SecureApiKeyRepository` 생성자 파라미터 존재
    - SettingsViewModel.kt에 `sealed interface ApiKeyValidationState` 존재
    - SettingsViewModel.kt에 `fun validateAndSaveApiKey(apiKey: String)` 존재
    - SettingsViewModel.kt에 `GenerativeModel(` 존재 (테스트 호출)
    - SettingsViewModel.kt에 `withTimeout(10_000)` 존재
    - SettingsViewModel.kt에 `secureApiKeyRepository.saveGeminiApiKey(apiKey)` 존재
    - SettingsViewModel.kt에 `val hasApiKey: StateFlow<Boolean>` 존재
    - SettingsViewModel.kt에 `fun clearApiKey()` 존재
    - SettingsScreen.kt에 `"Gemini API"` 텍스트 존재
    - SettingsScreen.kt에 `PasswordVisualTransformation` 존재
    - SettingsScreen.kt에 `Icons.Default.Visibility` 존재
    - SettingsScreen.kt에 `validateAndSaveApiKey` 존재
    - SettingsScreen.kt에 `"검증 후 저장"` 텍스트 존재
    - SettingsScreen.kt에 `CircularProgressIndicator` 존재
    - SettingsScreen.kt에 `"API 키가 저장되었습니다"` 텍스트 존재
    - SettingsScreen.kt에 `"사용자 API 키 사용 중"` 텍스트 존재
    - `./gradlew :app:compileDebugKotlin` 성공 (BUILD SUCCESSFUL)
  </acceptance_criteria>
  <done>설정 화면에 Gemini API 키 입력/마스킹/검증/저장 UI가 추가되고, 검증 성공 시 EncryptedSharedPreferences에 암호화 저장된다. BuildConfig 대신 사용자 키가 우선 사용된다. 컴파일 성공.</done>
</task>

</tasks>

<verification>
1. `./gradlew :app:compileDebugKotlin` 빌드 성공
2. SecureApiKeyRepository가 EncryptedSharedPreferences로 API 키 암호화 저장
3. GeminiEngine이 사용자 키 우선 + BuildConfig 폴백 구조로 동작
4. SettingsScreen에 API 키 입력/마스킹/검증/저장 UI 존재
5. 검증 시 로딩 인디케이터 표시, 성공/실패 메시지 표시
</verification>

<success_criteria>
- SecureApiKeyRepository가 API 키를 EncryptedSharedPreferences로 암호화 저장/조회한다
- GeminiEngine이 SecureApiKeyRepository를 주입받아 사용자 키 우선, BuildConfig 폴백으로 동작한다
- 설정 화면에서 API 키를 입력/마스킹 토글/검증/저장할 수 있다
- 검증 실패 시 에러 메시지가 표시된다
- 전체 빌드 성공
</success_criteria>

<output>
After completion, create `.planning/phases/08-foundation/08-02-SUMMARY.md`
</output>
