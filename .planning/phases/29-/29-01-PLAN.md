---
phase: 29-전사-카드-ux-개선
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/com/autominuting/presentation/dashboard/DashboardViewModel.kt
  - app/src/main/java/com/autominuting/presentation/dashboard/DashboardScreen.kt
  - app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt
autonomous: true
requirements: [PIPE-01, PIPE-02]

must_haves:
  truths:
    - "하이브리드 모드에서 전사가 완료되면 대시보드 배너에 '회의록 생성' 버튼이 나타난다"
    - "사용자가 확인 버튼을 누르면 회의록 생성이 시작된다"
    - "사용자가 '무시' 버튼을 누르면 배너가 닫히고 전사만 유지된다"
    - "전사 카드에서 '회의록 작성'과 '재생성' 버튼이 카드 하단이 아닌 MoreVert 메뉴 안에 있다"
    - "전사 카드의 세로 높이가 이전보다 줄어든다"
  artifacts:
    - path: "app/src/main/java/com/autominuting/presentation/dashboard/DashboardViewModel.kt"
      provides: "automationMode 관찰 + generateMinutes + dismissPipeline 함수"
      contains: "automationMode"
    - path: "app/src/main/java/com/autominuting/presentation/dashboard/DashboardScreen.kt"
      provides: "TRANSCRIBED + HYBRID 상태에서 확인/무시 버튼 렌더링"
      contains: "회의록 생성"
    - path: "app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt"
      provides: "회의록 작성/재생성이 DropdownMenuItem으로 이동"
      contains: "회의록 작성"
  key_links:
    - from: "DashboardScreen.kt"
      to: "DashboardViewModel.kt"
      via: "automationMode StateFlow 구독"
      pattern: "automationMode.*collectAsState"
    - from: "DashboardViewModel.kt"
      to: "UserPreferencesRepository"
      via: "DI 주입 후 automationMode Flow 관찰"
      pattern: "userPreferencesRepository.automationMode"
    - from: "TranscriptsScreen.kt"
      to: "DropdownMenu"
      via: "회의록 작성/재생성 메뉴 항목"
      pattern: "DropdownMenuItem.*회의록"
---

<objective>
하이브리드 모드에서 전사 완료 시 대시보드에 회의록 생성 확인 플로우를 추가하고,
전사 카드의 회의록 관련 버튼을 MoreVert 드롭다운 메뉴로 이동하여 카드 면적을 줄인다.

Purpose: 하이브리드 모드 사용자가 전사 후 회의록 생성 여부를 명시적으로 선택할 수 있고, 전사 목록의 카드 높이를 줄여 가독성을 높인다.
Output: DashboardScreen 배너에 확인 버튼 + TranscriptsScreen 카드에서 버튼 제거 및 메뉴 이동
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

@app/src/main/java/com/autominuting/presentation/dashboard/DashboardViewModel.kt
@app/src/main/java/com/autominuting/presentation/dashboard/DashboardScreen.kt
@app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt
@app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsViewModel.kt
@app/src/main/java/com/autominuting/domain/model/AutomationMode.kt
@app/src/main/java/com/autominuting/domain/model/PipelineStatus.kt

<interfaces>
<!-- DashboardViewModel에 주입해야 할 의존성 -->
From data/preferences/UserPreferencesRepository.kt:
```kotlin
val automationMode: Flow<AutomationMode>  // FULL_AUTO | HYBRID
suspend fun getAutomationModeOnce(): AutomationMode
```

From worker/MinutesGenerationWorker.kt:
```kotlin
companion object {
    const val KEY_MEETING_ID = "meetingId"
    const val KEY_TRANSCRIPT_PATH = "transcriptPath"
    const val KEY_MINUTES_FORMAT = "minutesFormat"
}
```

From data/preferences/UserPreferencesRepository.kt:
```kotlin
suspend fun getMinutesFormatOnce(): MinutesFormat
```

<!-- TranscriptsScreen 기존 MoreVert 메뉴 구조 -->
기존 DropdownMenu 항목 순서: 재전사 → 공유 → 삭제
추가할 항목: "회의록 작성" (TRANSCRIBED/FAILED+transcriptPath), "회의록 재생성" (COMPLETED)

<!-- DashboardScreen activePipeline 배너 조건 -->
activeStatuses: AUDIO_RECEIVED, TRANSCRIBING, TRANSCRIBED, GENERATING_MINUTES
TRANSCRIBED 상태 + HYBRID 모드일 때만 확인/무시 버튼 표시
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 대시보드 하이브리드 모드 확인 플로우 (PIPE-01)</name>
  <files>
    app/src/main/java/com/autominuting/presentation/dashboard/DashboardViewModel.kt
    app/src/main/java/com/autominuting/presentation/dashboard/DashboardScreen.kt
  </files>
  <read_first>
    app/src/main/java/com/autominuting/presentation/dashboard/DashboardViewModel.kt
    app/src/main/java/com/autominuting/presentation/dashboard/DashboardScreen.kt
    app/src/main/java/com/autominuting/data/preferences/UserPreferencesRepository.kt
    app/src/main/java/com/autominuting/domain/model/AutomationMode.kt
  </read_first>
  <action>
**DashboardViewModel.kt 수정:**

1. 생성자에 `UserPreferencesRepository` 주입 추가:
   ```kotlin
   @HiltViewModel
   class DashboardViewModel @Inject constructor(
       private val meetingRepository: MeetingRepository,
       private val userPreferencesRepository: UserPreferencesRepository,
       @ApplicationContext private val context: Context
   ) : ViewModel()
   ```
   import: `com.autominuting.data.preferences.UserPreferencesRepository`

2. `automationMode` StateFlow 추가:
   ```kotlin
   val automationMode: StateFlow<AutomationMode> = userPreferencesRepository.automationMode
       .stateIn(
           scope = viewModelScope,
           started = SharingStarted.WhileSubscribed(5_000),
           initialValue = AutomationMode.FULL_AUTO
       )
   ```
   import: `com.autominuting.domain.model.AutomationMode`

3. `generateMinutesForPipeline(meetingId: Long)` 함수 추가 -- TranscriptsViewModel.generateMinutes 로직과 동일하게 MinutesGenerationWorker enqueue:
   ```kotlin
   fun generateMinutesForPipeline(meetingId: Long) {
       viewModelScope.launch {
           val meeting = meetingRepository.getMeetingById(meetingId).first() ?: return@launch
           if (meeting.transcriptPath == null) return@launch
           val minutesFormat = userPreferencesRepository.getMinutesFormatOnce()
           val workRequest = OneTimeWorkRequestBuilder<MinutesGenerationWorker>()
               .setInputData(workDataOf(
                   MinutesGenerationWorker.KEY_MEETING_ID to meetingId,
                   MinutesGenerationWorker.KEY_TRANSCRIPT_PATH to meeting.transcriptPath,
                   MinutesGenerationWorker.KEY_MINUTES_FORMAT to minutesFormat.name
               ))
               .build()
           WorkManager.getInstance(context).enqueue(workRequest)
       }
   }
   ```
   import: `com.autominuting.worker.MinutesGenerationWorker`, `androidx.work.OneTimeWorkRequestBuilder`, `kotlinx.coroutines.flow.first`

4. `dismissPipeline(meetingId: Long)` 함수 추가 -- activePipeline 배너를 닫기 위해 pipelineStatus를 TRANSCRIBED 그대로 두되, dismissed 목록에 추가:
   ```kotlin
   private val _dismissedPipelineIds = MutableStateFlow<Set<Long>>(emptySet())

   // activePipeline을 수정: dismissed된 ID는 필터링
   val activePipeline: StateFlow<Meeting?> = meetingRepository.getMeetings()
       .combine(_dismissedPipelineIds) { meetings, dismissed ->
           meetings.firstOrNull { it.pipelineStatus in activeStatuses && it.id !in dismissed }
       }
       .stateIn(...)

   fun dismissPipeline(meetingId: Long) {
       _dismissedPipelineIds.value = _dismissedPipelineIds.value + meetingId
   }
   ```
   import: `kotlinx.coroutines.flow.combine`

**DashboardScreen.kt 수정:**

5. `automationMode` 상태 구독 추가:
   ```kotlin
   val automationMode by viewModel.automationMode.collectAsStateWithLifecycle()
   ```

6. activePipeline 배너의 `PipelineStatus.TRANSCRIBED` 분기에서 `automationMode == AutomationMode.HYBRID`일 때 CircularProgressIndicator 대신 아이콘(Icons.Default.CheckCircle 등)을 사용하고, statusText 아래에 "회의록 생성" 버튼과 "무시" 버튼을 Row로 추가:
   ```kotlin
   PipelineStatus.TRANSCRIBED -> {
       if (automationMode == AutomationMode.HYBRID) {
           // 하이브리드 모드: 확인 버튼 표시
           "전사 완료"  // statusText를 간결하게
       } else {
           "전사 완료 — 회의록 생성 대기"
       }
   }
   ```

   TRANSCRIBED + HYBRID일 때 카드 하단에 버튼 Row 추가:
   ```kotlin
   if (meeting.pipelineStatus == PipelineStatus.TRANSCRIBED
       && automationMode == AutomationMode.HYBRID) {
       Spacer(modifier = Modifier.height(8.dp))
       Row(
           modifier = Modifier.fillMaxWidth(),
           horizontalArrangement = Arrangement.End,
           verticalAlignment = Alignment.CenterVertically
       ) {
           TextButton(onClick = { viewModel.dismissPipeline(meeting.id) }) {
               Text("무시")
           }
           Spacer(modifier = Modifier.width(8.dp))
           Button(onClick = { viewModel.generateMinutesForPipeline(meeting.id) }) {
               Text("회의록 생성")
           }
       }
   }
   ```
   import: `com.autominuting.domain.model.AutomationMode`, `androidx.compose.foundation.layout.Arrangement`
  </action>
  <verify>
    <automated>cd d:/workspace/temp/Auto_Minuting && ./gradlew compileDebugKotlin 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "automationMode" app/src/main/java/com/autominuting/presentation/dashboard/DashboardViewModel.kt
    - grep -q "AutomationMode.HYBRID" app/src/main/java/com/autominuting/presentation/dashboard/DashboardScreen.kt
    - grep -q "generateMinutesForPipeline" app/src/main/java/com/autominuting/presentation/dashboard/DashboardViewModel.kt
    - grep -q "dismissPipeline" app/src/main/java/com/autominuting/presentation/dashboard/DashboardViewModel.kt
    - grep -q "회의록 생성" app/src/main/java/com/autominuting/presentation/dashboard/DashboardScreen.kt
    - grep -q "무시" app/src/main/java/com/autominuting/presentation/dashboard/DashboardScreen.kt
  </acceptance_criteria>
  <done>
    DashboardViewModel에 automationMode StateFlow, generateMinutesForPipeline(), dismissPipeline()이 존재하고,
    DashboardScreen의 activePipeline 배너에서 TRANSCRIBED + HYBRID일 때 "회의록 생성"/"무시" 버튼이 렌더링되며,
    compileDebugKotlin이 성공한다.
  </done>
</task>

<task type="auto">
  <name>Task 2: 전사 카드 회의록 버튼 MoreVert 메뉴 이동 (PIPE-02)</name>
  <files>
    app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt
  </files>
  <read_first>
    app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt
  </read_first>
  <action>
**TranscriptsScreen.kt의 TranscriptMeetingCard 수정:**

1. 카드 하단의 when(meeting.pipelineStatus) 블록 전체를 삭제한다 (358~402행의 TRANSCRIBED/FAILED/COMPLETED 분기에서 FilledTonalButton "회의록 작성"과 OutlinedButton "회의록 재생성"을 렌더링하는 코드).

2. 기존 DropdownMenu 내부에 "회의록 작성"과 "회의록 재생성" 메뉴 항목을 추가한다. 위치는 "재전사" 항목 바로 아래, "공유" 항목 위에 배치한다.

   **"회의록 작성" 항목 -- TRANSCRIBED 또는 (FAILED + transcriptPath != null)일 때 표시:**
   ```kotlin
   if (meeting.pipelineStatus == PipelineStatus.TRANSCRIBED ||
       (meeting.pipelineStatus == PipelineStatus.FAILED && meeting.transcriptPath != null)) {
       DropdownMenuItem(
           text = { Text("회의록 작성") },
           onClick = {
               showMenu = false
               onGenerateMinutes(meeting.id)
           },
           leadingIcon = {
               Icon(Icons.Default.Description, contentDescription = null)
           }
       )
   }
   ```
   import: `androidx.compose.material.icons.filled.Description`

   **"회의록 재생성" 항목 -- COMPLETED일 때 표시:**
   ```kotlin
   if (meeting.pipelineStatus == PipelineStatus.COMPLETED) {
       DropdownMenuItem(
           text = { Text("회의록 재생성") },
           onClick = {
               showMenu = false
               onRegenerateMinutes(meeting)
           },
           leadingIcon = {
               Icon(Icons.Default.Refresh, contentDescription = null)
           }
       )
   }
   ```

최종 DropdownMenu 항목 순서: 재전사 → 회의록 작성 → 회의록 재생성 → 공유 → 삭제
  </action>
  <verify>
    <automated>cd d:/workspace/temp/Auto_Minuting && ./gradlew compileDebugKotlin 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "DropdownMenuItem" app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt | grep -c "회의록" 가 2 이상
    - grep -qv "FilledTonalButton" app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt (FilledTonalButton이 없어야 함)
    - grep -qv "OutlinedButton.*회의록" app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt (OutlinedButton 회의록 재생성이 없어야 함)
    - grep -q "회의록 작성" app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt
    - grep -q "회의록 재생성" app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt
  </acceptance_criteria>
  <done>
    TranscriptMeetingCard에서 카드 하단의 FilledTonalButton("회의록 작성")과 OutlinedButton("회의록 재생성")이 제거되고,
    MoreVert DropdownMenu 안에 "회의록 작성"과 "회의록 재생성" 항목이 추가되어,
    카드 높이가 줄어들고 compileDebugKotlin이 성공한다.
  </done>
</task>

</tasks>

<verification>
1. `./gradlew compileDebugKotlin` 성공
2. DashboardScreen: TRANSCRIBED + HYBRID 상태에서 "회의록 생성"/"무시" 버튼이 배너에 표시됨
3. TranscriptsScreen: 카드 하단에 회의록 관련 버튼이 없고, MoreVert 메뉴에 "회의록 작성"/"회의록 재생성" 항목이 있음
4. TranscriptsScreen: 카드 세로 높이가 줄어든 것을 확인 (버튼 Row + Spacer 제거로 ~40dp 절약)
</verification>

<success_criteria>
- 하이브리드 모드에서 전사 완료된 파이프라인의 배너에 확인/무시 버튼이 나타난다
- 확인 시 MinutesGenerationWorker가 enqueue되고, 무시 시 배너가 사라진다
- 전사 카드의 회의록 작성/재생성 버튼이 MoreVert 드롭다운 메뉴로 이동하여 카드 면적이 줄어든다
- 빌드가 성공한다
</success_criteria>

<output>
After completion, create `.planning/phases/29-/29-01-SUMMARY.md`
</output>
