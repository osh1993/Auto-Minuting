---
phase: 17-ui-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/res/drawable/ic_launcher_foreground.xml
  - app/src/main/res/drawable/ic_launcher_background.xml
  - app/src/main/java/com/autominuting/presentation/dashboard/DashboardScreen.kt
  - app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt
  - app/src/main/java/com/autominuting/spike/SamsungRecorderObserver.kt
  - app/src/main/java/com/autominuting/spike/SpikeLogActivity.kt
  - app/src/main/java/com/autominuting/spike/SpikeService.kt
  - app/src/main/java/com/autominuting/spike/SpikeVerificationHelper.kt
  - app/src/main/AndroidManifest.xml
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03

must_haves:
  truths:
    - "앱 런처에 새 아이콘(마이크+문서 결합 디자인)이 표시된다"
    - "NotebookLM 열기 버튼이 DashboardScreen(홈 탭)에 존재한다"
    - "SettingsScreen에서 NotebookLM 섹션이 제거되었다"
    - "spike 패키지 파일이 프로젝트에서 완전히 삭제되었다"
    - "AndroidManifest.xml에 spike 관련 등록이 없다"
    - "프로젝트가 정상 빌드된다"
  artifacts:
    - path: "app/src/main/res/drawable/ic_launcher_foreground.xml"
      provides: "새 앱 아이콘 전경 레이어"
      contains: "마이크+문서 결합 벡터"
    - path: "app/src/main/res/drawable/ic_launcher_background.xml"
      provides: "새 앱 아이콘 배경 레이어"
      contains: "그라데이션 배경"
    - path: "app/src/main/java/com/autominuting/presentation/dashboard/DashboardScreen.kt"
      provides: "NotebookLM 열기 버튼이 포함된 홈 화면"
      contains: "NotebookLmHelper.openNotebookLmWeb"
  key_links:
    - from: "DashboardScreen.kt"
      to: "NotebookLmHelper"
      via: "import + onClick 호출"
      pattern: "NotebookLmHelper\\.openNotebookLmWeb"
    - from: "AndroidManifest.xml"
      to: "spike 패키지"
      via: "service/activity 등록 제거"
      pattern: "spike 문자열 없음"
---

<objective>
앱 아이콘 교체, NotebookLM 버튼 이동, spike 테스트 코드 제거를 통해 배포 품질의 UI/UX를 완성한다.

Purpose: 테스트용 코드 정리 및 UX 개선으로 v2.1 안정화 마일스톤의 UI 정리를 완료한다.
Output: 새 앱 아이콘, 개선된 홈 화면 레이아웃, 클린한 코드베이스
</objective>

<execution_context>
@C:/Users/ADMIN/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ADMIN/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@app/src/main/res/drawable/ic_launcher_foreground.xml
@app/src/main/res/drawable/ic_launcher_background.xml
@app/src/main/java/com/autominuting/presentation/dashboard/DashboardScreen.kt
@app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt
@app/src/main/java/com/autominuting/util/NotebookLmHelper.kt
@app/src/main/AndroidManifest.xml
</context>

<tasks>

<task type="auto">
  <name>Task 1: 앱 아이콘 교체 (UI-01)</name>
  <files>
    app/src/main/res/drawable/ic_launcher_foreground.xml
    app/src/main/res/drawable/ic_launcher_background.xml
  </files>
  <read_first>
    app/src/main/res/drawable/ic_launcher_foreground.xml
    app/src/main/res/drawable/ic_launcher_background.xml
    app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml
  </read_first>
  <action>
    앱의 컨셉(녹음 -> 회의록 자동화)을 반영한 새 아이콘으로 교체한다. (per UI-01)

    1. `ic_launcher_background.xml` 수정:
       - 기존 단색(#2196F3) 배경을 Material You 친화적 그라데이션으로 변경
       - 진한 인디고(#1A237E)에서 밝은 블루(#42A5F5)로 대각선 그라데이션 적용
       - 108dp x 108dp 유지 (adaptive icon 표준)

    2. `ic_launcher_foreground.xml` 수정:
       - 기존 단순 마이크 아이콘을 마이크+문서 결합 디자인으로 교체
       - 왼쪽에 마이크 심볼(녹음), 오른쪽에 문서/노트 심볼(회의록)을 결합
       - 전경 아이콘 색상: 흰색(#FFFFFF)
       - safe zone (66dp 내부) 안에 핵심 그래픽이 위치하도록 pathData 조정
       - 108dp x 108dp 유지

    기존 mipmap-anydpi-v26/ic_launcher.xml과 ic_launcher_round.xml은 이미 drawable를 참조하므로 수정 불필요.
    mipmap-hdpi ~ mipmap-xxxhdpi 폴더는 비어있으므로 (adaptive icon이 우선) 별도 작업 불필요.
  </action>
  <acceptance_criteria>
    - ic_launcher_foreground.xml에 마이크+문서 결합 pathData가 존재
    - ic_launcher_background.xml에 그라데이션이 적용
    - 두 파일 모두 108dp viewportWidth/viewportHeight 유지
  </acceptance_criteria>
  <verify>
    grep -q "108" app/src/main/res/drawable/ic_launcher_foreground.xml && grep -q "108" app/src/main/res/drawable/ic_launcher_background.xml && echo "PASS: icon dimensions OK"
  </verify>
  <done>새 앱 아이콘 벡터가 foreground/background XML에 반영되어 adaptive icon으로 표시 가능한 상태</done>
</task>

<task type="auto">
  <name>Task 2: NotebookLM 버튼을 DashboardScreen으로 이동 (UI-02)</name>
  <files>
    app/src/main/java/com/autominuting/presentation/dashboard/DashboardScreen.kt
    app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt
  </files>
  <read_first>
    app/src/main/java/com/autominuting/presentation/dashboard/DashboardScreen.kt
    app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt
    app/src/main/java/com/autominuting/util/NotebookLmHelper.kt
  </read_first>
  <action>
    NotebookLM 열기 버튼을 SettingsScreen에서 제거하고 DashboardScreen에 추가한다. (per UI-02)

    1. DashboardScreen.kt 수정:
       - import 추가: `import com.autominuting.util.NotebookLmHelper`, `import androidx.compose.ui.platform.LocalContext`, `import androidx.compose.material.icons.automirrored.filled.MenuBook`
       - DashboardScreen 함수 상단에 `val context = LocalContext.current` 추가
       - "Auto Minuting" 타이틀 + 부제목 아래, "테스트 도구" 카드 위치(line 222 부근)에 NotebookLM 카드 삽입:
         ```kotlin
         // NotebookLM 바로가기
         Card(
             modifier = Modifier
                 .fillMaxWidth()
                 .padding(horizontal = 16.dp),
             colors = CardDefaults.cardColors(
                 containerColor = MaterialTheme.colorScheme.tertiaryContainer
             )
         ) {
             Column(modifier = Modifier.padding(16.dp)) {
                 Text(
                     text = "NotebookLM",
                     style = MaterialTheme.typography.titleMedium
                 )
                 Text(
                     text = "AI 기반 회의록 분석을 활용할 수 있습니다",
                     style = MaterialTheme.typography.bodySmall,
                     color = MaterialTheme.colorScheme.onTertiaryContainer
                 )
                 Spacer(modifier = Modifier.height(8.dp))
                 OutlinedButton(
                     onClick = { NotebookLmHelper.openNotebookLmWeb(context) }
                 ) {
                     Icon(
                         imageVector = Icons.AutoMirrored.Filled.MenuBook,
                         contentDescription = null,
                         modifier = Modifier.size(18.dp)
                     )
                     Spacer(modifier = Modifier.width(8.dp))
                     Text("NotebookLM 열기")
                 }
             }
         }
         Spacer(modifier = Modifier.height(16.dp))
         ```

    2. SettingsScreen.kt 수정:
       - "// --- NotebookLM 섹션 ---" 부터 "NotebookLM 열기" 버튼 닫는 중괄호까지 (line 274~301) 전체 제거
       - `import com.autominuting.util.NotebookLmHelper` import 제거
       - `import androidx.compose.material.icons.automirrored.filled.MenuBook` import 제거 (다른 곳에서 쓰지 않으면)
       - NotebookLM 섹션 위의 Divider + Spacer도 함께 제거하여 깔끔한 종료

    NotebookLmHelper.kt 자체는 수정하지 않는다 (MinutesDetailScreen에서도 사용 중).
  </action>
  <acceptance_criteria>
    - DashboardScreen.kt에 NotebookLmHelper.openNotebookLmWeb 호출이 존재
    - SettingsScreen.kt에 NotebookLmHelper 관련 코드가 없음
    - DashboardScreen.kt에 "NotebookLM 열기" 텍스트가 존재
  </acceptance_criteria>
  <verify>
    grep -q "NotebookLmHelper" app/src/main/java/com/autominuting/presentation/dashboard/DashboardScreen.kt && ! grep -q "NotebookLmHelper" app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt && echo "PASS: NotebookLM button moved"
  </verify>
  <done>NotebookLM 열기 버튼이 DashboardScreen의 카드 UI로 표시되고, SettingsScreen에서는 완전히 제거된 상태</done>
</task>

<task type="auto">
  <name>Task 3: spike 패키지 및 관련 코드 제거 (UI-03)</name>
  <files>
    app/src/main/java/com/autominuting/spike/SamsungRecorderObserver.kt (삭제)
    app/src/main/java/com/autominuting/spike/SpikeLogActivity.kt (삭제)
    app/src/main/java/com/autominuting/spike/SpikeService.kt (삭제)
    app/src/main/java/com/autominuting/spike/SpikeVerificationHelper.kt (삭제)
    app/src/main/AndroidManifest.xml
  </files>
  <read_first>
    app/src/main/AndroidManifest.xml
  </read_first>
  <action>
    spike 패키지의 모든 파일을 삭제하고 AndroidManifest.xml에서 관련 등록을 제거한다. (per UI-03)

    1. spike 패키지 파일 4개 삭제:
       - `rm app/src/main/java/com/autominuting/spike/SamsungRecorderObserver.kt`
       - `rm app/src/main/java/com/autominuting/spike/SpikeLogActivity.kt`
       - `rm app/src/main/java/com/autominuting/spike/SpikeService.kt`
       - `rm app/src/main/java/com/autominuting/spike/SpikeVerificationHelper.kt`
       - `rmdir app/src/main/java/com/autominuting/spike/` (빈 디렉토리 제거)

    2. AndroidManifest.xml 수정:
       - line 110~114 삭제 (SpikeService foreground service 등록):
         ```xml
         <!-- 삼성 녹음앱 감지 스파이크 Foreground Service (specialUse: 파일 모니터링) -->
         <service
             android:name=".spike.SpikeService"
             android:foregroundServiceType="specialUse"
             android:exported="false" />
         ```
       - line 116~120 삭제 (SpikeLogActivity 등록):
         ```xml
         <!-- 삼성 녹음앱 감지 스파이크 로그 UI -->
         <activity
             android:name=".spike.SpikeLogActivity"
             android:exported="false"
             android:theme="@style/Theme.AutoMinuting" />
         ```

    3. 프로젝트 전체에서 spike 참조가 없는지 확인:
       - `grep -r "spike" app/src/main/ --include="*.kt" --include="*.xml"` 결과가 0건이어야 함
       - 참조가 남아있으면 해당 import/코드도 제거

    DashboardScreen.kt의 "테스트 도구" 섹션(line 222~280)은 spike 패키지와 무관한 ViewModel 기능(insertTestData, testGeminiApi)이므로 이 태스크에서는 건드리지 않는다.
  </action>
  <acceptance_criteria>
    - spike 디렉토리가 존재하지 않음
    - AndroidManifest.xml에 ".spike." 문자열이 없음
    - app/src/main/ 하위에 "spike" 참조가 없음
  </acceptance_criteria>
  <verify>
    ! test -d app/src/main/java/com/autominuting/spike && ! grep -q "spike" app/src/main/AndroidManifest.xml && echo "PASS: spike removed"
  </verify>
  <done>spike 패키지 4개 파일이 삭제되고, AndroidManifest.xml에서 SpikeService/SpikeLogActivity 등록이 제거되어 빌드에 포함되지 않는 상태</done>
</task>

</tasks>

<verification>
1. `grep -rn "spike" app/src/main/ --include="*.kt" --include="*.xml"` — 결과 0건
2. `grep -q "NotebookLmHelper" app/src/main/java/com/autominuting/presentation/dashboard/DashboardScreen.kt` — 성공
3. `! grep -q "NotebookLmHelper" app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt` — 성공
4. ic_launcher_foreground.xml, ic_launcher_background.xml에 새 디자인 반영 확인
5. `cd app && ../gradlew assembleDebug` — 빌드 성공
</verification>

<success_criteria>
1. 앱 런처에 마이크+문서 결합 디자인의 새 아이콘이 표시된다
2. NotebookLM 열기 버튼이 DashboardScreen(홈 탭)에서 카드 UI로 접근 가능하다
3. spike 패키지 4개 파일과 AndroidManifest 등록이 완전히 제거되어 빌드에 포함되지 않는다
4. 프로젝트가 정상 빌드된다
</success_criteria>

<output>
After completion, create `.planning/phases/17-ui-cleanup/17-01-SUMMARY.md`
</output>
