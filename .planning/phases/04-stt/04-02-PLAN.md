---
phase: 04-stt
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt
  - app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsViewModel.kt
  - app/src/main/java/com/autominuting/presentation/transcripts/TranscriptEditScreen.kt
  - app/src/main/java/com/autominuting/presentation/transcripts/TranscriptEditViewModel.kt
  - app/src/main/java/com/autominuting/domain/repository/MeetingRepository.kt
  - app/src/main/java/com/autominuting/data/repository/MeetingRepositoryImpl.kt
  - app/src/main/java/com/autominuting/presentation/navigation/Screen.kt
  - app/src/main/java/com/autominuting/presentation/navigation/AppNavigation.kt
autonomous: true
requirements:
  - STT-03

must_haves:
  truths:
    - "사용자가 전사 목록 화면에서 전사 완료된 회의 목록을 볼 수 있다"
    - "사용자가 전사 텍스트를 탭하면 편집 화면으로 이동한다"
    - "사용자가 전사 텍스트를 수정하고 저장할 수 있다"
    - "수정된 텍스트가 파일에 반영되어 앱 재시작 후에도 유지된다"
  artifacts:
    - path: "app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt"
      provides: "전사 완료된 회의 목록 UI"
      contains: "TranscriptsScreen"
    - path: "app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsViewModel.kt"
      provides: "전사 목록 상태 관리"
      contains: "class TranscriptsViewModel"
    - path: "app/src/main/java/com/autominuting/presentation/transcripts/TranscriptEditScreen.kt"
      provides: "전사 텍스트 편집 UI"
      contains: "TranscriptEditScreen"
    - path: "app/src/main/java/com/autominuting/presentation/transcripts/TranscriptEditViewModel.kt"
      provides: "전사 편집 상태 관리 및 저장 로직"
      contains: "class TranscriptEditViewModel"
  key_links:
    - from: "TranscriptsScreen"
      to: "TranscriptEditScreen"
      via: "Navigation route (transcripts/{meetingId}/edit)"
      pattern: "transcripts/.*edit"
    - from: "TranscriptEditViewModel"
      to: "MeetingRepository"
      via: "전사 텍스트 로드/저장"
      pattern: "meetingRepository"
    - from: "TranscriptEditViewModel"
      to: "파일 시스템"
      via: "transcriptPath 파일 읽기/쓰기"
      pattern: "File.*transcriptPath"
---

<objective>
전사 완료된 회의 목록 화면과 전사 텍스트 편집 화면을 구현하여 사용자가 STT 결과를 확인하고 수정할 수 있게 한다.

Purpose: 사용자가 STT 오류를 수정하여 회의록 생성 품질을 높일 수 있는 편집 기능을 제공한다 (STT-03).
Output: TranscriptsScreen (목록), TranscriptEditScreen (편집), ViewModel 2개, Navigation 연동
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-stt/04-CONTEXT.md
@.planning/phases/04-stt/04-01-SUMMARY.md

<interfaces>
<!-- Plan 01에서 생성된 인터페이스 + 기존 코드 -->

From app/src/main/java/com/autominuting/data/local/entity/MeetingEntity.kt:
```kotlin
data class MeetingEntity(
    @PrimaryKey(autoGenerate = true) val id: Long = 0,
    val title: String,
    val recordedAt: Long,
    val audioFilePath: String,
    val transcriptPath: String? = null,
    val minutesPath: String? = null,
    val pipelineStatus: String,
    val errorMessage: String? = null,
    val createdAt: Long,
    val updatedAt: Long
)
```

From app/src/main/java/com/autominuting/domain/model/Meeting.kt:
```kotlin
data class Meeting(
    val id: Long = 0,
    val title: String,
    val recordedAt: Instant,
    val audioFilePath: String,
    val transcriptPath: String? = null,
    val minutesPath: String? = null,
    val pipelineStatus: PipelineStatus,
    val errorMessage: String? = null,
    val createdAt: Instant,
    val updatedAt: Instant
)
```

From app/src/main/java/com/autominuting/domain/model/PipelineStatus.kt:
```kotlin
enum class PipelineStatus {
    AUDIO_RECEIVED, TRANSCRIBING, TRANSCRIBED,
    GENERATING_MINUTES, COMPLETED, FAILED
}
```

From app/src/main/java/com/autominuting/domain/repository/MeetingRepository.kt:
```kotlin
interface MeetingRepository {
    fun getAllMeetings(): Flow<List<Meeting>>
    fun getMeetingById(id: Long): Flow<Meeting?>
    suspend fun insertMeeting(meeting: Meeting): Long
    suspend fun updateMeeting(meeting: Meeting)
    suspend fun deleteMeeting(id: Long)
}
```

From app/src/main/java/com/autominuting/data/local/dao/MeetingDao.kt (Plan 01에서 추가):
```kotlin
@Query("UPDATE meetings SET transcriptPath = :transcriptPath, pipelineStatus = :status, updatedAt = :updatedAt WHERE id = :id")
suspend fun updateTranscript(id: Long, transcriptPath: String, status: String, updatedAt: Long)
```

From app/src/main/java/com/autominuting/presentation/navigation/Screen.kt:
```kotlin
sealed class Screen(val route: String, val title: String, val icon: ImageVector) {
    data object Dashboard : Screen("dashboard", "대시보드", Icons.Default.Home)
    data object Transcripts : Screen("transcripts", "전사 목록", Icons.Default.Description)
    data object Minutes : Screen("minutes", "회의록", Icons.Default.List)
    data object Settings : Screen("settings", "설정", Icons.Default.Settings)
}
```

From app/src/main/java/com/autominuting/presentation/navigation/AppNavigation.kt:
```kotlin
composable(Screen.Transcripts.route) { TranscriptsScreen() }
// 편집 화면 route 추가 필요
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 전사 목록 화면 (TranscriptsScreen + ViewModel)</name>
  <files>
    app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt
    app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsViewModel.kt
  </files>
  <read_first>
    app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt
    app/src/main/java/com/autominuting/presentation/dashboard/DashboardScreen.kt
    app/src/main/java/com/autominuting/domain/repository/MeetingRepository.kt
    app/src/main/java/com/autominuting/data/repository/MeetingRepositoryImpl.kt
    app/src/main/java/com/autominuting/domain/model/Meeting.kt
    app/src/main/java/com/autominuting/domain/model/PipelineStatus.kt
    app/src/main/java/com/autominuting/presentation/navigation/AppNavigation.kt
  </read_first>
  <action>
    **1. TranscriptsViewModel (presentation/transcripts/TranscriptsViewModel.kt):**
    - `@HiltViewModel class TranscriptsViewModel @Inject constructor(private val meetingRepository: MeetingRepository)`.
    - `val meetings: StateFlow<List<Meeting>>` — meetingRepository.getAllMeetings()를 stateIn으로 수집.
    - 전사 완료된 항목만 필터링: `pipelineStatus >= TRANSCRIBED` (TRANSCRIBED, GENERATING_MINUTES, COMPLETED 포함).
    - 전사 중인 항목도 표시 (TRANSCRIBING 상태는 "전사 중..." 라벨로 표시).

    **2. TranscriptsScreen 교체 (presentation/transcripts/TranscriptsScreen.kt):**
    - 기존 빈 화면 "전사 목록" 텍스트를 완전히 교체.
    - `@Composable fun TranscriptsScreen(viewModel: TranscriptsViewModel = hiltViewModel(), onEditClick: (Long) -> Unit)`.
    - LazyColumn으로 전사 목록 표시.
    - 각 항목에 표시할 정보: 회의 제목, 녹음 시각 (포맷: "yyyy.MM.dd HH:mm"), 파이프라인 상태 칩 (TRANSCRIBING: "전사 중", TRANSCRIBED: "전사 완료", FAILED: "실패").
    - TRANSCRIBED 이상 상태의 항목 탭 시 `onEditClick(meeting.id)` 호출.
    - 빈 목록 시 "전사된 항목이 없습니다" 안내 표시.
    - Material 3 Card 컴포넌트 사용. 상태별 색상 구분 (TRANSCRIBING: tertiary, TRANSCRIBED: primary, FAILED: error).
  </action>
  <verify>
    <automated>cd "D:\workspace\temp\Auto_Minuting" && ./gradlew assembleDebug 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `TranscriptsViewModel`이 `@HiltViewModel`로 선언되고 `MeetingRepository`를 주입받음
    - `TranscriptsScreen`이 `LazyColumn`으로 전사 목록을 렌더링
    - 상태 칩에 "전사 중", "전사 완료", "실패" 텍스트 포함
    - `onEditClick` 콜백 파라미터가 존재
    - `assembleDebug` 빌드 성공
  </acceptance_criteria>
  <done>
    TranscriptsScreen이 전사 완료/진행 중인 회의 목록을 Card 형태로 표시하고, 전사 완료 항목 탭 시 편집 화면으로 이동할 수 있는 콜백을 제공한다.
  </done>
</task>

<task type="auto">
  <name>Task 2: 전사 편집 화면 + Navigation 연동 (TranscriptEditScreen + ViewModel)</name>
  <files>
    app/src/main/java/com/autominuting/presentation/transcripts/TranscriptEditScreen.kt
    app/src/main/java/com/autominuting/presentation/transcripts/TranscriptEditViewModel.kt
    app/src/main/java/com/autominuting/presentation/navigation/Screen.kt
    app/src/main/java/com/autominuting/presentation/navigation/AppNavigation.kt
  </files>
  <read_first>
    app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt
    app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsViewModel.kt
    app/src/main/java/com/autominuting/presentation/navigation/Screen.kt
    app/src/main/java/com/autominuting/presentation/navigation/AppNavigation.kt
    app/src/main/java/com/autominuting/domain/repository/MeetingRepository.kt
    app/src/main/java/com/autominuting/domain/model/Meeting.kt
  </read_first>
  <action>
    **1. TranscriptEditViewModel (presentation/transcripts/TranscriptEditViewModel.kt):**
    - `@HiltViewModel class TranscriptEditViewModel @Inject constructor(private val meetingRepository: MeetingRepository, private val savedStateHandle: SavedStateHandle)`.
    - `meetingId`를 savedStateHandle에서 추출 (Navigation arg).
    - `val meeting: StateFlow<Meeting?>` — meetingRepository.getMeetingById(meetingId)를 수집.
    - `val transcriptText: MutableStateFlow<String>` — 편집 가능한 전사 텍스트.
    - `val isSaving: StateFlow<Boolean>` — 저장 중 상태.
    - `val saveSuccess: SharedFlow<Boolean>` — 저장 완료 이벤트.
    - `init` 블록에서 meeting.transcriptPath 파일을 읽어 transcriptText에 로드.
    - `fun updateText(newText: String)` — transcriptText 업데이트.
    - `fun saveTranscript()`:
      1. transcriptPath 파일에 텍스트 쓰기 (`File(transcriptPath).writeText(transcriptText.value)`).
      2. meeting.updatedAt 업데이트: `meetingRepository.updateMeeting(meeting.copy(updatedAt = Instant.now()))`.
      3. saveSuccess emit.

    **2. TranscriptEditScreen (presentation/transcripts/TranscriptEditScreen.kt):**
    - `@Composable fun TranscriptEditScreen(viewModel: TranscriptEditViewModel = hiltViewModel(), onBack: () -> Unit)`.
    - TopAppBar: 뒤로가기 아이콘 버튼(onBack), 제목(회의 제목), 저장 아이콘 버튼(viewModel.saveTranscript).
    - 본문: `TextField` (BasicTextField 또는 OutlinedTextField)로 전사 텍스트 전체를 편집 가능하게 표시.
    - `fillMaxSize()` 모디파이어로 전체 화면 텍스트 편집.
    - 저장 중 상태에서 CircularProgressIndicator 표시.
    - 저장 성공 시 Snackbar로 "저장되었습니다" 표시.
    - 수정 사항이 있을 때 (transcriptText != 원본) 뒤로가기 시 "변경 사항을 저장하시겠습니까?" 확인 다이얼로그.

    **3. Screen.kt에 편집 화면 route 추가:**
    ```kotlin
    data object TranscriptEdit : Screen(
        "transcripts/{meetingId}/edit",
        "전사 편집",
        Icons.Default.Edit
    ) {
        fun createRoute(meetingId: Long) = "transcripts/$meetingId/edit"
    }
    ```

    **4. AppNavigation.kt에 편집 화면 composable 추가:**
    - TranscriptsScreen에 `onEditClick` 연결: `navController.navigate(Screen.TranscriptEdit.createRoute(meetingId))`.
    - 편집 화면 composable 등록:
      ```kotlin
      composable(
          route = Screen.TranscriptEdit.route,
          arguments = listOf(navArgument("meetingId") { type = NavType.LongType })
      ) {
          TranscriptEditScreen(onBack = { navController.popBackStack() })
      }
      ```
    - 기존 TranscriptsScreen composable에서 `TranscriptsScreen(onEditClick = { ... })` 형태로 변경.
  </action>
  <verify>
    <automated>cd "D:\workspace\temp\Auto_Minuting" && ./gradlew assembleDebug 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `TranscriptEditViewModel`이 `@HiltViewModel`로 선언되고 `MeetingRepository`, `SavedStateHandle` 주입
    - `TranscriptEditScreen`에 `TextField`로 전사 텍스트 편집 UI 존재
    - `saveTranscript()` 메서드가 파일에 텍스트를 쓰고 MeetingEntity를 업데이트
    - `Screen.TranscriptEdit`이 `transcripts/{meetingId}/edit` route로 선언
    - `AppNavigation`에 TranscriptEdit composable이 등록되고 `navArgument("meetingId")` 포함
    - TranscriptsScreen과 TranscriptEditScreen 간 Navigation 연동 완료
    - `assembleDebug` 빌드 성공
  </acceptance_criteria>
  <done>
    사용자가 전사 목록에서 항목을 탭하면 편집 화면으로 이동하고, 전사 텍스트를 수정한 뒤 저장 버튼으로 파일에 반영할 수 있다. 변경 사항이 앱 재시작 후에도 유지된다.
  </done>
</task>

</tasks>

<verification>
1. `./gradlew assembleDebug` 빌드 성공
2. `grep -r "class TranscriptsViewModel" app/src/main/java/` — ViewModel 존재
3. `grep -r "class TranscriptEditViewModel" app/src/main/java/` — 편집 ViewModel 존재
4. `grep -r "TranscriptEditScreen" app/src/main/java/` — 편집 화면 존재
5. `grep -r "transcripts/{meetingId}/edit" app/src/main/java/` — route 등록 확인
6. `grep -r "saveTranscript" app/src/main/java/` — 저장 기능 존재
</verification>

<success_criteria>
- 전사 목록 화면에서 전사 완료된 회의 목록이 Card 형태로 표시된다
- 전사 항목 탭 시 편집 화면으로 Navigation이 동작한다
- 편집 화면에서 전사 텍스트를 수정하고 저장할 수 있다
- 저장된 텍스트가 파일에 반영되어 앱 재시작 후에도 유지된다
- 전체 빌드가 성공한다
</success_criteria>

<output>
After completion, create `.planning/phases/04-stt/04-02-SUMMARY.md`
</output>
