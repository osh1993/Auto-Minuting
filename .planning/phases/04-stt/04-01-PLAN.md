---
phase: 04-stt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/build.gradle.kts
  - app/src/main/java/com/autominuting/data/stt/WhisperEngine.kt
  - app/src/main/java/com/autominuting/data/stt/MlKitEngine.kt
  - app/src/main/java/com/autominuting/data/stt/SttEngine.kt
  - app/src/main/java/com/autominuting/data/stt/AudioConverter.kt
  - app/src/main/java/com/autominuting/data/repository/TranscriptionRepositoryImpl.kt
  - app/src/main/java/com/autominuting/di/SttModule.kt
  - app/src/main/java/com/autominuting/di/RepositoryModule.kt
  - app/src/main/java/com/autominuting/data/local/dao/MeetingDao.kt
  - app/src/main/java/com/autominuting/worker/TranscriptionTriggerWorker.kt
autonomous: true
requirements:
  - STT-01
  - STT-02

must_haves:
  truths:
    - "오디오 파일 경로를 입력하면 한국어 텍스트 전사 결과가 반환된다"
    - "Whisper 실패 시 ML Kit 폴백으로 자동 전환되어 전사가 완료된다"
    - "전사된 텍스트가 파일로 저장되고 MeetingEntity.transcriptPath가 업데이트된다"
    - "TranscriptionTriggerWorker가 실제 전사 로직을 실행한다"
  artifacts:
    - path: "app/src/main/java/com/autominuting/data/stt/WhisperEngine.kt"
      provides: "whisper.cpp JNI 래퍼를 통한 온디바이스 STT"
      contains: "class WhisperEngine"
    - path: "app/src/main/java/com/autominuting/data/stt/MlKitEngine.kt"
      provides: "ML Kit GenAI 폴백 STT"
      contains: "class MlKitEngine"
    - path: "app/src/main/java/com/autominuting/data/repository/TranscriptionRepositoryImpl.kt"
      provides: "TranscriptionRepository 구현체 (Whisper 1차 + ML Kit 2차)"
      contains: "class TranscriptionRepositoryImpl"
    - path: "app/src/main/java/com/autominuting/worker/TranscriptionTriggerWorker.kt"
      provides: "실제 전사 파이프라인 실행"
      contains: "transcriptionRepository.transcribe"
  key_links:
    - from: "TranscriptionTriggerWorker"
      to: "TranscriptionRepository"
      via: "Hilt 주입 후 transcribe() 호출"
      pattern: "transcriptionRepository\\.transcribe"
    - from: "TranscriptionRepositoryImpl"
      to: "WhisperEngine / MlKitEngine"
      via: "1차 Whisper, 실패 시 ML Kit 폴백"
      pattern: "whisperEngine\\.transcribe.*catch.*mlKitEngine"
    - from: "TranscriptionRepositoryImpl"
      to: "MeetingDao"
      via: "전사 완료 후 transcriptPath 업데이트"
      pattern: "meetingDao\\.updateTranscript"
---

<objective>
Whisper 온디바이스 STT 엔진과 ML Kit 폴백을 구현하고, TranscriptionRepository 구현체를 통해 TranscriptionTriggerWorker에서 실제 전사를 수행한다.

Purpose: 오디오 파일이 한국어 텍스트로 전사되어 로컬에 저장되는 핵심 파이프라인을 완성한다.
Output: WhisperEngine, MlKitEngine, TranscriptionRepositoryImpl, Worker 연동
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-stt/04-CONTEXT.md

@poc/results/POC-03-stt-fallback.md
@poc/stt-test/whisper-evaluation.md

<interfaces>
<!-- 기존 코드 인터페이스 — executor가 반드시 참조해야 함 -->

From app/src/main/java/com/autominuting/domain/repository/TranscriptionRepository.kt:
```kotlin
interface TranscriptionRepository {
    suspend fun transcribe(audioFilePath: String): Result<String>
    fun isTranscribing(): Flow<Boolean>
}
```

From app/src/main/java/com/autominuting/data/local/dao/MeetingDao.kt:
```kotlin
@Dao
interface MeetingDao {
    @Query("SELECT * FROM meetings WHERE id = :id")
    fun getMeetingById(id: Long): Flow<MeetingEntity?>

    @Query("UPDATE meetings SET pipelineStatus = :status, errorMessage = :errorMessage, updatedAt = :updatedAt WHERE id = :id")
    suspend fun updatePipelineStatus(id: Long, status: String, errorMessage: String?, updatedAt: Long)

    @Update
    suspend fun update(entity: MeetingEntity)
}
```

From app/src/main/java/com/autominuting/data/local/entity/MeetingEntity.kt:
```kotlin
data class MeetingEntity(
    @PrimaryKey(autoGenerate = true) val id: Long = 0,
    val title: String,
    val recordedAt: Long,
    val audioFilePath: String,
    val transcriptPath: String? = null,    // <-- 전사 파일 경로
    val minutesPath: String? = null,
    val pipelineStatus: String,
    val errorMessage: String? = null,
    val createdAt: Long,
    val updatedAt: Long
)
```

From app/src/main/java/com/autominuting/domain/model/PipelineStatus.kt:
```kotlin
enum class PipelineStatus {
    AUDIO_RECEIVED, TRANSCRIBING, TRANSCRIBED,
    GENERATING_MINUTES, COMPLETED, FAILED
}
```

From app/src/main/java/com/autominuting/worker/TranscriptionTriggerWorker.kt:
```kotlin
@HiltWorker
class TranscriptionTriggerWorker @AssistedInject constructor(
    @Assisted appContext: Context,
    @Assisted workerParams: WorkerParameters,
    private val meetingDao: MeetingDao
) : CoroutineWorker(appContext, workerParams) {
    override suspend fun doWork(): Result {
        val audioFilePath = inputData.getString("audioFilePath") ?: return Result.failure()
        // Phase 4에서 실제 전사 로직 구현 예정
        return Result.success()
    }
}
```

From app/src/main/java/com/autominuting/di/RepositoryModule.kt:
```kotlin
@Module
@InstallIn(SingletonComponent::class)
abstract class RepositoryModule {
    @Binds abstract fun bindMeetingRepository(impl: MeetingRepositoryImpl): MeetingRepository
    @Binds abstract fun bindAudioRepository(impl: AudioRepositoryImpl): AudioRepository
    // TranscriptionRepository 바인딩 추가 대상
}
```

From app/src/main/java/com/autominuting/data/repository/AudioRepositoryImpl.kt (폴백 패턴 참조):
```kotlin
// 1차: SDK BLE 경로 시도
// catch (e: PlaudSdkException) -> 2차: Cloud API 폴백
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: STT 엔진 인터페이스 + Whisper/ML Kit 구현체 + TranscriptionRepositoryImpl</name>
  <files>
    app/build.gradle.kts
    app/src/main/java/com/autominuting/data/stt/SttEngine.kt
    app/src/main/java/com/autominuting/data/stt/WhisperEngine.kt
    app/src/main/java/com/autominuting/data/stt/MlKitEngine.kt
    app/src/main/java/com/autominuting/data/stt/AudioConverter.kt
    app/src/main/java/com/autominuting/data/repository/TranscriptionRepositoryImpl.kt
    app/src/main/java/com/autominuting/di/SttModule.kt
    app/src/main/java/com/autominuting/di/RepositoryModule.kt
    app/src/main/java/com/autominuting/data/local/dao/MeetingDao.kt
  </files>
  <read_first>
    app/build.gradle.kts
    app/src/main/java/com/autominuting/domain/repository/TranscriptionRepository.kt
    app/src/main/java/com/autominuting/data/repository/AudioRepositoryImpl.kt
    app/src/main/java/com/autominuting/di/RepositoryModule.kt
    app/src/main/java/com/autominuting/data/local/dao/MeetingDao.kt
    app/src/main/java/com/autominuting/data/local/entity/MeetingEntity.kt
    app/src/main/java/com/autominuting/domain/model/PipelineStatus.kt
    poc/results/POC-03-stt-fallback.md
    poc/stt-test/whisper-evaluation.md
    gradle/libs.versions.toml
  </read_first>
  <action>
    **1. Gradle 의존성 추가 (app/build.gradle.kts):**
    - whisper.cpp Android 라이브러리 의존성 추가. whispercpp-android 라이브러리가 Maven Central에 있는지 확인하고, 없으면 whisper.cpp 소스를 서브모듈 또는 JNI 프리빌트로 통합한다. 현실적으로 `com.whispercpp:whisper-android` Maven artifact가 없으면, **스텁 패턴**을 적용한다 (Phase 3의 NiceBuildSdkWrapper 스텁 패턴과 동일 — per D-08). 스텁은 실제 JNI 호출 대신 빈 문자열을 반환하고, 실기기에서 네이티브 라이브러리가 로드되면 실제 전사를 수행한다.
    - ML Kit GenAI Speech Recognition 의존성 추가: `com.google.mlkit:genai-speech` (alpha 버전 확인)이 존재하지 않으면 `com.google.android.gms:play-services-mlkit-speech-recognition` 또는 Android SpeechRecognizer 래퍼로 대체. 역시 **스텁 패턴** 적용 가능.
    - libs.versions.toml에 whisper 및 mlkit 관련 버전/라이브러리 추가.

    **2. SttEngine 인터페이스 (data/stt/SttEngine.kt):**
    ```kotlin
    package com.autominuting.data.stt

    // STT 엔진의 공통 인터페이스
    interface SttEngine {
        suspend fun transcribe(audioFilePath: String): Result<String>
        suspend fun isAvailable(): Boolean
        fun engineName(): String
    }
    ```

    **3. WhisperEngine (data/stt/WhisperEngine.kt) — per D-01:**
    - `SttEngine` 구현체.
    - whisper.cpp JNI 래퍼. 모델 파일 경로는 앱 내부 저장소 `files/models/ggml-small.bin`.
    - `isAvailable()`: 네이티브 라이브러리 로드 가능 여부 + 모델 파일 존재 여부 체크.
    - `transcribe()`: AudioConverter로 16kHz mono WAV 변환 후 JNI 호출. temperature=0.0, language="ko" 설정 (per PoC hallucination 대응).
    - JNI 로드 실패 시 (UnsatisfiedLinkError 등) 스텁 모드로 `Result.failure()` 반환.
    - `@Inject constructor(context: Context)` — Hilt 주입.

    **4. MlKitEngine (data/stt/MlKitEngine.kt) — per D-02:**
    - `SttEngine` 구현체. 2차 폴백.
    - ML Kit SpeechRecognizer 또는 Android SpeechRecognizer 래핑.
    - `isAvailable()`: SpeechRecognizer.isRecognitionAvailable() 체크.
    - `transcribe()`: 오디오 파일을 읽어 SpeechRecognizer로 전사. Basic 모드, 한국어(ko-KR).
    - Samsung 기기에서 동작하지 않을 수 있으므로, 실패 시 `Result.failure()` 반환.
    - `@Inject constructor(context: Context)` — Hilt 주입.

    **5. AudioConverter (data/stt/AudioConverter.kt):**
    - 오디오 파일을 Whisper 입력 포맷(16kHz, mono, PCM WAV)으로 변환하는 유틸리티.
    - `suspend fun convertToWhisperFormat(inputPath: String, outputDir: String): String` — 변환된 WAV 파일 경로 반환.
    - Android MediaCodec/MediaExtractor 사용하여 디코딩 후 리샘플링.
    - 이미 16kHz WAV이면 변환 생략.

    **6. TranscriptionRepositoryImpl (data/repository/TranscriptionRepositoryImpl.kt):**
    - `TranscriptionRepository` 구현체.
    - AudioRepositoryImpl의 이중 경로 폴백 패턴과 동일 구조 — per D-08.
    - `@Inject constructor(whisperEngine: WhisperEngine, mlKitEngine: MlKitEngine, context: Context)`.
    - `transcribe()` 로직:
      1. `_isTranscribing.value = true`
      2. 1차: `whisperEngine.transcribe(audioFilePath)` 시도 (per D-01)
      3. Whisper 실패 시 2차: `mlKitEngine.transcribe(audioFilePath)` (per D-02)
      4. 둘 다 실패 시 `Result.failure()` 반환
      5. 성공 시 전사 텍스트를 파일로 저장 (앱 내부 저장소 `files/transcripts/{meetingId}.txt`)
      6. `_isTranscribing.value = false`
      7. `Result.success(transcriptText)` 반환
    - `isTranscribing()`: MutableStateFlow<Boolean> 노출.

    **7. MeetingDao에 updateTranscript 메서드 추가:**
    ```kotlin
    @Query("UPDATE meetings SET transcriptPath = :transcriptPath, pipelineStatus = :status, updatedAt = :updatedAt WHERE id = :id")
    suspend fun updateTranscript(id: Long, transcriptPath: String, status: String, updatedAt: Long)
    ```

    **8. SttModule (di/SttModule.kt):**
    - `@Module @InstallIn(SingletonComponent::class)` Hilt 모듈.
    - WhisperEngine, MlKitEngine을 `@Singleton`으로 제공.

    **9. RepositoryModule에 TranscriptionRepository 바인딩 추가:**
    ```kotlin
    @Binds
    abstract fun bindTranscriptionRepository(
        impl: TranscriptionRepositoryImpl
    ): TranscriptionRepository
    ```
  </action>
  <verify>
    <automated>cd "D:\workspace\temp\Auto_Minuting" && ./gradlew assembleDebug 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `SttEngine` 인터페이스가 `data/stt/SttEngine.kt`에 존재하고 `transcribe`, `isAvailable`, `engineName` 메서드를 선언
    - `WhisperEngine`이 `SttEngine`을 구현하고 `language="ko"`, `temperature=0.0` 설정 포함
    - `MlKitEngine`이 `SttEngine`을 구현하고 `ko-KR` 로케일 설정 포함
    - `TranscriptionRepositoryImpl`이 `TranscriptionRepository`를 구현하고 Whisper 1차 -> ML Kit 2차 폴백 로직 포함
    - `RepositoryModule`에 `bindTranscriptionRepository` 바인딩 존재
    - `MeetingDao`에 `updateTranscript` 메서드 존재
    - `assembleDebug` 빌드 성공
  </acceptance_criteria>
  <done>
    SttEngine 인터페이스와 WhisperEngine(1차)/MlKitEngine(2차) 구현체가 존재하고, TranscriptionRepositoryImpl이 이중 경로 폴백 패턴으로 전사를 수행하며, Hilt DI 바인딩이 완료되어 빌드가 성공한다.
  </done>
</task>

<task type="auto">
  <name>Task 2: TranscriptionTriggerWorker 실제 전사 로직 연동</name>
  <files>
    app/src/main/java/com/autominuting/worker/TranscriptionTriggerWorker.kt
  </files>
  <read_first>
    app/src/main/java/com/autominuting/worker/TranscriptionTriggerWorker.kt
    app/src/main/java/com/autominuting/domain/repository/TranscriptionRepository.kt
    app/src/main/java/com/autominuting/data/repository/TranscriptionRepositoryImpl.kt
    app/src/main/java/com/autominuting/data/local/dao/MeetingDao.kt
    app/src/main/java/com/autominuting/data/local/entity/MeetingEntity.kt
    app/src/main/java/com/autominuting/domain/model/PipelineStatus.kt
  </read_first>
  <action>
    **TranscriptionTriggerWorker 스텁을 실제 전사 로직으로 교체 — per D-06, D-07:**

    1. 생성자에 `TranscriptionRepository`를 Hilt 주입 추가:
       ```kotlin
       @HiltWorker
       class TranscriptionTriggerWorker @AssistedInject constructor(
           @Assisted appContext: Context,
           @Assisted workerParams: WorkerParameters,
           private val meetingDao: MeetingDao,
           private val transcriptionRepository: TranscriptionRepository
       ) : CoroutineWorker(appContext, workerParams)
       ```

    2. `doWork()` 구현:
       - inputData에서 `audioFilePath`(String)와 `meetingId`(Long) 읽기.
       - `meetingId`가 없으면 `audioFilePath`로 MeetingDao에서 조회 (기존 호환성).
       - PipelineStatus를 `TRANSCRIBING`으로 업데이트: `meetingDao.updatePipelineStatus(meetingId, PipelineStatus.TRANSCRIBING.name, null, now)`.
       - `transcriptionRepository.transcribe(audioFilePath)` 호출.
       - 성공 시:
         - 전사 텍스트를 파일로 저장: `context.filesDir/transcripts/{meetingId}.txt`.
         - `meetingDao.updateTranscript(meetingId, transcriptFilePath, PipelineStatus.TRANSCRIBED.name, now)`.
         - `Result.success()` 반환. outputData에 `transcriptPath` 포함 (Phase 5 체이닝용).
       - 실패 시:
         - `meetingDao.updatePipelineStatus(meetingId, PipelineStatus.FAILED.name, errorMessage, now)`.
         - `Result.failure()` 반환.

    3. 기존 스텁 코드 (TODO 주석, 로그만 남기는 부분) 완전히 제거.

    4. `audioFilePath`뿐 아니라 `meetingId`도 inputData로 전달받도록 companion object에 상수 추가:
       ```kotlin
       companion object {
           const val KEY_AUDIO_FILE_PATH = "audioFilePath"
           const val KEY_MEETING_ID = "meetingId"
           private const val TAG = "TranscriptionTrigger"
       }
       ```
  </action>
  <verify>
    <automated>cd "D:\workspace\temp\Auto_Minuting" && ./gradlew assembleDebug 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `TranscriptionTriggerWorker` 생성자에 `transcriptionRepository: TranscriptionRepository` 파라미터가 존재
    - `doWork()`에서 `transcriptionRepository.transcribe(audioFilePath)` 호출이 존재
    - 성공 시 `PipelineStatus.TRANSCRIBED`로 상태 업데이트
    - 실패 시 `PipelineStatus.FAILED`로 상태 업데이트
    - 스텁 코드(TODO 주석, "Phase 4에서 실제 전사 로직 구현 예정") 완전 제거
    - `assembleDebug` 빌드 성공
  </acceptance_criteria>
  <done>
    TranscriptionTriggerWorker가 TranscriptionRepository를 주입받아 실제 전사를 수행하고, 결과에 따라 PipelineStatus를 TRANSCRIBED 또는 FAILED로 업데이트하며, 전사 텍스트 파일 경로를 MeetingEntity.transcriptPath에 저장한다.
  </done>
</task>

</tasks>

<verification>
1. `./gradlew assembleDebug` 빌드 성공
2. `grep -r "class WhisperEngine" app/src/main/java/` — WhisperEngine 존재
3. `grep -r "class MlKitEngine" app/src/main/java/` — MlKitEngine 존재
4. `grep -r "class TranscriptionRepositoryImpl" app/src/main/java/` — 구현체 존재
5. `grep -r "transcriptionRepository.transcribe" app/src/main/java/` — Worker 연동 확인
6. `grep -r "bindTranscriptionRepository" app/src/main/java/` — DI 바인딩 확인
7. `grep -r "updateTranscript" app/src/main/java/` — DAO 메서드 확인
</verification>

<success_criteria>
- Whisper 1차 STT 엔진이 구현되어 한국어 전사를 수행한다 (스텁 모드에서도 컴파일 성공)
- ML Kit 2차 폴백 엔진이 구현되어 Whisper 실패 시 자동 전환된다
- TranscriptionTriggerWorker가 실제 전사 파이프라인을 실행한다
- 전사 결과가 파일로 저장되고 MeetingEntity에 경로가 기록된다
- 전체 빌드가 성공한다
</success_criteria>

<output>
After completion, create `.planning/phases/04-stt/04-01-SUMMARY.md`
</output>
