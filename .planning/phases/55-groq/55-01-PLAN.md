---
phase: 55-groq
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/com/autominuting/data/stt/AudioConverter.kt
  - app/src/main/java/com/autominuting/util/AudioChunker.kt
  - app/src/test/java/com/autominuting/util/AudioChunkerTest.kt
autonomous: true
requirements:
  - GROQ-01
must_haves:
  truths:
    - "AudioChunker.split()이 입력 오디오(M4A/MP3/WAV)를 16kHz mono WAV 청크 리스트로 반환한다"
    - "각 청크는 유효한 WAV 헤더를 가진 독립 파일이며 크기가 25MB 이하다"
    - "청크 간 10초 오버랩이 존재한다 (600초 청크 기준 step=590초)"
    - "AudioChunkerTest 단위 테스트가 모두 통과한다"
  artifacts:
    - path: "app/src/main/java/com/autominuting/util/AudioChunker.kt"
      provides: "PCM 시간 기반 WAV 청크 분할 유틸리티"
      contains: "object AudioChunker"
    - path: "app/src/test/java/com/autominuting/util/AudioChunkerTest.kt"
      provides: "AudioChunker 단위 테스트"
      contains: "class AudioChunkerTest"
  key_links:
    - from: "AudioChunker.split()"
      to: "AudioConverter.decodeAudioToPcm() + writeWavFile()"
      via: "internal 가시성으로 공유 사용"
      pattern: "audioConverter\\.(decodeAudioToPcm|writeWavFile)"
---

<objective>
Groq 25MB 초과 파일 자동 분할을 위해 `AudioChunker` 유틸을 신설한다. `AudioConverter`의 PCM 디코딩/WAV 작성 로직을 재사용할 수 있도록 가시성을 `private`에서 `internal`로 상향 조정한다.

Purpose: Phase 55 Plan 02의 GroqSttEngine이 "25MB 초과 → 청크 분할 → 순차 전사" 경로를 구현할 수 있도록 분할 단계의 독립 단위를 먼저 완성한다 (interface-first).
Output: AudioChunker object + 단위 테스트. GROQ-01 요구사항의 "자동 분할" 부분을 담당.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-groq/55-RESEARCH.md
@app/src/main/java/com/autominuting/data/stt/AudioConverter.kt
@app/src/test/java/com/autominuting/util/Mp3MergerTest.kt

<interfaces>
<!-- AudioChunker가 호출할 기존 AudioConverter API (이 Plan에서 internal로 가시성 변경) -->

From app/src/main/java/com/autominuting/data/stt/AudioConverter.kt:
```kotlin
@Singleton
class AudioConverter @Inject constructor() {
    // 현재 private — 이 Plan에서 internal로 변경
    internal fun decodeAudioToPcm(inputPath: String): ByteArray

    // 현재 private — 이 Plan에서 internal로 변경
    internal fun writeWavFile(outputFile: File, pcmData: ByteArray)

    // 기존 public — 변경 없음
    suspend fun convertToWhisperFormat(inputPath: String, outputDir: String): String
}
```

Target AudioChunker API (이 Plan에서 신설):
```kotlin
object AudioChunker {
    const val CHUNK_SECONDS = 600
    const val OVERLAP_SECONDS = 10
    const val TARGET_SAMPLE_RATE = 16_000
    const val BYTES_PER_SECOND = 32_000   // 16kHz * 1ch * 2byte

    suspend fun split(
        inputPath: String,
        outputDir: File,
        audioConverter: AudioConverter
    ): List<File>

    // 단위 테스트용 pure 함수 (PCM 배열 → 청크 바이트 배열 리스트)
    internal fun splitPcmForTest(
        pcm: ByteArray,
        chunkSec: Int = CHUNK_SECONDS,
        overlapSec: Int = OVERLAP_SECONDS,
        bytesPerSec: Int = BYTES_PER_SECOND
    ): List<ByteArray>
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AudioConverter 가시성 상향 및 AudioChunker 신설</name>
  <files>
    app/src/main/java/com/autominuting/data/stt/AudioConverter.kt,
    app/src/main/java/com/autominuting/util/AudioChunker.kt
  </files>
  <read_first>
    - app/src/main/java/com/autominuting/data/stt/AudioConverter.kt (현재 구현 전체 — decodeAudioToPcm/writeWavFile 시그니처 확인)
    - app/src/main/java/com/autominuting/data/stt/GroqSttEngine.kt (MAX_FILE_SIZE 상수 및 호출 컨텍스트)
    - .planning/phases/55-groq/55-RESEARCH.md (Pattern 1, Pitfall 1-4)
    - app/src/test/java/com/autominuting/util/Mp3MergerTest.kt (테스트 패턴 참조)
  </read_first>
  <behavior>
    - 1800초(30분) 분량 PCM → 3청크 생성 (600s + 590s step × 2)
    - 600초 미만 PCM → 1청크 (입력 전체)
    - 600초 정확히 → 1청크 (추가 오버랩 청크 생성 안 함)
    - 청크 바이트 크기: chunkSec * bytesPerSec (마지막 청크는 remaining)
    - 오버랩 10초 → step = (600 - 10) * BYTES_PER_SECOND = 18,880,000 bytes
    - 각 청크 WAV 헤더 dataSize = 실제 PCM 크기와 일치
  </behavior>
  <action>
    Step 1 — `AudioConverter.kt` 수정 (가시성만 변경, 로직 불변):

    Line 99 `private fun decodeAudioToPcm(inputPath: String): ByteArray`
    → `internal fun decodeAudioToPcm(inputPath: String): ByteArray`

    Line 240 `private fun writeWavFile(outputFile: File, pcmData: ByteArray)`
    → `internal fun writeWavFile(outputFile: File, pcmData: ByteArray)`

    다른 private 헬퍼(`isAlreadyWhisperFormat`, `resampleToTarget`, `shortsToBytes`)는 변경 금지.

    Step 2 — 신규 파일 `app/src/main/java/com/autominuting/util/AudioChunker.kt` 생성:

    ```kotlin
    package com.autominuting.util

    import com.autominuting.data.stt.AudioConverter
    import kotlinx.coroutines.Dispatchers
    import kotlinx.coroutines.withContext
    import java.io.File

    /**
     * 25MB 초과 오디오 파일을 Groq 전송 가능한 시간 기반 WAV 청크로 분할한다.
     * AudioConverter를 재사용하여 16kHz mono PCM으로 디코딩한 뒤,
     * 600초 청크 + 10초 오버랩으로 잘라 각 청크를 독립 WAV 파일로 작성한다.
     */
    object AudioChunker {
        const val CHUNK_SECONDS = 600
        const val OVERLAP_SECONDS = 10
        const val TARGET_SAMPLE_RATE = 16_000
        const val TARGET_CHANNELS = 1
        const val BITS_PER_SAMPLE = 16
        const val BYTES_PER_SECOND = TARGET_SAMPLE_RATE * TARGET_CHANNELS * (BITS_PER_SAMPLE / 8)

        suspend fun split(
            inputPath: String,
            outputDir: File,
            audioConverter: AudioConverter
        ): List<File> = withContext(Dispatchers.IO) {
            require(File(inputPath).exists()) { "입력 파일 없음: $inputPath" }
            outputDir.mkdirs()

            // 1. 16kHz mono PCM 디코딩 (AudioConverter 재사용)
            val pcm = audioConverter.decodeAudioToPcm(inputPath)

            // 2. 시간 기반 청크 분할
            val chunkByteArrays = splitPcmForTest(pcm, CHUNK_SECONDS, OVERLAP_SECONDS, BYTES_PER_SECOND)

            // 3. 각 청크를 독립 WAV 파일로 작성
            chunkByteArrays.mapIndexed { index, chunkPcm ->
                val chunkFile = File(outputDir, "chunk_${"%03d".format(index)}.wav")
                audioConverter.writeWavFile(chunkFile, chunkPcm)
                chunkFile
            }
        }

        /** 순수 함수: PCM 바이트 배열을 시간 기반 청크로 분할 (단위 테스트용). */
        internal fun splitPcmForTest(
            pcm: ByteArray,
            chunkSec: Int = CHUNK_SECONDS,
            overlapSec: Int = OVERLAP_SECONDS,
            bytesPerSec: Int = BYTES_PER_SECOND
        ): List<ByteArray> {
            if (pcm.isEmpty()) return emptyList()
            val chunkBytes = chunkSec * bytesPerSec
            val overlapBytes = overlapSec * bytesPerSec
            val stepBytes = chunkBytes - overlapBytes
            require(stepBytes > 0) { "overlapSec must be < chunkSec" }

            // 입력이 단일 청크로 충분하면 오버랩 생성 안 함
            if (pcm.size <= chunkBytes) return listOf(pcm)

            val result = mutableListOf<ByteArray>()
            var offset = 0
            while (offset < pcm.size) {
                val end = minOf(offset + chunkBytes, pcm.size)
                result += pcm.copyOfRange(offset, end)
                if (end >= pcm.size) break
                offset += stepBytes
            }
            return result
        }
    }
    ```

    참조 결정:
    - 패키지 `com.autominuting.util` — 55-RESEARCH.md "Recommended Project Structure" 준수
    - 재인코딩 없이 기존 PCM 디코딩 재사용 — 55-RESEARCH.md Pattern 1 및 "Don't Hand-Roll" 표
    - `splitPcmForTest`는 `internal`로 JVM 단위 테스트에서 호출 가능 (AudioConverter 없이)
    - 단일 청크(`pcm.size <= chunkBytes`)는 오버랩 없이 그대로 반환 — 작은 초과 파일에서 불필요한 2청크 생성 방지
  </action>
  <verify>
    <automated>cd D:/workspace/temp/Auto_Minuting && ./gradlew :app:compileDebugKotlin 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "internal fun decodeAudioToPcm" app/src/main/java/com/autominuting/data/stt/AudioConverter.kt` 매칭 1건
    - `grep -n "internal fun writeWavFile" app/src/main/java/com/autominuting/data/stt/AudioConverter.kt` 매칭 1건
    - `grep -cn "private fun decodeAudioToPcm\|private fun writeWavFile" app/src/main/java/com/autominuting/data/stt/AudioConverter.kt` 결과 0
    - `test -f app/src/main/java/com/autominuting/util/AudioChunker.kt` 성공
    - `grep -n "object AudioChunker" app/src/main/java/com/autominuting/util/AudioChunker.kt` 매칭 1건
    - `grep -n "const val CHUNK_SECONDS = 600" app/src/main/java/com/autominuting/util/AudioChunker.kt` 매칭 1건
    - `grep -n "const val OVERLAP_SECONDS = 10" app/src/main/java/com/autominuting/util/AudioChunker.kt` 매칭 1건
    - `grep -n "const val BYTES_PER_SECOND" app/src/main/java/com/autominuting/util/AudioChunker.kt` 매칭 1건
    - `grep -n "internal fun splitPcmForTest" app/src/main/java/com/autominuting/util/AudioChunker.kt` 매칭 1건
    - `grep -n "audioConverter\.decodeAudioToPcm\|audioConverter\.writeWavFile" app/src/main/java/com/autominuting/util/AudioChunker.kt` 매칭 2건 이상
    - `./gradlew :app:compileDebugKotlin` 성공 (exit code 0)
  </acceptance_criteria>
  <done>
    AudioConverter 헬퍼가 internal 가시성으로 변경되고, AudioChunker object가 신설되어 `split()` 진입 API와 `splitPcmForTest()` 순수 함수를 제공한다. Kotlin 컴파일 성공.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AudioChunker 단위 테스트 작성</name>
  <files>app/src/test/java/com/autominuting/util/AudioChunkerTest.kt</files>
  <read_first>
    - app/src/main/java/com/autominuting/util/AudioChunker.kt (Task 1에서 생성됨)
    - app/src/test/java/com/autominuting/util/Mp3MergerTest.kt (JUnit 테스트 패턴 참조)
    - .planning/phases/55-groq/55-RESEARCH.md (Example 3 테스트 예제)
  </read_first>
  <behavior>
    - 600초 청크가 25MB 제한 안에 있음을 수식으로 검증
    - 1800초(30분) PCM → 정확히 3청크
    - 600초 미만(예: 300초) PCM → 1청크, 크기 = 원본 크기
    - 600초 정확히 PCM → 1청크
    - 빈 PCM → 빈 리스트
    - 청크 간 오버랩 크기 정확성(10초 = 320,000 bytes) 검증
  </behavior>
  <action>
    신규 파일 `app/src/test/java/com/autominuting/util/AudioChunkerTest.kt` 생성:

    ```kotlin
    package com.autominuting.util

    import org.junit.Assert.assertEquals
    import org.junit.Assert.assertTrue
    import org.junit.Test

    /**
     * AudioChunker.splitPcmForTest() 순수 함수 단위 테스트.
     * 실제 오디오 디코딩(MediaCodec)은 Android 환경이 필요하므로 여기서 테스트하지 않고,
     * 시간 기반 분할 로직 자체를 PCM 배열로 검증한다.
     */
    class AudioChunkerTest {

        private val bytesPerSec = AudioChunker.BYTES_PER_SECOND  // 32_000

        @Test
        fun `600초 청크는 25MB 제한 안에 들어간다`() {
            val chunkBytes = AudioChunker.CHUNK_SECONDS * bytesPerSec
            val wavHeader = 44
            val total = chunkBytes + wavHeader
            assertTrue("청크 크기 $total bytes가 25MB 초과", total < 25 * 1024 * 1024)
        }

        @Test
        fun `BYTES_PER_SECOND 상수가 16kHz mono 16bit 값과 일치한다`() {
            assertEquals(32_000, AudioChunker.BYTES_PER_SECOND)
        }

        @Test
        fun `빈 PCM 입력은 빈 리스트를 반환한다`() {
            val result = AudioChunker.splitPcmForTest(ByteArray(0))
            assertEquals(0, result.size)
        }

        @Test
        fun `300초 입력은 1청크로 반환된다 (오버랩 분할 없음)`() {
            val pcm = ByteArray(300 * bytesPerSec)
            val result = AudioChunker.splitPcmForTest(pcm)
            assertEquals(1, result.size)
            assertEquals(pcm.size, result[0].size)
        }

        @Test
        fun `600초 정확히 입력은 1청크로 반환된다`() {
            val pcm = ByteArray(600 * bytesPerSec)
            val result = AudioChunker.splitPcmForTest(pcm)
            assertEquals(1, result.size)
            assertEquals(pcm.size, result[0].size)
        }

        @Test
        fun `1800초 입력은 3청크로 분할된다 (오버랩 10초)`() {
            val pcm = ByteArray(1800 * bytesPerSec)
            val result = AudioChunker.splitPcmForTest(pcm)
            assertEquals(3, result.size)
            // 첫/둘째 청크는 600초 full size
            assertEquals(600 * bytesPerSec, result[0].size)
            assertEquals(600 * bytesPerSec, result[1].size)
            // 셋째 청크는 남은 분량
            // step = 590s, offset after 2 steps = 1180s, remaining = 1800-1180 = 620s
            // min(620, 600) = 600 → 세번째 청크 full 600s, 이후 end >= pcm.size로 종료
            assertEquals(600 * bytesPerSec, result[2].size)
        }

        @Test
        fun `오버랩 10초가 정확히 320,000 bytes다`() {
            val overlapBytes = AudioChunker.OVERLAP_SECONDS * bytesPerSec
            assertEquals(320_000, overlapBytes)
        }

        @Test
        fun `청크 간 step은 590초(18,880,000 bytes)다`() {
            val stepBytes =
                (AudioChunker.CHUNK_SECONDS - AudioChunker.OVERLAP_SECONDS) * bytesPerSec
            assertEquals(18_880_000, stepBytes)
        }

        @Test
        fun `900초 입력은 2청크로 분할된다`() {
            // step = 590s, first chunk = 0..600, second chunk offset = 590, end = min(1190, 900) = 900
            val pcm = ByteArray(900 * bytesPerSec)
            val result = AudioChunker.splitPcmForTest(pcm)
            assertEquals(2, result.size)
            assertEquals(600 * bytesPerSec, result[0].size)
            assertEquals((900 - 590) * bytesPerSec, result[1].size)
        }

        @Test
        fun `overlapSec이 chunkSec보다 크거나 같으면 예외`() {
            val pcm = ByteArray(100)
            try {
                AudioChunker.splitPcmForTest(pcm, chunkSec = 10, overlapSec = 10, bytesPerSec = 1)
                throw AssertionError("IllegalArgumentException 발생 기대")
            } catch (_: IllegalArgumentException) {
                // OK
            }
        }
    }
    ```

    참조 결정:
    - JUnit 4 사용 — 기존 `Mp3MergerTest`/`WavMergerTest`와 동일한 스타일 유지
    - MockK 불필요 — `splitPcmForTest`는 순수 함수
    - 백틱(`)으로 한국어 테스트명 작성 — 기존 프로젝트 관례
  </action>
  <verify>
    <automated>cd D:/workspace/temp/Auto_Minuting && ./gradlew :app:testDebugUnitTest --tests "com.autominuting.util.AudioChunkerTest" 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `test -f app/src/test/java/com/autominuting/util/AudioChunkerTest.kt` 성공
    - `grep -n "class AudioChunkerTest" app/src/test/java/com/autominuting/util/AudioChunkerTest.kt` 매칭 1건
    - `grep -c "@Test" app/src/test/java/com/autominuting/util/AudioChunkerTest.kt` 결과 >= 8
    - `grep -n "splitPcmForTest" app/src/test/java/com/autominuting/util/AudioChunkerTest.kt` 매칭 >= 5
    - `./gradlew :app:testDebugUnitTest --tests "com.autominuting.util.AudioChunkerTest"` 결과 "BUILD SUCCESSFUL" 포함, 모든 테스트 통과
  </acceptance_criteria>
  <done>
    AudioChunkerTest 9개 이상 테스트 케이스 작성, 모두 통과. 600초 청크가 25MB 안에 들어감을 수식으로 증명, 1800초 → 3청크 분할이 정확히 계산됨을 검증.
  </done>
</task>

</tasks>

<verification>
- AudioConverter.decodeAudioToPcm / writeWavFile가 internal 가시성
- AudioChunker object가 존재하고 split() / splitPcmForTest() API 제공
- 컴파일 성공 (`./gradlew :app:compileDebugKotlin`)
- 단위 테스트 전부 통과 (`./gradlew :app:testDebugUnitTest --tests "com.autominuting.util.AudioChunkerTest"`)
</verification>

<success_criteria>
- 25MB 초과 파일 분할을 위한 독립 유틸이 완성되어, 다음 Plan(55-02)에서 `GroqSttEngine`이 `AudioChunker.split()`을 호출하여 즉시 사용할 수 있다
- AudioConverter의 PCM 디코딩/WAV 작성 로직 재사용으로 코드 중복 없음
- 순수 함수 테스트로 시간 계산의 정확성 보장 (Android 디바이스 없이 검증 가능)
</success_criteria>

<output>
작업 완료 후 `.planning/phases/55-groq/55-01-SUMMARY.md` 생성
</output>
