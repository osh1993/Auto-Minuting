---
phase: 55-groq
plan: 02
type: execute
wave: 2
depends_on:
  - "55-01"
files_modified:
  - app/src/main/java/com/autominuting/data/stt/GroqSttEngine.kt
autonomous: true
requirements:
  - GROQ-01
  - GROQ-02
  - GROQ-03
must_haves:
  truths:
    - "25MB 이하 파일은 기존 단일 요청 경로로 처리된다 (회귀 없음)"
    - "25MB 초과 파일은 AudioChunker로 분할되어 각 청크가 순차적으로 Groq API에 전송된다"
    - "모든 청크 전사 결과가 순서대로 이어붙여져 단일 문자열로 반환된다"
    - "청크 전사 완료 후 임시 청크 디렉토리가 삭제된다 (성공/실패 모두)"
    - "진행률 콜백이 청크 완료 기반 이산 값(`(i+1)/total`)으로 호출된다"
  artifacts:
    - path: "app/src/main/java/com/autominuting/data/stt/GroqSttEngine.kt"
      provides: "크기 기반 분기 + 청크 순차 전사 로직"
      contains: "transcribeChunked"
  key_links:
    - from: "GroqSttEngine.transcribe()"
      to: "AudioChunker.split()"
      via: "fileSize > MAX_FILE_SIZE 분기"
      pattern: "AudioChunker\\.split"
    - from: "GroqSttEngine.transcribeChunked()"
      to: "GroqSttEngine.transcribeSingle()"
      via: "chunks.forEachIndexed 순차 호출"
      pattern: "forEachIndexed"
    - from: "GroqSttEngine"
      to: "AudioConverter"
      via: "@Inject 생성자 의존성"
      pattern: "audioConverter: AudioConverter"
---

<objective>
`GroqSttEngine.transcribe()`의 "25MB 초과 시 실패 반환" 경로(Line 85-92)를 "자동 분할 → 순차 전사 → 이어붙이기" 경로로 교체한다. 기존 25MB 이하 경로는 `transcribeSingle()`로 추출하여 단일 파일과 각 청크 전사에서 동일하게 재사용한다.

Purpose: GROQ-01/02/03 요구사항 완성 — 사용자 개입 없이 25MB 초과 파일도 전사 파이프라인 통과.
Output: 수정된 GroqSttEngine.kt. 기능 확인은 실기기 테스트(execute-phase의 UAT 체크포인트).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-groq/55-RESEARCH.md
@.planning/phases/55-groq/55-01-SUMMARY.md
@app/src/main/java/com/autominuting/data/stt/GroqSttEngine.kt
@app/src/main/java/com/autominuting/data/stt/AudioConverter.kt
@app/src/main/java/com/autominuting/util/AudioChunker.kt

<interfaces>
<!-- Plan 55-01에서 완성된 AudioChunker 시그니처 -->

From app/src/main/java/com/autominuting/util/AudioChunker.kt:
```kotlin
object AudioChunker {
    const val CHUNK_SECONDS = 600
    const val OVERLAP_SECONDS = 10
    const val BYTES_PER_SECOND = 32_000

    suspend fun split(
        inputPath: String,
        outputDir: File,
        audioConverter: AudioConverter
    ): List<File>
}
```

From app/src/main/java/com/autominuting/data/stt/AudioConverter.kt:
```kotlin
@Singleton
class AudioConverter @Inject constructor() {
    suspend fun convertToWhisperFormat(inputPath: String, outputDir: String): String
    internal fun decodeAudioToPcm(inputPath: String): ByteArray
    internal fun writeWavFile(outputFile: File, pcmData: ByteArray)
}
```

Current GroqSttEngine constructor (변경 대상):
```kotlin
class GroqSttEngine @Inject constructor(
    private val secureApiKeyRepository: SecureApiKeyRepository,
    private val apiUsageTracker: ApiUsageTracker
) : SttEngine
```

Target GroqSttEngine constructor:
```kotlin
class GroqSttEngine @Inject constructor(
    private val secureApiKeyRepository: SecureApiKeyRepository,
    private val apiUsageTracker: ApiUsageTracker,
    private val audioConverter: AudioConverter
) : SttEngine
```
(Hilt 자동 바인딩 — AudioConverter는 이미 @Singleton + @Inject constructor())
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: transcribe() 크기 분기 + transcribeSingle/transcribeChunked 분리</name>
  <files>app/src/main/java/com/autominuting/data/stt/GroqSttEngine.kt</files>
  <read_first>
    - app/src/main/java/com/autominuting/data/stt/GroqSttEngine.kt (현재 구현 전체 — 특히 Line 64-185 transcribe 본문)
    - app/src/main/java/com/autominuting/util/AudioChunker.kt (Plan 55-01 산출물)
    - app/src/main/java/com/autominuting/data/stt/AudioConverter.kt (internal 헬퍼 가시성 확인)
    - .planning/phases/55-groq/55-RESEARCH.md (Pattern 2, Pitfall 2/3/4/5)
  </read_first>
  <action>
    `GroqSttEngine.kt`를 아래 구조로 재작성한다. 기존 파일을 읽고 **전체 교체**(Write) 또는 구간 Edit로 반영한다. 패키지, import, companion, `client` 멤버, `engineName()`, `isAvailable()`, `GroqSttException`은 그대로 유지.

    Step 1 — 생성자에 `AudioConverter` 추가:

    기존:
    ```kotlin
    class GroqSttEngine @Inject constructor(
        private val secureApiKeyRepository: SecureApiKeyRepository,
        private val apiUsageTracker: ApiUsageTracker
    ) : SttEngine {
    ```

    수정 후:
    ```kotlin
    class GroqSttEngine @Inject constructor(
        private val secureApiKeyRepository: SecureApiKeyRepository,
        private val apiUsageTracker: ApiUsageTracker,
        private val audioConverter: AudioConverter
    ) : SttEngine {
    ```

    필요 import 추가:
    ```kotlin
    import com.autominuting.util.AudioChunker
    ```

    Step 2 — `transcribe()` 본문을 크기 분기로 교체:

    기존 Line 64-185 전체를 다음으로 교체:

    ```kotlin
    override suspend fun transcribe(
        audioFilePath: String,
        onProgress: (Float) -> Unit
    ): Result<String> = withContext(Dispatchers.IO) {
        try {
            val apiKey = secureApiKeyRepository.getGroqApiKey()
            if (apiKey.isNullOrBlank()) {
                return@withContext Result.failure(
                    GroqSttException("Groq API 키가 설정되지 않았습니다")
                )
            }

            val audioFile = File(audioFilePath)
            if (!audioFile.exists()) {
                return@withContext Result.failure(
                    IllegalArgumentException("오디오 파일이 존재하지 않습니다: $audioFilePath")
                )
            }

            val fileSize = audioFile.length()
            Log.d(TAG, "Groq STT 진입: $audioFilePath (${fileSize / 1024 / 1024}MB)")

            // 크기 기반 분기 (GROQ-01): 25MB 이하는 단일 요청, 초과 시 청크 분할
            if (fileSize <= MAX_FILE_SIZE) {
                return@withContext transcribeSingle(audioFile, apiKey, onProgress)
            }
            return@withContext transcribeChunked(audioFile, apiKey, onProgress)
        } catch (e: Exception) {
            Log.e(TAG, "Groq STT 전사 중 오류: ${e.message}", e)
            Result.failure(GroqSttException("Groq STT 전사 실패: ${e.message}", e))
        }
    }
    ```

    Step 3 — `transcribeSingle()` private 함수 신설 (기존 단일 요청 로직을 그대로 이동):

    ```kotlin
    /**
     * 단일 파일 전사 (25MB 이하 또는 분할된 청크 개별 전송).
     * 기존 transcribe() 본문의 단일 요청 로직을 그대로 유지.
     */
    private suspend fun transcribeSingle(
        audioFile: File,
        apiKey: String,
        onProgress: (Float) -> Unit
    ): Result<String> = withContext(Dispatchers.IO) {
        val extension = audioFile.extension.lowercase()
        val mimeType = MIME_MAP[extension]
            ?: return@withContext Result.failure(
                GroqSttException("지원하지 않는 오디오 형식: $extension")
            )

        Log.d(TAG, "Groq 단일 요청: ${audioFile.name} (${audioFile.length()} bytes, $mimeType)")

        var lastException: Exception? = null
        for (attempt in 1..MAX_RETRIES) {
            try {
                val requestBody = MultipartBody.Builder()
                    .setType(MultipartBody.FORM)
                    .addFormDataPart(
                        "file", audioFile.name,
                        audioFile.asRequestBody(mimeType.toMediaType())
                    )
                    .addFormDataPart("model", MODEL)
                    .addFormDataPart("language", "ko")
                    .addFormDataPart("response_format", "verbose_json")
                    .build()

                val request = Request.Builder()
                    .url(API_URL)
                    .header("Authorization", "Bearer $apiKey")
                    .post(requestBody)
                    .build()

                Log.d(TAG, "Groq API 호출 (시도 $attempt/$MAX_RETRIES)")
                val callResult = client.newCall(request).execute().use { resp ->
                    val responseBody = resp.body?.string()

                    if (resp.code == 429) {
                        lastException = GroqSttException("할당량 초과 (HTTP 429)")
                        if (attempt < MAX_RETRIES) {
                            Log.w(TAG, "할당량 초과, ${RETRY_DELAY_MS / 1000}초 후 재시도 ($attempt/$MAX_RETRIES)")
                        }
                        return@use null
                    }

                    if (!resp.isSuccessful || responseBody == null) {
                        return@withContext Result.failure<String>(
                            GroqSttException("Groq API 오류: HTTP ${resp.code} — ${responseBody?.take(200)}")
                        )
                    }

                    val json = JSONObject(responseBody)
                    val text = json.optString("text").trim()

                    if (text.isBlank()) {
                        return@withContext Result.failure<String>(
                            GroqSttException("Groq 전사 결과가 비어있습니다")
                        )
                    }

                    Log.d(TAG, "Groq 단일 요청 완료: ${text.length}자 (시도 $attempt)")
                    Result.success(text)
                }

                if (callResult != null) {
                    apiUsageTracker.record(ApiUsageTracker.KEY_GROQ_STT)
                    return@withContext callResult
                }

                if (attempt < MAX_RETRIES) {
                    delay(RETRY_DELAY_MS)
                } else {
                    break
                }
            } catch (e: java.net.SocketTimeoutException) {
                lastException = e
                if (attempt < MAX_RETRIES) {
                    Log.w(TAG, "타임아웃, ${RETRY_DELAY_MS / 1000}초 후 재시도 ($attempt/$MAX_RETRIES)")
                    delay(RETRY_DELAY_MS)
                }
            }
        }

        Result.failure(
            GroqSttException("Groq API 요청 실패 (${MAX_RETRIES}회 재시도)", lastException)
        )
    }
    ```

    Step 4 — `transcribeChunked()` private 함수 신설 (분할 전사):

    ```kotlin
    /**
     * 25MB 초과 파일을 청크 분할하여 순차 전사 후 이어붙인다 (GROQ-01/02/03).
     */
    private suspend fun transcribeChunked(
        audioFile: File,
        apiKey: String,
        onProgress: (Float) -> Unit
    ): Result<String> = withContext(Dispatchers.IO) {
        // 청크 임시 디렉토리: 회의 폴더 하위 (Pitfall 2/4 참조 — cacheDir 대신 meeting dir 사용)
        val parentDir = audioFile.parentFile
            ?: return@withContext Result.failure(
                GroqSttException("오디오 파일의 부모 디렉토리를 찾을 수 없습니다: ${audioFile.absolutePath}")
            )
        val tempDir = File(parentDir, "groq_chunks_${System.currentTimeMillis()}")

        try {
            Log.d(TAG, "Groq 청크 분할 시작: ${audioFile.absolutePath} (${audioFile.length() / 1024 / 1024}MB)")
            val chunks = AudioChunker.split(
                inputPath = audioFile.absolutePath,
                outputDir = tempDir,
                audioConverter = audioConverter
            )
            Log.d(TAG, "Groq 청크 분할 완료: ${chunks.size}개 청크")

            if (chunks.isEmpty()) {
                return@withContext Result.failure(
                    GroqSttException("오디오 분할 결과가 비어있습니다")
                )
            }

            val transcripts = mutableListOf<String>()
            chunks.forEachIndexed { index, chunk ->
                // GROQ-02: 순서대로 전사
                Log.d(TAG, "청크 ${index + 1}/${chunks.size} 전사 시작: ${chunk.name} (${chunk.length()} bytes)")
                val result = transcribeSingle(chunk, apiKey, onProgress = { /* 청크별 내부 진행률 무시 */ })
                if (result.isFailure) {
                    Log.e(TAG, "청크 ${index + 1}/${chunks.size} 전사 실패")
                    return@withContext result
                }
                transcripts += result.getOrThrow()

                // 청크 완료 기반 이산 진행률 (Pitfall 5 회피)
                onProgress((index + 1).toFloat() / chunks.size)

                // RPM 20 안전 마진 (마지막 청크 뒤에는 delay 불필요)
                if (index < chunks.lastIndex) delay(CHUNK_DELAY_MS)
            }

            // GROQ-03: 순서대로 이어붙임 (단순 concat, 55-RESEARCH.md Open Question 1 참조)
            val combined = transcripts.joinToString("\n")
            Log.d(TAG, "Groq 청크 전사 전체 완료: ${chunks.size}개 청크, 총 ${combined.length}자")
            Result.success(combined)
        } finally {
            // 임시 청크 정리 (Pitfall 4)
            if (tempDir.exists()) {
                val deleted = tempDir.deleteRecursively()
                Log.d(TAG, "청크 임시 디렉토리 정리: ${tempDir.absolutePath} (success=$deleted)")
            }
        }
    }
    ```

    Step 5 — `companion object`에 `CHUNK_DELAY_MS` 추가:

    기존 `companion object` 블록(Line 33-50) 안에 다음 라인을 `RETRY_DELAY_MS` 바로 아래에 추가:
    ```kotlin
    private const val CHUNK_DELAY_MS = 500L  // Free tier RPM 20 안전 마진 (55-RESEARCH.md)
    ```

    Step 6 — 기존 25MB 초과 실패 로직 삭제 확인:

    교체 후 `GroqSttEngine.kt` 안에 아래 문자열이 **존재하지 않아야** 한다:
    - `"Groq 제한(25MB)을 초과합니다"` — 자동 분할로 대체되므로 완전 제거
    - `"Groq Dev 플랜으로 업그레이드하세요"` — 동일

    참조 결정:
    - Hilt는 `AudioConverter`를 자동 바인딩 (@Singleton + @Inject constructor) — `SttModule.kt` 수정 불필요
    - 청크 임시 디렉토리는 회의 폴더 하위 (55-RESEARCH.md Open Question 2 권고 — cacheDir은 WorkManager 재시도 시 비워질 수 있음)
    - 단순 concat stitching (LCS 미적용) — 55-RESEARCH.md Open Question 1: 요구사항(GROQ-03)은 "이어붙이기"만 명시
    - 진행률은 청크 완료 이산 값 — Pitfall 5
    - `CHUNK_DELAY_MS = 500L` — 55-RESEARCH.md "500~1000ms 권장"의 하한
  </action>
  <verify>
    <automated>cd D:/workspace/temp/Auto_Minuting && ./gradlew :app:compileDebugKotlin 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "private val audioConverter: AudioConverter" app/src/main/java/com/autominuting/data/stt/GroqSttEngine.kt` 매칭 1건
    - `grep -n "import com.autominuting.util.AudioChunker" app/src/main/java/com/autominuting/data/stt/GroqSttEngine.kt` 매칭 1건
    - `grep -n "private const val CHUNK_DELAY_MS = 500L" app/src/main/java/com/autominuting/data/stt/GroqSttEngine.kt` 매칭 1건
    - `grep -n "private suspend fun transcribeSingle" app/src/main/java/com/autominuting/data/stt/GroqSttEngine.kt` 매칭 1건
    - `grep -n "private suspend fun transcribeChunked" app/src/main/java/com/autominuting/data/stt/GroqSttEngine.kt` 매칭 1건
    - `grep -n "AudioChunker\.split" app/src/main/java/com/autominuting/data/stt/GroqSttEngine.kt` 매칭 1건
    - `grep -n "fileSize <= MAX_FILE_SIZE" app/src/main/java/com/autominuting/data/stt/GroqSttEngine.kt` 매칭 1건
    - `grep -n "transcripts.joinToString" app/src/main/java/com/autominuting/data/stt/GroqSttEngine.kt` 매칭 1건
    - `grep -n "forEachIndexed" app/src/main/java/com/autominuting/data/stt/GroqSttEngine.kt` 매칭 1건
    - `grep -n "tempDir.deleteRecursively" app/src/main/java/com/autominuting/data/stt/GroqSttEngine.kt` 매칭 1건
    - `grep -n "groq_chunks_" app/src/main/java/com/autominuting/data/stt/GroqSttEngine.kt` 매칭 1건
    - `grep -cn "Groq 제한.25MB.을 초과합니다\|Dev 플랜으로 업그레이드" app/src/main/java/com/autominuting/data/stt/GroqSttEngine.kt` 결과 0
    - `./gradlew :app:compileDebugKotlin` 성공 (exit code 0)
    - `./gradlew :app:assembleDebug` 성공 (Hilt 생성 바인딩이 AudioConverter를 GroqSttEngine에 주입하는지 검증)
  </acceptance_criteria>
  <done>
    GroqSttEngine.transcribe()가 파일 크기에 따라 단일/청크 경로로 분기한다. 25MB 초과 시 AudioChunker로 분할 → 각 청크 순차 전사 → 결과 concat. 기존 25MB 실패 경로는 완전히 제거됨. Debug APK 빌드 성공.
  </done>
</task>

</tasks>

<verification>
- `./gradlew :app:compileDebugKotlin` 성공
- `./gradlew :app:assembleDebug` 성공 (Hilt DI 그래프 유효성 포함)
- 기존 `./gradlew :app:testDebugUnitTest` 전체 테스트 통과 (회귀 확인)
- GroqSttEngine에 크기 분기 / transcribeSingle / transcribeChunked 세 메서드 존재
- 임시 청크 디렉토리(`groq_chunks_<timestamp>/`)가 finally 블록에서 삭제됨
</verification>

<success_criteria>
- GROQ-01: 25MB 초과 파일이 자동으로 AudioChunker로 분할된다 (사용자 개입 없음)
- GROQ-02: 분할된 청크가 `forEachIndexed` 순차 호출로 Groq API에 전송된다 (병렬 금지)
- GROQ-03: 모든 청크 전사 결과가 `joinToString("\n")`으로 순서대로 이어붙여진다
- 기존 25MB 이하 경로는 `transcribeSingle()`로 회귀 없이 작동
- 청크 임시 파일은 성공/실패 모두 정리됨 (디스크 고갈 방지)
</success_criteria>

<output>
작업 완료 후 `.planning/phases/55-groq/55-02-SUMMARY.md` 생성
</output>
