---
phase: 38-independent-ui
plan: 02
type: execute
wave: 2
depends_on: [38-01]
files_modified:
  - app/src/main/java/com/autominuting/presentation/minutes/MinutesScreen.kt
  - app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt
  - app/src/main/java/com/autominuting/presentation/navigation/AppNavigation.kt
autonomous: true
requirements: [UI5-01, UI5-02]

must_haves:
  truths:
    - "회의록 카드에 출처 전사 이름이 표시된다 (meetingId NULL이면 '삭제된 전사')"
    - "회의록 카드에서 출처 전사 이름을 탭하면 전사 편집 화면으로 이동한다 (meetingId NULL이면 비활성)"
    - "전사 카드에 연결된 회의록 수가 badge로 표시된다 (0개면 badge 미표시)"
    - "assembleDebug 빌드가 성공한다"
  artifacts:
    - path: "app/src/main/java/com/autominuting/presentation/minutes/MinutesScreen.kt"
      provides: "MinutesCard에 출처 전사명 Row + 탭 네비게이션"
      contains: "삭제된 전사"
    - path: "app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt"
      provides: "MinutesCountBadge 컴포저블 + TranscriptMeetingCard에 badge 통합"
      contains: "MinutesCountBadge"
    - path: "app/src/main/java/com/autominuting/presentation/navigation/AppNavigation.kt"
      provides: "MinutesScreen에 onSourceTranscriptClick 콜백 연결"
      contains: "onSourceTranscriptClick"
  key_links:
    - from: "MinutesScreen"
      to: "MinutesViewModel.minutesUiModels"
      via: "collectAsState"
      pattern: "minutesUiModels\\.collectAsState"
    - from: "TranscriptsScreen TranscriptMeetingCard"
      to: "TranscriptsViewModel.minutesCountMap"
      via: "collectAsState + Map lookup"
      pattern: "minutesCountMap"
    - from: "AppNavigation MinutesScreen"
      to: "Screen.TranscriptEdit.createRoute"
      via: "onSourceTranscriptClick 콜백"
      pattern: "onSourceTranscriptClick"
---

<objective>
Phase 38 UI 레이어: 회의록 카드에 출처 전사명을 표시하고, 전사 카드에 회의록 수 badge를 표시하며, 네비게이션을 연결한다.

Purpose: Plan 01에서 구축한 데이터 계약을 UI에 반영하여 사용자가 전사-회의록 1:N 관계를 직관적으로 파악할 수 있게 한다.
Output: MinutesScreen 출처 전사 표시, TranscriptsScreen 회의록 수 badge, AppNavigation 콜백 연결, assembleDebug 빌드 성공
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-independent-ui/38-RESEARCH.md
@.planning/phases/38-independent-ui/38-01-SUMMARY.md

@app/src/main/java/com/autominuting/presentation/minutes/MinutesScreen.kt
@app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt
@app/src/main/java/com/autominuting/presentation/navigation/AppNavigation.kt
@app/src/main/java/com/autominuting/presentation/navigation/Screen.kt

<interfaces>
<!-- Plan 01에서 생성된 인터페이스 (38-01-SUMMARY.md 참조) -->

From MinutesViewModel.kt (Plan 01 변경 후):
```kotlin
data class MinutesUiModel(
    val minutes: Minutes,
    val meetingTitle: String?  // null이면 "삭제된 전사"
)

class MinutesViewModel {
    val minutesUiModels: StateFlow<List<MinutesUiModel>>
    // 기존: deleteMinutes(), deleteSelectedMinutes(), updateMinutesTitle(), shareMinutes() 유지
}
```

From TranscriptsViewModel.kt (Plan 01 변경 후):
```kotlin
class TranscriptsViewModel {
    val meetings: StateFlow<List<Meeting>>
    val minutesCountMap: StateFlow<Map<Long, Int>>  // 추가됨
}
```

From Screen.kt:
```kotlin
sealed class Screen {
    data object TranscriptEdit : Screen("transcripts/{meetingId}/edit", ...) {
        fun createRoute(meetingId: Long) = "transcripts/$meetingId/edit"
    }
}
```

From AppNavigation.kt (현재):
```kotlin
composable(Screen.Minutes.route) {
    MinutesScreen(
        onMinutesClick = { minutesId ->
            navController.navigate(Screen.MinutesDetail.createRoute(minutesId))
        }
    )
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MinutesScreen 출처 전사 표시 + TranscriptsScreen badge + AppNavigation 연결</name>
  <files>
    app/src/main/java/com/autominuting/presentation/minutes/MinutesScreen.kt,
    app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt,
    app/src/main/java/com/autominuting/presentation/navigation/AppNavigation.kt
  </files>
  <action>
    **MinutesScreen.kt 변경:**

    1. `MinutesScreen` 컴포저블에 `onSourceTranscriptClick: (Long) -> Unit = {}` 파라미터 추가.

    2. `viewModel.minutes.collectAsState()` → `viewModel.minutesUiModels.collectAsState()` 로 변경.
       변수명도 `minutesList` → `minutesUiModels`로 변경.

    3. `MinutesUiModel` import 추가: `import com.autominuting.presentation.minutes.MinutesUiModel`
       (같은 패키지이므로 import 불필요할 수 있음 — 파일 위치 확인 후 결정)

    4. LazyColumn의 `items` 호출 수정:
       ```kotlin
       items(minutesUiModels, key = { it.minutes.id }) { uiModel ->
           MinutesCard(
               minutes = uiModel.minutes,
               meetingTitle = uiModel.meetingTitle,
               isSelectionMode = isSelectionMode,
               isSelected = uiModel.minutes.id in selectedIds,
               onClick = { ... },  // 기존과 동일하되 minutes → uiModel.minutes
               onLongClick = { ... },
               onRenameRequest = { m -> minutesToRename = m },
               onDeleteRequest = { id -> minutesToDelete = minutesUiModels.map { it.minutes }.find { it.id == id } },
               onShare = { id -> viewModel.shareMinutes(id, context) },
               onSourceClick = { meetingId -> onSourceTranscriptClick(meetingId) }
           )
       }
       ```

    5. `minutesToDelete` 관련 코드에서 `minutesList` 참조를 `minutesUiModels.map { it.minutes }`로 변경.

    6. `MinutesCard` 컴포저블 시그니처 수정 — `meetingTitle: String?`과 `onSourceClick: (Long) -> Unit` 파라미터 추가:
       ```kotlin
       @Composable
       private fun MinutesCard(
           minutes: Minutes,
           meetingTitle: String?,
           isSelectionMode: Boolean,
           isSelected: Boolean,
           onClick: () -> Unit,
           onLongClick: () -> Unit,
           onRenameRequest: (Minutes) -> Unit,
           onDeleteRequest: (Long) -> Unit,
           onShare: (Long) -> Unit,
           onSourceClick: (Long) -> Unit
       )
       ```

    7. MinutesCard 내부 Column에서 회의록 제목과 생성 시각 사이에 출처 전사 이름 Row 추가:
       ```kotlin
       // 회의록 제목
       Text(
           text = minutes.minutesTitle ?: "회의록",
           ...
       )

       Spacer(modifier = Modifier.height(4.dp))

       // 출처 전사 이름 (UI5-01)
       Text(
           text = meetingTitle ?: "삭제된 전사",
           style = MaterialTheme.typography.bodySmall,
           color = if (minutes.meetingId != null)
               MaterialTheme.colorScheme.primary
           else
               MaterialTheme.colorScheme.onSurfaceVariant,
           modifier = if (minutes.meetingId != null) {
               Modifier.clickable { onSourceClick(minutes.meetingId) }
           } else {
               Modifier
           }
       )

       Spacer(modifier = Modifier.height(4.dp))

       // 생성 시각 (기존)
       Text(...)
       ```
       meetingId가 null이면 클릭 불가 + onSurfaceVariant 색상, null이 아니면 클릭 가능 + primary 색상으로 링크 느낌을 준다.

    **TranscriptsScreen.kt 변경:**

    1. TranscriptsScreen 컴포저블 내부에서 `minutesCountMap` 수집:
       ```kotlin
       val minutesCountMap by viewModel.minutesCountMap.collectAsState()
       ```

    2. `TranscriptMeetingCard` 호출에 `minutesCount` 파라미터 추가:
       ```kotlin
       TranscriptMeetingCard(
           meeting = meeting,
           minutesCount = minutesCountMap[meeting.id] ?: 0,
           ...
       )
       ```

    3. `TranscriptMeetingCard` 시그니처에 `minutesCount: Int` 추가.

    4. 기존 `MinutesStatusBadge(meeting)` 호출을 `MinutesCountBadge(minutesCount)` + 기존 PipelineStatus 기반 배지의 조합으로 교체:
       - GENERATING_MINUTES 상태일 때는 기존 "회의록 생성 중" 표시 유지
       - 그 외에는 MinutesCountBadge로 실제 count 표시

       상태 배지 Row 내부 수정:
       ```kotlin
       TranscriptionStatusBadge(meeting)
       // 회의록 상태: 생성 중이면 기존 배지, 아니면 count 배지
       if (meeting.pipelineStatus == PipelineStatus.GENERATING_MINUTES) {
           SuggestionChip(
               onClick = {},
               label = { Text("회의록 생성 중", style = MaterialTheme.typography.labelSmall) },
               colors = SuggestionChipDefaults.suggestionChipColors(
                   containerColor = MaterialTheme.colorScheme.tertiaryContainer,
                   labelColor = MaterialTheme.colorScheme.onTertiaryContainer
               )
           )
       } else {
           MinutesCountBadge(minutesCount)
       }
       ```

    5. 새로운 `MinutesCountBadge` 컴포저블 추가 (UI5-02):
       ```kotlin
       @Composable
       private fun MinutesCountBadge(count: Int) {
           if (count > 0) {
               SuggestionChip(
                   onClick = {},
                   label = {
                       Text(
                           text = "회의록 ${count}개",
                           style = MaterialTheme.typography.labelSmall
                       )
                   },
                   colors = SuggestionChipDefaults.suggestionChipColors(
                       containerColor = MaterialTheme.colorScheme.primaryContainer,
                       labelColor = MaterialTheme.colorScheme.onPrimaryContainer
                   )
               )
           }
       }
       ```
       0개이면 badge 자체를 렌더링하지 않는다.

    6. 기존 `MinutesStatusBadge` private 함수는 더 이상 사용하지 않으므로 삭제한다.

    **AppNavigation.kt 변경:**

    1. MinutesScreen 호출에 `onSourceTranscriptClick` 콜백 추가:
       ```kotlin
       composable(Screen.Minutes.route) {
           MinutesScreen(
               onMinutesClick = { minutesId ->
                   navController.navigate(Screen.MinutesDetail.createRoute(minutesId))
               },
               onSourceTranscriptClick = { meetingId ->
                   navController.navigate(Screen.TranscriptEdit.createRoute(meetingId))
               }
           )
       }
       ```
  </action>
  <verify>
    <automated>cd d:/workspace/temp/Auto_Minuting && ./gradlew :app:kspDebugKotlin 2>&1 | tail -5</automated>
  </verify>
  <done>MinutesScreen에 출처 전사명이 표시되고, TranscriptsScreen에 회의록 수 badge가 표시되며, 출처 전사 클릭 시 TranscriptEdit 화면으로 이동한다</done>
</task>

<task type="auto">
  <name>Task 2: assembleDebug 빌드 검증</name>
  <files></files>
  <action>
    `./gradlew :app:assembleDebug`를 실행하여 전체 빌드가 성공하는지 확인한다.
    빌드 실패 시 오류 메시지를 분석하고 수정한다.

    확인 사항:
    - Room @Query JOIN 쿼리의 POJO 필드명과 SQL alias 일치 여부
    - Hilt DI 그래프 컴파일 성공 (TranscriptsViewModel에 MinutesDataRepository 주입)
    - Compose 컴파일러 오류 없음 (새 파라미터 추가로 인한 호출부 불일치 등)
    - MinutesScreen의 minutesUiModels 참조가 모두 정확한지
  </action>
  <verify>
    <automated>cd d:/workspace/temp/Auto_Minuting && ./gradlew :app:assembleDebug 2>&1 | tail -5</automated>
  </verify>
  <done>assembleDebug가 BUILD SUCCESSFUL로 완료된다</done>
</task>

</tasks>

<verification>
- `./gradlew :app:assembleDebug` BUILD SUCCESSFUL
- MinutesScreen에 출처 전사명이 표시되는 코드가 존재 (grep "삭제된 전사" MinutesScreen.kt)
- TranscriptsScreen에 MinutesCountBadge가 존재 (grep "MinutesCountBadge" TranscriptsScreen.kt)
- AppNavigation에 onSourceTranscriptClick 콜백이 연결됨 (grep "onSourceTranscriptClick" AppNavigation.kt)
- meetingId == null일 때 클릭 비활성화 처리됨
- count == 0일 때 badge 미표시 처리됨
</verification>

<success_criteria>
assembleDebug 빌드 성공. 회의록 카드에 출처 전사명이 표시되고 (meetingId null이면 "삭제된 전사"), 전사 카드에 회의록 수 badge가 표시되며 (0개면 미표시), 출처 전사 탭 시 전사 편집 화면으로 이동하는 네비게이션이 연결된다.
</success_criteria>

<output>
After completion, create `.planning/phases/38-independent-ui/38-02-SUMMARY.md`
</output>
