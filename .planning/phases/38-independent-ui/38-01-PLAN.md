---
phase: 38-independent-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/com/autominuting/data/local/dao/MinutesDao.kt
  - app/src/main/java/com/autominuting/domain/repository/MinutesDataRepository.kt
  - app/src/main/java/com/autominuting/data/repository/MinutesDataRepositoryImpl.kt
  - app/src/main/java/com/autominuting/presentation/minutes/MinutesViewModel.kt
  - app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsViewModel.kt
autonomous: true
requirements: [UI5-01, UI5-02]

must_haves:
  truths:
    - "MinutesDao가 LEFT JOIN으로 Minutes + Meeting.title을 한 번에 반환한다"
    - "MinutesDao가 GROUP BY로 meetingId별 회의록 수를 일괄 반환한다"
    - "MinutesViewModel이 meetingTitle 포함 데이터를 UI에 제공한다"
    - "TranscriptsViewModel이 meetingId별 minutes count Map을 UI에 제공한다"
  artifacts:
    - path: "app/src/main/java/com/autominuting/data/local/dao/MinutesDao.kt"
      provides: "MinutesWithMeetingTitle POJO + getAllMinutesWithMeetingTitle() JOIN 쿼리 + MinutesCountPerMeeting POJO + getMinutesCountPerMeeting() 쿼리"
      contains: "LEFT JOIN meetings"
    - path: "app/src/main/java/com/autominuting/domain/repository/MinutesDataRepository.kt"
      provides: "getAllMinutesWithMeetingTitle(), getMinutesCountPerMeeting() 인터페이스"
    - path: "app/src/main/java/com/autominuting/presentation/minutes/MinutesViewModel.kt"
      provides: "MinutesUiModel 리스트 StateFlow (meetingTitle 포함)"
    - path: "app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsViewModel.kt"
      provides: "minutesCountMap StateFlow<Map<Long, Int>>"
  key_links:
    - from: "MinutesDao.getAllMinutesWithMeetingTitle()"
      to: "MinutesDataRepositoryImpl"
      via: "Flow mapping"
      pattern: "getAllMinutesWithMeetingTitle"
    - from: "MinutesViewModel.minutes"
      to: "MinutesDataRepository.getAllMinutesWithMeetingTitle()"
      via: "Flow collection"
      pattern: "getAllMinutesWithMeetingTitle"
    - from: "TranscriptsViewModel.minutesCountMap"
      to: "MinutesDataRepository.getMinutesCountPerMeeting()"
      via: "Flow stateIn"
      pattern: "getMinutesCountPerMeeting"
---

<objective>
Phase 38 데이터/ViewModel 레이어: 회의록 목록에 출처 전사명을 함께 제공하고, 전사 목록에 회의록 수를 제공하는 쿼리 및 ViewModel StateFlow를 구현한다.

Purpose: UI 레이어(Plan 02)가 소비할 데이터 계약을 먼저 확립하여 UI 작업이 명확한 인터페이스에 의존하도록 한다.
Output: MinutesDao JOIN/count 쿼리, Repository 인터페이스/구현, ViewModel StateFlow 변경
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-independent-ui/38-RESEARCH.md

@app/src/main/java/com/autominuting/data/local/dao/MinutesDao.kt
@app/src/main/java/com/autominuting/domain/repository/MinutesDataRepository.kt
@app/src/main/java/com/autominuting/data/repository/MinutesDataRepositoryImpl.kt
@app/src/main/java/com/autominuting/presentation/minutes/MinutesViewModel.kt
@app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsViewModel.kt
@app/src/main/java/com/autominuting/domain/model/Minutes.kt

<interfaces>
<!-- MinutesDao 현재 인터페이스 (추가할 메서드의 기반) -->
From MinutesDao.kt:
```kotlin
@Dao
interface MinutesDao {
    @Query("SELECT * FROM minutes ORDER BY createdAt DESC")
    fun getAllMinutes(): Flow<List<MinutesEntity>>
    @Query("SELECT COUNT(*) FROM minutes WHERE meetingId = :meetingId")
    fun getMinutesCountByMeetingId(meetingId: Long): Flow<Int>
}
```

From MinutesDataRepository.kt:
```kotlin
interface MinutesDataRepository {
    fun getAllMinutes(): Flow<List<Minutes>>
    fun getMinutesCountByMeetingId(meetingId: Long): Flow<Int>
}
```

From Minutes.kt (도메인 모델):
```kotlin
data class Minutes(
    val id: Long = 0,
    val meetingId: Long?,
    val minutesPath: String,
    val minutesTitle: String?,
    val templateId: Long?,
    val createdAt: Instant,
    val updatedAt: Instant
)
```

From MinutesViewModel.kt:
```kotlin
@HiltViewModel
class MinutesViewModel @Inject constructor(
    private val minutesDataRepository: MinutesDataRepository
) : ViewModel() {
    val minutes: StateFlow<List<Minutes>> = ...
}
```

From TranscriptsViewModel.kt:
```kotlin
@HiltViewModel
class TranscriptsViewModel @Inject constructor(
    private val meetingRepository: MeetingRepository,
    private val userPreferencesRepository: UserPreferencesRepository,
    private val promptTemplateRepository: PromptTemplateRepository,
    @ApplicationContext private val context: Context
) : ViewModel()
// MinutesDataRepository 미주입 상태
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MinutesDao JOIN/count 쿼리 + Repository 인터페이스/구현 추가</name>
  <files>
    app/src/main/java/com/autominuting/data/local/dao/MinutesDao.kt,
    app/src/main/java/com/autominuting/domain/repository/MinutesDataRepository.kt,
    app/src/main/java/com/autominuting/data/repository/MinutesDataRepositoryImpl.kt
  </files>
  <action>
    **MinutesDao.kt에 POJO + 쿼리 2개 추가:**

    1. `MinutesWithMeetingTitle` data class를 MinutesDao.kt 파일 상단(인터페이스 밖)에 선언:
       ```kotlin
       data class MinutesWithMeetingTitle(
           val id: Long,
           val meetingId: Long?,
           val minutesPath: String,
           val minutesTitle: String?,
           val templateId: Long?,
           val createdAt: Long,
           val updatedAt: Long,
           val meetingTitle: String?  // LEFT JOIN으로 가져옴
       )
       ```
       주의: Room POJO이므로 createdAt/updatedAt은 Long(epoch millis)이다 (MinutesEntity와 동일).

    2. `MinutesCountPerMeeting` data class를 같은 파일에 선언:
       ```kotlin
       data class MinutesCountPerMeeting(
           val meetingId: Long,
           val count: Int
       )
       ```

    3. MinutesDao 인터페이스 안에 두 쿼리 추가:
       ```kotlin
       @Query("""
           SELECT m.*, mt.title AS meetingTitle
           FROM minutes m
           LEFT JOIN meetings mt ON m.meetingId = mt.id
           ORDER BY m.createdAt DESC
       """)
       fun getAllMinutesWithMeetingTitle(): Flow<List<MinutesWithMeetingTitle>>

       @Query("SELECT meetingId, COUNT(*) AS count FROM minutes WHERE meetingId IS NOT NULL GROUP BY meetingId")
       fun getMinutesCountPerMeeting(): Flow<List<MinutesCountPerMeeting>>
       ```

    **MinutesDataRepository.kt 인터페이스에 2개 메서드 추가:**
    ```kotlin
    /** 모든 회의록을 출처 Meeting 제목과 함께 조회한다. */
    fun getAllMinutesWithMeetingTitle(): Flow<List<Pair<Minutes, String?>>>

    /** 모든 Meeting별 회의록 수를 일괄 조회한다. */
    fun getMinutesCountPerMeeting(): Flow<Map<Long, Int>>
    ```
    반환 타입은 도메인 레이어답게 DAO POJO를 직접 노출하지 않고 Pair<Minutes, String?> (Minutes + meetingTitle)과 Map<Long, Int>로 변환한다.

    **MinutesDataRepositoryImpl.kt에 구현 추가:**
    ```kotlin
    override fun getAllMinutesWithMeetingTitle(): Flow<List<Pair<Minutes, String?>>> =
        minutesDao.getAllMinutesWithMeetingTitle().map { list ->
            list.map { row ->
                Pair(
                    Minutes(
                        id = row.id,
                        meetingId = row.meetingId,
                        minutesPath = row.minutesPath,
                        minutesTitle = row.minutesTitle,
                        templateId = row.templateId,
                        createdAt = Instant.ofEpochMilli(row.createdAt),
                        updatedAt = Instant.ofEpochMilli(row.updatedAt)
                    ),
                    row.meetingTitle
                )
            }
        }

    override fun getMinutesCountPerMeeting(): Flow<Map<Long, Int>> =
        minutesDao.getMinutesCountPerMeeting().map { list ->
            list.associate { it.meetingId to it.count }
        }
    ```
  </action>
  <verify>
    <automated>cd d:/workspace/temp/Auto_Minuting && ./gradlew :app:kspDebugKotlin 2>&1 | tail -5</automated>
  </verify>
  <done>MinutesDao에 JOIN/count 쿼리가 추가되고, Repository 인터페이스/구현이 컴파일된다</done>
</task>

<task type="auto">
  <name>Task 2: MinutesViewModel + TranscriptsViewModel StateFlow 변경</name>
  <files>
    app/src/main/java/com/autominuting/presentation/minutes/MinutesViewModel.kt,
    app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsViewModel.kt
  </files>
  <action>
    **MinutesViewModel.kt 변경:**

    1. 파일 상단에 UI 모델 data class 추가 (ViewModel 클래스 밖):
       ```kotlin
       /** 회의록 목록 UI에 표시할 모델. Minutes + 출처 전사명 포함. */
       data class MinutesUiModel(
           val minutes: Minutes,
           val meetingTitle: String?  // null이면 "삭제된 전사" 표시
       )
       ```

    2. `val minutes: StateFlow<List<Minutes>>` → `val minutesUiModels: StateFlow<List<MinutesUiModel>>`로 변경:
       ```kotlin
       @OptIn(FlowPreview::class)
       val minutesUiModels: StateFlow<List<MinutesUiModel>> = _searchQuery
           .debounce(300)
           .flatMapLatest { query ->
               minutesDataRepository.getAllMinutesWithMeetingTitle().map { list ->
                   val filtered = if (query.isBlank()) list
                   else list.filter { (minutes, _) ->
                       (minutes.minutesTitle ?: "").contains(query, ignoreCase = true)
                   }
                   filtered.map { (minutes, meetingTitle) ->
                       MinutesUiModel(minutes = minutes, meetingTitle = meetingTitle)
                   }
               }
           }
           .stateIn(
               scope = viewModelScope,
               started = SharingStarted.WhileSubscribed(5_000),
               initialValue = emptyList()
           )
       ```

    3. 기존 `val minutes` 필드가 MinutesScreen에서 사용되므로 이름 변경에 주의. MinutesScreen에서의 참조는 Plan 02에서 수정한다.

    **TranscriptsViewModel.kt 변경:**

    1. 생성자에 `MinutesDataRepository` 의존성 추가:
       ```kotlin
       @HiltViewModel
       class TranscriptsViewModel @Inject constructor(
           private val meetingRepository: MeetingRepository,
           private val minutesDataRepository: MinutesDataRepository,  // 추가
           private val userPreferencesRepository: UserPreferencesRepository,
           private val promptTemplateRepository: PromptTemplateRepository,
           @ApplicationContext private val context: Context
       ) : ViewModel() {
       ```
       Hilt 모듈에 MinutesDataRepository 바인딩이 이미 존재하므로 DI 모듈 수정 불필요.

    2. `import com.autominuting.domain.repository.MinutesDataRepository` 추가.

    3. 클래스 내부에 `minutesCountMap` StateFlow 추가:
       ```kotlin
       /** meetingId별 회의록 수 맵. UI에서 badge 표시에 사용한다. */
       val minutesCountMap: StateFlow<Map<Long, Int>> = minutesDataRepository
           .getMinutesCountPerMeeting()
           .stateIn(
               scope = viewModelScope,
               started = SharingStarted.WhileSubscribed(5_000),
               initialValue = emptyMap()
           )
       ```
  </action>
  <verify>
    <automated>cd d:/workspace/temp/Auto_Minuting && ./gradlew :app:kspDebugKotlin 2>&1 | tail -5</automated>
  </verify>
  <done>MinutesViewModel이 MinutesUiModel(meetingTitle 포함) 리스트를 제공하고, TranscriptsViewModel이 minutesCountMap을 제공한다. KSP 컴파일 성공.</done>
</task>

</tasks>

<verification>
- `./gradlew :app:kspDebugKotlin` 성공 (Room/Hilt 코드 생성 포함)
- MinutesDao에 LEFT JOIN 쿼리와 GROUP BY count 쿼리가 존재
- MinutesDataRepository 인터페이스에 새 메서드 2개 존재
- MinutesViewModel.minutesUiModels가 MinutesUiModel 리스트 반환
- TranscriptsViewModel.minutesCountMap이 Map<Long, Int> 반환
</verification>

<success_criteria>
KSP 컴파일이 통과하고, ViewModel이 UI 레이어에 meetingTitle과 minutesCountMap을 제공할 준비가 된 상태
</success_criteria>

<output>
After completion, create `.planning/phases/38-independent-ui/38-01-SUMMARY.md`
</output>
