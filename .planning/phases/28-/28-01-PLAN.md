---
phase: 28-settings-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt
  - app/src/main/java/com/autominuting/presentation/dashboard/DashboardScreen.kt
  - app/src/main/java/com/autominuting/presentation/dashboard/DashboardViewModel.kt
autonomous: true
requirements: [UX-02]

must_haves:
  truths:
    - "설정 화면에 섹션 헤더로 논리적 그룹이 구분되어 표시된다"
    - "대시보드에서 테스트 도구(더미 삽입, Gemini 테스트) UI가 완전히 제거된다"
    - "DashboardViewModel에서 테스트 전용 메서드와 상태가 완전히 제거된다"
  artifacts:
    - path: "app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt"
      provides: "섹션별 그룹화된 설정 UI"
      contains: "SettingsSection"
    - path: "app/src/main/java/com/autominuting/presentation/dashboard/DashboardScreen.kt"
      provides: "테스트 도구 제거된 대시보드"
    - path: "app/src/main/java/com/autominuting/presentation/dashboard/DashboardViewModel.kt"
      provides: "테스트 코드 제거된 ViewModel"
  key_links:
    - from: "SettingsScreen.kt"
      to: "SettingsViewModel"
      via: "collectAsStateWithLifecycle"
      pattern: "viewModel\\."
---

<objective>
설정 화면의 메뉴 항목을 논리적 섹션으로 그룹화하고, 대시보드의 테스트 도구(더미 데이터 삽입, Gemini API 테스트) 코드를 완전히 제거하여 프로덕션 품질의 깔끔한 앱을 만든다.

Purpose: v3.1 마지막 정리 작업으로 앱의 완성도를 높인다
Output: 섹션 그룹화된 설정 화면 + 테스트 코드가 제거된 대시보드
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

@app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt
@app/src/main/java/com/autominuting/presentation/dashboard/DashboardScreen.kt
@app/src/main/java/com/autominuting/presentation/dashboard/DashboardViewModel.kt

<interfaces>
<!-- SettingsScreen.kt 현재 구조: 플랫 레이아웃, 섹션 구분 없음 -->
<!-- 현재 항목 순서: 프롬프트 템플릿 버튼 → 회의록 형식 → 자동화 모드 → STT 엔진 → Gemini 인증 -->
<!-- HorizontalDivider로 일부 구분은 있으나, 섹션 헤더가 없음 -->

<!-- DashboardScreen.kt 테스트 도구 (lines 261-319): Card 안에 "더미 회의록 3건 삽입" + "Gemini API 테스트" -->
<!-- DashboardViewModel.kt 테스트 코드: testStatus, isTestingGemini, insertTestData(), testGeminiApi() -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 설정 화면 섹션 그룹화</name>
  <files>app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt</files>
  <read_first>
    - app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt
  </read_first>
  <action>
SettingsScreen.kt의 Column 내부 항목들을 논리적 섹션으로 재구성한다.

1. 재사용 가능한 `SettingsSection` composable 헬퍼를 private으로 생성:
   - `title: String` 파라미터로 섹션 헤더 표시
   - `MaterialTheme.typography.titleSmall` + `colorScheme.primary` 색상으로 헤더 텍스트
   - 하단에 `HorizontalDivider` 포함
   - `content: @Composable ColumnScope.() -> Unit` 슬롯

2. 기존 항목을 3개 섹션으로 그룹화:
   - **"회의록 설정"**: 프롬프트 템플릿 관리 버튼 + 회의록 형식 드롭다운
   - **"전사 설정"**: 자동화 모드 스위치 + STT 엔진 드롭다운 + Whisper 모델 관리
   - **"인증"**: Gemini 인증 모드 (API 키/OAuth) 전체

3. 각 섹션 사이 간격: `Spacer(modifier = Modifier.height(24.dp))`
4. 기존 로직(ViewModel 호출, state 수집 등)은 변경하지 않는다 — UI 구조만 변경
  </action>
  <verify>
    <automated>cd D:/workspace/temp/Auto_Minuting && grep -c "SettingsSection" app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt</automated>
  </verify>
  <acceptance_criteria>
    - SettingsScreen.kt에 "SettingsSection" composable이 존재한다
    - "회의록 설정", "전사 설정", "인증" 텍스트가 섹션 헤더로 존재한다
    - 기존 기능(프롬프트 템플릿, 회의록 형식, 자동화 모드, STT 엔진, Gemini 인증)이 모두 유지된다
  </acceptance_criteria>
  <done>설정 화면이 3개 논리적 섹션으로 그룹화되어 표시된다</done>
</task>

<task type="auto">
  <name>Task 2: 대시보드 테스트 도구 코드 제거</name>
  <files>
    app/src/main/java/com/autominuting/presentation/dashboard/DashboardScreen.kt
    app/src/main/java/com/autominuting/presentation/dashboard/DashboardViewModel.kt
  </files>
  <read_first>
    - app/src/main/java/com/autominuting/presentation/dashboard/DashboardScreen.kt
    - app/src/main/java/com/autominuting/presentation/dashboard/DashboardViewModel.kt
  </read_first>
  <action>
DashboardScreen.kt와 DashboardViewModel.kt에서 테스트 전용 코드를 완전히 제거한다.

**DashboardScreen.kt:**
1. `testStatus` 및 `isTestingGemini` state 수집 라인 제거 (line 56-57 근처)
2. "테스트 도구" Card 전체 제거 (line 261-319 근처: Card부터 닫는 중괄호까지)
3. 사용하지 않는 import 정리 (제거 후 미참조 import 삭제)

**DashboardViewModel.kt:**
1. `_testStatus`, `testStatus` StateFlow 제거 (line 66-67)
2. `_isTestingGemini`, `isTestingGemini` StateFlow 제거 (line 69-71)
3. `insertTestData()` 메서드 전체 제거 (line 74-124)
4. `testGeminiApi()` 메서드 전체 제거 (line 341-405)
5. 제거 후 미참조 import 정리: `MinutesRepositoryImpl`, `MinutesFormat`, `MinutesRepository`, `Instant` (다른 곳에서 사용하지 않는 경우만)
6. `minutesRepository` 생성자 파라미터가 다른 곳에서 사용되지 않으면 제거
7. MinutesRepository 관련 import도 제거

주의: `activePipeline`, `downloadState`, `downloadFromUrl()`, `clearDownloadError()`, `PlaudAudioExtractorWebView` 등 URL 다운로드 관련 코드는 반드시 유지한다.
  </action>
  <verify>
    <automated>cd D:/workspace/temp/Auto_Minuting && grep -c "insertTestData\|testGeminiApi\|isTestingGemini\|테스트 도구" app/src/main/java/com/autominuting/presentation/dashboard/DashboardScreen.kt app/src/main/java/com/autominuting/presentation/dashboard/DashboardViewModel.kt</automated>
  </verify>
  <acceptance_criteria>
    - DashboardScreen.kt에 "테스트 도구", "insertTestData", "testGeminiApi" 문자열이 없다
    - DashboardViewModel.kt에 "insertTestData", "testGeminiApi", "isTestingGemini", "testStatus" 가 없다
    - URL 다운로드 기능(downloadFromUrl, downloadState, PlaudAudioExtractorWebView)은 정상 유지된다
    - activePipeline 배너 기능이 정상 유지된다
  </acceptance_criteria>
  <done>대시보드에서 테스트 도구 UI와 관련 ViewModel 코드가 완전히 제거된다</done>
</task>

</tasks>

<verification>
1. `grep -r "insertTestData\|testGeminiApi\|테스트 도구" app/src/main/java/` 결과 0건
2. `grep -c "SettingsSection" app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt` 결과 3 이상
3. `grep "회의록 설정\|전사 설정\|인증" app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt` 결과 3건
4. 빌드 성공: `./gradlew assembleDebug` (선택적)
</verification>

<success_criteria>
- 설정 화면이 "회의록 설정", "전사 설정", "인증" 3개 섹션으로 논리적 그룹화
- 대시보드에서 테스트 도구(더미 데이터 삽입, Gemini API 테스트) 완전 제거
- 기존 기능(URL 다운로드, 파이프라인 배너, 설정 항목)은 모두 정상 동작 유지
</success_criteria>

<output>
After completion, create `.planning/phases/28-/28-01-SUMMARY.md`
</output>
