---
phase: 26-minutes-title-actions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/com/autominuting/data/local/AppDatabase.kt
  - app/src/main/java/com/autominuting/data/local/entity/MeetingEntity.kt
  - app/src/main/java/com/autominuting/domain/model/Meeting.kt
  - app/src/main/java/com/autominuting/data/local/dao/MeetingDao.kt
  - app/src/main/java/com/autominuting/domain/repository/MeetingRepository.kt
  - app/src/main/java/com/autominuting/data/repository/MeetingRepositoryImpl.kt
  - app/src/main/java/com/autominuting/worker/MinutesGenerationWorker.kt
autonomous: true
requirements: [NAME-03]

must_haves:
  truths:
    - "Meeting 엔티티에 minutesTitle 필드가 존재한다"
    - "DB 마이그레이션 v3->v4가 정의되어 앱 업데이트 시 데이터 유실이 없다"
    - "회의록 생성 완료 시 Gemini 응답 첫 줄이 minutesTitle로 자동 저장된다"
  artifacts:
    - path: "app/src/main/java/com/autominuting/data/local/AppDatabase.kt"
      provides: "MIGRATION_3_4 정의, version=4"
      contains: "MIGRATION_3_4"
    - path: "app/src/main/java/com/autominuting/data/local/entity/MeetingEntity.kt"
      provides: "minutesTitle 컬럼"
      contains: "minutesTitle"
    - path: "app/src/main/java/com/autominuting/domain/model/Meeting.kt"
      provides: "minutesTitle 프로퍼티"
      contains: "minutesTitle"
    - path: "app/src/main/java/com/autominuting/worker/MinutesGenerationWorker.kt"
      provides: "Gemini 응답에서 제목 추출 후 DB 저장"
      contains: "minutesTitle"
  key_links:
    - from: "MinutesGenerationWorker"
      to: "MeetingDao.updateMinutes"
      via: "minutesTitle 파라미터 추가"
      pattern: "updateMinutes.*minutesTitle"
---

<objective>
회의록 제목을 위한 minutesTitle 데이터 레이어를 구축하고, MinutesGenerationWorker에서 Gemini 응답의 첫 줄을 자동 제목으로 추출하여 저장한다.

Purpose: 회의록 카드에 전사 제목과 별도의 내용 기반 자동 제목을 표시하기 위한 데이터 기반 마련
Output: minutesTitle 필드가 추가된 DB 스키마 + Worker에서 자동 제목 추출/저장 로직
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

@app/src/main/java/com/autominuting/data/local/AppDatabase.kt
@app/src/main/java/com/autominuting/data/local/entity/MeetingEntity.kt
@app/src/main/java/com/autominuting/domain/model/Meeting.kt
@app/src/main/java/com/autominuting/data/local/dao/MeetingDao.kt
@app/src/main/java/com/autominuting/worker/MinutesGenerationWorker.kt
@app/src/main/java/com/autominuting/domain/repository/MeetingRepository.kt
@app/src/main/java/com/autominuting/data/repository/MeetingRepositoryImpl.kt

<interfaces>
<!-- 현재 MeetingDao.updateMinutes 시그니처 -->
From MeetingDao.kt:
```kotlin
@Query("UPDATE meetings SET minutesPath = :minutesPath, pipelineStatus = :status, updatedAt = :updatedAt WHERE id = :id")
suspend fun updateMinutes(id: Long, minutesPath: String, status: String, updatedAt: Long)
```

<!-- 현재 MeetingEntity 구조 -->
From MeetingEntity.kt:
```kotlin
data class MeetingEntity(
    @PrimaryKey(autoGenerate = true) val id: Long = 0,
    val title: String,
    val recordedAt: Long,
    val audioFilePath: String,
    val transcriptPath: String? = null,
    val minutesPath: String? = null,
    val pipelineStatus: String,
    val errorMessage: String? = null,
    val createdAt: Long,
    val updatedAt: Long,
    val source: String = "PLAUD_BLE"
)
```

<!-- DB 마이그레이션 패턴 -->
From AppDatabase.kt:
```kotlin
val MIGRATION_1_2 = object : Migration(1, 2) {
    override fun migrate(db: SupportSQLiteDatabase) {
        db.execSQL("ALTER TABLE meetings ADD COLUMN source TEXT NOT NULL DEFAULT 'PLAUD_BLE'")
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: minutesTitle 컬럼 추가 (DB 마이그레이션 v3->v4 + 모델 업데이트)</name>
  <files>
    app/src/main/java/com/autominuting/data/local/AppDatabase.kt
    app/src/main/java/com/autominuting/data/local/entity/MeetingEntity.kt
    app/src/main/java/com/autominuting/domain/model/Meeting.kt
    app/src/main/java/com/autominuting/data/local/dao/MeetingDao.kt
    app/src/main/java/com/autominuting/domain/repository/MeetingRepository.kt
    app/src/main/java/com/autominuting/data/repository/MeetingRepositoryImpl.kt
  </files>
  <read_first>
    app/src/main/java/com/autominuting/data/local/AppDatabase.kt
    app/src/main/java/com/autominuting/data/local/entity/MeetingEntity.kt
    app/src/main/java/com/autominuting/domain/model/Meeting.kt
    app/src/main/java/com/autominuting/data/local/dao/MeetingDao.kt
    app/src/main/java/com/autominuting/domain/repository/MeetingRepository.kt
    app/src/main/java/com/autominuting/data/repository/MeetingRepositoryImpl.kt
    app/src/main/java/com/autominuting/di/DatabaseModule.kt
  </read_first>
  <action>
1. MeetingEntity에 `val minutesTitle: String? = null` 필드 추가. toDomain()과 fromDomain()에 minutesTitle 매핑 추가.

2. Meeting 도메인 모델에 `val minutesTitle: String? = null` 프로퍼티 추가.

3. MeetingDao.updateMinutes 쿼리에 minutesTitle 파라미터 추가:
   ```kotlin
   @Query("UPDATE meetings SET minutesPath = :minutesPath, minutesTitle = :minutesTitle, pipelineStatus = :status, updatedAt = :updatedAt WHERE id = :id")
   suspend fun updateMinutes(id: Long, minutesPath: String, minutesTitle: String?, status: String, updatedAt: Long)
   ```
   또한 minutesTitle만 업데이트하는 쿼리 추가:
   ```kotlin
   @Query("UPDATE meetings SET minutesTitle = :minutesTitle, updatedAt = :updatedAt WHERE id = :id")
   suspend fun updateMinutesTitle(id: Long, minutesTitle: String, updatedAt: Long)
   ```

4. MeetingRepository 인터페이스에 `suspend fun updateMinutesTitle(id: Long, title: String)` 추가.

5. MeetingRepositoryImpl에 updateMinutesTitle 구현:
   ```kotlin
   override suspend fun updateMinutesTitle(id: Long, title: String) {
       meetingDao.updateMinutesTitle(id, title, System.currentTimeMillis())
   }
   ```

6. AppDatabase: version을 3에서 4로 변경. MIGRATION_3_4 추가:
   ```kotlin
   val MIGRATION_3_4 = object : Migration(3, 4) {
       override fun migrate(db: SupportSQLiteDatabase) {
           db.execSQL("ALTER TABLE meetings ADD COLUMN minutesTitle TEXT DEFAULT NULL")
       }
   }
   ```
   DatabaseModule.kt (Hilt 모듈)에서 .addMigrations()에 MIGRATION_3_4 추가.

7. MeetingDao.clearMinutesPath 쿼리에도 minutesTitle = NULL 추가 (회의록 삭제 시 제목도 초기화):
   ```kotlin
   @Query("UPDATE meetings SET minutesPath = NULL, minutesTitle = NULL, pipelineStatus = 'TRANSCRIBED', updatedAt = :updatedAt WHERE id = :id")
   suspend fun clearMinutesPath(id: Long, updatedAt: Long)
   ```
  </action>
  <verify>
    <automated>cd D:/workspace/temp/Auto_Minuting && grep -n "minutesTitle" app/src/main/java/com/autominuting/data/local/entity/MeetingEntity.kt app/src/main/java/com/autominuting/domain/model/Meeting.kt app/src/main/java/com/autominuting/data/local/dao/MeetingDao.kt app/src/main/java/com/autominuting/data/local/AppDatabase.kt</automated>
  </verify>
  <acceptance_criteria>
    - MeetingEntity.kt에 "minutesTitle" 필드 존재
    - Meeting.kt에 "minutesTitle" 프로퍼티 존재
    - MeetingDao.kt에 "updateMinutesTitle" 쿼리 존재
    - AppDatabase.kt에 "MIGRATION_3_4" 존재하고 version = 4
    - MeetingDao.updateMinutes에 "minutesTitle" 파라미터 포함
    - clearMinutesPath에 "minutesTitle = NULL" 포함
  </acceptance_criteria>
  <done>minutesTitle 필드가 전체 데이터 레이어(Entity, Domain, DAO, Repository, Migration)에 일관되게 추가됨</done>
</task>

<task type="auto">
  <name>Task 2: MinutesGenerationWorker에서 Gemini 응답 첫 줄 제목 추출</name>
  <files>
    app/src/main/java/com/autominuting/worker/MinutesGenerationWorker.kt
  </files>
  <read_first>
    app/src/main/java/com/autominuting/worker/MinutesGenerationWorker.kt
  </read_first>
  <action>
MinutesGenerationWorker.doWork()의 성공 분기에서 meetingDao.updateMinutes 호출 직전에 제목 추출 로직 추가:

1. Gemini 응답 텍스트(minutesText)에서 첫 줄을 추출하여 제목으로 사용:
   ```kotlin
   // Gemini 응답의 첫 줄에서 제목 추출 (# 마크다운 헤더 제거)
   val minutesTitle = minutesText.lineSequence()
       .map { it.trim() }
       .firstOrNull { it.isNotBlank() }
       ?.removePrefix("#")
       ?.trim()
       ?.take(100) // 제목 최대 100자 제한
   ```

2. meetingDao.updateMinutes 호출에 minutesTitle 파라미터 추가:
   ```kotlin
   meetingDao.updateMinutes(
       id = meetingId,
       minutesPath = minutesPath,
       minutesTitle = minutesTitle,
       status = PipelineStatus.COMPLETED.name,
       updatedAt = completedAt
   )
   ```

3. 로그에 추출된 제목 출력:
   ```kotlin
   Log.d(TAG, "회의록 제목 추출: $minutesTitle")
   ```
  </action>
  <verify>
    <automated>cd D:/workspace/temp/Auto_Minuting && grep -n "minutesTitle" app/src/main/java/com/autominuting/worker/MinutesGenerationWorker.kt</automated>
  </verify>
  <acceptance_criteria>
    - MinutesGenerationWorker.kt에 "minutesTitle" 변수 선언 존재
    - "lineSequence" 또는 "lines()" 사용하여 첫 줄 추출
    - "removePrefix" 로 마크다운 헤더 제거
    - updateMinutes 호출에 minutesTitle 파라미터 포함
  </acceptance_criteria>
  <done>회의록 생성 완료 시 Gemini 응답의 첫 줄이 minutesTitle로 DB에 저장됨</done>
</task>

</tasks>

<verification>
- MeetingEntity, Meeting, MeetingDao, AppDatabase 모두에 minutesTitle 관련 코드가 존재
- MIGRATION_3_4가 정의되고 DatabaseModule에 등록됨
- MinutesGenerationWorker에서 제목 추출 + DB 저장 로직 존재
</verification>

<success_criteria>
- minutesTitle 필드가 데이터 레이어 전체에 일관되게 추가됨
- DB 마이그레이션 v3->v4가 정의됨
- Worker에서 Gemini 응답 첫 줄 제목 추출 로직이 동작함
</success_criteria>

<output>
After completion, create `.planning/phases/26-/26-01-SUMMARY.md`
</output>
