---
phase: 26-minutes-title-actions
plan: 02
type: execute
wave: 2
depends_on: [26-01]
files_modified:
  - app/src/main/java/com/autominuting/presentation/minutes/MinutesViewModel.kt
  - app/src/main/java/com/autominuting/presentation/minutes/MinutesScreen.kt
autonomous: true
requirements: [NAME-04, UX-01]

must_haves:
  truths:
    - "회의록 카드에 minutesTitle이 있으면 minutesTitle이, 없으면 title이 표시된다"
    - "사용자가 회의록 카드의 이름을 탭하여 편집 다이얼로그로 변경할 수 있다"
    - "회의록 카드의 MoreVert 메뉴에서 삭제와 공유 액션을 사용할 수 있다"
    - "공유 시 회의록 텍스트가 ACTION_SEND Intent로 외부 앱에 전달된다"
  artifacts:
    - path: "app/src/main/java/com/autominuting/presentation/minutes/MinutesViewModel.kt"
      provides: "updateMinutesTitle, shareMinutes 함수"
      contains: "updateMinutesTitle"
    - path: "app/src/main/java/com/autominuting/presentation/minutes/MinutesScreen.kt"
      provides: "MoreVert 드롭다운 메뉴 + 이름 편집 다이얼로그"
      contains: "DropdownMenu"
  key_links:
    - from: "MinutesScreen.kt"
      to: "MinutesViewModel.updateMinutesTitle"
      via: "이름 편집 다이얼로그 확인 버튼"
      pattern: "updateMinutesTitle"
    - from: "MinutesScreen.kt"
      to: "MinutesViewModel.shareMinutes"
      via: "DropdownMenu 공유 항목 클릭"
      pattern: "shareMinutes"
---

<objective>
MinutesScreen의 회의록 카드에 MoreVert 액션 메뉴(삭제/공유)와 이름 편집 다이얼로그를 추가하고, minutesTitle을 카드 제목으로 표시한다.

Purpose: 회의록 목록에서 삭제/공유/이름편집 UX를 제공하여 사용성 향상
Output: 완성된 MinutesScreen UI + MinutesViewModel 함수
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-/26-01-SUMMARY.md

@app/src/main/java/com/autominuting/presentation/minutes/MinutesScreen.kt
@app/src/main/java/com/autominuting/presentation/minutes/MinutesViewModel.kt
@app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt
@app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsViewModel.kt

<interfaces>
<!-- Phase 25에서 확립된 이름 편집 다이얼로그 패턴 (TranscriptsScreen.kt) -->
```kotlin
// meetingToRename state로 편집 대상 관리
var meetingToRename by remember { mutableStateOf<Meeting?>(null) }

// AlertDialog + OutlinedTextField
meetingToRename?.let { meeting ->
    var editedName by remember(meeting.id) { mutableStateOf(meeting.title) }
    AlertDialog(
        onDismissRequest = { meetingToRename = null },
        title = { Text("이름 편집") },
        text = {
            OutlinedTextField(
                value = editedName,
                onValueChange = { editedName = it },
                label = { Text("이름") },
                singleLine = true,
                modifier = Modifier.fillMaxWidth()
            )
        },
        confirmButton = {
            TextButton(onClick = {
                val trimmed = editedName.trim()
                if (trimmed.isNotBlank()) {
                    viewModel.updateTitle(meeting.id, trimmed)
                }
                meetingToRename = null
            }) { Text("확인") }
        },
        dismissButton = {
            TextButton(onClick = { meetingToRename = null }) { Text("취소") }
        }
    )
}
```

<!-- Phase 20에서 확립된 MoreVert + DropdownMenu 패턴 (TranscriptsScreen.kt) -->
```kotlin
var showMenu by remember { mutableStateOf(false) }
Box {
    IconButton(onClick = { showMenu = true }) {
        Icon(imageVector = Icons.Default.MoreVert, contentDescription = "더보기")
    }
    DropdownMenu(expanded = showMenu, onDismissRequest = { showMenu = false }) {
        DropdownMenuItem(
            text = { Text("공유") },
            onClick = { showMenu = false; onShare(meeting.id) },
            leadingIcon = { Icon(Icons.Default.Share, contentDescription = null) }
        )
        DropdownMenuItem(
            text = { Text("삭제", color = MaterialTheme.colorScheme.error) },
            onClick = { showMenu = false; onDeleteRequest(meeting.id) },
            leadingIcon = { Icon(Icons.Default.Delete, contentDescription = null, tint = MaterialTheme.colorScheme.error) }
        )
    }
}
```

<!-- Plan 01에서 추가된 Meeting.minutesTitle, MeetingRepository.updateMinutesTitle -->
```kotlin
// Meeting 도메인 모델
data class Meeting(
    ...
    val minutesTitle: String? = null,
    ...
)

// MeetingRepository 인터페이스
suspend fun updateMinutesTitle(id: Long, title: String)
```

<!-- 현재 MinutesViewModel 함수 목록 -->
```kotlin
class MinutesViewModel @Inject constructor(private val meetingRepository: MeetingRepository) {
    val searchQuery: StateFlow<String>
    val meetings: StateFlow<List<Meeting>>
    fun onSearchQueryChange(query: String)
    fun deleteMeeting(id: Long)           // deleteMinutesOnly 호출
    fun deleteSelectedMinutes(ids: Set<Long>)
    fun getMinutesContent(minutesPath: String): String?
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MinutesViewModel에 updateMinutesTitle + shareMinutes 함수 추가</name>
  <files>
    app/src/main/java/com/autominuting/presentation/minutes/MinutesViewModel.kt
  </files>
  <read_first>
    app/src/main/java/com/autominuting/presentation/minutes/MinutesViewModel.kt
    app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsViewModel.kt
  </read_first>
  <action>
MinutesViewModel에 다음 함수 2개를 추가한다. TranscriptsViewModel의 shareTranscript/updateTitle 패턴을 참고하되 회의록용으로 변경한다.

1. **updateMinutesTitle 함수**: 회의록 제목(minutesTitle)을 변경한다.
   ```kotlin
   /** 회의록 제목을 변경한다. */
   fun updateMinutesTitle(meetingId: Long, newTitle: String) {
       viewModelScope.launch {
           meetingRepository.updateMinutesTitle(meetingId, newTitle)
       }
   }
   ```

2. **shareMinutes 함수**: 회의록 텍스트를 외부 앱으로 공유한다. TranscriptsViewModel.shareTranscript 패턴과 동일.
   ```kotlin
   /** 회의록 텍스트를 외부 앱으로 공유한다. */
   fun shareMinutes(meetingId: Long, activityContext: Context) {
       viewModelScope.launch {
           val meeting = meetingRepository.getMeetingById(meetingId).first()
           if (meeting?.minutesPath == null) return@launch

           val minutesText = try {
               File(meeting.minutesPath).readText()
           } catch (e: Exception) {
               return@launch
           }
           if (minutesText.isBlank()) return@launch

           val sendIntent = Intent().apply {
               action = Intent.ACTION_SEND
               putExtra(Intent.EXTRA_SUBJECT, meeting.minutesTitle ?: meeting.title)
               putExtra(Intent.EXTRA_TEXT, minutesText)
               type = "text/plain"
           }
           activityContext.startActivity(Intent.createChooser(sendIntent, null))
       }
   }
   ```

3. 필요한 import 추가: `android.content.Context`, `android.content.Intent`, `kotlinx.coroutines.flow.first`, `java.io.File` (이미 있는 것은 생략).
  </action>
  <verify>
    <automated>cd D:/workspace/temp/Auto_Minuting && grep -n "updateMinutesTitle\|shareMinutes\|ACTION_SEND" app/src/main/java/com/autominuting/presentation/minutes/MinutesViewModel.kt</automated>
  </verify>
  <acceptance_criteria>
    - MinutesViewModel.kt에 "fun updateMinutesTitle" 함수 존재
    - MinutesViewModel.kt에 "fun shareMinutes" 함수 존재
    - shareMinutes에 "ACTION_SEND" Intent 생성 코드 존재
    - shareMinutes에 "minutesTitle ?: meeting.title" 패턴으로 제목 설정
  </acceptance_criteria>
  <done>MinutesViewModel에 회의록 제목 편집과 공유 기능이 추가됨</done>
</task>

<task type="auto">
  <name>Task 2: MinutesScreen 카드에 MoreVert 메뉴 + minutesTitle 표시 + 이름 편집</name>
  <files>
    app/src/main/java/com/autominuting/presentation/minutes/MinutesScreen.kt
  </files>
  <read_first>
    app/src/main/java/com/autominuting/presentation/minutes/MinutesScreen.kt
    app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt
  </read_first>
  <action>
MinutesScreen.kt의 MinutesMeetingCard를 리팩터링한다. TranscriptsScreen의 TranscriptMeetingCard 패턴(MoreVert + DropdownMenu)을 참고한다.

1. **MinutesScreen composable에 state 추가**:
   - `var meetingToRename by remember { mutableStateOf<Meeting?>(null) }` (이름 편집 대상)
   - `val context = LocalContext.current` (공유 Intent용)

2. **MinutesMeetingCard 파라미터 변경**:
   - `onDeleteRequest` 유지 (기존)
   - `onRenameRequest: (Meeting) -> Unit` 추가
   - `onShare: (Long) -> Unit` 추가
   - 기존 단건 삭제 IconButton(Icons.Default.Delete)을 제거하고 MoreVert + DropdownMenu로 교체

3. **MinutesMeetingCard 내부 리팩터링**:
   - 카드 제목 텍스트: `meeting.minutesTitle ?: meeting.title`로 변경. 제목 Text에 `.clickable { onRenameRequest(meeting) }` 적용 (Phase 25 패턴).
   - MoreVert + DropdownMenu를 Row 우측에 배치 (비선택 모드일 때):
     ```
     - 공유 (minutesPath가 있을 때만)
     - 삭제 (항상, error 색상)
     ```
   - 선택 모드에서는 기존 체크박스 유지, MoreVert 숨김

4. **이름 편집 다이얼로그 추가** (MinutesScreen composable 하단에):
   Phase 25 패턴과 동일하되 label을 "회의록 이름"으로, 초기값을 `meeting.minutesTitle ?: meeting.title`로 설정.
   확인 시 `viewModel.updateMinutesTitle(meeting.id, trimmed)` 호출.

5. **MinutesScreen에서 MinutesMeetingCard 호출부 업데이트**:
   - `onRenameRequest = { m -> meetingToRename = m }` 추가
   - `onShare = { id -> viewModel.shareMinutes(id, context) }` 추가

6. **필요한 import 추가**:
   - `Icons.Default.MoreVert`, `Icons.Default.Share`
   - `DropdownMenu`, `DropdownMenuItem`
   - `LocalContext`
   - `clickable`
  </action>
  <verify>
    <automated>cd D:/workspace/temp/Auto_Minuting && grep -n "DropdownMenu\|MoreVert\|minutesTitle\|meetingToRename\|shareMinutes\|updateMinutesTitle" app/src/main/java/com/autominuting/presentation/minutes/MinutesScreen.kt</automated>
  </verify>
  <acceptance_criteria>
    - MinutesScreen.kt에 "DropdownMenu" 사용 코드 존재
    - MinutesScreen.kt에 "MoreVert" 아이콘 사용 존재
    - MinutesScreen.kt에 "minutesTitle ?: meeting.title" 또는 "minutesTitle" 표시 로직 존재
    - MinutesScreen.kt에 "meetingToRename" state 존재
    - MinutesScreen.kt에 "updateMinutesTitle" 호출 존재
    - MinutesScreen.kt에 "shareMinutes" 호출 존재
    - MinutesScreen.kt에 AlertDialog 기반 이름 편집 다이얼로그 존재
  </acceptance_criteria>
  <done>회의록 카드에 minutesTitle 표시, MoreVert 메뉴(삭제/공유), 이름 편집 다이얼로그가 모두 동작함</done>
</task>

</tasks>

<verification>
- MinutesViewModel에 updateMinutesTitle, shareMinutes 함수 존재
- MinutesScreen에 DropdownMenu + MoreVert 패턴 적용됨
- 카드 제목이 minutesTitle 우선 표시됨
- 이름 편집 다이얼로그가 Phase 25 패턴으로 구현됨
- 공유 시 ACTION_SEND Intent가 생성됨
</verification>

<success_criteria>
- 회의록 카드에 Gemini 자동 제목(minutesTitle)이 표시된다 (NAME-03 UI 완성)
- 회의록 이름 편집이 다이얼로그로 가능하다 (NAME-04)
- 회의록 삭제/공유 액션이 MoreVert 메뉴로 제공된다 (UX-01)
</success_criteria>

<output>
After completion, create `.planning/phases/26-/26-02-SUMMARY.md`
</output>
