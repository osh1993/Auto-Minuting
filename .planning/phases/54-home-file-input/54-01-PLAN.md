---
phase: 54-home-file-input
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/com/autominuting/presentation/dashboard/DashboardViewModel.kt
autonomous: true
requirements: [INPUT-02]
must_haves:
  truths:
    - "DashboardViewModel.processLocalFile(uri, title) 메서드가 존재한다"
    - "processLocalFile 호출 시 Content URI가 앱 내부 audio 디렉터리로 복사된다"
    - "복사 완료 후 Meeting(source=\"LOCAL_FILE\", pipelineStatus=AUDIO_RECEIVED)이 insert 된다"
    - "TranscriptionTriggerWorker가 audioFilePath + meetingId + automationMode와 함께 enqueue 된다"
    - "LocalFileState sealed interface (또는 DownloadState 재사용)가 Idle/Processing/Error 상태를 표현한다"
  artifacts:
    - path: "app/src/main/java/com/autominuting/presentation/dashboard/DashboardViewModel.kt"
      provides: "processLocalFile(uri, title) 메서드 + LocalFileState (또는 DownloadState 재사용) + localFileState StateFlow"
      contains: "fun processLocalFile"
  key_links:
    - from: "DashboardViewModel.processLocalFile"
      to: "meetingRepository.insertMeeting"
      via: "Meeting(source=\"LOCAL_FILE\") 생성 후 insert"
      pattern: "source = \"LOCAL_FILE\""
    - from: "DashboardViewModel.processLocalFile"
      to: "TranscriptionTriggerWorker"
      via: "OneTimeWorkRequestBuilder + WorkManager.enqueue"
      pattern: "OneTimeWorkRequestBuilder<TranscriptionTriggerWorker>"
    - from: "DashboardViewModel.processLocalFile"
      to: "ContentResolver.openInputStream"
      via: "URI → File.copyTo (ShareReceiverActivity 패턴)"
      pattern: "contentResolver.openInputStream"
---

<objective>
DashboardViewModel에 `processLocalFile(uri: Uri, title: String)` 메서드를 추가하여, SAF 피커로 선택한 로컬 오디오 파일을 앱 내부 저장소로 복사하고 기존 STT → 회의록 파이프라인(TranscriptionTriggerWorker)에 진입시킨다.

Purpose: INPUT-02 요구사항(선택 파일이 기존 STT → 회의록 파이프라인으로 처리됨)을 ViewModel 레이어에서 구현한다. UI 연결은 Plan 02에서 수행.
Output: processLocalFile() 메서드, 상태 StateFlow (localFileState 또는 downloadState 재사용), Meeting.source "LOCAL_FILE" 진입점.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/54-home-file-input/54-CONTEXT.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: DashboardViewModel.processLocalFile() 메서드 추가</name>
  <files>app/src/main/java/com/autominuting/presentation/dashboard/DashboardViewModel.kt</files>
  <read_first>
    - app/src/main/java/com/autominuting/presentation/dashboard/DashboardViewModel.kt (현재 상태 — downloadFromUrl/downloadDirectUrl 패턴 + DownloadState sealed interface 확인)
    - app/src/main/java/com/autominuting/presentation/share/ShareReceiverActivity.kt (processSharedAudio() 580~658줄 — ContentResolver 복사 + Meeting insert + Worker enqueue 패턴 복제 소스)
    - app/src/main/java/com/autominuting/domain/model/Meeting.kt (source 필드 — "LOCAL_FILE" 값 사용)
    - .planning/phases/54-home-file-input/54-CONTEXT.md (D-08 ~ D-12 결정사항)
  </read_first>
  <action>
DashboardViewModel.kt에 다음을 구현한다.

1. **상태 sealed interface 정의** (class 내부, 기존 DownloadState와 유사한 위치):
   ```kotlin
   sealed interface LocalFileState {
       data object Idle : LocalFileState
       data object Processing : LocalFileState
       data class Error(val message: String) : LocalFileState
   }
   ```
   (Claude 재량: DownloadState에 케이스를 추가 재사용해도 무방. 단, 기존 URL 다운로드 흐름과 상태가 혼동되지 않도록 별도 StateFlow 권장.)

2. **StateFlow 추가**:
   ```kotlin
   private val _localFileState = MutableStateFlow<LocalFileState>(LocalFileState.Idle)
   val localFileState: StateFlow<LocalFileState> = _localFileState.asStateFlow()
   ```

3. **processLocalFile(uri: Uri, title: String) 메서드 추가** — 아래 로직 정확히 구현:
   ```kotlin
   fun processLocalFile(uri: android.net.Uri, title: String) {
       if (_localFileState.value is LocalFileState.Processing) return
       viewModelScope.launch(Dispatchers.IO) {
           try {
               _localFileState.value = LocalFileState.Processing
               val now = System.currentTimeMillis()

               // 1. 확장자 결정 (MIME 우선, 폴백 m4a) — ShareReceiverActivity 패턴 복제
               val mimeType = context.contentResolver.getType(uri) ?: "audio/mp4"
               val extension = when {
                   mimeType.contains("m4a") || mimeType.contains("mp4") -> "m4a"
                   mimeType.contains("mp3") || mimeType.contains("mpeg") -> "mp3"
                   mimeType.contains("wav") -> "wav"
                   mimeType.contains("ogg") -> "ogg"
                   mimeType.contains("flac") -> "flac"
                   else -> "m4a"
               }

               // 2. 앱 내부 저장소로 복사
               val audioDir = File(context.filesDir, "audio")
               audioDir.mkdirs()
               val audioFile = File(audioDir, "local_${now}.$extension")
               context.contentResolver.openInputStream(uri)?.use { input ->
                   audioFile.outputStream().use { output ->
                       input.copyTo(output)
                   }
               } ?: run {
                   _localFileState.value = LocalFileState.Error("파일을 읽을 수 없습니다")
                   return@launch
               }
               Log.d(TAG, "로컬 파일 복사 완료: ${audioFile.absolutePath} (${audioFile.length()} bytes)")

               // 3. Meeting insert (source = "LOCAL_FILE")
               val instantNow = Instant.ofEpochMilli(now)
               val meeting = Meeting(
                   title = title,
                   recordedAt = instantNow,
                   audioFilePath = audioFile.absolutePath,
                   pipelineStatus = PipelineStatus.AUDIO_RECEIVED,
                   source = "LOCAL_FILE",
                   createdAt = instantNow,
                   updatedAt = instantNow
               )
               val meetingId = meetingRepository.insertMeeting(meeting)

               // 4. TranscriptionTriggerWorker enqueue — automationMode 전달 필수
               val currentAutomationMode = automationMode.value
               val workRequest = OneTimeWorkRequestBuilder<TranscriptionTriggerWorker>()
                   .setInputData(
                       workDataOf(
                           TranscriptionTriggerWorker.KEY_AUDIO_FILE_PATH to audioFile.absolutePath,
                           TranscriptionTriggerWorker.KEY_MEETING_ID to meetingId,
                           TranscriptionTriggerWorker.KEY_AUTOMATION_MODE to currentAutomationMode.name
                       )
                   )
                   .build()
               WorkManager.getInstance(context).enqueue(workRequest)
               observeTranscriptionProgress(workRequest.id)

               Log.d(TAG, "로컬 파일 파이프라인 진입: meetingId=$meetingId, source=LOCAL_FILE")
               _localFileState.value = LocalFileState.Idle
           } catch (e: Exception) {
               Log.e(TAG, "로컬 파일 처리 오류: ${e.message}", e)
               _localFileState.value = LocalFileState.Error("파일 처리 오류: ${e.message}")
           }
       }
   }

   fun clearLocalFileError() {
       _localFileState.value = LocalFileState.Idle
   }
   ```

4. **import 추가 필요 시** (기존 import 확인 후 누락분만): `android.net.Uri` — 이미 context 관련 import 존재.

결정 추적: D-08 (ViewModel 직접 구현), D-09 (source="LOCAL_FILE"), D-11 (LocalFileState 별도 분리 권장), D-12 (ContentResolver.openInputStream + File.copyTo 방식).
  </action>
  <verify>
    <automated>cd app && ..\gradlew.bat :app:compileDebugKotlin</automated>
  </verify>
  <acceptance_criteria>
    - DashboardViewModel.kt에 `fun processLocalFile(` 문자열 포함 (grep 확인)
    - DashboardViewModel.kt에 `source = "LOCAL_FILE"` 문자열 포함
    - DashboardViewModel.kt에 `OneTimeWorkRequestBuilder<TranscriptionTriggerWorker>` 문자열 포함
    - DashboardViewModel.kt에 `contentResolver.openInputStream` 호출 포함
    - DashboardViewModel.kt에 `LocalFileState` (또는 DownloadState에 LocalFile* 케이스) 선언 포함
    - DashboardViewModel.kt에 `val localFileState: StateFlow` (또는 기존 downloadState 재사용 시 관련 필드) 선언 포함
    - DashboardViewModel.kt에 `fun clearLocalFileError` 포함
    - `gradlew :app:compileDebugKotlin` 성공 (exit 0)
  </acceptance_criteria>
  <done>
    DashboardViewModel이 processLocalFile(uri, title) 호출 시 파일 복사 → Meeting(source="LOCAL_FILE") insert → TranscriptionTriggerWorker enqueue까지 수행하며, 상태는 localFileState StateFlow로 Idle/Processing/Error 3상태를 노출한다. 컴파일 에러 없음.
  </done>
</task>

</tasks>

<verification>
- `gradlew :app:compileDebugKotlin` 성공
- grep 검증: processLocalFile, "LOCAL_FILE", TranscriptionTriggerWorker enqueue 패턴 모두 DashboardViewModel.kt에 존재
- 기존 downloadFromUrl 흐름은 변경 없음 (DownloadState 재사용하더라도 기존 케이스 그대로 유지)
</verification>

<success_criteria>
INPUT-02 요구사항의 ViewModel 레이어 구현 완료:
- processLocalFile() 단일 진입점으로 URI → 파이프라인 진입 완전 자동화
- ShareReceiverActivity.processSharedAudio()와 동일한 파이프라인 세만틱 (automationMode 전달, observeTranscriptionProgress 호출)
- source 필드로 "LOCAL_FILE" 추적 가능
</success_criteria>

<output>
After completion, create `.planning/phases/54-home-file-input/54-01-SUMMARY.md`
</output>
