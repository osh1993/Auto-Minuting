---
phase: 54-home-file-input
plan: 02
type: execute
wave: 2
depends_on: ["54-01"]
files_modified:
  - app/src/main/java/com/autominuting/presentation/dashboard/DashboardScreen.kt
autonomous: false
requirements: [INPUT-01, INPUT-02]
must_haves:
  truths:
    - "홈 화면(DashboardScreen)의 URL 카드 안에 '파일 불러오기' OutlinedButton이 표시된다"
    - "버튼 탭 시 SAF GetContent 런처가 실행되고 audio/* MIME 필터가 적용된다"
    - "파일 선택 후 제목 입력 AlertDialog가 표시되며 기본값은 파일명(확장자 제외)이다"
    - "제목이 비어 있으면 '확인' 버튼이 비활성화된다"
    - "'확인' 탭 시 viewModel.processLocalFile(uri, title)이 호출된다"
    - "취소 시 아무 동작 없이 다이얼로그가 닫힌다"
    - "localFileState가 Processing이면 진행 UI, Error면 오류 메시지 + 다시 시도 버튼 표시"
    - "파이프라인 진입 후 기존 activePipeline 배너가 정상 동작한다 (기존 로직 변경 없음)"
  artifacts:
    - path: "app/src/main/java/com/autominuting/presentation/dashboard/DashboardScreen.kt"
      provides: "'파일 불러오기' 버튼 + rememberLauncherForActivityResult + 제목 입력 AlertDialog + localFileState UI 반영"
      contains: "파일 불러오기"
  key_links:
    - from: "OutlinedButton('파일 불러오기')"
      to: "rememberLauncherForActivityResult(GetContent())"
      via: "onClick { launcher.launch(\"audio/*\") }"
      pattern: "launcher\\.launch\\(\"audio/\\*\"\\)"
    - from: "GetContent() 결과 URI"
      to: "제목 입력 AlertDialog"
      via: "local state (pendingUri, titleInput)"
      pattern: "pendingUri"
    - from: "AlertDialog 확인 버튼"
      to: "viewModel.processLocalFile"
      via: "onClick { viewModel.processLocalFile(uri, title) }"
      pattern: "viewModel\\.processLocalFile"
---

<objective>
DashboardScreen의 "URL에서 음성 다운로드" 카드 내부에 '파일 불러오기' 버튼을 추가하고, SAF 파일 피커 런처 + 제목 입력 AlertDialog + localFileState 반영 UI를 구현한다. 사용자가 로컬 M4A/MP3 파일을 선택하면 제목 확인 후 Plan 01에서 구현된 `viewModel.processLocalFile()`을 호출하여 파이프라인에 진입시킨다.

Purpose: INPUT-01 (홈 화면 파일 선택 버튼) + INPUT-02 (기존 파이프라인 진입)의 UI 레이어 완성. 최종 사용자 접점.
Output: DashboardScreen의 UI 추가 — 버튼, SAF 런처, 제목 다이얼로그, 진행/에러 UI. Human verify 체크포인트로 실기기 확인.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/54-home-file-input/54-CONTEXT.md
@.planning/phases/54-home-file-input/54-01-SUMMARY.md

<interfaces>
<!-- Plan 01에서 추가되는 ViewModel API (Plan 02 executor는 코드베이스 탐색 없이 아래만 사용) -->

From DashboardViewModel.kt (Plan 01 결과):
```kotlin
sealed interface LocalFileState {
    data object Idle : LocalFileState
    data object Processing : LocalFileState
    data class Error(val message: String) : LocalFileState
}

val localFileState: StateFlow<LocalFileState>
fun processLocalFile(uri: android.net.Uri, title: String)
fun clearLocalFileError()
```

(Plan 01에서 DownloadState 재사용을 선택한 경우 Plan 02 executor는 DashboardViewModel.kt 실제 구현을 먼저 read_first로 확인한 뒤 해당 API 이름에 맞추어 연결할 것.)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: '파일 불러오기' 버튼 + SAF 런처 + 제목 입력 AlertDialog 추가</name>
  <files>app/src/main/java/com/autominuting/presentation/dashboard/DashboardScreen.kt</files>
  <read_first>
    - app/src/main/java/com/autominuting/presentation/dashboard/DashboardScreen.kt (현재 상태 — 특히 280~365줄 URL 다운로드 카드 구조, import 섹션, collectAsStateWithLifecycle 패턴)
    - app/src/main/java/com/autominuting/presentation/dashboard/DashboardViewModel.kt (Plan 01 완료 후 — processLocalFile/localFileState 실제 시그니처 확인)
    - app/src/main/java/com/autominuting/presentation/transcripts/ManualMinutesDialog.kt (AlertDialog + OutlinedTextField 패턴 참조)
    - .planning/phases/54-home-file-input/54-CONTEXT.md (D-01 ~ D-07 결정사항)
  </read_first>
  <action>
DashboardScreen.kt에 다음 변경을 적용한다.

**1) Import 추가** (파일 상단, 기존 import 위치에 알파벳 순):
```kotlin
import android.net.Uri
import androidx.activity.compose.rememberLauncherForActivityResult
import androidx.activity.result.contract.ActivityResultContracts
import androidx.compose.material3.AlertDialog
import androidx.compose.material.icons.filled.FolderOpen
import androidx.compose.material3.HorizontalDivider
```
(일부 import는 이미 존재할 수 있음 — 중복 추가 금지. Read로 확인 후 누락분만 추가.)

**2) DashboardScreen composable 내부 상단 state 선언부에 추가** (기존 `urlText` 선언 근처):
```kotlin
val localFileState by viewModel.localFileState.collectAsStateWithLifecycle()

// 제목 입력 다이얼로그 제어용 state
var pendingFileUri by remember { mutableStateOf<Uri?>(null) }
var pendingFileTitle by remember { mutableStateOf("") }

// SAF 파일 피커 런처 — audio/* MIME (M4A/MP3 모두 포함)
val filePickerLauncher = rememberLauncherForActivityResult(
    contract = ActivityResultContracts.GetContent()
) { uri: Uri? ->
    if (uri != null) {
        // 파일명에서 확장자 제외한 기본 제목 추출 (D-05)
        val displayName = uri.lastPathSegment?.substringAfterLast("/") ?: ""
        val defaultTitle = displayName.substringBeforeLast(".", displayName)
            .ifBlank { "로컬 파일 회의" }
        pendingFileUri = uri
        pendingFileTitle = defaultTitle
    }
}
```

**3) URL 카드의 Column 내부 (기존 when(downloadState) 블록 바로 아래, Card 닫히기 전)** 에 다음 UI 블록을 추가 — D-01/D-02:
```kotlin
Spacer(modifier = Modifier.height(12.dp))
HorizontalDivider()
Spacer(modifier = Modifier.height(12.dp))

Text(
    text = "로컬 파일 불러오기",
    style = MaterialTheme.typography.titleSmall
)
Text(
    text = "기기에 저장된 M4A/MP3 파일을 선택하여 전사합니다",
    style = MaterialTheme.typography.bodySmall,
    color = MaterialTheme.colorScheme.onSecondaryContainer
)
Spacer(modifier = Modifier.height(8.dp))

when (val lfs = localFileState) {
    is DashboardViewModel.LocalFileState.Idle -> {
        OutlinedButton(
            onClick = { filePickerLauncher.launch("audio/*") },
            modifier = Modifier.fillMaxWidth()
        ) {
            Icon(
                imageVector = Icons.Default.FolderOpen,
                contentDescription = null
            )
            Spacer(modifier = Modifier.width(8.dp))
            Text("파일 불러오기")
        }
    }
    is DashboardViewModel.LocalFileState.Processing -> {
        LinearProgressIndicator(modifier = Modifier.fillMaxWidth())
        Spacer(modifier = Modifier.height(4.dp))
        Text(
            text = "파일 처리 중...",
            style = MaterialTheme.typography.bodySmall
        )
    }
    is DashboardViewModel.LocalFileState.Error -> {
        Text(
            text = lfs.message,
            style = MaterialTheme.typography.bodySmall,
            color = MaterialTheme.colorScheme.error
        )
        Spacer(modifier = Modifier.height(4.dp))
        OutlinedButton(onClick = { viewModel.clearLocalFileError() }) {
            Text("다시 시도")
        }
    }
}
```

**4) 화면 최상단 레벨(Scaffold 바깥 또는 기존 다이얼로그들 근처)에 제목 입력 AlertDialog 추가** — D-04 ~ D-07:
```kotlin
// 로컬 파일 제목 입력 다이얼로그
pendingFileUri?.let { uri ->
    AlertDialog(
        onDismissRequest = {
            pendingFileUri = null
            pendingFileTitle = ""
        },
        title = { Text("회의 제목 입력") },
        text = {
            OutlinedTextField(
                value = pendingFileTitle,
                onValueChange = { pendingFileTitle = it },
                label = { Text("제목") },
                singleLine = true,
                modifier = Modifier.fillMaxWidth()
            )
        },
        confirmButton = {
            TextButton(
                onClick = {
                    viewModel.processLocalFile(uri, pendingFileTitle.trim())
                    pendingFileUri = null
                    pendingFileTitle = ""
                },
                enabled = pendingFileTitle.isNotBlank()
            ) {
                Text("확인")
            }
        },
        dismissButton = {
            TextButton(onClick = {
                pendingFileUri = null
                pendingFileTitle = ""
            }) {
                Text("취소")
            }
        }
    )
}
```

**5) 주의사항**:
- Plan 01이 DownloadState 재사용 방식을 선택한 경우(D-11 Claude 재량), `DashboardViewModel.LocalFileState.*` 참조를 실제 sealed 타입명으로 교체한다. Plan 01의 DashboardViewModel.kt를 read_first로 먼저 확인하여 실제 API에 맞출 것.
- 기존 URL 다운로드 카드의 기존 UI/로직은 변경 금지. 파일 불러오기 섹션은 HorizontalDivider 아래에만 추가.
- `Icons.Default.FolderOpen` 아이콘 사용 (D-03).

결정 추적: D-01(URL 카드 내부 통합), D-02(HorizontalDivider 구분), D-03(FolderOpen), D-04(AlertDialog), D-05(파일명 확장자 제외 기본값), D-06(취소 시 no-op), D-07(빈 제목 시 확인 비활성화), D-10(GetContent + audio/*).
  </action>
  <verify>
    <automated>cd app && ..\gradlew.bat :app:compileDebugKotlin :app:assembleDebug</automated>
  </verify>
  <acceptance_criteria>
    - DashboardScreen.kt에 문자열 `"파일 불러오기"` 포함
    - DashboardScreen.kt에 `rememberLauncherForActivityResult` 호출 포함
    - DashboardScreen.kt에 `ActivityResultContracts.GetContent()` 포함
    - DashboardScreen.kt에 `launcher.launch("audio/*")` 또는 `filePickerLauncher.launch("audio/*")` 포함
    - DashboardScreen.kt에 `viewModel.processLocalFile(` 호출 포함
    - DashboardScreen.kt에 `AlertDialog(` 신규 추가 (파일 제목 입력용)
    - DashboardScreen.kt에 `pendingFileUri` state 선언 포함
    - DashboardScreen.kt에 `enabled = pendingFileTitle.isNotBlank()` (또는 동등한 빈 제목 비활성 로직) 포함
    - DashboardScreen.kt에 `Icons.Default.FolderOpen` 또는 `Icons.Default.AttachFile` 포함
    - DashboardScreen.kt에 `HorizontalDivider` 포함 (파일 섹션 구분용)
    - `gradlew :app:assembleDebug` 성공 (exit 0)
  </acceptance_criteria>
  <done>
    DashboardScreen이 컴파일되며 URL 카드 내부에 "파일 불러오기" 버튼이 렌더링될 구조가 완성된다. 버튼 탭 → SAF audio/* 피커 → 제목 입력 AlertDialog → viewModel.processLocalFile 호출까지 코드 경로가 완전히 연결된다.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 실기기 End-to-End 검증</name>
  <files>app/src/main/java/com/autominuting/presentation/dashboard/DashboardScreen.kt, app/src/main/java/com/autominuting/presentation/dashboard/DashboardViewModel.kt</files>
  <read_first>
    - .planning/phases/54-home-file-input/54-01-SUMMARY.md
    - .planning/phases/54-home-file-input/54-02-PLAN.md (현 파일)
  </read_first>
  <action>Plan 01 + Plan 02 Task 1 완료 후 실기기에서 아래 how-to-verify 항목을 순서대로 수행하여 사용자 승인을 받는다. 문제 발견 시 resume-signal에 재현 절차 기술.</action>
  <acceptance_criteria>
    - Success Criteria 1 PASS: '파일 불러오기' 버튼 표시 확인
    - Success Criteria 2 PASS: SAF 피커 audio/* 필터 적용 확인
    - Success Criteria 3 PASS: 파일 선택 후 파이프라인 자동 시작
    - Success Criteria 4 PASS: activePipeline 배너 정상 표시
    - Meeting.source="LOCAL_FILE" 저장 확인 (전사 탭 일정정 Meeting 생성)
    - 사용자로부터 "approved" resume signal 수신
  </acceptance_criteria>
  <verify>실기기 수동 검증 (how-to-verify 블록 참조). 자동 검증 불가 — SAF 피커 + 실동작 파이프라인은 UI 상호작용 필요. 사용자 승인 필수.</verify>
  <done>사용자가 "approved" 또는 동등 승인 신호를 보내며, 모든 Success Criteria 1~4가 실기기에서 PASS 된다.</done>
  <what-built>
    DashboardScreen '파일 불러오기' 버튼 + SAF 파일 피커 + 제목 입력 다이얼로그 + DashboardViewModel.processLocalFile() (Plan 01) 연결.
    Plan 01 + Plan 02가 함께 제공하는 완전한 홈 화면 로컬 파일 입력 흐름.
  </what-built>
  <how-to-verify>
    1. Release/Debug APK 설치: `gradlew :app:installDebug`
    2. 앱 실행 → '대시보드' 탭으로 이동
    3. "URL에서 음성 다운로드" 카드 내부 하단에 "로컬 파일 불러오기" 섹션 + '파일 불러오기' 버튼(FolderOpen 아이콘)이 보이는지 확인 → **Success Criteria 1**
    4. '파일 불러오기' 버튼 탭 → SAF 파일 피커가 열리는지 확인. 오디오 파일(M4A 또는 MP3)이 필터링되어 선택 가능한지 확인 → **Success Criteria 2**
    5. M4A 파일 하나 선택 → 제목 입력 AlertDialog 표시, 기본값이 파일명(확장자 제외)인지 확인
    6. 제목을 빈 문자열로 지우면 '확인' 버튼이 비활성화되는지 확인 (D-07)
    7. 제목을 "테스트 로컬 M4A"로 입력 후 '확인' 탭 → 다이얼로그 닫힘
    8. 기존 파이프라인 배너(activePipeline)가 표시되며 전사 진행 상태가 나타나는지 확인 → **Success Criteria 3, 4**
    9. 전사 완료 후 '전사' 탭에서 새 Meeting이 source "LOCAL_FILE"로 저장되어 있고 회의록이 자동/수동(모드에 따라) 생성되는지 확인
    10. MP3 파일로 동일 흐름 반복 (포맷 분기가 기존 파이프라인 경유이므로 정상 처리되어야 함)
    11. 파일 피커에서 취소(뒤로가기) → 다이얼로그가 뜨지 않고 아무 변화 없음 확인
    12. 에러 케이스(선택사항): 손상된 파일 또는 빈 파일 선택 시 Error 상태 + '다시 시도' 버튼 표시 확인
  </how-to-verify>
  <resume-signal>"approved" 입력 또는 발견된 문제(스크린샷/재현 절차 포함) 기술</resume-signal>
</task>

</tasks>

<verification>
- `gradlew :app:assembleDebug` 성공
- grep 확인: "파일 불러오기", rememberLauncherForActivityResult, viewModel.processLocalFile, AlertDialog 모두 DashboardScreen.kt에 존재
- 실기기 human-verify 체크포인트 통과 (Success Criteria 1~4 모두 PASS)
</verification>

<success_criteria>
Phase 54 Success Criteria 전부 충족:
1. ✅ 홈 화면에 '파일 불러오기' 버튼 표시 (INPUT-01)
2. ✅ SAF 피커가 audio/* 필터로 열림 (INPUT-01)
3. ✅ 파일 선택 후 기존 STT → 회의록 파이프라인 자동 시작 (INPUT-02)
4. ✅ 파이프라인 진행 상태가 기존 activePipeline 배너로 동일하게 표시
</success_criteria>

<output>
After completion, create `.planning/phases/54-home-file-input/54-02-SUMMARY.md`
</output>
