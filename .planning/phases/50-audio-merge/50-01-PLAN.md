---
phase: 50-audio-merge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/com/autominuting/util/WavMerger.kt
  - app/src/test/java/com/autominuting/util/WavMergerTest.kt
  - app/src/main/java/com/autominuting/presentation/share/ShareReceiverActivity.kt
autonomous: true
requirements: [MERGE-01, MERGE-02, MERGE-03]

must_haves:
  truths:
    - "여러 오디오 파일을 Share Intent로 공유하면 하나의 WAV로 합쳐져 전사가 시작된다"
    - "합쳐진 파일명이 첫 번째 파일명과 동일하다"
    - "합쳐진 파일이 기존 STT -> 회의록 파이프라인을 그대로 통과한다"
    - "단일 파일 Share Intent는 기존과 동일하게 동작한다 (회귀 없음)"
  artifacts:
    - path: "app/src/main/java/com/autominuting/util/WavMerger.kt"
      provides: "WAV 파일 합치기 유틸리티 — 헤더 파싱, fmt 검증, PCM concat, 헤더 재계산"
      exports: ["WavMerger.merge", "WavHeader"]
    - path: "app/src/test/java/com/autominuting/util/WavMergerTest.kt"
      provides: "WavMerger 단위 테스트"
      min_lines: 40
    - path: "app/src/main/java/com/autominuting/presentation/share/ShareReceiverActivity.kt"
      provides: "SEND_MULTIPLE + audio 분기 추가, handleMultipleAudioShare 메서드"
      contains: "handleMultipleAudioShare"
  key_links:
    - from: "ShareReceiverActivity.onCreate()"
      to: "handleMultipleAudioShare()"
      via: "SEND_MULTIPLE + audio MIME 감지 분기"
      pattern: "ACTION_SEND_MULTIPLE.*allAudio.*handleMultipleAudioShare"
    - from: "handleMultipleAudioShare()"
      to: "WavMerger.merge()"
      via: "Content URI InputStream 리스트 전달"
      pattern: "WavMerger\\.merge"
    - from: "handleMultipleAudioShare()"
      to: "processSharedAudio() 동일 로직"
      via: "Meeting 생성 + TranscriptionTriggerWorker enqueue"
      pattern: "meetingRepository\\.insertMeeting.*TranscriptionTriggerWorker"
---

<objective>
Share Intent로 여러 오디오 파일을 받았을 때 자동으로 하나의 WAV 파일로 합쳐 기존 STT -> 회의록 파이프라인으로 처리하는 기능을 구현한다.

Purpose: 사용자가 여러 녹음 파일을 한 번에 공유하면 수동 합치기 없이 하나의 회의록으로 자동 생성되어야 한다.
Output: WavMerger 유틸리티 + ShareReceiverActivity 다중 파일 분기 + 단위 테스트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-audio-merge/50-RESEARCH.md

<interfaces>
<!-- ShareReceiverActivity 핵심 인터페이스 — executor가 참조할 기존 코드 -->

From app/src/main/java/com/autominuting/presentation/share/ShareReceiverActivity.kt:
```kotlin
class ShareReceiverActivity : ComponentActivity() {
    // 라인 112: 단일 EXTRA_STREAM URI 추출
    @Suppress("DEPRECATION")
    val streamUri = intent.getParcelableExtra<android.net.Uri>(Intent.EXTRA_STREAM)

    // 라인 123-125: 현재 isAudioShare 감지 (단일 URI만)
    val isAudioShare = intent.getStringExtra(Intent.EXTRA_TEXT).isNullOrBlank()
        && streamUri != null
        && mimeType?.startsWith("audio/") == true

    // 라인 127-148: isAudioShare == true일 때 처리 (RECORD_AUDIO 권한 확인 후 processSharedAudio 호출)

    // 라인 353-426: processSharedAudio(audioUri: android.net.Uri)
    //   - ContentResolver로 앱 내부 filesDir/audio/에 복사
    //   - Meeting 엔티티 생성 (source="SAMSUNG_SHARE", status=AUDIO_RECEIVED)
    //   - TranscriptionTriggerWorker enqueue (audioFilePath, meetingId, automationMode)

    // 라인 435-450: getDisplayName(uri: android.net.Uri): String?
    //   - ContentResolver.query로 DISPLAY_NAME 추출, 확장자 제거

    // 주입된 의존성:
    //   @Inject lateinit var meetingRepository: MeetingRepository
    //   @Inject lateinit var userPreferencesRepository: UserPreferencesRepository
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WavMerger 유틸리티 클래스 생성</name>
  <files>app/src/main/java/com/autominuting/util/WavMerger.kt, app/src/test/java/com/autominuting/util/WavMergerTest.kt</files>
  <behavior>
    - Test 1: 동일 포맷 WAV 2개 합치기 — 출력 파일의 PCM 데이터 크기 = 입력1 + 입력2 PCM 크기 합
    - Test 2: 합친 파일의 WAV 헤더 ChunkSize = 36 + 총 PCM 데이터 크기
    - Test 3: fmt 불일치 (샘플레이트 다름) 시 IllegalArgumentException 발생
    - Test 4: WAV가 아닌 파일 (RIFF 헤더 없음) 시 IllegalArgumentException 발생
    - Test 5: 단일 파일 리스트 입력 시 해당 파일 그대로 복사
  </behavior>
  <action>
    `app/src/main/java/com/autominuting/util/WavMerger.kt` 신규 생성:

    1. `WavHeader` data class 정의 — audioFormat, numChannels, sampleRate, byteRate, blockAlign, bitsPerSample, dataSize, dataOffset 필드
    2. `parseWavHeader(inputStream: InputStream): WavHeader` 함수:
       - 최소 44바이트 읽기
       - RIFF/WAVE 매직 넘버 검증 (실패 시 IllegalArgumentException)
       - `ByteBuffer.order(ByteOrder.LITTLE_ENDIAN)`으로 fmt 청크 파싱
       - data 청크 찾기: 36바이트 오프셋부터 "data" 문자열 순차 검색 (비표준 청크 대응, Pitfall 4)
       - dataOffset = "data" 문자열 위치 + 8 (4바이트 "data" + 4바이트 size)
    3. `object WavMerger` with `fun merge(inputStreams: List<InputStream>, outputFile: File)`:
       - 각 InputStream에서 parseWavHeader() 호출
       - 첫 번째 헤더의 sampleRate/numChannels/bitsPerSample를 기준으로 나머지 검증 (불일치 시 IllegalArgumentException)
       - 총 PCM 데이터 크기 = headers.sumOf { it.dataSize.toLong() }
       - 새 WAV 헤더 작성: ByteBuffer(44) LITTLE_ENDIAN, ChunkSize = 36 + totalDataSize, SubChunk2Size = totalDataSize
       - 각 InputStream에서 data 청크 이후 PCM 바이트를 BufferedOutputStream으로 copyTo (8KB 버퍼)
       - 주의: InputStream은 parseWavHeader()에서 헤더 바이트를 이미 소비했으므로 dataOffset까지 추가 skip 필요 (dataOffset > 44인 경우)

    `app/src/test/java/com/autominuting/util/WavMergerTest.kt` 신규 생성:
    - 테스트용 WAV 바이트 생성 헬퍼: createTestWav(sampleRate, numChannels, bitsPerSample, pcmData) — 44바이트 헤더 + PCM 데이터로 ByteArray 생성
    - JUnit 5 사용, behavior에 명시된 5개 테스트 케이스 구현
  </action>
  <verify>
    <automated>cd d:/workspace/temp/Auto_Minuting && ./gradlew testDebugUnitTest --tests "com.autominuting.util.WavMergerTest" --no-daemon</automated>
  </verify>
  <done>WavMerger.merge()가 동일 포맷 WAV 파일들을 올바르게 합치고 헤더를 재계산하며, fmt 불일치/비WAV 파일에 대해 예외를 발생시킨다. 5개 테스트 모두 통과.</done>
</task>

<task type="auto">
  <name>Task 2: ShareReceiverActivity에 다중 오디오 파일 합치기 분기 추가</name>
  <files>app/src/main/java/com/autominuting/presentation/share/ShareReceiverActivity.kt</files>
  <action>
    ShareReceiverActivity.kt 수정 — MERGE-01, MERGE-02, MERGE-03 구현:

    1. **onCreate() 다중 오디오 감지 분기 추가** (라인 127 `if (isAudioShare)` 블록 직전에 삽입):
       ```kotlin
       // 다중 오디오 파일 합치기 (MERGE-01)
       if (!isAudioShare && intent?.action == Intent.ACTION_SEND_MULTIPLE) {
           @Suppress("DEPRECATION")
           val streamUris = intent.getParcelableArrayListExtra<android.net.Uri>(Intent.EXTRA_STREAM)
           if (!streamUris.isNullOrEmpty()) {
               val allAudio = streamUris.all { uri ->
                   val type = try { contentResolver.getType(uri) } catch (e: Exception) { null }
                       ?: intent.type
                   type?.startsWith("audio/") == true
               }
               if (allAudio && streamUris.size >= 2) {
                   // RECORD_AUDIO 권한 확인 후 handleMultipleAudioShare 호출
                   // (기존 isAudioShare 블록과 동일한 권한 체크 패턴 적용)
                   handleMultipleAudioShare(streamUris)
                   return
               } else if (allAudio && streamUris.size == 1) {
                   // 단일 파일 SEND_MULTIPLE → 기존 단일 파일 처리로 위임 (Pitfall 5)
                   // processSharedAudio 호출 (기존 isAudioShare 블록과 동일)
               }
           }
       }
       ```
       - 단일 파일 SEND_MULTIPLE (Pitfall 5): streamUris.size == 1이면 기존 processSharedAudio(streamUris.first()) 호출

    2. **handleMultipleAudioShare(uris: List<Uri>) private 메서드 추가**:
       - lifecycleScope.launch 내에서 실행 (Content URI 수명 보장 — finish() 전 완료)
       - 첫 번째 URI에서 `getDisplayName(uris.first())` 호출하여 파일명 추출 (MERGE-02)
       - 각 URI에서 contentResolver.openInputStream() 호출하여 InputStream 리스트 생성
       - audioDir = File(filesDir, "audio"), audioDir.mkdirs()
       - mergedFile = File(audioDir, "share_${System.currentTimeMillis()}.wav")
       - `WavMerger.merge(inputStreams, mergedFile)` 호출
       - 모든 InputStream을 finally 블록에서 close
       - Meeting 엔티티 생성: title = firstName (getDisplayName 결과), source = "SAMSUNG_SHARE", audioFilePath = mergedFile.absolutePath (MERGE-03)
       - TranscriptionTriggerWorker enqueue — processSharedAudio() 라인 398-425와 동일한 패턴 복사
       - WAV가 아닌 파일 감지 시 (WavMerger에서 IllegalArgumentException): 토스트 "WAV 파일만 합치기를 지원합니다" 표시 후 finish()
       - fmt 불일치 시: 토스트 "오디오 파일 형식이 일치하지 않아 합칠 수 없습니다" 표시 후 finish()
       - catch 블록에서 일반 Exception: 토스트 "오디오 합치기 실패" + finish()
       - RECORD_AUDIO 권한 체크: 기존 isAudioShare 블록과 동일 패턴. 권한 없으면 pendingAudioUris (복수형 필드 추가) 저장 후 권한 요청, 콜백에서 handleMultipleAudioShare 재호출

    3. **import 추가**: `com.autominuting.util.WavMerger`

    주의사항:
    - 기존 단일 파일 isAudioShare 경로는 절대 수정하지 않는다 (회귀 방지)
    - processSharedAudio()는 수정하지 않는다 — 합친 파일용 로직은 handleMultipleAudioShare 내에서 직접 구현
    - Content URI는 finish() 전에 모두 읽어야 한다 (Pitfall 1)
  </action>
  <verify>
    <automated>cd d:/workspace/temp/Auto_Minuting && ./gradlew assembleDebug --no-daemon 2>&1 | tail -5</automated>
  </verify>
  <done>ShareReceiverActivity가 SEND_MULTIPLE + audio 다중 파일을 감지하여 WavMerger로 합친 뒤 기존 파이프라인에 진입시킨다. 단일 파일 Share Intent는 기존 경로 그대로 동작한다. assembleDebug 빌드 성공.</done>
</task>

</tasks>

<verification>
1. `./gradlew testDebugUnitTest --tests "com.autominuting.util.WavMergerTest"` — WavMerger 단위 테스트 5개 통과
2. `./gradlew assembleDebug` — 빌드 성공 (컴파일 에러 없음)
3. 기존 단일 파일 Share Intent 경로에 코드 변경이 없음을 diff로 확인
</verification>

<success_criteria>
- WavMerger.merge()가 동일 포맷 WAV 파일들을 정확히 합치고 헤더를 재계산한다
- fmt 불일치, 비WAV 파일에 대해 명확한 예외를 발생시킨다
- ShareReceiverActivity가 SEND_MULTIPLE + audio 조합을 감지하여 합치기 경로로 분기한다
- 합쳐진 파일명이 첫 번째 파일명을 사용한다 (MERGE-02)
- 합쳐진 파일이 Meeting 생성 + TranscriptionTriggerWorker enqueue로 기존 파이프라인에 진입한다 (MERGE-03)
- 기존 단일 파일 Share Intent 동작에 회귀가 없다
</success_criteria>

<output>
After completion, create `.planning/phases/50-audio-merge/50-01-SUMMARY.md`
</output>
