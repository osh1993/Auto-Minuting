---
phase: 05-minutes
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - app/src/main/java/com/autominuting/presentation/minutes/MinutesViewModel.kt
  - app/src/main/java/com/autominuting/presentation/minutes/MinutesScreen.kt
  - app/src/main/java/com/autominuting/presentation/minutes/MinutesDetailScreen.kt
  - app/src/main/java/com/autominuting/presentation/navigation/Screen.kt
  - app/src/main/java/com/autominuting/presentation/navigation/AppNavigation.kt
autonomous: true
requirements: [MIN-01, MIN-04]

must_haves:
  truths:
    - "사용자가 회의록 목록 화면에서 생성된 회의록들을 확인할 수 있다"
    - "사용자가 회의록을 선택하면 Markdown 내용을 텍스트로 읽을 수 있다"
    - "회의록이 없는 회의는 파이프라인 상태와 함께 표시된다"
  artifacts:
    - path: "app/src/main/java/com/autominuting/presentation/minutes/MinutesViewModel.kt"
      provides: "회의록 목록 상태 관리"
      contains: "class MinutesViewModel"
    - path: "app/src/main/java/com/autominuting/presentation/minutes/MinutesScreen.kt"
      provides: "회의록 목록 화면"
      contains: "fun MinutesScreen"
    - path: "app/src/main/java/com/autominuting/presentation/minutes/MinutesDetailScreen.kt"
      provides: "회의록 상세 읽기 화면"
      contains: "fun MinutesDetailScreen"
  key_links:
    - from: "MinutesScreen"
      to: "MinutesViewModel"
      via: "hiltViewModel()"
      pattern: "hiltViewModel"
    - from: "MinutesViewModel"
      to: "MeetingRepository.getMeetings()"
      via: "Flow 수집"
      pattern: "meetingRepository\\.getMeetings"
    - from: "MinutesDetailScreen"
      to: "minutesPath 파일"
      via: "File(minutesPath).readText()"
      pattern: "readText"
    - from: "AppNavigation"
      to: "MinutesDetailScreen"
      via: "composable route"
      pattern: "MinutesDetail"
---

<objective>
회의록 목록 화면과 상세 읽기 화면을 구현하여 사용자가 생성된 회의록을 앱 내에서 확인할 수 있게 한다.

Purpose: MIN-04 요구사항인 "저장된 회의록을 앱 내에서 텍스트로 읽을 수 있다"를 충족하고, Phase 7(UI 완성)의 회의록 뷰어 확장 기반을 마련한다.

Output: MinutesViewModel, MinutesScreen(목록), MinutesDetailScreen(상세), Navigation 경로 추가
</objective>

<execution_context>
@C:\Users\ADMIN\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\ADMIN\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-minutes/05-CONTEXT.md
@.planning/phases/05-minutes/05-01-SUMMARY.md

<interfaces>
<!-- Plan 01에서 생성된 파일 저장 패턴 -->
회의록 파일 경로: `context.filesDir/minutes/{meetingId}.md` (Markdown 형식)
MeetingEntity.minutesPath에 절대 경로 저장됨

<!-- 기존 전사 편집 화면 패턴 참조 -->
From app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt:
- TranscriptsViewModel + hiltViewModel() 패턴
- MeetingRepository.getMeetings() Flow 수집
- LazyColumn으로 목록 표시
- SuggestionChip으로 PipelineStatus 표시 (tertiary/primary/error 색상)

From app/src/main/java/com/autominuting/presentation/navigation/Screen.kt:
```kotlin
sealed class Screen(val route: String, val title: String, val icon: ImageVector) {
    data object Minutes : Screen("minutes", "회의록", Icons.Default.List)
    data object TranscriptEdit : Screen("transcripts/{meetingId}/edit", "전사 편집", Icons.Default.Edit) {
        fun createRoute(meetingId: Long) = "transcripts/$meetingId/edit"
    }
}
```

From app/src/main/java/com/autominuting/presentation/navigation/AppNavigation.kt:
```kotlin
composable(Screen.Minutes.route) { MinutesScreen() }
```

From app/src/main/java/com/autominuting/domain/repository/MeetingRepository.kt:
```kotlin
interface MeetingRepository {
    fun getMeetings(): Flow<List<Meeting>>
    fun getMeetingById(id: Long): Flow<Meeting?>
}
```

From app/src/main/java/com/autominuting/domain/model/Meeting.kt:
```kotlin
data class Meeting(
    val id: Long = 0,
    val title: String,
    val recordedAt: Instant,
    val audioFilePath: String,
    val transcriptPath: String? = null,
    val minutesPath: String? = null,
    val pipelineStatus: PipelineStatus,
    val errorMessage: String? = null,
    val createdAt: Instant,
    val updatedAt: Instant
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MinutesViewModel + MinutesScreen 목록 화면</name>
  <files>
    app/src/main/java/com/autominuting/presentation/minutes/MinutesViewModel.kt,
    app/src/main/java/com/autominuting/presentation/minutes/MinutesScreen.kt
  </files>
  <read_first>
    app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsViewModel.kt,
    app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt,
    app/src/main/java/com/autominuting/domain/repository/MeetingRepository.kt,
    app/src/main/java/com/autominuting/domain/model/Meeting.kt,
    app/src/main/java/com/autominuting/domain/model/PipelineStatus.kt,
    app/src/main/java/com/autominuting/presentation/minutes/MinutesScreen.kt
  </read_first>
  <action>
    **1. MinutesViewModel 생성**

    `app/src/main/java/com/autominuting/presentation/minutes/MinutesViewModel.kt`:

    TranscriptsViewModel 패턴을 따름:
    ```kotlin
    @HiltViewModel
    class MinutesViewModel @Inject constructor(
        private val meetingRepository: MeetingRepository
    ) : ViewModel()
    ```

    - `val meetings: StateFlow<List<Meeting>>` — `meetingRepository.getMeetings()`를 수집하여 `stateIn(viewModelScope, SharingStarted.WhileSubscribed(5000), emptyList())`
    - 회의록이 있는 회의(minutesPath != null)를 상단에 표시하되, 모든 회의를 표시하여 파이프라인 상태를 확인할 수 있게 함
    - `fun getMinutesContent(minutesPath: String): String?` — 파일을 읽어 내용 반환 (null 안전)

    **2. MinutesScreen 재작성**

    `app/src/main/java/com/autominuting/presentation/minutes/MinutesScreen.kt` 전체 재작성:

    - `@Composable fun MinutesScreen(onMinutesClick: (Long) -> Unit, viewModel: MinutesViewModel = hiltViewModel())`
    - `viewModel.meetings.collectAsStateWithLifecycle()`로 상태 수집
    - 빈 상태: `Box` 중앙에 "생성된 회의록이 없습니다" 텍스트 + Description 아이콘
    - 목록: `LazyColumn` + 각 아이템은 `Card` 형태:
      - 회의 제목 (title)
      - 녹음 일시 (recordedAt를 "yyyy-MM-dd HH:mm" 형식으로)
      - PipelineStatus 표시: `SuggestionChip` 사용
        - `COMPLETED` → primary 색상, "회의록 완료"
        - `GENERATING_MINUTES` → tertiary 색상, "생성 중..."
        - `TRANSCRIBED` → tertiary 색상, "전사 완료 (대기)"
        - 기타 → `outline` 스타일
      - 회의록 완료된 항목만 클릭 가능: `onClick = { onMinutesClick(meeting.id) }`
      - TranscriptsScreen의 Card/SuggestionChip 스타일을 참조하여 일관된 UI
  </action>
  <verify>
    <automated>cd D:/workspace/temp/Auto_Minuting && ./gradlew assembleDebug 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class MinutesViewModel" app/src/main/java/com/autominuting/presentation/minutes/MinutesViewModel.kt
    - grep -q "@HiltViewModel" app/src/main/java/com/autominuting/presentation/minutes/MinutesViewModel.kt
    - grep -q "meetingRepository" app/src/main/java/com/autominuting/presentation/minutes/MinutesViewModel.kt
    - grep -q "onMinutesClick" app/src/main/java/com/autominuting/presentation/minutes/MinutesScreen.kt
    - grep -q "LazyColumn" app/src/main/java/com/autominuting/presentation/minutes/MinutesScreen.kt
    - grep -q "SuggestionChip" app/src/main/java/com/autominuting/presentation/minutes/MinutesScreen.kt
    - grep -q "COMPLETED" app/src/main/java/com/autominuting/presentation/minutes/MinutesScreen.kt
  </acceptance_criteria>
  <done>
    - MinutesViewModel이 MeetingRepository에서 회의 목록을 수집하여 StateFlow로 제공한다
    - MinutesScreen이 LazyColumn으로 회의 목록을 표시하고 PipelineStatus별 상태 칩을 보여준다
    - 회의록 완료된 항목 클릭 시 onMinutesClick 콜백이 호출된다
  </done>
</task>

<task type="auto">
  <name>Task 2: MinutesDetailScreen + Navigation 경로 연동</name>
  <files>
    app/src/main/java/com/autominuting/presentation/minutes/MinutesDetailViewModel.kt,
    app/src/main/java/com/autominuting/presentation/minutes/MinutesDetailScreen.kt,
    app/src/main/java/com/autominuting/presentation/navigation/Screen.kt,
    app/src/main/java/com/autominuting/presentation/navigation/AppNavigation.kt
  </files>
  <read_first>
    app/src/main/java/com/autominuting/presentation/transcripts/TranscriptEditScreen.kt,
    app/src/main/java/com/autominuting/presentation/transcripts/TranscriptEditViewModel.kt,
    app/src/main/java/com/autominuting/presentation/navigation/Screen.kt,
    app/src/main/java/com/autominuting/presentation/navigation/AppNavigation.kt,
    app/src/main/java/com/autominuting/presentation/minutes/MinutesScreen.kt
  </read_first>
  <action>
    **1. MinutesDetailViewModel 생성**

    `app/src/main/java/com/autominuting/presentation/minutes/MinutesDetailViewModel.kt`:

    TranscriptEditViewModel 패턴을 따름:
    ```kotlin
    @HiltViewModel
    class MinutesDetailViewModel @Inject constructor(
        savedStateHandle: SavedStateHandle,
        private val meetingRepository: MeetingRepository
    ) : ViewModel()
    ```

    - `savedStateHandle`에서 `"meetingId"` 추출 (Long)
    - `val meeting: StateFlow<Meeting?>` — `meetingRepository.getMeetingById(meetingId)` 수집
    - `val minutesContent: StateFlow<String>` — meeting의 minutesPath를 감시하여 파일 내용을 읽음
      - `meeting` Flow를 `flatMapLatest`로 변환: minutesPath가 null이 아니면 `File(minutesPath).readText()`, null이면 빈 문자열
      - `Dispatchers.IO`에서 파일 읽기

    **2. MinutesDetailScreen 생성**

    `app/src/main/java/com/autominuting/presentation/minutes/MinutesDetailScreen.kt`:

    TranscriptEditScreen 패턴을 참조하되, 편집이 아닌 읽기 전용:
    ```kotlin
    @Composable
    fun MinutesDetailScreen(
        onBack: () -> Unit,
        viewModel: MinutesDetailViewModel = hiltViewModel()
    )
    ```

    UI 구성:
    - `Scaffold` + `TopAppBar`:
      - 제목: meeting.title (또는 "회의록")
      - 뒤로가기 아이콘 버튼 (`onBack`)
    - 본문: `Column` + `verticalScroll`:
      - 회의 정보 영역: 제목, 녹음 일시, 상태 칩
      - `Divider`
      - 회의록 내용: `Text(minutesContent)` — Markdown을 플레인텍스트로 표시
        - Phase 7에서 Markdown 렌더러로 업그레이드 예정
        - 현재는 `SelectionContainer { Text(content, style = MaterialTheme.typography.bodyMedium) }`로 텍스트 선택 가능하게
    - 로딩 상태: meeting이 null이면 `CircularProgressIndicator`
    - 에러 상태: minutesPath가 null이면 "회의록이 아직 생성되지 않았습니다" 메시지

    **3. Screen.kt에 MinutesDetail 경로 추가**

    `app/src/main/java/com/autominuting/presentation/navigation/Screen.kt`에 추가:
    ```kotlin
    /** 회의록 상세 - 회의록 내용 읽기 화면 (Bottom Navigation에 표시되지 않음) */
    data object MinutesDetail : Screen(
        "minutes/{meetingId}",
        "회의록 상세",
        Icons.Default.Description
    ) {
        fun createRoute(meetingId: Long) = "minutes/$meetingId"
    }
    ```

    TranscriptEdit 패턴과 동일한 구조.

    **4. AppNavigation.kt에 MinutesDetail composable 등록 + MinutesScreen 콜백 연결**

    `app/src/main/java/com/autominuting/presentation/navigation/AppNavigation.kt` 수정:

    기존 `composable(Screen.Minutes.route) { MinutesScreen() }` 를 다음으로 교체:
    ```kotlin
    composable(Screen.Minutes.route) {
        MinutesScreen(
            onMinutesClick = { meetingId ->
                navController.navigate(Screen.MinutesDetail.createRoute(meetingId))
            }
        )
    }
    composable(
        route = Screen.MinutesDetail.route,
        arguments = listOf(
            navArgument("meetingId") { type = NavType.LongType }
        )
    ) {
        MinutesDetailScreen(
            onBack = { navController.popBackStack() }
        )
    }
    ```

    import 추가: `MinutesDetailScreen`
  </action>
  <verify>
    <automated>cd D:/workspace/temp/Auto_Minuting && ./gradlew assembleDebug 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class MinutesDetailViewModel" app/src/main/java/com/autominuting/presentation/minutes/MinutesDetailViewModel.kt
    - grep -q "minutesContent" app/src/main/java/com/autominuting/presentation/minutes/MinutesDetailViewModel.kt
    - grep -q "fun MinutesDetailScreen" app/src/main/java/com/autominuting/presentation/minutes/MinutesDetailScreen.kt
    - grep -q "readText" app/src/main/java/com/autominuting/presentation/minutes/MinutesDetailViewModel.kt
    - grep -q "MinutesDetail" app/src/main/java/com/autominuting/presentation/navigation/Screen.kt
    - grep -q "createRoute" app/src/main/java/com/autominuting/presentation/navigation/Screen.kt
    - grep -q "MinutesDetailScreen" app/src/main/java/com/autominuting/presentation/navigation/AppNavigation.kt
    - grep -q "onMinutesClick" app/src/main/java/com/autominuting/presentation/navigation/AppNavigation.kt
  </acceptance_criteria>
  <done>
    - MinutesDetailViewModel이 meetingId로 회의 정보와 회의록 파일 내용을 로드한다
    - MinutesDetailScreen이 회의록 Markdown 내용을 플레인텍스트로 표시한다 (선택 가능)
    - Screen.MinutesDetail 경로가 추가되고 AppNavigation에 등록된다
    - MinutesScreen에서 완료된 회의록 클릭 시 MinutesDetailScreen으로 이동한다
  </done>
</task>

</tasks>

<verification>
1. `./gradlew assembleDebug`가 오류 없이 통과한다
2. MinutesScreen이 회의 목록을 LazyColumn으로 표시하고 PipelineStatus 칩을 보여준다
3. 완료된 회의록 클릭 시 MinutesDetailScreen으로 이동하여 내용을 읽을 수 있다
4. MinutesDetail 경로가 Navigation에 등록되어 back 동작이 정상 작동한다
</verification>

<success_criteria>
- 빌드가 성공한다
- 회의록 목록 화면에서 모든 회의의 파이프라인 상태를 확인할 수 있다
- 완료된 회의록을 선택하면 상세 화면에서 Markdown 내용을 텍스트로 읽을 수 있다
- Navigation이 정상 동작한다 (목록 -> 상세 -> 뒤로가기)
</success_criteria>

<output>
After completion, create `.planning/phases/05-minutes/05-02-SUMMARY.md`
</output>
