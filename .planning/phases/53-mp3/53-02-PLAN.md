---
phase: 53-mp3
plan: 02
type: execute
wave: 2
depends_on:
  - "53-01"
files_modified:
  - app/src/main/java/com/autominuting/presentation/share/ShareReceiverActivity.kt
autonomous: false
requirements:
  - MERGE-04
  - MERGE-05
must_haves:
  truths:
    - "Share Intent로 여러 MP3 파일을 공유하면 Mp3Merger로 합쳐져 하나의 .mp3 파일로 STT 파이프라인에 진입한다"
    - "Share Intent로 M4A와 MP3가 혼재된 파일을 공유하면 포맷별로 분리되어 각각 별도의 Meeting이 생성된다 (MP3 그룹은 Mp3Merger, M4A 그룹은 AudioMerger)"
    - "단일 MP3 파일 공유(SEND 또는 SEND_MULTIPLE size=1)는 기존 processSharedAudio 경로로 처리되어 합치기 로직이 실행되지 않는다"
    - "합쳐진 MP3 파일의 확장자는 .mp3, M4A 합치기의 확장자는 .m4a로 유지된다"
    - "Mp3Merger 실패(bitrate 불일치 등)는 Toast로 사용자에게 노출되고 Activity가 finish()한다"
  artifacts:
    - path: "app/src/main/java/com/autominuting/presentation/share/ShareReceiverActivity.kt"
      provides: "handleMultipleAudioShare()에 포맷 분류 분기 + 그룹별 Merger 호출 + 그룹별 Meeting insert/enqueue"
      contains: "Mp3Merger"
  key_links:
    - from: "ShareReceiverActivity.handleMultipleAudioShare()"
      to: "Mp3Merger.merge()"
      via: "MP3 그룹 임시 파일 경로 → 출력 .mp3"
      pattern: "Mp3Merger\\.merge\\("
    - from: "ShareReceiverActivity.handleMultipleAudioShare()"
      to: "AudioMerger.merge()"
      via: "M4A 그룹 임시 파일 경로 → 출력 .m4a"
      pattern: "AudioMerger\\.merge\\("
    - from: "그룹별 Meeting insert"
      to: "TranscriptionTriggerWorker enqueue"
      via: "WorkManager OneTimeWorkRequest"
      pattern: "TranscriptionTriggerWorker\\.KEY_MEETING_ID"
---

<objective>
`ShareReceiverActivity.handleMultipleAudioShare()`를 포맷별 분리 처리 구조로 리팩터링하여 MERGE-04(MP3 합치기)와 MERGE-05(포맷 혼재 분리 처리)를 동시에 만족한다.

Purpose: Plan 01에서 Mp3Merger 유틸을 완성했다. 이 Plan은 실제 Share Intent 진입점에 포맷 분류 로직을 추가해 MP3 그룹 → Mp3Merger, M4A/WAV 그룹 → AudioMerger로 분기시키고, 각 그룹마다 별도의 Meeting 엔티티를 insert + TranscriptionTriggerWorker를 enqueue한다.

Output: ShareReceiverActivity.kt 수정 (파일 1개) — 기존 단일 호출 `AudioMerger.merge(paths, merged.m4a)` 경로를 그룹별 분기 루프로 대체한다.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-mp3/53-RESEARCH.md
@.planning/phases/53-mp3/53-01-SUMMARY.md
@app/src/main/java/com/autominuting/presentation/share/ShareReceiverActivity.kt
@app/src/main/java/com/autominuting/util/Mp3Merger.kt

<interfaces>
<!-- Plan 01이 제공한 API -->

```kotlin
object Mp3Merger {
    fun merge(inputPaths: List<String>, outputFile: File)  // throws IllegalArgumentException, IOException
}
```

<!-- 기존 재사용 API -->

```kotlin
// AudioMerger (기존, 수정 없음) — M4A/MP4 컨테이너 전용
object AudioMerger {
    fun merge(inputPaths: List<String>, outputFile: File)
}

// 기존 ShareReceiverActivity 헬퍼 (그대로 사용)
private fun getDisplayName(uri: Uri): String?

// TranscriptionTriggerWorker enqueue key들
TranscriptionTriggerWorker.KEY_AUDIO_FILE_PATH
TranscriptionTriggerWorker.KEY_MEETING_ID
TranscriptionTriggerWorker.KEY_AUTOMATION_MODE
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: handleMultipleAudioShare에 포맷 분류 + 그룹별 Merger 분기 추가</name>
  <files>app/src/main/java/com/autominuting/presentation/share/ShareReceiverActivity.kt</files>
  <read_first>
    - app/src/main/java/com/autominuting/presentation/share/ShareReceiverActivity.kt (현재 handleMultipleAudioShare 구현, getDisplayName 헬퍼, processSharedAudio 확장자 분기 로직, import 구조)
    - app/src/main/java/com/autominuting/util/Mp3Merger.kt (Plan 01 결과 — merge 시그니처 재확인)
    - app/src/main/java/com/autominuting/util/AudioMerger.kt (기존 M4A merger — 변경 없이 재사용, 시그니처 확인)
    - .planning/phases/53-mp3/53-RESEARCH.md Pattern 2, Pattern 3 (의사코드, 분류 기준)
  </read_first>
  <action>
    `app/src/main/java/com/autominuting/presentation/share/ShareReceiverActivity.kt`를 수정한다.

    **1) Import 추가** (파일 상단 import 블록에):
    ```kotlin
    import com.autominuting.util.Mp3Merger
    ```

    **2) 파일 내부에 포맷 enum 추가** (ShareReceiverActivity 클래스 내부 또는 파일 최상단 private enum으로):
    ```kotlin
    private enum class SharedAudioFormat { MP3, M4A_COMPATIBLE }
    ```

    **3) 포맷 분류 헬퍼 추가** (ShareReceiverActivity 클래스 내부 private 함수):
    ```kotlin
    /**
     * URI의 MIME 타입과 파일명 확장자를 기반으로 오디오 포맷을 분류한다.
     * MIME 우선, 실패 시 확장자로 폴백.
     */
    private fun classifyAudioFormat(uri: android.net.Uri): SharedAudioFormat {
        val mime = try { contentResolver.getType(uri) } catch (e: Exception) { null } ?: ""
        val name = getDisplayName(uri) ?: ""
        return when {
            "mpeg" in mime || "mp3" in mime -> SharedAudioFormat.MP3
            name.endsWith(".mp3", ignoreCase = true) -> SharedAudioFormat.MP3
            else -> SharedAudioFormat.M4A_COMPATIBLE
        }
    }
    ```

    **4) handleMultipleAudioShare() 본문 전면 교체**:
    기존 함수(라인 415-491, 단일 AudioMerger 호출 구조)를 아래 구조로 대체한다. 핵심 변경점은 (a) 임시 파일 복사 시 URI를 그룹별로 분류하고 (b) 그룹마다 별도 Merger + 별도 Meeting + 별도 enqueue를 실행하는 것이다.

    ```kotlin
    private fun handleMultipleAudioShare(uris: List<android.net.Uri>) {
        lifecycleScope.launch {
            val tempFiles = mutableListOf<File>()
            try {
                // 1. Content URI → 임시 파일 복사 + 포맷 분류 (원래 순서 유지)
                val tempDir = File(cacheDir, "merge_temp")
                tempDir.mkdirs()

                // GroupedTemp: 포맷별로 (임시 파일, 원본 URI) 리스트 보관
                data class ClassifiedTemp(
                    val tempFile: File,
                    val sourceUri: android.net.Uri,
                    val format: SharedAudioFormat
                )
                val classified = mutableListOf<ClassifiedTemp>()

                withContext(Dispatchers.IO) {
                    for ((index, uri) in uris.withIndex()) {
                        val format = classifyAudioFormat(uri)
                        val ext = when (format) {
                            SharedAudioFormat.MP3 -> "mp3"
                            SharedAudioFormat.M4A_COMPATIBLE -> getDisplayName(uri)?.substringAfterLast('.') ?: "m4a"
                        }
                        val tempFile = File(tempDir, "input_${index}_${System.currentTimeMillis()}.$ext")
                        contentResolver.openInputStream(uri)?.use { input ->
                            tempFile.outputStream().use { output -> input.copyTo(output) }
                        } ?: throw IOException("URI를 열 수 없습니다: $uri")
                        tempFiles.add(tempFile)
                        classified.add(ClassifiedTemp(tempFile, uri, format))
                    }
                }

                // 2. 포맷별 그룹핑 (LinkedHashMap으로 순서 유지)
                val grouped: Map<SharedAudioFormat, List<ClassifiedTemp>> =
                    classified.groupBy { it.format }

                Log.d(TAG, "다중 오디오 포맷 분류: ${grouped.mapValues { it.value.size }}")

                // 3. 각 그룹을 별도 Merger + 별도 Meeting으로 처리
                val audioDir = File(filesDir, "audio")
                audioDir.mkdirs()

                var createdMeetingCount = 0
                for ((format, group) in grouped) {
                    if (group.isEmpty()) continue

                    val firstName = getDisplayName(group.first().sourceUri) ?: "합쳐진 회의"
                    // 포맷 혼재 시 제목에 포맷 접미사 추가하여 사용자가 구분 가능 (Research Open Question 1)
                    val title = if (grouped.size > 1) {
                        when (format) {
                            SharedAudioFormat.MP3 -> "$firstName (MP3)"
                            SharedAudioFormat.M4A_COMPATIBLE -> "$firstName (M4A)"
                        }
                    } else {
                        firstName
                    }

                    val now = System.currentTimeMillis()
                    val mergedFile = when (format) {
                        SharedAudioFormat.MP3 -> File(audioDir, "share_${now}_mp3.mp3")
                        SharedAudioFormat.M4A_COMPATIBLE -> File(audioDir, "share_${now}_m4a.m4a")
                    }

                    // 단일 파일 그룹 최적화: Merger 호출 없이 임시 파일을 audioDir로 이동
                    // (MERGE-04 의도는 "여러 MP3를 하나로 합침" — 그룹 크기 1이면 합치기 불필요)
                    if (group.size == 1) {
                        withContext(Dispatchers.IO) {
                            group.first().tempFile.copyTo(mergedFile, overwrite = true)
                        }
                    } else {
                        withContext(Dispatchers.IO) {
                            val paths = group.map { it.tempFile.absolutePath }
                            when (format) {
                                SharedAudioFormat.MP3 -> Mp3Merger.merge(paths, mergedFile)
                                SharedAudioFormat.M4A_COMPATIBLE -> AudioMerger.merge(paths, mergedFile)
                            }
                        }
                    }

                    Log.d(TAG, "[$format] 합치기 완료: ${mergedFile.absolutePath} (${mergedFile.length()} bytes, ${group.size}개)")

                    // Meeting insert + TranscriptionTriggerWorker enqueue (기존 패턴 그대로)
                    val meeting = Meeting(
                        title = title,
                        recordedAt = Instant.ofEpochMilli(now),
                        audioFilePath = mergedFile.absolutePath,
                        pipelineStatus = PipelineStatus.AUDIO_RECEIVED,
                        source = "SAMSUNG_SHARE",
                        createdAt = Instant.ofEpochMilli(now),
                        updatedAt = Instant.ofEpochMilli(now)
                    )
                    val meetingId = meetingRepository.insertMeeting(meeting)
                    Log.d(TAG, "[$format] MeetingEntity 생성: meetingId=$meetingId, title=$title")

                    val automationMode = userPreferencesRepository.getAutomationModeOnce()
                    val workRequest = OneTimeWorkRequestBuilder<TranscriptionTriggerWorker>()
                        .setInputData(
                            workDataOf(
                                TranscriptionTriggerWorker.KEY_AUDIO_FILE_PATH to mergedFile.absolutePath,
                                TranscriptionTriggerWorker.KEY_MEETING_ID to meetingId,
                                TranscriptionTriggerWorker.KEY_AUTOMATION_MODE to automationMode.name
                            )
                        )
                        .build()
                    WorkManager.getInstance(this@ShareReceiverActivity).enqueue(workRequest)
                    createdMeetingCount++
                }

                // 4. 사용자 피드백
                PipelineNotificationHelper.updateProgress(this@ShareReceiverActivity, "음성 파일 전사 중...")
                val message = if (createdMeetingCount > 1) {
                    "포맷별로 ${createdMeetingCount}개 파일 생성, 전사 중..."
                } else {
                    "음성 파일 합치기 완료, 전사 중..."
                }
                Toast.makeText(this@ShareReceiverActivity, message, Toast.LENGTH_SHORT).show()

            } catch (e: Exception) {
                Log.e(TAG, "다중 오디오 합치기 실패: ${e.message}", e)
                Toast.makeText(this@ShareReceiverActivity, "오디오 합치기 실패: ${e.message}", Toast.LENGTH_LONG).show()
            } finally {
                tempFiles.forEach { file ->
                    try { file.delete() } catch (_: Exception) {}
                }
                finish()
            }
        }
    }
    ```

    **5) 기존 로직 중 변경하지 말아야 할 부분**:
    - onCreate 내부의 SEND_MULTIPLE 분기(라인 140-185) — 단일 파일 (size==1)을 processSharedAudio로 위임하는 로직은 그대로 유지 (Research Pitfall 5 — 단일 MP3 공유는 기존 경로).
    - `processSharedAudio()` (라인 498~) — 단일 파일 경로 변경 없음.
    - AudioMerger.kt — 수정 없음.
    - getDisplayName, readTextAutoDetectEncoding 등 기존 헬퍼 — 변경 없음.

    **6) 주의사항**:
    - `classifyAudioFormat` 호출 시 MIME 조회 실패 대비 try/catch 포함 (기존 라인 128-133 패턴 그대로).
    - Log TAG는 기존 `TAG` 상수 재사용.
    - `Dispatchers.IO`, `withContext`는 기존 import 그대로 사용 (이미 import 되어 있음).
    - `OneTimeWorkRequestBuilder`, `workDataOf`, `WorkManager`, `Meeting`, `PipelineStatus`, `Instant`, `IOException`, `File`, `Toast`, `Log`, `Dispatchers`, `lifecycleScope`, `launch`, `withContext`, `PipelineNotificationHelper` 모두 기존 파일에 import되어 있음 — 추가 import는 `Mp3Merger`만 필요.
    - 혼재 케이스에서 `createdMeetingCount > 1` 판정으로 Toast 메시지 분기 — 단일 포맷일 때 기존 메시지 유지.
    - 단일 파일 그룹(group.size == 1) 최적화는 Research Pitfall 5의 원칙("단일 MP3 공유는 기존과 동일하게")을 그룹 단위로 확장한 것: M4A 3개 + MP3 1개 혼재 시 MP3는 Merger를 거치지 않고 그대로 복사.
  </action>
  <verify>
    <automated>cd /d/workspace/temp/Auto_Minuting &amp;&amp; ./gradlew :app:assembleDebug</automated>
  </verify>
  <acceptance_criteria>
    - ShareReceiverActivity.kt에 `import com.autominuting.util.Mp3Merger` 포함 (grep: `import com\.autominuting\.util\.Mp3Merger`)
    - ShareReceiverActivity.kt에 `private enum class SharedAudioFormat` 또는 최상단 `enum class SharedAudioFormat` 선언 포함 (grep: `enum class SharedAudioFormat`)
    - ShareReceiverActivity.kt에 `SharedAudioFormat.MP3` 와 `SharedAudioFormat.M4A_COMPATIBLE` 두 값 모두 사용됨 (grep -c 각각 >= 1)
    - ShareReceiverActivity.kt에 `fun classifyAudioFormat` 함수 정의 포함 (grep: `fun classifyAudioFormat`)
    - handleMultipleAudioShare 내부에 `Mp3Merger.merge(` 호출 포함 (grep: `Mp3Merger\.merge\(`)
    - handleMultipleAudioShare 내부에 `AudioMerger.merge(` 호출 포함 (grep: `AudioMerger\.merge\(`)
    - handleMultipleAudioShare 내부에 `groupBy { it.format }` 또는 `groupBy { .*format.* }` 형태의 그룹핑 코드 포함
    - `./gradlew :app:assembleDebug` exit 0, 출력에 `BUILD SUCCESSFUL`
    - 기존 processSharedAudio 함수가 여전히 존재하고 시그니처 미변경 (grep: `private suspend fun processSharedAudio\(audioUri: android\.net\.Uri\)`)
    - AudioMerger.kt 파일은 수정되지 않음 (git diff로 확인 — 변경 파일 목록에 AudioMerger.kt 없음)
  </acceptance_criteria>
  <done>
    ShareReceiverActivity가 MP3 그룹과 M4A 그룹을 분리하여 각각의 Merger로 합치고, 각각 Meeting + Worker enqueue를 수행한다. Debug 빌드가 성공한다.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 실기기 Share Intent 검증 (MP3 합치기 + 포맷 혼재)</name>
  <files>(검증 전용 — 파일 수정 없음)</files>
  <read_first>
    - app/src/main/java/com/autominuting/presentation/share/ShareReceiverActivity.kt (Task 1 수정 결과 확인)
    - app/src/main/java/com/autominuting/util/Mp3Merger.kt (Plan 01 결과 확인)
  </read_first>
  <action>
    사용자가 실기기에서 Share Intent 테스트를 수행하여 MERGE-04(다중 MP3), MERGE-05(포맷 혼재), 단일 MP3 기존 경로 보존, 포맷 불일치 실패 케이스를 확인한다. Claude는 APK 설치 명령과 Logcat 모니터링을 제공한다.
  </action>
  <acceptance_criteria>
    - 테스트 A: 다중 MP3 공유 후 audio 디렉터리에 share_*_mp3.mp3 파일 존재 + Meeting 1개 생성
    - 테스트 B: MP3+M4A 혼재 공유 후 Meeting 2개 생성, 각 audioFilePath 확장자가 .mp3 / .m4a
    - 테스트 C: 단일 MP3 공유 시 제목에 (MP3) 접미사 없음 — 기존 processSharedAudio 경로 유지
    - 테스트 D: 비트레이트 불일치 MP3 공유 시 Toast 에러 + Meeting 미생성
  </acceptance_criteria>
  <verify>
    <automated>MISSING — 실기기 UI 검증, Claude는 ./gradlew :app:installDebug와 adb logcat으로 지원</automated>
  </verify>
  <done>모든 4개 테스트 통과 후 사용자가 "approved" 입력</done>
  <what-built>
    - Mp3Merger 유틸 (Plan 01)
    - ShareReceiverActivity.handleMultipleAudioShare 포맷 분류 분기 (Task 1)
  </what-built>
  <how-to-verify>
    1. 디버그 APK 설치: `./gradlew :app:installDebug`
    2. 테스트 A — 다중 MP3 합치기 (MERGE-04):
       - 동일 인코딩(같은 비트레이트/샘플레이트)의 MP3 파일 2~3개를 갤러리/파일 앱에서 "여러 항목 선택" 후 공유 → Auto Minuting 선택
       - 홈 화면에 Meeting 1개가 생성되고 제목이 첫 파일명인지 확인
       - 해당 Meeting의 audioFilePath에 대응하는 `/data/data/com.autominuting/files/audio/share_*_mp3.mp3` 파일이 생성되었는지 adb shell로 확인: `adb shell run-as com.autominuting ls files/audio`
       - 전사가 진행되어 완료되는지 확인 (Gemini/Whisper/Groq 등 현재 설정 엔진 기준)
    3. 테스트 B — MP3 + M4A 혼재 (MERGE-05):
       - MP3 1~2개 + M4A 1~2개를 함께 선택 공유
       - 홈 화면에 Meeting 2개가 각각 생성되고, 제목에 `(MP3)`, `(M4A)` 접미사가 붙는지 확인
       - 각 Meeting의 audioFilePath가 서로 다르고 확장자가 `.mp3` / `.m4a`로 분리되었는지 확인
       - Toast 메시지가 "포맷별로 2개 파일 생성, 전사 중..."인지 확인
    4. 테스트 C — 단일 MP3 공유 (기존 경로 보존):
       - MP3 1개를 공유 (SEND 또는 SEND_MULTIPLE size=1)
       - 기존과 동일하게 Meeting 1개 생성, 제목에 `(MP3)` 접미사 **없음** 확인 (기존 processSharedAudio 경로 유지)
    5. 테스트 D — 포맷 불일치 실패 케이스 (선택):
       - 의도적으로 비트레이트가 다른 2개 MP3를 공유
       - Toast "오디오 합치기 실패: ..." 메시지 확인, Meeting 미생성 확인
  </how-to-verify>
  <resume-signal>
    - 모든 테스트 통과 시 "approved" 입력
    - 이슈 발견 시 구체 증상 기술 (어느 테스트, Logcat 스택트레이스, 사용자 관찰 내용)
  </resume-signal>
</task>

</tasks>

<verification>
- `./gradlew :app:assembleDebug` BUILD SUCCESSFUL
- 실기기 테스트 A (다중 MP3) → Meeting 1개, 파이프라인 진입
- 실기기 테스트 B (MP3+M4A 혼재) → Meeting 2개, 포맷별 확장자 분리
- 실기기 테스트 C (단일 MP3) → 기존 경로 보존, 접미사 없음
</verification>

<success_criteria>
1. MERGE-04: 여러 MP3 공유 시 재인코딩 없이 하나의 .mp3로 합쳐져 STT 파이프라인 정상 진입
2. MERGE-05: M4A + MP3 혼재 공유 시 포맷별로 분리되어 각각 Meeting 생성
3. 단일 MP3 공유는 기존 processSharedAudio 경로로 처리되어 동작 변화 없음
4. Plan 01의 Mp3Merger가 실제 Share Intent 경로에서 호출되어 사용됨
</success_criteria>

<output>
After completion, create `.planning/phases/53-mp3/53-02-SUMMARY.md` with:
- handleMultipleAudioShare의 포맷 분류 → 그룹별 Merger 호출 → 그룹별 Meeting insert/enqueue 구조
- classifyAudioFormat 판정 로직 (MIME 우선 + 확장자 폴백)
- 혼재 케이스에서 Meeting 제목 `(MP3)` / `(M4A)` 접미사 정책
- 단일 파일 그룹 최적화 (그룹 크기 1일 때 Merger 스킵, 임시 파일 copy)
- 실기기 테스트 결과 (테스트 A/B/C/D 케이스별)
</output>
