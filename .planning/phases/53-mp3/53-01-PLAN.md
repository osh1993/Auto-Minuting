---
phase: 53-mp3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/com/autominuting/util/Mp3Merger.kt
  - app/src/test/java/com/autominuting/util/Mp3MergerTest.kt
autonomous: true
requirements:
  - MERGE-04
must_haves:
  truths:
    - "Mp3Merger.merge(inputPaths, outputFile)는 ID3v2 태그와 ID3v1 트레일러를 제거한 MP3 프레임 바이트를 순서대로 이어붙여 단일 MP3 파일을 생성한다"
    - "첫 번째 파일의 ID3v2 태그는 출력 파일에 유지된다 (플레이어 메타데이터 호환)"
    - "두 번째 이후 파일의 ID3v2/ID3v1 태그는 출력 파일에 포함되지 않는다"
    - "입력 파일들의 MP3 frame header bitrate/sampleRate/channelMode가 불일치하면 IllegalArgumentException을 throw한다"
    - "재인코딩 없이 순수 바이트 concat으로 동작한다 (MediaCodec/MediaMuxer 미사용)"
  artifacts:
    - path: "app/src/main/java/com/autominuting/util/Mp3Merger.kt"
      provides: "object Mp3Merger with merge(inputPaths: List<String>, outputFile: File) + parseFrameHeader + stripTags"
      min_lines: 120
    - path: "app/src/test/java/com/autominuting/util/Mp3MergerTest.kt"
      provides: "JUnit 단위 테스트: ID3 스트립, 프레임 concat, 포맷 불일치 검증"
      min_lines: 80
  key_links:
    - from: "Mp3Merger.merge()"
      to: "java.io.File.outputStream"
      via: "BufferedOutputStream write"
      pattern: "outputFile\\.outputStream\\(\\)\\.buffered\\(\\)"
    - from: "Mp3Merger.stripTags()"
      to: "synchsafeToInt 로직"
      via: "ID3v2 size 필드 파싱"
      pattern: "and 0x7F.*shl 21"
---

<objective>
MP3 파일 여러 개를 재인코딩 없이 하나의 MP3 파일로 합치는 독립 유틸리티 `Mp3Merger`를 신규 생성한다.

Purpose: Phase 50에서 도입된 AudioMerger(MediaMuxer 기반)는 MP3 출력 컨테이너를 지원하지 않음. MP3는 프레임 self-contained 특성을 활용해 ID3 태그만 스트립하고 바이트 단위로 concat하면 재인코딩 없이 합칠 수 있다. 이 Plan은 순수 로직 계층(Android Framework 의존성 없음)을 먼저 완성하여 단위 테스트로 정확성을 검증한 뒤, Plan 02에서 ShareReceiverActivity에 연동한다.

Output: `app/src/main/java/com/autominuting/util/Mp3Merger.kt` + `app/src/test/java/com/autominuting/util/Mp3MergerTest.kt`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-mp3/53-RESEARCH.md
@app/src/main/java/com/autominuting/util/WavMerger.kt
@app/src/test/java/com/autominuting/util/WavMergerTest.kt

<interfaces>
<!-- Mp3Merger가 따라야 할 object 패턴 (WavMerger.kt에서 추출) -->

```kotlin
// WavMerger.kt 시그니처 (참고)
object WavMerger {
    fun merge(inputStreams: List<InputStream>, outputFile: File)
    fun parseWavHeader(inputStream: InputStream): WavHeader
}
```

<!-- Plan 02가 소비할 Mp3Merger 공개 API (이 Plan에서 생성) -->

```kotlin
object Mp3Merger {
    /**
     * 여러 MP3 파일을 하나의 MP3 파일로 합친다 (재인코딩 없음).
     *
     * @param inputPaths 합칠 MP3 파일들의 절대 경로 (순서대로 이어붙임)
     * @param outputFile 합쳐진 결과 MP3 파일
     * @throws IllegalArgumentException MP3 frame header가 없거나 bitrate/sampleRate/channel이 불일치할 때
     * @throws java.io.IOException 파일 읽기/쓰기 실패 시
     */
    fun merge(inputPaths: List<String>, outputFile: File)
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mp3Merger 유틸리티 + 단위 테스트 신규 생성</name>
  <files>
    app/src/main/java/com/autominuting/util/Mp3Merger.kt
    app/src/test/java/com/autominuting/util/Mp3MergerTest.kt
  </files>
  <read_first>
    - app/src/main/java/com/autominuting/util/WavMerger.kt (object 패턴, fmt 검증 로직, merge 시그니처를 그대로 본딸 것)
    - app/src/test/java/com/autominuting/util/WavMergerTest.kt (JUnit 4 vs 5 사용 컨벤션 확인, MergerTest 네이밍 패턴)
    - .planning/phases/53-mp3/53-RESEARCH.md (의사코드, synchsafe 파싱, frame header 파싱, Pitfall 6 ID3v2 footer)
    - app/src/main/java/com/autominuting/util/AudioMerger.kt 존재 여부만 확인 (파일 경로 구조 파악)
  </read_first>
  <behavior>
    - Test 1 (ID3v2 스트립): "ID3" + synchsafe size(10 bytes payload) + MP3 frame bytes 형태의 ByteArray를 파일로 저장 → Mp3Merger.merge(listOf(pathA, pathA), output) 호출 → output에 첫 파일의 ID3v2(10+10=20 bytes)는 포함되고 두 번째 파일의 ID3v2는 제외됨 (총 길이 = 20 + frames_A + frames_A)
    - Test 2 (ID3v1 트레일러 스트립): 파일 끝 "TAG"+125 bytes 트레일러가 있는 MP3 파일을 입력 → output에서 모든 입력 파일의 ID3v1 트레일러가 제거됨
    - Test 3 (태그 없는 순수 frame): ID3 태그가 전혀 없는 MP3 파일들을 merge → 입력 전체 바이트의 합과 output 크기가 동일
    - Test 4 (frame header 불일치 실패): 44100Hz MP3와 48000Hz MP3를 merge 시도 → IllegalArgumentException throw
    - Test 5 (빈 리스트 실패): Mp3Merger.merge(emptyList(), output) → IllegalArgumentException throw
    - Test 6 (frame sync 없음 실패): 첫 4바이트가 0xFF 0xE0 패턴이 아닌 파일 → IllegalArgumentException throw
  </behavior>
  <action>
    `app/src/main/java/com/autominuting/util/Mp3Merger.kt` 파일을 다음 구조로 신규 생성한다:

    ```kotlin
    package com.autominuting.util

    import java.io.File

    /**
     * MP3 프레임 헤더에서 추출한 오디오 포맷 정보.
     * frame concat 전 포맷 일치 검증용.
     */
    data class Mp3FrameInfo(
        val mpegVersion: Int,      // 00=2.5, 01=reserved, 10=MPEG2, 11=MPEG1
        val layer: Int,            // 00=reserved, 01=Layer3, 10=Layer2, 11=Layer1
        val bitrateIdx: Int,       // 0..15 (11=reserved에 주의)
        val sampleRateIdx: Int,    // 0..3 (11=reserved)
        val channelMode: Int       // 00=Stereo, 01=JointStereo, 10=Dual, 11=Mono
    )

    /**
     * 여러 MP3 파일을 재인코딩 없이 하나의 MP3 파일로 합치는 유틸리티.
     *
     * - ID3v2 헤더는 첫 번째 파일의 것만 유지 (플레이어 메타데이터 호환).
     * - 이후 파일들의 ID3v2/ID3v1 태그는 제거하고 MP3 프레임 바이트만 concat.
     * - 모든 입력 파일의 첫 frame header에서 version/layer/bitrateIdx/sampleRateIdx/channelMode가 일치해야 한다.
     * - MediaMuxer/MediaCodec을 사용하지 않으므로 Android Framework 의존성 없음 (JVM 단위 테스트 가능).
     */
    object Mp3Merger {

        fun merge(inputPaths: List<String>, outputFile: File) {
            require(inputPaths.isNotEmpty()) { "합칠 MP3 파일이 없습니다" }

            // 1. 모든 파일 읽기 + 태그 경계 계산 + frame header 파싱
            val parsed = inputPaths.mapIndexed { idx, path ->
                val bytes = File(path).readBytes()
                val (audioStart, audioEnd) = stripTags(bytes)
                val frameInfo = parseFrameHeader(bytes, audioStart)
                    ?: throw IllegalArgumentException(
                        "파일 ${idx + 1}에서 MP3 frame sync를 찾을 수 없습니다 (offset=$audioStart)"
                    )
                Triple(bytes, audioStart to audioEnd, frameInfo)
            }

            // 2. frame header 일치 검증 (첫 파일 기준)
            val first = parsed.first().third
            parsed.drop(1).forEachIndexed { idx, (_, _, info) ->
                require(
                    info.mpegVersion == first.mpegVersion &&
                        info.layer == first.layer &&
                        info.bitrateIdx == first.bitrateIdx &&
                        info.sampleRateIdx == first.sampleRateIdx &&
                        info.channelMode == first.channelMode
                ) {
                    "파일 ${idx + 2}의 MP3 포맷이 첫 번째 파일과 다릅니다 " +
                        "(version=${info.mpegVersion}/${first.mpegVersion}, " +
                        "layer=${info.layer}/${first.layer}, " +
                        "bitrateIdx=${info.bitrateIdx}/${first.bitrateIdx}, " +
                        "sampleRateIdx=${info.sampleRateIdx}/${first.sampleRateIdx}, " +
                        "channelMode=${info.channelMode}/${first.channelMode})"
                }
            }

            // 3. 출력 파일 작성
            outputFile.outputStream().buffered().use { out ->
                parsed.forEachIndexed { idx, (bytes, range, _) ->
                    val (audioStart, audioEnd) = range
                    if (idx == 0) {
                        // 첫 파일: ID3v2 태그 유지 (0 ~ audioEnd), ID3v1 트레일러는 이미 audioEnd에서 잘림
                        out.write(bytes, 0, audioEnd)
                    } else {
                        // 이후 파일: 태그 제거, audio frame만
                        out.write(bytes, audioStart, audioEnd - audioStart)
                    }
                }
            }
        }

        /**
         * ID3v2 헤더와 ID3v1 트레일러 경계를 계산한다.
         * @return Pair(audioStart, audioEnd) — audioEnd는 exclusive (audioEnd - audioStart = 유효 MP3 바이트 수)
         */
        internal fun stripTags(bytes: ByteArray): Pair<Int, Int> {
            var start = 0
            var end = bytes.size

            // ID3v2 헤더 감지 (파일 시작의 "ID3" 3바이트 magic)
            if (bytes.size > 10 &&
                bytes[0] == 'I'.code.toByte() &&
                bytes[1] == 'D'.code.toByte() &&
                bytes[2] == '3'.code.toByte()
            ) {
                val size = synchsafeToInt(bytes, 6)
                // flags 바이트(bytes[5])의 bit 4 = footer present → +10 바이트
                val hasFooter = (bytes[5].toInt() and 0x10) != 0
                start = 10 + size + if (hasFooter) 10 else 0
                require(start <= bytes.size) { "ID3v2 size가 파일 크기를 초과합니다 (size=$size, file=${bytes.size})" }
            }

            // ID3v1 트레일러 감지 (파일 끝 128바이트, "TAG" 3바이트 magic)
            if (bytes.size >= 128 &&
                bytes[bytes.size - 128] == 'T'.code.toByte() &&
                bytes[bytes.size - 127] == 'A'.code.toByte() &&
                bytes[bytes.size - 126] == 'G'.code.toByte()
            ) {
                end = bytes.size - 128
            }

            require(end > start) { "유효한 MP3 오디오 데이터가 없습니다 (start=$start, end=$end)" }
            return start to end
        }

        /**
         * synchsafe 32-bit 정수를 읽는다 (ID3v2 size 필드용).
         * 각 바이트의 MSB=0, 7비트씩 유효. bytes[offset..offset+3] 사용.
         */
        internal fun synchsafeToInt(bytes: ByteArray, offset: Int): Int {
            return ((bytes[offset].toInt() and 0x7F) shl 21) or
                ((bytes[offset + 1].toInt() and 0x7F) shl 14) or
                ((bytes[offset + 2].toInt() and 0x7F) shl 7) or
                (bytes[offset + 3].toInt() and 0x7F)
        }

        /**
         * MP3 frame header를 파싱한다.
         * 4바이트 header: AAAAAAAA AAABBCCD EEEEFFGH IIJJKLMM
         *   A=sync(11), B=version(2), C=layer(2), D=CRC(1)
         *   E=bitrate(4), F=samplerate(2), G=padding(1), H=private(1)
         *   I=channel(2), J=modeExt(2), K=copyright(1), L=original(1), M=emphasis(2)
         *
         * @return Mp3FrameInfo 또는 null (frame sync 불일치 시)
         */
        internal fun parseFrameHeader(bytes: ByteArray, offset: Int): Mp3FrameInfo? {
            if (offset + 4 > bytes.size) return null
            val b0 = bytes[offset].toInt() and 0xFF
            val b1 = bytes[offset + 1].toInt() and 0xFF
            val b2 = bytes[offset + 2].toInt() and 0xFF
            val b3 = bytes[offset + 3].toInt() and 0xFF
            // frame sync: 11 bits = 0xFFE
            if (b0 != 0xFF || (b1 and 0xE0) != 0xE0) return null
            return Mp3FrameInfo(
                mpegVersion = (b1 shr 3) and 0x03,
                layer = (b1 shr 1) and 0x03,
                bitrateIdx = (b2 shr 4) and 0x0F,
                sampleRateIdx = (b2 shr 2) and 0x03,
                channelMode = (b3 shr 6) and 0x03
            )
        }
    }
    ```

    그리고 `app/src/test/java/com/autominuting/util/Mp3MergerTest.kt`를 다음 구조로 생성한다:

    ```kotlin
    package com.autominuting.util

    import org.junit.Assert.assertEquals
    import org.junit.Assert.assertThrows
    import org.junit.Rule
    import org.junit.Test
    import org.junit.rules.TemporaryFolder
    import java.io.File

    class Mp3MergerTest {

        @get:Rule
        val tempFolder = TemporaryFolder()

        // MPEG1 Layer3 128kbps 44100Hz Stereo frame header: FF FB 90 00
        private val frameHeader44100Stereo = byteArrayOf(0xFF.toByte(), 0xFB.toByte(), 0x90.toByte(), 0x00)
        // MPEG1 Layer3 128kbps 48000Hz Stereo frame header: FF FB 94 00 (sampleRateIdx=01)
        private val frameHeader48000Stereo = byteArrayOf(0xFF.toByte(), 0xFB.toByte(), 0x94.toByte(), 0x00)

        private fun fakeMp3Frames(header: ByteArray, payloadSize: Int): ByteArray {
            return header + ByteArray(payloadSize) { (it and 0xFF).toByte() }
        }

        private fun id3v2Tag(payloadSize: Int): ByteArray {
            // "ID3" + version(2) + flags(1) + synchsafe size(4)
            val header = byteArrayOf(
                'I'.code.toByte(), 'D'.code.toByte(), '3'.code.toByte(),
                0x03, 0x00, 0x00,
                ((payloadSize shr 21) and 0x7F).toByte(),
                ((payloadSize shr 14) and 0x7F).toByte(),
                ((payloadSize shr 7) and 0x7F).toByte(),
                (payloadSize and 0x7F).toByte()
            )
            return header + ByteArray(payloadSize) { 0x41 }  // 'A'
        }

        private fun id3v1Tag(): ByteArray {
            return byteArrayOf('T'.code.toByte(), 'A'.code.toByte(), 'G'.code.toByte()) + ByteArray(125) { 0x20 }
        }

        private fun writeFile(name: String, bytes: ByteArray): String {
            val f = tempFolder.newFile(name)
            f.writeBytes(bytes)
            return f.absolutePath
        }

        @Test
        fun `첫 파일의 ID3v2 태그는 유지되고 두 번째 파일의 ID3v2는 제거된다`() {
            val framesA = fakeMp3Frames(frameHeader44100Stereo, 100)
            val fileA = writeFile("a.mp3", id3v2Tag(10) + framesA)
            val fileB = writeFile("b.mp3", id3v2Tag(10) + framesA)
            val out = File(tempFolder.root, "out.mp3")

            Mp3Merger.merge(listOf(fileA, fileB), out)

            // 첫 파일 전체(10+10+104) + 두 번째 파일 frames만(104) = 228
            assertEquals((10 + 10 + 104) + 104, out.length().toInt())
        }

        @Test
        fun `모든 파일의 ID3v1 트레일러는 제거된다`() {
            val framesA = fakeMp3Frames(frameHeader44100Stereo, 200)
            val fileA = writeFile("a.mp3", framesA + id3v1Tag())
            val fileB = writeFile("b.mp3", framesA + id3v1Tag())
            val out = File(tempFolder.root, "out.mp3")

            Mp3Merger.merge(listOf(fileA, fileB), out)

            // frames_A(204) + frames_A(204), ID3v1(128) 2개 모두 제거
            assertEquals(204 + 204, out.length().toInt())
        }

        @Test
        fun `태그가 없는 순수 frame 파일들의 concat 크기는 입력 합과 같다`() {
            val framesA = fakeMp3Frames(frameHeader44100Stereo, 300)
            val fileA = writeFile("a.mp3", framesA)
            val fileB = writeFile("b.mp3", framesA)
            val out = File(tempFolder.root, "out.mp3")

            Mp3Merger.merge(listOf(fileA, fileB), out)

            assertEquals(304 + 304, out.length().toInt())
        }

        @Test
        fun `sampleRate가 다른 파일들은 IllegalArgumentException`() {
            val fileA = writeFile("a.mp3", fakeMp3Frames(frameHeader44100Stereo, 100))
            val fileB = writeFile("b.mp3", fakeMp3Frames(frameHeader48000Stereo, 100))
            val out = File(tempFolder.root, "out.mp3")

            assertThrows(IllegalArgumentException::class.java) {
                Mp3Merger.merge(listOf(fileA, fileB), out)
            }
        }

        @Test
        fun `빈 리스트는 IllegalArgumentException`() {
            val out = File(tempFolder.root, "out.mp3")
            assertThrows(IllegalArgumentException::class.java) {
                Mp3Merger.merge(emptyList(), out)
            }
        }

        @Test
        fun `frame sync가 없는 파일은 IllegalArgumentException`() {
            val garbage = ByteArray(100) { 0x00 }
            val fileA = writeFile("a.mp3", garbage)
            val out = File(tempFolder.root, "out.mp3")
            assertThrows(IllegalArgumentException::class.java) {
                Mp3Merger.merge(listOf(fileA), out)
            }
        }
    }
    ```

    RED→GREEN 순서:
    1. Mp3MergerTest.kt 먼저 작성, `./gradlew :app:testDebugUnitTest --tests com.autominuting.util.Mp3MergerTest` 실행 → 컴파일 실패(Mp3Merger 미존재) 확인.
    2. Mp3Merger.kt를 위 구현대로 작성.
    3. 테스트 재실행 → 6개 테스트 모두 pass 확인.

    주의사항:
    - `junit` (JUnit 4) 사용 — WavMergerTest.kt의 import와 동일하게 `org.junit.Test`, `org.junit.Assert.*` 기반.
    - `TemporaryFolder`로 임시 파일 관리 (tearDown 불필요).
    - `android.util.Log` 사용 금지 — 이 object는 순수 JVM 단위 테스트여야 함 (testOptions.isReturnDefaultValues 의존 회피).
    - WavMerger 패턴과의 차이: WavMerger는 InputStream 리스트 받지만 Mp3Merger는 절대 경로 리스트 받음 (bytes 전체를 메모리에 올려 ID3v1 트레일러를 파일 끝에서 역방향 탐색해야 하므로 스트리밍보다 전체 read가 단순).
  </action>
  <verify>
    <automated>cd /d/workspace/temp/Auto_Minuting &amp;&amp; ./gradlew :app:testDebugUnitTest --tests "com.autominuting.util.Mp3MergerTest"</automated>
  </verify>
  <acceptance_criteria>
    - `app/src/main/java/com/autominuting/util/Mp3Merger.kt` 파일이 존재하고 `object Mp3Merger` 선언 포함
    - Mp3Merger.kt가 `fun merge(inputPaths: List<String>, outputFile: File)` 공개 함수 포함 (grep: `fun merge\(inputPaths: List<String>`)
    - Mp3Merger.kt가 `import android.media` 를 포함하지 않음 (grep -L 결과 확인 — 순수 JVM)
    - Mp3Merger.kt가 `synchsafeToInt` 함수 포함 (grep: `fun synchsafeToInt`)
    - Mp3Merger.kt가 `parseFrameHeader` 함수 포함 (grep: `fun parseFrameHeader`)
    - Mp3Merger.kt가 `data class Mp3FrameInfo` 포함 (grep: `data class Mp3FrameInfo`)
    - `app/src/test/java/com/autominuting/util/Mp3MergerTest.kt` 파일이 존재
    - Mp3MergerTest.kt가 최소 6개 `@Test` 함수 포함 (grep -c `@Test` >= 6)
    - `./gradlew :app:testDebugUnitTest --tests com.autominuting.util.Mp3MergerTest` 명령이 exit 0으로 종료되고 출력에 `BUILD SUCCESSFUL` 포함
    - 테스트 실행 로그에 `6 tests completed, 0 failed` 또는 동등 표현 존재
  </acceptance_criteria>
  <done>
    Mp3Merger 유틸이 생성되고 6개 단위 테스트가 모두 통과한다. Plan 02에서 ShareReceiverActivity가 Mp3Merger.merge()를 호출할 준비가 완료된다.
  </done>
</task>

</tasks>

<verification>
- `./gradlew :app:testDebugUnitTest --tests com.autominuting.util.Mp3MergerTest` BUILD SUCCESSFUL
- Mp3Merger.kt가 Android Framework API(android.media.*, android.util.*) import를 포함하지 않음 → 순수 JVM
- WavMerger.kt 패턴과 object 선언 + merge() 시그니처 일관성 유지
</verification>

<success_criteria>
1. Mp3Merger 단위 테스트 6개 모두 통과
2. MERGE-04의 "재인코딩 없이" 제약을 충족하는 순수 바이트 concat 로직 존재
3. Frame header 불일치 시 명시적 IllegalArgumentException으로 실패 (Plan 02의 Toast 에러 메시지 경로 확보)
4. Plan 02에서 `import com.autominuting.util.Mp3Merger`로 바로 사용 가능
</success_criteria>

<output>
After completion, create `.planning/phases/53-mp3/53-01-SUMMARY.md` with:
- Mp3Merger 공개 API (merge 시그니처)
- 검증된 태그 처리 정책 (첫 파일 ID3v2 유지, 이후 제거, 모든 파일 ID3v1 제거)
- Frame header 일치 검증 규칙 (version/layer/bitrateIdx/sampleRateIdx/channelMode)
- Plan 02에서 사용할 호출 예시
</output>
