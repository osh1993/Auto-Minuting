---
phase: 13-plaud-ble
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - app/src/main/java/com/autominuting/data/repository/AudioRepositoryImpl.kt
  - app/src/main/java/com/autominuting/presentation/dashboard/DashboardViewModel.kt
  - app/src/main/java/com/autominuting/presentation/dashboard/DashboardScreen.kt
autonomous: true
requirements: [PLUD-01]

must_haves:
  truths:
    - "AudioRepositoryImpl이 getFileList()로 실제 sessionId를 획득하여 exportAudio에 전달한다"
    - "DashboardScreen에 BLE 연결 상태가 실시간으로 표시된다"
    - "BLE 디버그 이벤트 로그가 UI에서 확인 가능하다"
  artifacts:
    - path: "app/src/main/java/com/autominuting/data/repository/AudioRepositoryImpl.kt"
      provides: "SDK getFileList 연동, 실제 세션 기반 오디오 수집"
      contains: "getFileList"
    - path: "app/src/main/java/com/autominuting/presentation/dashboard/DashboardScreen.kt"
      provides: "BLE 상태 표시 UI"
      contains: "connectionState"
  key_links:
    - from: "AudioRepositoryImpl.kt"
      to: "PlaudSdkManager.getFileList()"
      via: "sessionId 획득 후 exportAudio 호출"
      pattern: "getFileList"
    - from: "DashboardScreen.kt"
      to: "DashboardViewModel"
      via: "connectionState StateFlow 구독"
      pattern: "connectionState"
---

<objective>
AudioRepositoryImpl의 하드코딩된 "latest" sessionId를 실제 getFileList() API로 교체하고, DashboardScreen에 BLE 연결 상태 및 디버그 로그를 표시한다.

Purpose: sessionId "latest"는 실제 SDK에서 동작하지 않으며, BLE 디버깅에는 실시간 상태 확인이 필수다.
Output: AudioRepositoryImpl.kt (수정), DashboardViewModel.kt (수정), DashboardScreen.kt (수정)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-plaud-ble/13-RESEARCH.md
@.planning/phases/13-plaud-ble/13-01-SUMMARY.md

@app/src/main/java/com/autominuting/data/repository/AudioRepositoryImpl.kt
@app/src/main/java/com/autominuting/presentation/dashboard/DashboardScreen.kt
@app/src/main/java/com/autominuting/presentation/dashboard/DashboardViewModel.kt

<interfaces>
<!-- Plan 01에서 생성/수정된 인터페이스 -->
From PlaudSdkManager.kt (Plan 01 이후):
```kotlin
@Singleton
class PlaudSdkManager @Inject constructor(@ApplicationContext context: Context) {
    val connectionState: StateFlow<BleConnectionState>
    var onProgressUpdate: ((Float) -> Unit)?
    fun initialize(appKey: String, appSecret: String)
    suspend fun scanAndConnect()
    suspend fun getFileList(): List<String>
    suspend fun exportAudio(sessionId: String, outputDir: File, format: String = "WAV"): String
    fun disconnect()
    fun isAvailable(): Boolean
}
```

From PlaudBleAgentListener.kt (Plan 01 신규):
```kotlin
enum class BleConnectionState {
    IDLE, SCANNING, DEVICE_FOUND, CONNECTING, CONNECTED, DISCONNECTED, ERROR
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AudioRepositoryImpl에 getFileList 연동 및 세션 기반 수집</name>
  <files>app/src/main/java/com/autominuting/data/repository/AudioRepositoryImpl.kt</files>
  <read_first>
    - app/src/main/java/com/autominuting/data/repository/AudioRepositoryImpl.kt
    - app/src/main/java/com/autominuting/data/audio/PlaudSdkManager.kt (Plan 01에서 수정된 버전)
  </read_first>
  <action>
    AudioRepositoryImpl.startAudioCollection()의 SDK BLE 경로를 수정한다.

    1. 하드코딩된 `val sessionId = "latest"` 를 제거한다.
    2. scanAndConnect() 성공 후 getFileList()를 호출하여 세션 목록을 가져온다:
       ```kotlin
       val sessions = plaudSdkManager.getFileList()
       if (sessions.isEmpty()) {
           throw PlaudSdkException("녹음기에 다운로드 가능한 파일이 없습니다")
       }
       ```
    3. 최신 세션(마지막 항목)을 선택하여 exportAudio에 전달한다:
       ```kotlin
       val latestSession = sessions.last()
       val filePath = plaudSdkManager.exportAudio(
           sessionId = latestSession,
           outputDir = outputDir,
           format = "WAV"  // 명시적으로 WAV 지정
       )
       ```
    4. format 인자를 "WAV"로 명시적으로 전달한다 (기본값에만 의존하지 않기).
    5. 복수 세션 지원: 모든 세션을 순회하여 각각 exportAudio를 호출하고 send()한다.
       한 세션 실패 시에도 나머지는 계속 처리한다 (try-catch 개별 감싸기).
    6. connectionState를 Flow로 노출하여 UI에서 구독할 수 있게 한다:
       ```kotlin
       fun getBleConnectionState(): StateFlow<BleConnectionState> = plaudSdkManager.connectionState
       ```

    주석은 한국어로 작성한다.
  </action>
  <verify>
    <automated>cd "D:\workspace\temp\Auto_Minuting" && grep -c "getFileList" app/src/main/java/com/autominuting/data/repository/AudioRepositoryImpl.kt</automated>
  </verify>
  <acceptance_criteria>
    - "latest" 하드코딩이 제거되었다
    - getFileList() 호출로 실제 sessionId를 획득한다
    - exportAudio에 format = "WAV"가 명시적으로 전달된다
    - 빈 세션 목록일 때 적절한 에러를 throw한다
    - getBleConnectionState() 메서드가 존재한다
  </acceptance_criteria>
  <done>AudioRepositoryImpl이 실제 SDK 세션 목록에서 sessionId를 가져와 WAV 포맷으로 오디오를 수집하며, BLE 상태를 외부에 노출한다</done>
</task>

<task type="auto">
  <name>Task 2: DashboardScreen에 BLE 연결 상태 및 디버그 로그 표시</name>
  <files>
    app/src/main/java/com/autominuting/presentation/dashboard/DashboardViewModel.kt,
    app/src/main/java/com/autominuting/presentation/dashboard/DashboardScreen.kt
  </files>
  <read_first>
    - app/src/main/java/com/autominuting/presentation/dashboard/DashboardViewModel.kt
    - app/src/main/java/com/autominuting/presentation/dashboard/DashboardScreen.kt
    - app/src/main/java/com/autominuting/data/repository/AudioRepositoryImpl.kt (Task 1에서 수정된 버전)
  </read_first>
  <action>
    DashboardViewModel과 DashboardScreen에 BLE 연결 상태 실시간 표시를 추가한다.

    1. DashboardViewModel에 BLE 상태 노출:
       - AudioRepository(Impl)에서 getBleConnectionState()를 호출하여 StateFlow를 받는다.
         (AudioRepository 인터페이스에 메서드 추가 필요할 수 있음 -- 인터페이스도 확인하고 필요시 추가)
       - `val bleConnectionState: StateFlow<BleConnectionState>` 를 노출한다.

    2. DashboardScreen의 녹음기 연결 카드를 수정:
       - bleConnectionState를 collectAsStateWithLifecycle()으로 구독한다.
       - 연결 상태에 따라 상태 텍스트를 표시한다:
         - IDLE: "대기 중"
         - SCANNING: "기기 검색 중..."
         - DEVICE_FOUND: "기기 발견됨 - 연결 중..."
         - CONNECTING: "연결 중..."
         - CONNECTED: "연결됨" (primaryContainer 색상)
         - DISCONNECTED: "연결 끊어짐"
         - ERROR: "오류: {reason}" (errorContainer 색상)
       - 상태 아이콘/색상을 BleConnectionState에 따라 분기한다.

    3. BLE 디버그 로그 섹션 (테스트 도구 카드 아래에 추가):
       - "BLE 로그" 타이틀의 Card를 추가한다.
       - PlaudBleAgentListener에서 로그 이벤트를 수집하여 최근 20건을 표시한다.
         (ViewModel에서 bleConnectionState의 변화를 감지하여 로그 리스트에 타임스탬프와 함께 추가)
       - 각 로그 항목: "[HH:mm:ss] 상태변경: SCANNING -> DEVICE_FOUND" 형태
       - LazyColumn 또는 Column으로 표시. 최신 항목이 상단.
       - "로그 지우기" 버튼 포함.

    주석은 한국어로 작성한다.
  </action>
  <verify>
    <automated>cd "D:\workspace\temp\Auto_Minuting" && grep -c "connectionState\|BleConnectionState" app/src/main/java/com/autominuting/presentation/dashboard/DashboardScreen.kt</automated>
  </verify>
  <acceptance_criteria>
    - DashboardViewModel에 bleConnectionState가 StateFlow로 노출된다
    - DashboardScreen의 녹음기 연결 카드에 BLE 상태 텍스트가 표시된다
    - 상태별로 다른 텍스트/색상이 적용된다 (최소 IDLE, SCANNING, CONNECTED, ERROR 분기)
    - BLE 로그 섹션이 추가되어 최근 이벤트를 확인할 수 있다
  </acceptance_criteria>
  <done>DashboardScreen에서 BLE 연결 상태(IDLE/SCANNING/CONNECTED/ERROR 등)가 실시간으로 표시되고, 디버그 로그 섹션에서 BLE 이벤트 이력을 확인할 수 있다</done>
</task>

</tasks>

<verification>
- AudioRepositoryImpl.kt에서 "latest" 하드코딩이 없다
- AudioRepositoryImpl.kt에서 getFileList 호출이 있다
- DashboardScreen.kt에서 BleConnectionState 참조가 있다
- DashboardViewModel.kt에서 connectionState 노출이 있다
</verification>

<success_criteria>
- SDK 파일 목록 API를 통해 실제 sessionId를 획득한다
- DashboardScreen에서 BLE 연결 상태를 실시간으로 확인할 수 있다
- BLE 디버그 로그가 UI에서 시간순으로 표시된다
</success_criteria>

<output>
After completion, create `.planning/phases/13-plaud-ble/13-02-SUMMARY.md`
</output>
