---
phase: 48-api-dashboard
plan: 48-01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/com/autominuting/data/quota/ApiUsageTracker.kt
  - app/src/main/java/com/autominuting/data/quota/ApiUsageState.kt
  - app/src/main/java/com/autominuting/data/stt/GeminiSttEngine.kt
  - app/src/main/java/com/autominuting/data/stt/GroqSttEngine.kt
  - app/src/main/java/com/autominuting/data/stt/DeepgramSttEngine.kt
  - app/src/main/java/com/autominuting/data/stt/NaverClovaSttEngine.kt
  - app/src/main/java/com/autominuting/data/stt/WhisperEngine.kt
  - app/src/main/java/com/autominuting/data/repository/MinutesRepositoryImpl.kt
  - app/src/main/java/com/autominuting/presentation/dashboard/DashboardViewModel.kt
  - app/src/main/java/com/autominuting/presentation/dashboard/DashboardScreen.kt
autonomous: true
requirements:
  - DASH-01
gap_closure: false

must_haves:
  truths:
    - "사용자가 DashboardScreen에서 STT 엔진별 API 호출 횟수를 확인할 수 있다"
    - "사용자가 DashboardScreen에서 Minutes 엔진별 API 호출 횟수를 확인할 수 있다"
    - "사용자가 엔진별 예상 비용(USD)을 확인할 수 있다"
    - "호출 수가 0인 엔진은 카드에 표시되지 않는다"
    - "호출 기록이 전혀 없으면 '아직 API 사용 기록이 없습니다' 메시지가 표시된다"
    - "실제 API 호출 성공 시 ApiUsageTracker.record()가 호출된다"
    - "기존 GeminiQuotaTracker 기반 일일 쿼터 경고 배너는 그대로 동작한다"
  artifacts:
    - path: "app/src/main/java/com/autominuting/data/quota/ApiUsageTracker.kt"
      provides: "엔진별 누적 API 호출 횟수 DataStore 기반 추적기"
      exports: ["ApiUsageTracker", "companion constants (KEY_GEMINI_STT 등)"]
    - path: "app/src/main/java/com/autominuting/data/quota/ApiUsageState.kt"
      provides: "UI용 엔진별 통계 데이터 모델"
      exports: ["ApiUsageState", "EngineCallStat"]
    - path: "app/src/main/java/com/autominuting/presentation/dashboard/DashboardViewModel.kt"
      provides: "apiUsageState StateFlow 노출"
      contains: "apiUsageState: StateFlow<ApiUsageState>"
    - path: "app/src/main/java/com/autominuting/presentation/dashboard/DashboardScreen.kt"
      provides: "엔진별 API 사용량 카드 UI"
      contains: "API 사용량 요약"
  key_links:
    - from: "GeminiSttEngine.kt (성공 분기)"
      to: "ApiUsageTracker.record(KEY_GEMINI_STT)"
      via: "apiUsageTracker.record() 호출"
    - from: "GroqSttEngine.kt (callResult != null 분기)"
      to: "ApiUsageTracker.record(KEY_GROQ_STT)"
      via: "apiUsageTracker.record() 호출"
    - from: "DeepgramSttEngine.kt (Result.success 반환 전)"
      to: "ApiUsageTracker.record(KEY_DEEPGRAM_STT)"
      via: "apiUsageTracker.record() 호출"
    - from: "NaverClovaSttEngine.kt (Result.success 반환 전)"
      to: "ApiUsageTracker.record(KEY_NAVER_STT)"
      via: "apiUsageTracker.record() 호출"
    - from: "WhisperEngine.kt (Result.success 반환 전)"
      to: "ApiUsageTracker.record(KEY_WHISPER_STT)"
      via: "apiUsageTracker.record() 호출"
    - from: "MinutesRepositoryImpl.kt (quotaTracker.recordUsage 아래)"
      to: "ApiUsageTracker.record(엔진별 key)"
      via: "userPreferencesRepository.getMinutesEngineTypeOnce() 기반 when(MinutesEngineType) 분기"
    - from: "DashboardViewModel.apiUsageState"
      to: "DashboardScreen ApiUsageCard"
      via: "collectAsStateWithLifecycle()"
---

<objective>
엔진별 API 사용량 추적 레이어를 구축하고 대시보드에 사용량 요약 카드를 표시한다.

Purpose: 사용자가 STT/Minutes 각 엔진의 누적 API 호출 횟수와 예상 비용을 한눈에 파악하여 유료 엔진 사용량을 모니터링할 수 있다.
Output: ApiUsageTracker(신규), ApiUsageState(신규), 5개 STT 엔진 + MinutesRepositoryImpl 호출 기록 추가, DashboardViewModel/Screen 교체
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<!-- 핵심 인터페이스 — 실행자가 탐색 없이 바로 사용 가능 -->
<interfaces>
<!-- GeminiQuotaTracker (기존 — 건드리지 않음) -->
From app/src/main/java/com/autominuting/data/quota/GeminiQuotaTracker.kt:
```kotlin
@Singleton
class GeminiQuotaTracker @Inject constructor(
    private val dataStore: DataStore<Preferences>
)
// 기존 DataStore 키: "gemini_quota_date", "gemini_quota_stt_count", "gemini_quota_minutes_count"
```

<!-- GeminiSttEngine 성공 분기 (Line 189) -->
From GeminiSttEngine.kt:
```kotlin
// Line 189: 성공 직전
quotaTracker.recordUsage(QuotaCategory.STT)
return@withContext Result.success(text)
```

<!-- GroqSttEngine 성공 분기 (Line 152~157) -->
From GroqSttEngine.kt:
```kotlin
// callResult가 null이 아닐 때 return@withContext callResult
// 즉 "if (callResult != null) { return@withContext callResult }" 직전,
// callResult = Result.success(text) 할당 직후에 record() 삽입
```

<!-- DeepgramSttEngine 성공 분기 (Line 141) -->
From DeepgramSttEngine.kt:
```kotlin
Log.d(TAG, "Deepgram STT 전사 완료: ${text.length}자 (시도 $attempt)")
return@withContext Result.success(text)   // <- 이 return 직전에 record() 삽입
```

<!-- NaverClovaSttEngine 성공 분기 (Line 175) -->
From NaverClovaSttEngine.kt:
```kotlin
Log.d(TAG, "CLOVA Speech 전사 완료: ${text.length}자 (시도 $attempt)")
return@withContext Result.success(text)   // <- 이 return 직전에 record() 삽입
```

<!-- WhisperEngine 클래스 시그니처 -->
From WhisperEngine.kt:
```kotlin
@Singleton
class WhisperEngine @Inject constructor(
    @ApplicationContext private val context: Context,
    private val audioConverter: AudioConverter
) : SttEngine
// engineName(): String = 구현 찾아서 확인 후 성공 반환 직전에 record() 삽입
```

<!-- MinutesRepositoryImpl 성공 분기 (Line 73~75) -->
From MinutesRepositoryImpl.kt:
```kotlin
// 성공 분기 (Line 71~75):
if (result.isSuccess) {
    val minutesText = result.getOrThrow()
    quotaTracker.recordUsage(QuotaCategory.MINUTES)   // <- 이 아래에 apiUsageTracker.record() 추가
    return@withContext Result.success(minutesText)
}
// minutesEngine.engineName() 반환값:
// GeminiMinutesEngine → "Gemini" (확인 후 정확한 문자열 사용)
// DeepgramMinutesEngine → "Deepgram"
// NaverClovaMinutesEngine → "NaverClova"
```

<!-- DashboardViewModel 현재 구조 -->
From DashboardViewModel.kt:
```kotlin
@HiltViewModel
class DashboardViewModel @Inject constructor(
    private val meetingRepository: MeetingRepository,
    private val userPreferencesRepository: UserPreferencesRepository,
    private val promptTemplateRepository: PromptTemplateRepository,
    private val quotaTracker: GeminiQuotaTracker,   // 유지
    @ApplicationContext private val context: Context
    // apiUsageTracker: ApiUsageTracker 추가 필요
) : ViewModel()
// quotaUsage: StateFlow<QuotaUsage> 유지, apiUsageState: StateFlow<ApiUsageState> 추가
```

<!-- DashboardScreen 기존 Gemini 사용량 카드 위치 (Line 276~321) -->
From DashboardScreen.kt:
```kotlin
// Line 276~321: "Gemini API 사용량" Card — 이 Card 전체를 새 ApiUsageCard Composable로 교체
// quotaUsage 수집은 유지 (Line 71: val quotaUsage by viewModel.quotaUsage...)
// apiUsageState 추가 수집 필요
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ApiUsageTracker + ApiUsageState 신규 생성</name>
  <files>
    app/src/main/java/com/autominuting/data/quota/ApiUsageTracker.kt
    app/src/main/java/com/autominuting/data/quota/ApiUsageState.kt
  </files>
  <action>
두 파일을 신규 생성한다.

**ApiUsageTracker.kt** — `com.autominuting.data.quota` 패키지, `@Singleton` 클래스:

```kotlin
package com.autominuting.data.quota

import androidx.datastore.core.DataStore
import androidx.datastore.preferences.core.Preferences
import androidx.datastore.preferences.core.edit
import androidx.datastore.preferences.core.intPreferencesKey
import kotlinx.coroutines.flow.Flow
import kotlinx.coroutines.flow.map
import javax.inject.Inject
import javax.inject.Singleton

/**
 * 엔진별 API 호출 횟수를 DataStore에 누적 저장한다.
 * GeminiQuotaTracker와 달리 날짜 리셋 없이 총 누적 횟수를 관리한다.
 * DataStore 키: "api_usage_{engineKey}" (intPreferencesKey)
 */
@Singleton
class ApiUsageTracker @Inject constructor(
    private val dataStore: DataStore<Preferences>
) {

    companion object {
        // STT 엔진 키
        const val KEY_GEMINI_STT = "GEMINI_STT"
        const val KEY_WHISPER_STT = "WHISPER_STT"
        const val KEY_GROQ_STT = "GROQ_STT"
        const val KEY_DEEPGRAM_STT = "DEEPGRAM_STT"
        const val KEY_NAVER_STT = "NAVER_STT"

        // Minutes 엔진 키
        const val KEY_GEMINI_MINUTES = "GEMINI_MINUTES"
        const val KEY_DEEPGRAM_MINUTES = "DEEPGRAM_MINUTES"
        const val KEY_NAVER_MINUTES = "NAVER_MINUTES"

        /** 모든 엔진 키 목록 */
        val ALL_KEYS = listOf(
            KEY_GEMINI_STT, KEY_WHISPER_STT, KEY_GROQ_STT, KEY_DEEPGRAM_STT, KEY_NAVER_STT,
            KEY_GEMINI_MINUTES, KEY_DEEPGRAM_MINUTES, KEY_NAVER_MINUTES
        )

        /**
         * 엔진별 예상 단가 (USD/call).
         * 무료 엔진은 0.0, 유료 엔진은 추정 단가 사용.
         */
        val ESTIMATED_COST_PER_CALL: Map<String, Double> = mapOf(
            KEY_GEMINI_STT to 0.0,
            KEY_WHISPER_STT to 0.0,
            KEY_GROQ_STT to 0.001,
            KEY_DEEPGRAM_STT to 0.005,
            KEY_NAVER_STT to 0.003,
            KEY_GEMINI_MINUTES to 0.0,
            KEY_DEEPGRAM_MINUTES to 0.01,
            KEY_NAVER_MINUTES to 0.005
        )

        /** DataStore 키 생성 헬퍼 */
        internal fun prefKey(engineKey: String) =
            intPreferencesKey("api_usage_$engineKey")
    }

    /**
     * 모든 엔진 키 → 호출 횟수 Map을 Flow로 노출한다.
     * 저장되지 않은 엔진은 0으로 반환한다.
     */
    val usageMap: Flow<Map<String, Int>> = dataStore.data.map { prefs ->
        ALL_KEYS.associateWith { key -> prefs[prefKey(key)] ?: 0 }
    }

    /**
     * API 호출 성공 시 해당 엔진의 횟수를 1 증가시킨다.
     * @param engineKey ApiUsageTracker.KEY_* 상수 중 하나
     */
    suspend fun record(engineKey: String) {
        dataStore.edit { prefs ->
            val key = prefKey(engineKey)
            prefs[key] = (prefs[key] ?: 0) + 1
        }
    }

    /**
     * 모든 엔진의 호출 횟수를 0으로 초기화한다 (테스트/개발용).
     */
    suspend fun resetAll() {
        dataStore.edit { prefs ->
            ALL_KEYS.forEach { engineKey -> prefs[prefKey(engineKey)] = 0 }
        }
    }
}
```

**ApiUsageState.kt** — 같은 패키지:

```kotlin
package com.autominuting.data.quota

/**
 * 대시보드 UI용 엔진별 API 사용량 집계 상태.
 */
data class ApiUsageState(
    val sttEngineStats: List<EngineCallStat>,
    val minutesEngineStats: List<EngineCallStat>
) {
    /** 전체 예상 비용 합계 (USD) */
    val totalEstimatedCostUsd: Double
        get() = (sttEngineStats + minutesEngineStats).sumOf { it.estimatedCostUsd }

    /** 사용 기록이 하나도 없는지 여부 */
    val isEmpty: Boolean
        get() = (sttEngineStats + minutesEngineStats).all { it.callCount == 0 }
}

/**
 * 단일 엔진의 호출 통계.
 * @param engineKey ApiUsageTracker.KEY_* 상수
 * @param displayName UI에 표시할 엔진 이름 (한국어 포함)
 * @param callCount 누적 호출 횟수
 * @param estimatedCostUsd 예상 비용 (callCount * costPerCall)
 */
data class EngineCallStat(
    val engineKey: String,
    val displayName: String,
    val callCount: Int,
    val estimatedCostUsd: Double
)
```
  </action>
  <verify>
    <automated>cd d:/workspace/temp/Auto_Minuting && ./gradlew :app:compileDebugKotlin --quiet 2>&1 | grep -E "error:" | head -20 || echo "컴파일 오류 없음"</automated>
    <automated>grep -c "ALL_KEYS" d:/workspace/temp/Auto_Minuting/app/src/main/java/com/autominuting/data/quota/ApiUsageTracker.kt && grep -c "record" d:/workspace/temp/Auto_Minuting/app/src/main/java/com/autominuting/data/quota/ApiUsageTracker.kt && grep -c "isEmpty" d:/workspace/temp/Auto_Minuting/app/src/main/java/com/autominuting/data/quota/ApiUsageState.kt</automated>
  </verify>
  <done>
    ApiUsageTracker.kt, ApiUsageState.kt 파일이 존재하고 컴파일 오류 없이 빌드된다.
    ApiUsageTracker에 KEY_* 상수 8개, ALL_KEYS, usageMap Flow, record(), resetAll() 메서드 포함.
    ApiUsageState에 isEmpty 계산 프로퍼티, EngineCallStat data class 포함.
    grep으로 ALL_KEYS, record(), isEmpty 심볼 존재 확인 완료.
  </done>
</task>

<task type="auto">
  <name>Task 2: 각 STT 엔진 + MinutesRepositoryImpl에 ApiUsageTracker 호출 추가</name>
  <files>
    app/src/main/java/com/autominuting/data/stt/GeminiSttEngine.kt
    app/src/main/java/com/autominuting/data/stt/GroqSttEngine.kt
    app/src/main/java/com/autominuting/data/stt/DeepgramSttEngine.kt
    app/src/main/java/com/autominuting/data/stt/NaverClovaSttEngine.kt
    app/src/main/java/com/autominuting/data/stt/WhisperEngine.kt
    app/src/main/java/com/autominuting/data/repository/MinutesRepositoryImpl.kt
  </files>
  <action>
각 파일의 constructor와 성공 분기에 최소한의 수정을 가한다.

**GeminiSttEngine.kt:**
- constructor에 `private val apiUsageTracker: ApiUsageTracker` 추가 (quotaTracker 뒤)
- Line 189 (`quotaTracker.recordUsage(QuotaCategory.STT)`) 바로 아래에 추가:
  `apiUsageTracker.record(ApiUsageTracker.KEY_GEMINI_STT)`
- import `com.autominuting.data.quota.ApiUsageTracker` 추가

**GroqSttEngine.kt:**
- constructor에 `private val apiUsageTracker: ApiUsageTracker` 추가 (secureApiKeyRepository 뒤)
- `Result.success(text)` 할당(Line 153) 직후, `callResult` 변수에 대입되는 시점에서
  `if (callResult != null)` 블록 내 `return@withContext callResult` 직전에 추가:
  `apiUsageTracker.record(ApiUsageTracker.KEY_GROQ_STT)`
  정확한 위치: `if (callResult != null) {` 블록 안, `return@withContext callResult` 바로 전
- import 추가

**DeepgramSttEngine.kt:**
- constructor에 `private val apiUsageTracker: ApiUsageTracker` 추가
- `Log.d(TAG, "Deepgram STT 전사 완료: ...")` 아래, `return@withContext Result.success(text)` 바로 전에 추가:
  `apiUsageTracker.record(ApiUsageTracker.KEY_DEEPGRAM_STT)`
- import 추가

**NaverClovaSttEngine.kt:**
- constructor에 `private val apiUsageTracker: ApiUsageTracker` 추가
- `Log.d(TAG, "CLOVA Speech 전사 완료: ...")` 아래, `return@withContext Result.success(text)` 바로 전에 추가:
  `apiUsageTracker.record(ApiUsageTracker.KEY_NAVER_STT)`
- import 추가

**WhisperEngine.kt:**
- constructor에 `private val apiUsageTracker: ApiUsageTracker` 추가
- `Log.d(TAG, "전사 완료: ${result.length}자")` (Line 136) 아래, `Result.success(result)` (Line 137) 바로 전에 추가:
  `apiUsageTracker.record(ApiUsageTracker.KEY_WHISPER_STT)`
- import 추가

**MinutesRepositoryImpl.kt:**
- constructor에 `private val apiUsageTracker: ApiUsageTracker`와 `private val userPreferencesRepository: UserPreferencesRepository` 추가 (quotaTracker 뒤)
- Line 74 (`quotaTracker.recordUsage(QuotaCategory.MINUTES)`) 바로 아래에 추가:
  ```kotlin
  val minutesEngineType = userPreferencesRepository.getMinutesEngineTypeOnce()
  val usageKey = when (minutesEngineType) {
      MinutesEngineType.GEMINI -> ApiUsageTracker.KEY_GEMINI_MINUTES
      MinutesEngineType.DEEPGRAM -> ApiUsageTracker.KEY_DEEPGRAM_MINUTES
      MinutesEngineType.NAVER_CLOVA -> ApiUsageTracker.KEY_NAVER_MINUTES
  }
  apiUsageTracker.record(usageKey)
  ```
  주의: `minutesEngine.engineName()`은 MinutesEngineSelector가 주입되어 항상 "MinutesEngineSelector"를 반환하므로 사용하지 않는다. 대신 UserPreferencesRepository에서 현재 설정값을 직접 조회한다.
- import `com.autominuting.data.preferences.UserPreferencesRepository` 추가
- import `com.autominuting.domain.model.MinutesEngineType` 추가
- import `com.autominuting.data.quota.ApiUsageTracker` 추가

모든 수정은 기존 동작을 변경하지 않는다. 성공 경로에만 record()를 추가하고 실패/재시도 경로에는 추가하지 않는다.
  </action>
  <verify>
    <automated>cd /d/workspace/temp/Auto_Minuting && ./gradlew :app:compileDebugKotlin --quiet 2>&1 | grep -E "error:" | head -20 || echo "컴파일 오류 없음"</automated>
  </verify>
  <done>
    6개 파일 모두 컴파일 오류 없이 빌드된다.
    각 파일에서 성공 분기에만 apiUsageTracker.record() 호출이 추가되어 있다.
    기존 quotaTracker.recordUsage() 호출은 그대로 유지된다.
  </done>
</task>

<task type="auto">
  <name>Task 3: DashboardViewModel에 ApiUsageState 추가 + DashboardScreen 카드 교체</name>
  <files>
    app/src/main/java/com/autominuting/presentation/dashboard/DashboardViewModel.kt
    app/src/main/java/com/autominuting/presentation/dashboard/DashboardScreen.kt
  </files>
  <action>
**DashboardViewModel.kt:**

constructor에 `private val apiUsageTracker: ApiUsageTracker` 추가 (quotaTracker 뒤):
```kotlin
@HiltViewModel
class DashboardViewModel @Inject constructor(
    private val meetingRepository: MeetingRepository,
    private val userPreferencesRepository: UserPreferencesRepository,
    private val promptTemplateRepository: PromptTemplateRepository,
    private val quotaTracker: GeminiQuotaTracker,
    private val apiUsageTracker: ApiUsageTracker,   // 추가
    @ApplicationContext private val context: Context
) : ViewModel()
```

`quotaUsage` StateFlow 선언 아래에 `apiUsageState` StateFlow를 추가한다:
```kotlin
/** 엔진별 누적 API 호출 횟수 및 예상 비용 */
val apiUsageState: StateFlow<ApiUsageState> = apiUsageTracker.usageMap
    .map { map ->
        val sttKeys = listOf(
            ApiUsageTracker.KEY_GEMINI_STT to "Gemini STT",
            ApiUsageTracker.KEY_WHISPER_STT to "Whisper (온디바이스)",
            ApiUsageTracker.KEY_GROQ_STT to "Groq Whisper",
            ApiUsageTracker.KEY_DEEPGRAM_STT to "Deepgram",
            ApiUsageTracker.KEY_NAVER_STT to "Naver CLOVA"
        )
        val minutesKeys = listOf(
            ApiUsageTracker.KEY_GEMINI_MINUTES to "Gemini",
            ApiUsageTracker.KEY_DEEPGRAM_MINUTES to "Deepgram",
            ApiUsageTracker.KEY_NAVER_MINUTES to "Naver CLOVA"
        )
        ApiUsageState(
            sttEngineStats = sttKeys.map { (key, name) ->
                val count = map[key] ?: 0
                EngineCallStat(
                    engineKey = key,
                    displayName = name,
                    callCount = count,
                    estimatedCostUsd = count * (ApiUsageTracker.ESTIMATED_COST_PER_CALL[key] ?: 0.0)
                )
            },
            minutesEngineStats = minutesKeys.map { (key, name) ->
                val count = map[key] ?: 0
                EngineCallStat(
                    engineKey = key,
                    displayName = name,
                    callCount = count,
                    estimatedCostUsd = count * (ApiUsageTracker.ESTIMATED_COST_PER_CALL[key] ?: 0.0)
                )
            }
        )
    }
    .stateIn(
        scope = viewModelScope,
        started = SharingStarted.WhileSubscribed(5_000),
        initialValue = ApiUsageState(emptyList(), emptyList())
    )
```

import 추가:
```kotlin
import com.autominuting.data.quota.ApiUsageTracker
import com.autominuting.data.quota.ApiUsageState
import com.autominuting.data.quota.EngineCallStat
```

---

**DashboardScreen.kt:**

1. `quotaUsage` 수집(Line 71) 아래에 추가:
```kotlin
val apiUsageState by viewModel.apiUsageState.collectAsStateWithLifecycle()
```

2. Line 276~321의 "Gemini API 사용량" Card 전체를 `ApiUsageCard` 호출로 교체:
```kotlin
// 엔진별 API 사용량 요약 카드
ApiUsageCard(apiUsageState = apiUsageState)
```

3. 파일 끝(PlaudAudioExtractorWebView 함수 앞)에 private composable 추가:
```kotlin
@Composable
private fun ApiUsageCard(apiUsageState: com.autominuting.data.quota.ApiUsageState) {
    Card(
        modifier = Modifier
            .fillMaxWidth()
            .padding(horizontal = 16.dp),
        colors = CardDefaults.cardColors(
            containerColor = MaterialTheme.colorScheme.surfaceVariant
        )
    ) {
        Column(modifier = Modifier.padding(16.dp)) {
            Text(
                text = "API 사용량 요약",
                style = MaterialTheme.typography.titleMedium
            )
            Spacer(modifier = Modifier.height(8.dp))

            if (apiUsageState.isEmpty) {
                Text(
                    text = "아직 API 사용 기록이 없습니다",
                    style = MaterialTheme.typography.bodySmall,
                    color = MaterialTheme.colorScheme.onSurfaceVariant
                )
            } else {
                // STT 섹션
                val activeSttStats = apiUsageState.sttEngineStats.filter { it.callCount > 0 }
                if (activeSttStats.isNotEmpty()) {
                    Text(
                        text = "STT 전사 엔진",
                        style = MaterialTheme.typography.labelMedium,
                        color = MaterialTheme.colorScheme.primary
                    )
                    Spacer(modifier = Modifier.height(4.dp))
                    activeSttStats.forEach { stat ->
                        EngineStatRow(stat)
                    }
                }

                // Minutes 섹션
                val activeMinutesStats = apiUsageState.minutesEngineStats.filter { it.callCount > 0 }
                if (activeMinutesStats.isNotEmpty()) {
                    if (activeSttStats.isNotEmpty()) {
                        Spacer(modifier = Modifier.height(8.dp))
                        androidx.compose.material3.HorizontalDivider()
                        Spacer(modifier = Modifier.height(8.dp))
                    }
                    Text(
                        text = "회의록 생성 엔진",
                        style = MaterialTheme.typography.labelMedium,
                        color = MaterialTheme.colorScheme.primary
                    )
                    Spacer(modifier = Modifier.height(4.dp))
                    activeMinutesStats.forEach { stat ->
                        EngineStatRow(stat)
                    }
                }

                // 총 예상 비용 (무료만 사용 시 숨김)
                if (apiUsageState.totalEstimatedCostUsd > 0.0) {
                    Spacer(modifier = Modifier.height(8.dp))
                    androidx.compose.material3.HorizontalDivider()
                    Spacer(modifier = Modifier.height(8.dp))
                    Row(
                        modifier = Modifier.fillMaxWidth(),
                        horizontalArrangement = Arrangement.SpaceBetween
                    ) {
                        Text(
                            text = "총 예상 비용",
                            style = MaterialTheme.typography.bodyMedium
                        )
                        Text(
                            text = "$%.4f".format(apiUsageState.totalEstimatedCostUsd),
                            style = MaterialTheme.typography.bodyMedium,
                            color = MaterialTheme.colorScheme.primary
                        )
                    }
                }
            }
        }
    }
}

@Composable
private fun EngineStatRow(stat: com.autominuting.data.quota.EngineCallStat) {
    Row(
        modifier = Modifier
            .fillMaxWidth()
            .padding(vertical = 2.dp),
        horizontalArrangement = Arrangement.SpaceBetween,
        verticalAlignment = Alignment.CenterVertically
    ) {
        Text(
            text = stat.displayName,
            style = MaterialTheme.typography.bodySmall,
            modifier = Modifier.weight(1f)
        )
        Text(
            text = "${stat.callCount}회",
            style = MaterialTheme.typography.bodySmall,
            color = MaterialTheme.colorScheme.onSurfaceVariant
        )
        if (stat.estimatedCostUsd > 0.0) {
            Spacer(modifier = Modifier.width(8.dp))
            Text(
                text = "$%.4f".format(stat.estimatedCostUsd),
                style = MaterialTheme.typography.bodySmall,
                color = MaterialTheme.colorScheme.onSurfaceVariant
            )
        }
    }
}
```

기존 Gemini 쿼터 경고 배너(Line 193~228)와 `quotaUsage` State 수집은 그대로 유지한다.
  </action>
  <verify>
    <automated>cd /d/workspace/temp/Auto_Minuting && ./gradlew assembleDebug --quiet 2>&1 | tail -5</automated>
  </verify>
  <done>
    DashboardViewModel에 apiUsageState: StateFlow&lt;ApiUsageState&gt;가 추가되었다.
    DashboardScreen에 ApiUsageCard composable이 추가되고 기존 Gemini API 사용량 Card가 교체되었다.
    ./gradlew assembleDebug → BUILD SUCCESSFUL.
    기존 Gemini 쿼터 경고 배너는 그대로 동작한다.
  </done>
</task>

</tasks>

<verification>
1. `./gradlew assembleDebug` → BUILD SUCCESSFUL
2. `ApiUsageTracker.kt` 파일에 ALL_KEYS 8개, usageMap Flow, record(), resetAll() 포함 확인
3. `ApiUsageState.kt` 파일에 isEmpty 프로퍼티, EngineCallStat data class 포함 확인
4. 각 STT 엔진 파일에서 `apiUsageTracker.record(ApiUsageTracker.KEY_*_STT)` grep 확인:
   `grep -r "apiUsageTracker.record" app/src/main/java/com/autominuting/data/stt/`
5. MinutesRepositoryImpl에 when 분기 + apiUsageTracker.record() 확인:
   `grep -A5 "usageKey = when" app/src/main/java/com/autominuting/data/repository/MinutesRepositoryImpl.kt`
6. DashboardViewModel에 apiUsageState StateFlow 확인:
   `grep "apiUsageState" app/src/main/java/com/autominuting/presentation/dashboard/DashboardViewModel.kt`
7. DashboardScreen에 ApiUsageCard 호출 확인:
   `grep "ApiUsageCard" app/src/main/java/com/autominuting/presentation/dashboard/DashboardScreen.kt`
</verification>

<success_criteria>
1. `ApiUsageTracker.kt` + `ApiUsageState.kt` 신규 파일 생성 완료
2. GeminiSttEngine, GroqSttEngine, DeepgramSttEngine, NaverClovaSttEngine, WhisperEngine 5개 파일에서
   성공 분기에 `apiUsageTracker.record(KEY_*_STT)` 호출 추가 완료
3. MinutesRepositoryImpl에서 성공 분기에 엔진명 기반 when 분기로 `apiUsageTracker.record()` 호출 추가 완료
4. DashboardViewModel에 `apiUsageState: StateFlow<ApiUsageState>` 추가 완료
5. DashboardScreen의 "Gemini API 사용량" 카드가 "API 사용량 요약" 카드로 교체 완료
   - 호출 수 0인 엔진은 숨김
   - 호출 기록 없으면 "아직 API 사용 기록이 없습니다" 표시
   - 유료 엔진 사용 시 예상 비용 표시
6. `./gradlew assembleDebug` → BUILD SUCCESSFUL
</success_criteria>

<output>
완료 후 `.planning/phases/48-api-dashboard/48-01-SUMMARY.md`를 생성한다.
</output>
