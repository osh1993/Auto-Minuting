---
phase: 10-notebooklm
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gradle/libs.versions.toml
  - app/build.gradle.kts
  - app/src/main/AndroidManifest.xml
  - app/src/main/java/com/autominuting/util/NotebookLmHelper.kt
  - app/src/main/java/com/autominuting/presentation/minutes/MinutesDetailScreen.kt
  - app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt
autonomous: true
requirements: [NLMK-01, NLMK-02]

must_haves:
  truths:
    - "회의록 상세화면에서 NotebookLM 전용 버튼이 표시된다"
    - "NotebookLM 앱 설치 시 직접 Intent로 회의록이 전달된다"
    - "NotebookLM 앱 미설치 시 Custom Tabs로 NotebookLM 웹이 열린다"
    - "설정화면에서 NotebookLM 열기 버튼이 존재한다"
  artifacts:
    - path: "app/src/main/java/com/autominuting/util/NotebookLmHelper.kt"
      provides: "NotebookLM 연동 유틸리티 (앱 설치 확인, 공유 Intent, Custom Tabs)"
      contains: "NOTEBOOKLM_PACKAGE"
    - path: "app/src/main/java/com/autominuting/presentation/minutes/MinutesDetailScreen.kt"
      provides: "NotebookLM 전용 버튼이 TopAppBar actions에 추가"
      contains: "NotebookLmHelper"
    - path: "app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt"
      provides: "NotebookLM 열기 링크 섹션"
      contains: "NotebookLM"
  key_links:
    - from: "MinutesDetailScreen.kt"
      to: "NotebookLmHelper.kt"
      via: "shareToNotebookLm 함수 호출"
      pattern: "NotebookLmHelper\\.shareToNotebookLm"
    - from: "SettingsScreen.kt"
      to: "NotebookLmHelper.kt"
      via: "openNotebookLmWeb 함수 호출"
      pattern: "NotebookLmHelper\\.openNotebookLmWeb"
    - from: "AndroidManifest.xml"
      to: "NotebookLM 앱"
      via: "queries 블록"
      pattern: "com\\.google\\.android\\.apps\\.labs\\.language\\.tailwind"
---

<objective>
NotebookLM 앱 공유 Intent + Custom Tabs 폴백 기능을 구현한다.

Purpose: 사용자가 회의록 상세화면에서 한 번의 탭으로 NotebookLM에 회의록을 전달할 수 있게 한다 (NLMK-01, NLMK-02).
Output: NotebookLmHelper 유틸리티, MinutesDetailScreen NotebookLM 버튼, SettingsScreen NotebookLM 링크
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-notebooklm/10-CONTEXT.md
@.planning/phases/10-notebooklm/10-RESEARCH.md

<interfaces>
<!-- 기존 MinutesDetailScreen.kt TopAppBar actions 구조 (line 76-95) -->
```kotlin
actions = {
    // 공유 버튼: 회의록 내용이 있을 때만 표시
    if (minutesContent.isNotBlank()) {
        IconButton(onClick = {
            val sendIntent = Intent().apply {
                action = Intent.ACTION_SEND
                putExtra(Intent.EXTRA_SUBJECT, meeting?.title ?: "회의록")
                putExtra(Intent.EXTRA_TEXT, minutesContent)
                type = "text/plain"
            }
            val shareIntent = Intent.createChooser(sendIntent, null)
            context.startActivity(shareIntent)
        }) {
            Icon(imageVector = Icons.Default.Share, contentDescription = "공유")
        }
    }
},
```

<!-- 기존 SettingsScreen.kt 구조: 섹션 패턴 (titleMedium + bodySmall + 컨트롤) -->
<!-- Gemini API 섹션 마지막: line 283 (hasApiKey 텍스트) -->

<!-- 기존 libs.versions.toml: browser 미등록 상태 -->
<!-- 기존 app/build.gradle.kts: browser 의존성 미포함 -->

<!-- AndroidManifest.xml: queries 블록 미존재 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: androidx.browser 의존성 추가 + NotebookLmHelper 유틸리티 생성 + AndroidManifest queries 등록</name>
  <files>
    gradle/libs.versions.toml,
    app/build.gradle.kts,
    app/src/main/AndroidManifest.xml,
    app/src/main/java/com/autominuting/util/NotebookLmHelper.kt
  </files>
  <read_first>
    gradle/libs.versions.toml,
    app/build.gradle.kts,
    app/src/main/AndroidManifest.xml,
    app/src/main/java/com/autominuting/presentation/minutes/MinutesDetailScreen.kt
  </read_first>
  <action>
    1. **libs.versions.toml** (per D-07):
       - [versions] 섹션에 `browser = "1.9.0"` 추가
       - [libraries] 섹션에 `browser = { group = "androidx.browser", name = "browser", version.ref = "browser" }` 추가

    2. **app/build.gradle.kts** (per D-07):
       - dependencies 블록에 `implementation(libs.browser)` 추가 (// Custom Tabs (NotebookLM 웹 연동) 주석과 함께)

    3. **AndroidManifest.xml** (Android 11+ 패키지 가시성 대응):
       - `<manifest>` 태그 바로 아래, `<application>` 앞에 `<queries>` 블록 추가:
         ```xml
         <queries>
             <!-- NotebookLM 앱 설치 확인용 (Android 11+ 패키지 가시성) -->
             <package android:name="com.google.android.apps.labs.language.tailwind" />
         </queries>
         ```

    4. **NotebookLmHelper.kt** 신규 생성 (per D-01, D-02, D-03, D-04, D-05):
       - 패키지: `com.autominuting.util`
       - `object NotebookLmHelper` 구조
       - 상수: `NOTEBOOKLM_PACKAGE = "com.google.android.apps.labs.language.tailwind"` (리서치에서 확인된 실제 패키지명. CONTEXT.md D-03의 `com.google.android.apps.notebooklm`은 잘못된 값이므로 실제 패키지명 사용)
       - 상수: `NOTEBOOKLM_WEB_URL = "https://notebooklm.google.com"`
       - `fun isNotebookLmInstalled(context: Context): Boolean` — PackageManager.getPackageInfo로 확인, NameNotFoundException catch 시 false
       - `fun shareToNotebookLm(context: Context, title: String, content: String)` — 앱 설치 시 Intent.ACTION_SEND에 setPackage(NOTEBOOKLM_PACKAGE)로 직접 전송, ActivityNotFoundException catch 시 openNotebookLmWeb 폴백. 미설치 시 바로 openNotebookLmWeb 호출
       - `fun openNotebookLmWeb(context: Context)` — CustomTabsIntent.Builder()로 Custom Tabs 실행. setShowTitle(true) 설정. Uri.parse(NOTEBOOKLM_WEB_URL) 사용
       - 한국어 주석으로 각 함수 설명 작성
  </action>
  <verify>
    <automated>cd "D:\workspace\temp\Auto_Minuting" && grep -q "browser" gradle/libs.versions.toml && grep -q "libs.browser" app/build.gradle.kts && grep -q "tailwind" app/src/main/AndroidManifest.xml && grep -q "NOTEBOOKLM_PACKAGE" app/src/main/java/com/autominuting/util/NotebookLmHelper.kt && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - gradle/libs.versions.toml에 `browser = "1.9.0"` 버전과 `browser = { group = "androidx.browser"` 라이브러리 등록
    - app/build.gradle.kts에 `implementation(libs.browser)` 존재
    - AndroidManifest.xml에 `<package android:name="com.google.android.apps.labs.language.tailwind" />` 존재
    - NotebookLmHelper.kt에 `NOTEBOOKLM_PACKAGE`, `NOTEBOOKLM_WEB_URL`, `isNotebookLmInstalled`, `shareToNotebookLm`, `openNotebookLmWeb` 정의
    - NotebookLmHelper.kt에 `CustomTabsIntent` import 존재
    - NotebookLmHelper.kt에 `ActivityNotFoundException` catch 블록 존재
  </acceptance_criteria>
  <done>androidx.browser 의존성 등록, NotebookLmHelper 유틸리티 생성, AndroidManifest queries 블록 등록 완료</done>
</task>

<task type="auto">
  <name>Task 2: MinutesDetailScreen NotebookLM 전용 버튼 + SettingsScreen NotebookLM 링크 추가</name>
  <files>
    app/src/main/java/com/autominuting/presentation/minutes/MinutesDetailScreen.kt,
    app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt
  </files>
  <read_first>
    app/src/main/java/com/autominuting/presentation/minutes/MinutesDetailScreen.kt,
    app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt,
    app/src/main/java/com/autominuting/util/NotebookLmHelper.kt
  </read_first>
  <action>
    1. **MinutesDetailScreen.kt** (per D-01, D-02, D-03, D-04):
       - import 추가: `com.autominuting.util.NotebookLmHelper`, `Icons.AutoMirrored.Filled.MenuBook`
       - TopAppBar `actions` 블록 내, 기존 Share IconButton **앞에** NotebookLM 전용 IconButton 추가:
         ```kotlin
         // NotebookLM 전용 버튼 (per D-02)
         IconButton(onClick = {
             NotebookLmHelper.shareToNotebookLm(
                 context = context,
                 title = meeting?.title ?: "회의록",
                 content = minutesContent
             )
         }) {
             Icon(
                 imageVector = Icons.AutoMirrored.Filled.MenuBook,
                 contentDescription = "NotebookLM으로 보내기"
             )
         }
         ```
       - 기존 Share 버튼(ACTION_SEND + createChooser)은 그대로 유지 (per D-01)
       - 두 버튼 모두 `if (minutesContent.isNotBlank())` 조건 내에 위치

    2. **SettingsScreen.kt** (per D-06):
       - import 추가: `com.autominuting.util.NotebookLmHelper`, `androidx.compose.ui.platform.LocalContext`, `Icons.AutoMirrored.Filled.MenuBook`
       - `val context = LocalContext.current` 를 Scaffold 바로 앞 또는 Column 시작 부분에 추가 (이미 없으면)
       - Gemini API 섹션 마지막 (line 283 `}` 닫힘 이후, Column의 닫는 `}` 이전) 에 NotebookLM 섹션 추가:
         ```kotlin
         // --- NotebookLM 섹션 ---
         Spacer(modifier = Modifier.height(24.dp))

         Text(
             text = "NotebookLM",
             style = MaterialTheme.typography.titleMedium
         )
         Text(
             text = "NotebookLM에서 AI 기반 회의록 분석을 활용할 수 있습니다",
             style = MaterialTheme.typography.bodySmall,
             color = MaterialTheme.colorScheme.onSurfaceVariant
         )

         Spacer(modifier = Modifier.height(8.dp))

         OutlinedButton(
             onClick = { NotebookLmHelper.openNotebookLmWeb(context) }
         ) {
             Icon(
                 imageVector = Icons.AutoMirrored.Filled.MenuBook,
                 contentDescription = null,
                 modifier = Modifier.size(18.dp)
             )
             Spacer(modifier = Modifier.width(8.dp))
             Text("NotebookLM 열기")
         }
         ```
  </action>
  <verify>
    <automated>cd "D:\workspace\temp\Auto_Minuting" && grep -q "NotebookLmHelper" app/src/main/java/com/autominuting/presentation/minutes/MinutesDetailScreen.kt && grep -q "NotebookLM으로 보내기" app/src/main/java/com/autominuting/presentation/minutes/MinutesDetailScreen.kt && grep -q "NotebookLmHelper" app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt && grep -q "NotebookLM 열기" app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - MinutesDetailScreen.kt에 `NotebookLmHelper.shareToNotebookLm` 호출 존재
    - MinutesDetailScreen.kt에 `"NotebookLM으로 보내기"` contentDescription 존재
    - MinutesDetailScreen.kt에 기존 `Icons.Default.Share` 공유 버튼이 여전히 존재 (per D-01)
    - SettingsScreen.kt에 `NotebookLmHelper.openNotebookLmWeb` 호출 존재
    - SettingsScreen.kt에 `"NotebookLM 열기"` 텍스트 존재
    - SettingsScreen.kt에 `"NotebookLM"` titleMedium 텍스트 존재
  </acceptance_criteria>
  <done>MinutesDetailScreen에 NotebookLM 전용 버튼(기존 Share 유지), SettingsScreen에 NotebookLM 열기 링크 추가 완료</done>
</task>

</tasks>

<verification>
1. `grep -rn "NotebookLmHelper" app/src/main/java/` — NotebookLmHelper가 MinutesDetailScreen과 SettingsScreen에서 참조됨
2. `grep -n "tailwind" app/src/main/AndroidManifest.xml` — queries 블록에 NotebookLM 패키지 등록됨
3. `grep -n "browser" gradle/libs.versions.toml` — browser 의존성 등록됨
4. MinutesDetailScreen.kt의 actions 블록에 IconButton이 2개 (NotebookLM + Share) 존재
</verification>

<success_criteria>
- NotebookLmHelper.kt가 생성되어 shareToNotebookLm, openNotebookLmWeb, isNotebookLmInstalled 함수를 제공한다
- MinutesDetailScreen TopAppBar에 NotebookLM 전용 버튼이 기존 Share 버튼 옆에 표시된다
- NotebookLM 앱 설치 시 직접 Intent, 미설치 시 Custom Tabs 폴백이 동작하는 코드 경로가 존재한다
- SettingsScreen에 NotebookLM 열기 버튼이 존재한다
- AndroidManifest에 queries 블록으로 패키지 가시성이 선언되어 있다
</success_criteria>

<output>
After completion, create `.planning/phases/10-notebooklm/10-01-SUMMARY.md`
</output>
