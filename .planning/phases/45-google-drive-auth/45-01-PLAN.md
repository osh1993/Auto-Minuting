---
phase: 45-google-drive-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/com/autominuting/data/auth/DriveAuthState.kt
  - app/src/main/java/com/autominuting/data/auth/GoogleAuthRepository.kt
  - app/src/main/java/com/autominuting/data/preferences/UserPreferencesRepository.kt
  - app/src/main/java/com/autominuting/presentation/settings/SettingsViewModel.kt
  - app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt
autonomous: true
requirements:
  - DRIVE-01

must_haves:
  truths:
    - "설정 화면에 'Google Drive 연결' 버튼이 표시된다 (미연결 상태)"
    - "버튼 탭 시 Google 계정 선택 + Drive 접근 동의 화면이 표시된다"
    - "동의 완료 후 설정 화면에 연결된 계정 이메일이 표시된다"
    - "'연결 해제' 버튼으로 Drive 인증을 취소할 수 있다"
    - "Drive 인증 여부(boolean)가 DataStore에 저장되어 앱 재시작 후 복원된다"
    - "assembleDebug BUILD SUCCESSFUL"
  artifacts:
    - path: "app/src/main/java/com/autominuting/data/auth/DriveAuthState.kt"
      provides: "Drive 전용 인증 상태 sealed interface"
      exports: ["DriveAuthState"]
    - path: "app/src/main/java/com/autominuting/data/auth/GoogleAuthRepository.kt"
      provides: "authorizeDrive(), revokeDriveAuthorization(), getDriveAccessToken(), restoreDriveAuthState() 메서드"
    - path: "app/src/main/java/com/autominuting/data/preferences/UserPreferencesRepository.kt"
      provides: "DRIVE_AUTHORIZED_KEY + driveAuthorized Flow + setDriveAuthorized() + clearDriveAuthorized()"
    - path: "app/src/main/java/com/autominuting/presentation/settings/SettingsViewModel.kt"
      provides: "driveAuthState StateFlow + authorizeDrive() + revokeDriveAuth() + onDriveAuthorizationResult() + onDriveAuthorizationFailed()"
    - path: "app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt"
      provides: "GoogleDriveSection 컴포저블 + rememberLauncherForActivityResult 등록"
  key_links:
    - from: "SettingsScreen.kt (GoogleDriveSection)"
      to: "SettingsViewModel.authorizeDrive()"
      via: "버튼 onClick"
      pattern: "viewModel\\.authorizeDrive"
    - from: "SettingsViewModel.kt"
      to: "GoogleAuthRepository.authorizeDrive()"
      via: "viewModelScope.launch"
      pattern: "googleAuthRepository\\.authorizeDrive"
    - from: "GoogleAuthRepository.authorizeDrive()"
      to: "Identity.getAuthorizationClient(activity)"
      via: "AuthorizationRequest(SCOPE_DRIVE_FILE)"
      pattern: "SCOPE_DRIVE_FILE"
    - from: "SettingsScreen.kt"
      to: "SettingsViewModel.onDriveAuthorizationResult()"
      via: "rememberLauncherForActivityResult 콜백"
      pattern: "onDriveAuthorizationResult"
    - from: "GoogleAuthRepository"
      to: "UserPreferencesRepository.setDriveAuthorized()"
      via: "authorizeDrive 성공 시"
      pattern: "setDriveAuthorized"
---

<objective>
Google Drive 인증(OAuth 2.0 / drive.file 스코프)을 설정 화면에 추가한다.

Purpose: Phase 46 Drive 업로드 파이프라인의 인증 기반을 마련한다. 사용자가 설정에서 Google Drive 계정을 연결하면 앱이 회의록 파일을 Drive에 업로드할 수 있게 된다.
Output: DriveAuthState sealed interface, GoogleAuthRepository.authorizeDrive() 메서드, SettingsScreen Google Drive 섹션 UI, assembleDebug 빌드 성공
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/45-google-drive-auth/RESEARCH.md

<interfaces>
<!-- 기존 코드 계약. 실행자는 탐색 없이 이 정보를 직접 사용한다. -->

From app/src/main/java/com/autominuting/data/auth/AuthState.kt:
```kotlin
sealed interface AuthState {
    data object NotSignedIn : AuthState
    data class SignedIn(val displayName: String, val email: String, val photoUrl: String? = null) : AuthState
    data object Loading : AuthState
    data class Error(val message: String) : AuthState
}
```

From app/src/main/java/com/autominuting/data/auth/GoogleAuthRepository.kt (핵심 필드/메서드):
```kotlin
@Singleton
class GoogleAuthRepository @Inject constructor(
    @ApplicationContext private val context: Context,
    private val userPreferencesRepository: UserPreferencesRepository,
    private val secureApiKeyRepository: SecureApiKeyRepository
) {
    private const val SCOPE_GENERATIVE_LANGUAGE = "https://www.googleapis.com/auth/generative-language.retriever"
    private const val SCOPE_CLOUD_PLATFORM = "https://www.googleapis.com/auth/cloud-platform"

    private val credentialManager = CredentialManager.create(context)
    private val _authState = MutableStateFlow<AuthState>(AuthState.NotSignedIn)
    val authState: StateFlow<AuthState> = _authState.asStateFlow()

    @Volatile private var cachedAccessToken: String? = null  // Gemini용

    suspend fun restoreAuthState()
    suspend fun signIn(activityContext: Context)
    suspend fun authorize(activity: Activity): Boolean  // Gemini 전용
    private suspend fun requestAccessToken(activity: Activity, scope: String): String?
    fun getAccessToken(): String?  // Gemini 전용
    suspend fun signOut()
    fun isSignedIn(): Boolean
}
```

From app/src/main/java/com/autominuting/data/preferences/UserPreferencesRepository.kt (기존 키):
```kotlin
val AUTOMATION_MODE_KEY = stringPreferencesKey("automation_mode")
val AUTH_MODE_KEY = stringPreferencesKey("auth_mode")
val GOOGLE_EMAIL_KEY = stringPreferencesKey("google_email")
val GOOGLE_DISPLAY_NAME_KEY = stringPreferencesKey("google_display_name")
val STT_ENGINE_KEY = stringPreferencesKey("stt_engine")
val DEFAULT_TEMPLATE_ID_KEY = longPreferencesKey("default_template_id")
val DEFAULT_CUSTOM_PROMPT_KEY = stringPreferencesKey("default_custom_prompt")
val MINUTES_ENGINE_KEY = stringPreferencesKey("minutes_engine")

// 기존 Google 계정 관련
suspend fun setGoogleAccount(displayName: String, email: String)
suspend fun clearGoogleAccount()
val googleEmail: Flow<String?>
val googleDisplayName: Flow<String?>
```

From app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt (Drive 섹션 삽입 위치):
```kotlin
// 라인 616-631 근처: GoogleAccountSection 바로 아래에 GoogleDriveSection 삽입
// 현재 구조:
if (authMode == AuthMode.OAUTH) {
    OAuthClientIdSection(viewModel = viewModel)
    Spacer(modifier = Modifier.height(16.dp))
    GoogleAccountSection(
        authState = authState,
        onSignIn = { viewModel.signInWithGoogle(context) },
        onSignOut = { viewModel.signOut() }
    )
    // 여기에 Drive 섹션 추가
} else { ... }
```

From app/src/main/java/com/autominuting/presentation/settings/SettingsViewModel.kt (기존 구조):
```kotlin
val authState: StateFlow<AuthState> = googleAuthRepository.authState
// driveAuthState StateFlow 추가 필요

fun signInWithGoogle(activityContext: Context)
fun signOut()
// authorizeDrive(activity), revokeDriveAuth(), onDriveAuthorizationResult(token), onDriveAuthorizationFailed(error) 추가 필요
```

임포트 패턴 (GoogleAuthRepository에서 이미 사용 중):
```kotlin
import com.google.android.gms.auth.api.identity.AuthorizationClient
import com.google.android.gms.auth.api.identity.AuthorizationRequest
import com.google.android.gms.auth.api.identity.Identity
import com.google.android.gms.common.api.Scope
import kotlinx.coroutines.tasks.await
```

SettingsScreen에서 Activity Result 패턴 (신규 추가):
```kotlin
import androidx.activity.compose.rememberLauncherForActivityResult
import androidx.activity.result.IntentSenderRequest
import androidx.activity.result.contract.ActivityResultContracts
import com.google.android.gms.common.api.ApiException
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DriveAuthState 신설 + GoogleAuthRepository Drive 메서드 추가 + UserPreferencesRepository Drive 키 추가</name>
  <files>
    app/src/main/java/com/autominuting/data/auth/DriveAuthState.kt
    app/src/main/java/com/autominuting/data/auth/GoogleAuthRepository.kt
    app/src/main/java/com/autominuting/data/preferences/UserPreferencesRepository.kt
  </files>
  <action>
**1. DriveAuthState.kt 신규 생성**

`app/src/main/java/com/autominuting/data/auth/DriveAuthState.kt` 파일을 생성한다:

```kotlin
package com.autominuting.data.auth

import android.app.PendingIntent

/**
 * Google Drive 전용 인증 상태 — AuthState(Sign-In)와 독립적으로 관리된다.
 *
 * Drive 스코프 승인은 Sign-In 이후 별도 단계이므로 별도 sealed interface로 분리한다.
 * 앱 재시작 시 DataStore의 driveAuthorized(boolean)를 읽어 NotAuthorized 또는
 * NeedsReauthorization 상태로 초기화한다. access token은 메모리 캐시에만 유지된다.
 */
sealed interface DriveAuthState {
    /** Drive 스코프 미승인 상태 */
    data object NotAuthorized : DriveAuthState

    /** 사용자 동의 화면 표시 필요 — PendingIntent를 Activity에서 실행해야 한다 */
    data class NeedsConsent(val pendingIntent: PendingIntent) : DriveAuthState

    /** Drive 스코프 승인 완료 — access token이 메모리 캐시에 보관된다 */
    data class Authorized(val email: String) : DriveAuthState

    /** 승인 진행 중 */
    data object Loading : DriveAuthState

    /** 승인 오류 */
    data class Error(val message: String) : DriveAuthState
}
```

**2. GoogleAuthRepository.kt 수정 — Drive 전용 필드 및 메서드 추가**

기존 파일에 다음을 추가한다. 기존 코드(Gemini 관련)는 수정하지 않는다:

companion object에 Drive 스코프 상수 추가:
```kotlin
/** Google Drive 파일 접근 스코프 (앱이 생성한 파일만 — Non-sensitive) */
private const val SCOPE_DRIVE_FILE =
    "https://www.googleapis.com/auth/drive.file"
```

클래스 필드에 Drive 전용 상태 추가:
```kotlin
/** Drive 전용 인증 상태 */
private val _driveAuthState = MutableStateFlow<DriveAuthState>(DriveAuthState.NotAuthorized)
val driveAuthState: StateFlow<DriveAuthState> = _driveAuthState.asStateFlow()

/** 캐시된 Drive access token (메모리에만 보관 — 민감 정보) */
@Volatile
private var cachedDriveAccessToken: String? = null
```

`restoreAuthState()` 메서드 내부에 Drive 복원 로직 추가 (기존 Gemini 복원 로직 뒤에 추가):
```kotlin
// Drive 인증 여부 복원
val driveAuthorized = userPreferencesRepository.driveAuthorized.first()
if (driveAuthorized && !savedEmail.isNullOrBlank()) {
    // 토큰은 만료됐을 수 있으므로 Authorized 상태로만 복원
    // 실제 토큰은 authorizeDrive() 재호출 시 획득
    _driveAuthState.value = DriveAuthState.Authorized(email = savedEmail)
}
```

신규 메서드 추가:

```kotlin
/**
 * Google Drive API drive.file 스코프 권한을 요청한다.
 *
 * hasResolution() == true 이면 사용자 동의 화면이 필요하며 NeedsConsent 상태를 반환한다.
 * Activity에서 rememberLauncherForActivityResult로 PendingIntent를 실행해야 한다.
 *
 * hasResolution() == false 이면 이미 동의된 상태이므로 즉시 Authorized를 반환한다.
 *
 * @param activity Activity 인스턴스 (AuthorizationClient 필수)
 */
suspend fun authorizeDrive(activity: Activity) {
    _driveAuthState.value = DriveAuthState.Loading
    try {
        val authorizationRequest = AuthorizationRequest.builder()
            .setRequestedScopes(listOf(Scope(SCOPE_DRIVE_FILE)))
            .build()

        val result = Identity.getAuthorizationClient(activity)
            .authorize(authorizationRequest)
            .await()

        if (result.hasResolution()) {
            // 최초 승인 요청 — PendingIntent를 UI에서 실행해야 함
            val pendingIntent = result.pendingIntent
            if (pendingIntent != null) {
                _driveAuthState.value = DriveAuthState.NeedsConsent(pendingIntent)
                Log.d(TAG, "Drive 인증: 사용자 동의 화면 필요")
            } else {
                _driveAuthState.value = DriveAuthState.Error("동의 화면 PendingIntent가 null입니다")
            }
        } else {
            // 이미 동의된 계정 — 즉시 token 획득
            val token = result.accessToken
            cachedDriveAccessToken = token
            val email = (authState.value as? AuthState.SignedIn)?.email ?: ""
            userPreferencesRepository.setDriveAuthorized(true)
            _driveAuthState.value = DriveAuthState.Authorized(email = email)
            Log.d(TAG, "Drive 인증 성공 (즉시): $email")
        }
    } catch (e: Exception) {
        Log.e(TAG, "Drive 인증 실패: ${e.message}", e)
        _driveAuthState.value = DriveAuthState.Error("Drive 인증 실패: ${e.message}")
    }
}

/**
 * Activity에서 사용자 동의 결과를 수신한 후 호출한다.
 * getAuthorizationResultFromIntent()로 access token을 추출하여 Authorized 상태로 전환한다.
 *
 * @param accessToken AuthorizationResult에서 추출한 access token
 */
suspend fun onDriveAuthorizationResult(accessToken: String) {
    cachedDriveAccessToken = accessToken
    val email = (authState.value as? AuthState.SignedIn)?.email ?: ""
    userPreferencesRepository.setDriveAuthorized(true)
    _driveAuthState.value = DriveAuthState.Authorized(email = email)
    Log.d(TAG, "Drive 인증 성공 (동의 후): $email")
}

/**
 * Drive 인증 실패 처리.
 */
fun onDriveAuthorizationFailed(errorMessage: String?) {
    _driveAuthState.value = DriveAuthState.Error(
        errorMessage ?: "Drive 인증이 취소되었습니다"
    )
    Log.w(TAG, "Drive 인증 실패: $errorMessage")
}

/**
 * Drive 인증을 해제한다. DataStore에서 드라이브 인증 여부를 삭제하고 상태를 초기화한다.
 */
suspend fun revokeDriveAuthorization() {
    cachedDriveAccessToken = null
    userPreferencesRepository.setDriveAuthorized(false)
    _driveAuthState.value = DriveAuthState.NotAuthorized
    Log.d(TAG, "Drive 인증 해제 완료")
}

/**
 * 캐시된 Drive access token을 반환한다. null이면 authorizeDrive() 재호출 필요.
 */
fun getDriveAccessToken(): String? = cachedDriveAccessToken
```

`signOut()` 메서드 내부에 Drive 상태 초기화 추가:
```kotlin
// signOut() 기존 코드 뒤에 추가
cachedDriveAccessToken = null
userPreferencesRepository.setDriveAuthorized(false)
_driveAuthState.value = DriveAuthState.NotAuthorized
```

**3. UserPreferencesRepository.kt 수정 — Drive 인증 여부 키 추가**

companion object에 키 추가:
```kotlin
/** Drive 인증 완료 여부 설정 키 (토큰은 저장하지 않음 — 민감 정보) */
val DRIVE_AUTHORIZED_KEY = booleanPreferencesKey("drive_authorized")
```

import 추가:
```kotlin
import androidx.datastore.preferences.core.booleanPreferencesKey
```

Flow 프로퍼티 추가:
```kotlin
/** Drive 인증 완료 여부를 관찰한다. 기본값: false */
val driveAuthorized: Flow<Boolean> = dataStore.data.map { prefs ->
    prefs[DRIVE_AUTHORIZED_KEY] ?: false
}
```

저장/삭제 메서드 추가:
```kotlin
/**
 * Drive 인증 완료 여부를 저장한다.
 * @param authorized true = 인증 완료, false = 인증 해제
 */
suspend fun setDriveAuthorized(authorized: Boolean) {
    dataStore.edit { prefs ->
        prefs[DRIVE_AUTHORIZED_KEY] = authorized
    }
}

/**
 * Drive 인증 여부를 삭제한다.
 */
suspend fun clearDriveAuthorized() {
    dataStore.edit { prefs ->
        prefs.remove(DRIVE_AUTHORIZED_KEY)
    }
}
```
  </action>
  <verify>
    <automated>cd /d/workspace/temp/Auto_Minuting && ./gradlew :app:compileDebugKotlin 2>&1 | tail -20</automated>
  </verify>
  <done>
    - DriveAuthState.kt 파일 존재, 5개 상태 (NotAuthorized, NeedsConsent, Authorized, Loading, Error) 정의됨
    - GoogleAuthRepository에 driveAuthState StateFlow, authorizeDrive(), onDriveAuthorizationResult(), onDriveAuthorizationFailed(), revokeDriveAuthorization(), getDriveAccessToken() 존재
    - UserPreferencesRepository에 DRIVE_AUTHORIZED_KEY, driveAuthorized Flow, setDriveAuthorized(), clearDriveAuthorized() 존재
    - compileDebugKotlin 오류 없음
  </done>
</task>

<task type="auto">
  <name>Task 2: SettingsViewModel Drive 연결 메서드 추가 + SettingsScreen GoogleDriveSection UI 추가 + assembleDebug 빌드 검증</name>
  <files>
    app/src/main/java/com/autominuting/presentation/settings/SettingsViewModel.kt
    app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt
  </files>
  <action>
**1. SettingsViewModel.kt 수정 — Drive 인증 StateFlow 및 메서드 추가**

기존 `authState` StateFlow 바로 아래에 Drive 상태 추가:
```kotlin
/** Google Drive 인증 상태 */
val driveAuthState: StateFlow<DriveAuthState> = googleAuthRepository.driveAuthState
```

import 추가:
```kotlin
import com.autominuting.data.auth.DriveAuthState
```

init 블록에서 `restoreAuthState()` 호출 시 Drive 상태도 자동 복원됨 (GoogleAuthRepository.restoreAuthState()가 처리).

신규 메서드 추가 (기존 `authorizeGeminiAccess()` 아래에):
```kotlin
/**
 * Google Drive drive.file 스코프 권한을 요청한다.
 * hasResolution() == true 이면 NeedsConsent 상태로 전환되며,
 * SettingsScreen에서 rememberLauncherForActivityResult로 PendingIntent를 실행해야 한다.
 *
 * @param activity Activity 인스턴스
 */
fun authorizeDrive(activity: Activity) {
    viewModelScope.launch {
        googleAuthRepository.authorizeDrive(activity)
    }
}

/**
 * Activity Result에서 Drive 동의 결과를 수신한 후 호출한다.
 * @param accessToken 동의 후 획득한 access token
 */
fun onDriveAuthorizationResult(accessToken: String) {
    viewModelScope.launch {
        googleAuthRepository.onDriveAuthorizationResult(accessToken)
    }
}

/**
 * Drive 인증 실패 처리.
 * @param errorMessage 오류 메시지 (null이면 취소로 간주)
 */
fun onDriveAuthorizationFailed(errorMessage: String?) {
    googleAuthRepository.onDriveAuthorizationFailed(errorMessage)
}

/**
 * Drive 인증을 해제한다.
 */
fun revokeDriveAuth() {
    viewModelScope.launch {
        googleAuthRepository.revokeDriveAuthorization()
    }
}
```

**2. SettingsScreen.kt 수정 — GoogleDriveSection 추가**

파일 상단 import 블록에 추가 (기존 import 뒤에):
```kotlin
import android.app.Activity
import androidx.activity.compose.rememberLauncherForActivityResult
import androidx.activity.result.IntentSenderRequest
import androidx.activity.result.contract.ActivityResultContracts
import com.autominuting.data.auth.DriveAuthState
import com.google.android.gms.auth.api.identity.Identity
import com.google.android.gms.common.api.ApiException
import kotlinx.coroutines.launch
import androidx.compose.runtime.rememberCoroutineScope
```

메인 SettingsScreen 컴포저블 내부 상태 수집 블록에 추가 (기존 `val authState by ...` 아래):
```kotlin
val driveAuthState by viewModel.driveAuthState.collectAsStateWithLifecycle()
```

**rememberLauncherForActivityResult 등록** — Pitfall 4 방지: 컴포저블 최상위(unconditional)에서 등록.
`SettingsScreen` 함수 본문 상단 (Scaffold 호출 전)에 추가:
```kotlin
val scope = rememberCoroutineScope()
val driveAuthLauncher = rememberLauncherForActivityResult(
    contract = ActivityResultContracts.StartIntentSenderForResult()
) { activityResult ->
    // Drive 동의 결과 처리
    val data = activityResult.data
    if (data != null) {
        scope.launch {
            try {
                val authResult = Identity.getAuthorizationClient(context)
                    .getAuthorizationResultFromIntent(data)
                val token = authResult.accessToken
                if (token != null) {
                    viewModel.onDriveAuthorizationResult(token)
                } else {
                    viewModel.onDriveAuthorizationFailed("access token이 null입니다")
                }
            } catch (e: ApiException) {
                viewModel.onDriveAuthorizationFailed(e.message)
            }
        }
    } else {
        viewModel.onDriveAuthorizationFailed("Drive 인증이 취소되었습니다")
    }
}
```

**DriveAuthState.NeedsConsent 처리** — LaunchedEffect로 상태 변화 감지:
`driveAuthLauncher` 선언 아래에 추가:
```kotlin
// NeedsConsent 상태 진입 시 PendingIntent 자동 실행
LaunchedEffect(driveAuthState) {
    val state = driveAuthState
    if (state is DriveAuthState.NeedsConsent) {
        driveAuthLauncher.launch(
            IntentSenderRequest.Builder(state.pendingIntent.intentSender).build()
        )
    }
}
```

import 추가:
```kotlin
import androidx.compose.runtime.LaunchedEffect
```

**GoogleDriveSection 호출 삽입** — 기존 GoogleAccountSection 호출 바로 아래에 추가:
```kotlin
// 기존 GoogleAccountSection 호출 (수정하지 않음)
GoogleAccountSection(
    authState = authState,
    onSignIn = { viewModel.signInWithGoogle(context) },
    onSignOut = { viewModel.signOut() }
)

// 신규: Google Drive 연결 섹션
// Google 계정으로 로그인된 경우에만 Drive 섹션 표시
if (authState is AuthState.SignedIn) {
    Spacer(modifier = Modifier.height(16.dp))
    GoogleDriveSection(
        driveAuthState = driveAuthState,
        onConnectDrive = {
            viewModel.authorizeDrive(context as Activity)
        },
        onRevokeDrive = { viewModel.revokeDriveAuth() }
    )
}
```

**GoogleDriveSection 컴포저블 신규 추가** — 파일 하단 GoogleAccountSection 함수 아래에 추가:
```kotlin
/**
 * Google Drive 연결/해제 섹션.
 *
 * 미연결 상태: "Google Drive 연결" 버튼 표시
 * NeedsConsent: 동의 화면 런처 자동 실행 (LaunchedEffect에서 처리)
 * Loading: 진행 중 인디케이터
 * Authorized: 연결된 계정 이메일 + "연결 해제" 버튼
 * Error: 오류 메시지 + 재시도 버튼
 *
 * @param driveAuthState Drive 인증 상태
 * @param onConnectDrive Drive 연결 버튼 클릭 콜백
 * @param onRevokeDrive 연결 해제 버튼 클릭 콜백
 */
@Composable
private fun GoogleDriveSection(
    driveAuthState: DriveAuthState,
    onConnectDrive: () -> Unit,
    onRevokeDrive: () -> Unit
) {
    Column {
        Text(
            text = "Google Drive",
            style = MaterialTheme.typography.labelMedium,
            color = MaterialTheme.colorScheme.onSurfaceVariant
        )
        Spacer(modifier = Modifier.height(8.dp))

        when (driveAuthState) {
            is DriveAuthState.NotAuthorized -> {
                FilledTonalButton(
                    onClick = onConnectDrive,
                    modifier = Modifier.fillMaxWidth()
                ) {
                    Text("Google Drive 연결")
                }
            }

            is DriveAuthState.NeedsConsent -> {
                // LaunchedEffect에서 PendingIntent 자동 실행 — 진행 중 표시
                Row(
                    verticalAlignment = Alignment.CenterVertically,
                    horizontalArrangement = Arrangement.Center,
                    modifier = Modifier.fillMaxWidth()
                ) {
                    CircularProgressIndicator(
                        modifier = Modifier.size(24.dp),
                        strokeWidth = 2.dp
                    )
                    Spacer(modifier = Modifier.width(12.dp))
                    Text(
                        text = "Drive 동의 화면 열기 중...",
                        style = MaterialTheme.typography.bodyMedium
                    )
                }
            }

            is DriveAuthState.Loading -> {
                Row(
                    verticalAlignment = Alignment.CenterVertically,
                    horizontalArrangement = Arrangement.Center,
                    modifier = Modifier.fillMaxWidth()
                ) {
                    CircularProgressIndicator(
                        modifier = Modifier.size(24.dp),
                        strokeWidth = 2.dp
                    )
                    Spacer(modifier = Modifier.width(12.dp))
                    Text(
                        text = "Drive 연결 중...",
                        style = MaterialTheme.typography.bodyMedium
                    )
                }
            }

            is DriveAuthState.Authorized -> {
                Column {
                    Row(
                        verticalAlignment = Alignment.CenterVertically,
                        modifier = Modifier.fillMaxWidth()
                    ) {
                        Icon(
                            imageVector = Icons.Default.AccountCircle,
                            contentDescription = "Drive 연결된 계정",
                            modifier = Modifier.size(32.dp),
                            tint = MaterialTheme.colorScheme.primary
                        )
                        Spacer(modifier = Modifier.width(12.dp))
                        Column(modifier = Modifier.weight(1f)) {
                            Text(
                                text = "Drive 연결됨",
                                style = MaterialTheme.typography.bodyMedium
                            )
                            if (driveAuthState.email.isNotBlank()) {
                                Text(
                                    text = driveAuthState.email,
                                    style = MaterialTheme.typography.bodySmall,
                                    color = MaterialTheme.colorScheme.onSurfaceVariant
                                )
                            }
                        }
                    }
                    Spacer(modifier = Modifier.height(8.dp))
                    OutlinedButton(onClick = onRevokeDrive) {
                        Text("연결 해제")
                    }
                }
            }

            is DriveAuthState.Error -> {
                Column {
                    Text(
                        text = driveAuthState.message,
                        style = MaterialTheme.typography.bodySmall,
                        color = MaterialTheme.colorScheme.error
                    )
                    Spacer(modifier = Modifier.height(8.dp))
                    FilledTonalButton(
                        onClick = onConnectDrive,
                        modifier = Modifier.fillMaxWidth()
                    ) {
                        Text("Drive 다시 연결")
                    }
                }
            }
        }
    }
}
```

**3. assembleDebug 빌드 검증**

모든 파일 수정 완료 후 아래 명령으로 빌드 성공을 확인한다:
```
./gradlew assembleDebug
```
  </action>
  <verify>
    <automated>cd /d/workspace/temp/Auto_Minuting && ./gradlew assembleDebug 2>&1 | tail -30</automated>
  </verify>
  <done>
    - SettingsViewModel에 driveAuthState StateFlow, authorizeDrive(), onDriveAuthorizationResult(), onDriveAuthorizationFailed(), revokeDriveAuth() 존재
    - SettingsScreen에 rememberLauncherForActivityResult가 컴포저블 최상위(unconditional)에서 등록됨
    - SettingsScreen에 LaunchedEffect(driveAuthState)가 NeedsConsent 상태 시 PendingIntent 자동 실행
    - SettingsScreen에 GoogleDriveSection 컴포저블 존재 (5개 상태 처리: NotAuthorized, NeedsConsent, Loading, Authorized, Error)
    - GoogleDriveSection은 AuthState.SignedIn 상태일 때만 표시됨
    - assembleDebug BUILD SUCCESSFUL
  </done>
</task>

</tasks>

<verification>
빌드 후 수동 확인 항목 (개발자 직접 수행):

1. 설정 화면 → 인증 모드를 OAuth로 전환 → Google 계정 로그인 → "Google Drive 연결" 버튼이 표시되는지 확인
2. "Google Drive 연결" 버튼 탭 → Google Drive 접근 동의 화면이 시스템 UI로 표시되는지 확인
3. 동의 완료 후 → "Drive 연결됨" + 계정 이메일이 표시되는지 확인
4. "연결 해제" 탭 → "Google Drive 연결" 버튼으로 돌아가는지 확인
5. 앱 재시작 후 → Drive 연결 상태가 복원되는지 확인 (DataStore 영속화 검증)
6. Google 계정 로그아웃 → Drive 섹션이 숨겨지는지 확인 (SignedIn 상태가 아닐 때)

Google Cloud Console 확인 (필요 시):
- OAuth consent screen → Scopes → drive.file 스코프가 등록되어 있어야 함
- Android Client ID가 현재 앱 패키지명(com.autominuting)으로 설정되어 있어야 함
</verification>

<success_criteria>
1. assembleDebug BUILD SUCCESSFUL (오류 없음)
2. DriveAuthState.kt 파일 존재: NotAuthorized, NeedsConsent(PendingIntent), Authorized(email), Loading, Error 상태 정의
3. GoogleAuthRepository.authorizeDrive(activity): drive.file 스코프 요청, hasResolution() 분기 처리
4. GoogleAuthRepository.revokeDriveAuthorization(): DataStore driveAuthorized=false, _driveAuthState=NotAuthorized
5. UserPreferencesRepository.driveAuthorized: Flow&lt;Boolean&gt; 존재, setDriveAuthorized() 메서드 존재
6. SettingsViewModel.driveAuthState: StateFlow&lt;DriveAuthState&gt; 노출
7. SettingsScreen: rememberLauncherForActivityResult가 unconditional 위치에 등록됨
8. SettingsScreen: GoogleDriveSection이 AuthState.SignedIn 상태에서만 표시됨
9. SettingsScreen: LaunchedEffect(driveAuthState)가 NeedsConsent 상태 시 PendingIntent 자동 실행
</success_criteria>

<output>
완료 후 `.planning/phases/45-google-drive-auth/45-01-SUMMARY.md` 파일을 생성한다.

요약에 포함할 내용:
- 생성/수정한 파일 목록
- DriveAuthState 5개 상태 정의
- authorizeDrive() 구현 패턴 (hasResolution() 분기 처리)
- DataStore 영속화 키 (DRIVE_AUTHORIZED_KEY)
- NeedsConsent 처리 패턴 (LaunchedEffect + rememberLauncherForActivityResult)
- 향후 Phase 46(Drive 업로드)에서 getDriveAccessToken() 사용 방법
</output>
