---
phase: 20-transcripts-action-menu
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsViewModel.kt
  - app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt
autonomous: true
requirements:
  - FILE-03
must_haves:
  truths:
    - "전사 카드에서 더보기(MoreVert) 아이콘을 탭하면 드롭다운 액션 메뉴가 나타난다"
    - "액션 메뉴에서 삭제를 선택하면 확인 다이얼로그 후 전사 파일이 삭제된다"
    - "액션 메뉴에서 재전사를 선택하면 오디오 파일이 있는 경우 TranscriptionTriggerWorker가 enqueue된다"
    - "액션 메뉴에서 공유를 선택하면 전사 텍스트가 ACTION_SEND Intent로 공유된다"
  artifacts:
    - path: "app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsViewModel.kt"
      provides: "retranscribe(), shareTranscript() 함수"
      contains: "fun retranscribe"
    - path: "app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt"
      provides: "DropdownMenu 기반 액션 메뉴 UI"
      contains: "DropdownMenu"
  key_links:
    - from: "TranscriptsScreen.kt"
      to: "TranscriptsViewModel.kt"
      via: "viewModel.retranscribe(id), viewModel.shareTranscript(id, context)"
      pattern: "viewModel\\.retranscribe|viewModel\\.shareTranscript"
    - from: "TranscriptsViewModel.kt"
      to: "TranscriptionTriggerWorker"
      via: "WorkManager enqueue"
      pattern: "TranscriptionTriggerWorker"
---

<objective>
전사 목록 카드에 더보기(MoreVert) 아이콘 기반 드롭다운 액션 메뉴를 추가한다.
메뉴 항목: 삭제, 재전사, 공유.

Purpose: 기존 long-press 삭제만 가능했던 전사 목록에 재전사/공유 기능을 추가하여 사용성을 높인다.
Output: 액션 메뉴가 포함된 TranscriptsScreen.kt, 재전사/공유 로직이 포함된 TranscriptsViewModel.kt
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

@app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt
@app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsViewModel.kt
@app/src/main/java/com/autominuting/domain/model/Meeting.kt
@app/src/main/java/com/autominuting/domain/model/PipelineStatus.kt
@app/src/main/java/com/autominuting/worker/TranscriptionTriggerWorker.kt

<interfaces>
<!-- 현재 TranscriptsViewModel 공개 API -->
From TranscriptsViewModel.kt:
```kotlin
val meetings: StateFlow<List<Meeting>>
fun deleteTranscript(id: Long)
fun generateMinutes(meetingId: Long)
```

<!-- Meeting 도메인 모델 -->
From Meeting.kt:
```kotlin
data class Meeting(
    val id: Long = 0,
    val title: String,
    val recordedAt: Instant,
    val audioFilePath: String,
    val transcriptPath: String? = null,
    val minutesPath: String? = null,
    val pipelineStatus: PipelineStatus,
    val errorMessage: String? = null,
    val createdAt: Instant,
    val updatedAt: Instant,
    val source: String = "PLAUD_BLE"
)
```

<!-- TranscriptionTriggerWorker 키 상수 -->
From TranscriptionTriggerWorker.kt:
```kotlin
companion object {
    const val KEY_AUDIO_FILE_PATH = "audioFilePath"
    const val KEY_MEETING_ID = "meetingId"
    const val KEY_TRANSCRIPT_PATH = "transcriptPath"
    const val KEY_AUTOMATION_MODE = "automationMode"
    const val KEY_MINUTES_FORMAT = "minutesFormat"
}
```

<!-- 공유 패턴 (MinutesDetailScreen 참고) -->
From MinutesDetailScreen.kt:
```kotlin
val sendIntent = Intent().apply {
    action = Intent.ACTION_SEND
    putExtra(Intent.EXTRA_SUBJECT, meeting?.title ?: "회의록")
    putExtra(Intent.EXTRA_TEXT, minutesContent)
    type = "text/plain"
}
val shareIntent = Intent.createChooser(sendIntent, null)
context.startActivity(shareIntent)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ViewModel에 재전사/공유 함수 추가</name>
  <files>app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsViewModel.kt</files>
  <read_first>
    - app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsViewModel.kt
    - app/src/main/java/com/autominuting/worker/TranscriptionTriggerWorker.kt
    - app/src/main/java/com/autominuting/domain/model/Meeting.kt
    - app/src/main/java/com/autominuting/domain/repository/MeetingRepository.kt
  </read_first>
  <action>
TranscriptsViewModel.kt에 두 개의 public 함수를 추가한다.

1. **retranscribe(meetingId: Long)** 함수:
   - viewModelScope.launch 내에서 실행
   - meetingRepository.getMeetingById(meetingId).first()로 Meeting 조회
   - meeting이 null이면 Log.w(TAG, "회의를 찾을 수 없습니다: meetingId=$meetingId")로 리턴
   - meeting.audioFilePath가 빈 문자열이면 Log.w(TAG, "오디오 파일 경로가 없습니다: meetingId=$meetingId")로 리턴
   - 기존 전사 파일 삭제: meetingRepository.deleteTranscript(meetingId) 호출 (전사 경로 초기화 + 파일 삭제)
   - TranscriptionTriggerWorker를 enqueue:
     ```kotlin
     val workRequest = OneTimeWorkRequestBuilder<TranscriptionTriggerWorker>()
         .setInputData(
             workDataOf(
                 TranscriptionTriggerWorker.KEY_MEETING_ID to meetingId,
                 TranscriptionTriggerWorker.KEY_AUDIO_FILE_PATH to meeting.audioFilePath
             )
         )
         .build()
     WorkManager.getInstance(context).enqueue(workRequest)
     ```
   - Log.d(TAG, "재전사 Worker enqueue 완료: meetingId=$meetingId")

2. **shareTranscript(meetingId: Long, activityContext: Context)** 함수:
   - 주의: activityContext 파라미터는 Activity Context (startActivity 호출용). ViewModel의 @ApplicationContext context와 구분.
   - viewModelScope.launch 내에서 실행
   - meetingRepository.getMeetingById(meetingId).first()로 Meeting 조회
   - meeting이 null이면 Log.w(TAG, "회의를 찾을 수 없습니다: meetingId=$meetingId")로 리턴
   - meeting.transcriptPath가 null이면 Log.w(TAG, "전사 파일 경로가 없습니다: meetingId=$meetingId")로 리턴
   - java.io.File(meeting.transcriptPath).readText()로 전사 텍스트 로드
   - 텍스트가 비어있으면 Log.w + return
   - Intent 생성 및 공유:
     ```kotlin
     val sendIntent = Intent().apply {
         action = Intent.ACTION_SEND
         putExtra(Intent.EXTRA_SUBJECT, meeting.title)
         putExtra(Intent.EXTRA_TEXT, transcriptText)
         type = "text/plain"
     }
     val shareIntent = Intent.createChooser(sendIntent, null)
     activityContext.startActivity(shareIntent)
     ```
   - Log.d(TAG, "전사 텍스트 공유: meetingId=$meetingId")

필요한 import 추가:
- android.content.Intent
- androidx.work.OneTimeWorkRequestBuilder
- androidx.work.workDataOf (이미 존재하면 확인)
- com.autominuting.worker.TranscriptionTriggerWorker
- java.io.File
  </action>
  <verify>
    <automated>cd D:/workspace/temp/Auto_Minuting && grep -n "fun retranscribe\|fun shareTranscript\|TranscriptionTriggerWorker" app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsViewModel.kt</automated>
  </verify>
  <acceptance_criteria>
    - TranscriptsViewModel.kt contains `fun retranscribe(meetingId: Long)`
    - TranscriptsViewModel.kt contains `fun shareTranscript(meetingId: Long, activityContext: Context)`
    - TranscriptsViewModel.kt contains `import com.autominuting.worker.TranscriptionTriggerWorker`
    - TranscriptsViewModel.kt contains `TranscriptionTriggerWorker.KEY_AUDIO_FILE_PATH`
    - TranscriptsViewModel.kt contains `Intent.ACTION_SEND`
    - TranscriptsViewModel.kt contains `Intent.createChooser`
  </acceptance_criteria>
  <done>ViewModel에 retranscribe()와 shareTranscript() 함수가 추가되어 재전사 Worker enqueue와 텍스트 공유가 가능하다</done>
</task>

<task type="auto">
  <name>Task 2: 전사 카드에 DropdownMenu 액션 메뉴 추가</name>
  <files>app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt</files>
  <read_first>
    - app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt
    - app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsViewModel.kt
    - app/src/main/java/com/autominuting/domain/model/PipelineStatus.kt
  </read_first>
  <action>
TranscriptsScreen.kt를 수정하여 각 전사 카드에 더보기(MoreVert) 아이콘과 DropdownMenu를 추가한다.

1. **import 추가:**
   - `import androidx.compose.material.icons.Icons`
   - `import androidx.compose.material.icons.filled.MoreVert`
   - `import androidx.compose.material.icons.filled.Delete`
   - `import androidx.compose.material.icons.filled.Refresh`
   - `import androidx.compose.material.icons.filled.Share`
   - `import androidx.compose.material3.DropdownMenu`
   - `import androidx.compose.material3.DropdownMenuItem`
   - `import androidx.compose.material3.Icon`
   - `import androidx.compose.material3.IconButton`
   - `import androidx.compose.ui.platform.LocalContext`

2. **TranscriptsScreen composable 수정:**
   - `val context = LocalContext.current`를 추가 (meetings collectAsState 다음)
   - TranscriptMeetingCard 호출에 새 콜백 추가:
     - `onRetranscribe = { id -> viewModel.retranscribe(id) }`
     - `onShare = { id -> viewModel.shareTranscript(id, context) }`

3. **TranscriptMeetingCard 시그니처 변경:**
   - 기존 `onDeleteRequest: (Long) -> Unit` 유지
   - 새 파라미터 추가: `onRetranscribe: (Long) -> Unit`, `onShare: (Long) -> Unit`

4. **TranscriptMeetingCard 내부 수정:**
   - `var showMenu by remember { mutableStateOf(false) }` 상태 추가
   - 제목 Row에서 오른쪽 끝에 Box + IconButton(MoreVert) + DropdownMenu 배치:
     ```kotlin
     Row(
         modifier = Modifier.fillMaxWidth(),
         horizontalArrangement = Arrangement.SpaceBetween,
         verticalAlignment = Alignment.CenterVertically
     ) {
         Text(
             text = meeting.title,
             style = MaterialTheme.typography.titleMedium,
             color = MaterialTheme.colorScheme.onSurface,
             modifier = Modifier.weight(1f)
         )
         Box {
             IconButton(onClick = { showMenu = true }) {
                 Icon(
                     imageVector = Icons.Default.MoreVert,
                     contentDescription = "더보기"
                 )
             }
             DropdownMenu(
                 expanded = showMenu,
                 onDismissRequest = { showMenu = false }
             ) {
                 // 재전사: audioFilePath가 있고 TRANSCRIBED/COMPLETED/FAILED 상태일 때만 표시
                 if (meeting.audioFilePath.isNotBlank() &&
                     meeting.pipelineStatus in setOf(
                         PipelineStatus.TRANSCRIBED,
                         PipelineStatus.COMPLETED,
                         PipelineStatus.FAILED
                     )
                 ) {
                     DropdownMenuItem(
                         text = { Text("재전사") },
                         onClick = {
                             showMenu = false
                             onRetranscribe(meeting.id)
                         },
                         leadingIcon = {
                             Icon(Icons.Default.Refresh, contentDescription = null)
                         }
                     )
                 }
                 // 공유: transcriptPath가 있을 때만 표시
                 if (meeting.transcriptPath != null) {
                     DropdownMenuItem(
                         text = { Text("공유") },
                         onClick = {
                             showMenu = false
                             onShare(meeting.id)
                         },
                         leadingIcon = {
                             Icon(Icons.Default.Share, contentDescription = null)
                         }
                     )
                 }
                 // 삭제: 항상 표시
                 DropdownMenuItem(
                     text = { Text("삭제", color = MaterialTheme.colorScheme.error) },
                     onClick = {
                         showMenu = false
                         onDeleteRequest(meeting.id)
                     },
                     leadingIcon = {
                         Icon(
                             Icons.Default.Delete,
                             contentDescription = null,
                             tint = MaterialTheme.colorScheme.error
                         )
                     }
                 )
             }
         }
     }
     ```

5. **기존 제목 Text를 위 Row로 교체.** 기존 단독 Text("meeting.title") 부분(165-170줄)을 위의 Row로 교체한다.

6. **long-press(combinedClickable의 onLongClick) 삭제:** Card의 combinedClickable에서 `onLongClick = { onDeleteRequest(meeting.id) }` 제거. 일반 clickable로 변경하거나, combinedClickable 유지하되 onLongClick 제거:
   ```kotlin
   Card(
       modifier = Modifier
           .fillMaxWidth()
           .combinedClickable(
               onClick = { if (isEditable) onClick() }
           ),
       ...
   )
   ```
   만약 combinedClickable에서 onLongClick이 필수 파라미터가 아니면 단순 제거. 아니면 `Modifier.clickable { if (isEditable) onClick() }`로 교체.
  </action>
  <verify>
    <automated>cd D:/workspace/temp/Auto_Minuting && grep -n "DropdownMenu\|MoreVert\|onRetranscribe\|onShare\|재전사\|공유" app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt</automated>
  </verify>
  <acceptance_criteria>
    - TranscriptsScreen.kt contains `import androidx.compose.material3.DropdownMenu`
    - TranscriptsScreen.kt contains `import androidx.compose.material.icons.filled.MoreVert`
    - TranscriptsScreen.kt contains `Icons.Default.MoreVert`
    - TranscriptsScreen.kt contains `DropdownMenu(`
    - TranscriptsScreen.kt contains `DropdownMenuItem(` (최소 3개: 재전사, 공유, 삭제)
    - TranscriptsScreen.kt contains `Text("재전사")`
    - TranscriptsScreen.kt contains `Text("공유")`
    - TranscriptsScreen.kt contains `Text("삭제"`
    - TranscriptsScreen.kt contains `onRetranscribe: (Long) -> Unit`
    - TranscriptsScreen.kt contains `onShare: (Long) -> Unit`
    - TranscriptsScreen.kt contains `LocalContext.current`
    - TranscriptsScreen.kt contains `viewModel.retranscribe`
    - TranscriptsScreen.kt contains `viewModel.shareTranscript`
  </acceptance_criteria>
  <done>전사 카드마다 MoreVert 아이콘이 표시되고, 탭하면 재전사/공유/삭제 드롭다운 메뉴가 나타난다. 재전사는 오디오 파일이 있고 적절한 상태일 때만, 공유는 전사 경로가 있을 때만 표시된다.</done>
</task>

</tasks>

<verification>
1. grep으로 TranscriptsViewModel.kt에 retranscribe, shareTranscript 함수 존재 확인
2. grep으로 TranscriptsScreen.kt에 DropdownMenu, MoreVert, 재전사, 공유, 삭제 메뉴 항목 존재 확인
3. 빌드 성공 확인 (가능한 경우)
</verification>

<success_criteria>
- 전사 목록 카드에 더보기 아이콘(MoreVert)이 표시된다
- 더보기 아이콘 탭 시 드롭다운 메뉴가 나타난다
- 메뉴 항목: 재전사 (조건부), 공유 (조건부), 삭제 (항상)
- 재전사 선택 시 TranscriptionTriggerWorker가 enqueue된다
- 공유 선택 시 전사 텍스트가 ACTION_SEND Intent로 공유된다
- 삭제 선택 시 기존 확인 다이얼로그가 표시되고 deleteTranscript가 호출된다
</success_criteria>

<output>
After completion, create `.planning/phases/20-/20-01-SUMMARY.md`
</output>
