---
phase: 33-gui-consistency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/com/autominuting/presentation/dashboard/DashboardScreen.kt
  - app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt
  - app/src/main/java/com/autominuting/presentation/minutes/MinutesScreen.kt
  - app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt
autonomous: true
requirements: [GUI-01, GUI-02, GUI-03, GUI-04]

must_haves:
  truths:
    - "DashboardScreen에 TopAppBar가 표시되어 'Auto Minuting' 타이틀이 앱바에 위치한다"
    - "TranscriptsScreen에 TopAppBar가 표시되어 '전사 목록' 타이틀이 앱바에 위치한다"
    - "모든 아이콘의 contentDescription이 null이 아닌 적절한 한국어 설명을 갖는다"
    - "전사 목록 빈 상태에 아이콘+텍스트가 함께 표시된다"
    - "회의록 목록 날짜가 yyyy.MM.dd HH:mm 포맷으로 표시된다"
  artifacts:
    - path: "app/src/main/java/com/autominuting/presentation/dashboard/DashboardScreen.kt"
      provides: "DashboardScreen with Scaffold + TopAppBar"
      contains: "TopAppBar"
    - path: "app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt"
      provides: "TranscriptsScreen with Scaffold + TopAppBar + icon empty state"
      contains: "TopAppBar"
    - path: "app/src/main/java/com/autominuting/presentation/minutes/MinutesScreen.kt"
      provides: "MinutesScreen with unified date format"
      contains: "yyyy.MM.dd HH:mm"
  key_links:
    - from: "DashboardScreen.kt"
      to: "Scaffold"
      via: "TopAppBar composable"
      pattern: "Scaffold.*topBar.*TopAppBar"
    - from: "TranscriptsScreen.kt"
      to: "Scaffold"
      via: "TopAppBar composable"
      pattern: "Scaffold.*topBar.*TopAppBar"
---

<objective>
앱 전반의 GUI를 일관되게 통일하고 접근성 표준을 준수한다.

Purpose: DashboardScreen과 TranscriptsScreen에 TopAppBar가 없어 MinutesScreen/SettingsScreen과 불일치하고, 아이콘에 contentDescription이 누락되어 TalkBack 접근성이 떨어지며, 빈 상태 표시와 날짜 포맷이 화면별로 다르다.
Output: 4개 화면의 GUI가 통일된 일관된 앱 UI
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@app/src/main/java/com/autominuting/presentation/dashboard/DashboardScreen.kt
@app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt
@app/src/main/java/com/autominuting/presentation/minutes/MinutesScreen.kt
@app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt

<interfaces>
<!-- 참조 패턴: SettingsScreen의 Scaffold + TopAppBar 구조를 따른다 -->

From SettingsScreen.kt (line 105-111):
```kotlin
Scaffold(
    topBar = {
        TopAppBar(
            title = { Text("설정") }
        )
    }
) { innerPadding ->
    Column(
        modifier = Modifier
            .fillMaxSize()
            .padding(innerPadding)
```

From MinutesScreen.kt empty state (line 148-167):
```kotlin
Column(
    horizontalAlignment = Alignment.CenterHorizontally,
    verticalArrangement = Arrangement.spacedBy(8.dp)
) {
    Icon(
        imageVector = Icons.Default.Description,
        contentDescription = null,
        tint = MaterialTheme.colorScheme.onSurfaceVariant,
        modifier = Modifier.padding(bottom = 8.dp)
    )
    Text(
        text = "생성된 회의록이 없습니다",
        style = MaterialTheme.typography.bodyLarge,
        color = MaterialTheme.colorScheme.onSurfaceVariant
    )
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DashboardScreen/TranscriptsScreen에 Scaffold+TopAppBar 추가 및 빈 상태/날짜 포맷 통일 (GUI-01, GUI-03, GUI-04)</name>
  <read_first>
    app/src/main/java/com/autominuting/presentation/dashboard/DashboardScreen.kt
    app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt
    app/src/main/java/com/autominuting/presentation/minutes/MinutesScreen.kt
  </read_first>
  <files>
    app/src/main/java/com/autominuting/presentation/dashboard/DashboardScreen.kt
    app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt
    app/src/main/java/com/autominuting/presentation/minutes/MinutesScreen.kt
  </files>
  <action>
    **DashboardScreen.kt (GUI-01):**
    1. import 추가: `import androidx.compose.material3.Scaffold`, `import androidx.compose.material3.TopAppBar`, `import androidx.compose.material3.ExperimentalMaterial3Api`
    2. DashboardScreen 함수에 `@OptIn(ExperimentalMaterial3Api::class)` 추가
    3. 현재 최상위 `Column(modifier = Modifier.fillMaxSize().verticalScroll(...))` 을 `Scaffold(topBar = { TopAppBar(title = { Text("Auto Minuting") }) }) { innerPadding -> ... }` 로 감싼다
    4. 내부 Column에 `.padding(innerPadding)` 추가
    5. 기존 "대시보드 타이틀" 섹션(line 200-212, `Text("Auto Minuting")` headlineMedium + 서브타이틀)을 제거한다 -- TopAppBar가 타이틀을 대체한다. 서브타이틀 "녹음에서 회의록까지, 자동으로."는 TopAppBar 아래 첫 번째 요소로 유지한다.

    **TranscriptsScreen.kt (GUI-01 + GUI-03):**
    1. import 추가: `import androidx.compose.material3.Scaffold`, `import androidx.compose.material3.TopAppBar`, `import androidx.compose.material3.ExperimentalMaterial3Api`, `import androidx.compose.material.icons.filled.TextSnippet` (빈 상태 아이콘용, 또는 `import androidx.compose.material.icons.automirrored.filled.TextSnippet` -- 이미 import 있음)
    2. TranscriptsScreen 함수에 `@OptIn(ExperimentalMaterial3Api::class)` 추가
    3. 현재 최상위 레벨(if-else 구조)을 `Scaffold(topBar = { TopAppBar(title = { Text("전사 목록") }) }) { innerPadding -> ... }` 로 감싼다
    4. 빈 목록 안내(line 95-106)를 MinutesScreen 패턴과 동일하게 변경:
       - 기존 단순 Text만 있는 것을 Column(horizontalAlignment=CenterHorizontally, verticalArrangement=spacedBy(8.dp))으로 감싸고
       - Icon(imageVector=Icons.AutoMirrored.Filled.TextSnippet, contentDescription="빈 목록", tint=onSurfaceVariant, modifier=Modifier.padding(bottom=8.dp)) 추가
       - 기존 Text("전사된 항목이 없습니다") 유지
    5. LazyColumn과 빈 상태 Box에 `.padding(innerPadding)` 또는 Scaffold content padding 적용

    **MinutesScreen.kt (GUI-04):**
    1. 파일 하단의 DATE_TIME_FORMATTER 패턴을 `"yyyy-MM-dd HH:mm"`에서 `"yyyy.MM.dd HH:mm"`으로 변경 (line 504-505)
    2. 이 한 줄 변경으로 회의록 목록의 모든 날짜 표시가 전사 목록과 동일한 포맷이 된다
  </action>
  <verify>
    <automated>cd D:/workspace/temp/Auto_Minuting && grep -n "TopAppBar" app/src/main/java/com/autominuting/presentation/dashboard/DashboardScreen.kt app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt && grep -n "yyyy.MM.dd" app/src/main/java/com/autominuting/presentation/minutes/MinutesScreen.kt && grep -c "yyyy-MM-dd" app/src/main/java/com/autominuting/presentation/minutes/MinutesScreen.kt</automated>
  </verify>
  <acceptance_criteria>
    - DashboardScreen.kt에 `TopAppBar` 가 1회 이상 나타남 (grep 결과)
    - TranscriptsScreen.kt에 `TopAppBar` 가 1회 이상 나타남 (grep 결과)
    - MinutesScreen.kt에 `yyyy.MM.dd HH:mm`이 나타남 (grep 결과)
    - MinutesScreen.kt에 `yyyy-MM-dd`가 0회 나타남 (grep -c 결과 = 0)
    - TranscriptsScreen.kt 빈 상태에 Icon이 포함됨
  </acceptance_criteria>
  <done>
    DashboardScreen과 TranscriptsScreen에 Scaffold+TopAppBar가 추가되어 SettingsScreen/MinutesScreen과 동일한 구조를 갖는다. 전사 목록 빈 상태에 아이콘이 추가되어 회의록 목록과 통일된다. 회의록 날짜 포맷이 yyyy.MM.dd HH:mm으로 전사 목록과 통일된다.
  </done>
</task>

<task type="auto">
  <name>Task 2: 모든 아이콘 contentDescription 접근성 설정 (GUI-02)</name>
  <read_first>
    app/src/main/java/com/autominuting/presentation/dashboard/DashboardScreen.kt
    app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt
    app/src/main/java/com/autominuting/presentation/minutes/MinutesScreen.kt
    app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt
  </read_first>
  <files>
    app/src/main/java/com/autominuting/presentation/dashboard/DashboardScreen.kt
    app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt
    app/src/main/java/com/autominuting/presentation/minutes/MinutesScreen.kt
    app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt
  </files>
  <action>
    모든 `contentDescription = null`을 적절한 한국어 설명으로 교체한다.
    DropdownMenuItem의 leadingIcon 내부 아이콘은 텍스트 라벨이 이미 있으므로 TalkBack이 중복 읽기를 하지만, 접근성 완전성을 위해 설정한다.

    **DashboardScreen.kt:**
    - line 241: NotebookLM 열기 아이콘 `contentDescription = null` → `contentDescription = "NotebookLM"`

    **TranscriptsScreen.kt:**
    - line 313: 재전사 메뉴 아이콘 `contentDescription = null` → `contentDescription = "재전사"`
    - line 327: 회의록 작성 메뉴 아이콘 `contentDescription = null` → `contentDescription = "회의록 작성"`
    - line 340: 회의록 재생성 메뉴 아이콘 `contentDescription = null` → `contentDescription = "회의록 재생성"`
    - line 353: 공유 메뉴 아이콘 `contentDescription = null` → `contentDescription = "공유"`
    - line 367: 삭제 메뉴 아이콘 `contentDescription = null` → `contentDescription = "삭제"`

    **MinutesScreen.kt:**
    - line 154: 빈 목록 아이콘 `contentDescription = null` → `contentDescription = "빈 목록"`
    - line 403: 공유 메뉴 아이콘 `contentDescription = null` → `contentDescription = "공유"`
    - line 417: 삭제 메뉴 아이콘 `contentDescription = null` → `contentDescription = "삭제"`

    **SettingsScreen.kt:**
    - line 473: Google 로그인 아이콘 `contentDescription = null` → `contentDescription = "Google 계정"`
    - line 507: 로그인 상태 아이콘 `contentDescription = null` → `contentDescription = "Google 계정"`
    - line 545: 오류 상태 로그인 아이콘 `contentDescription = null` → `contentDescription = "Google 계정"`
  </action>
  <verify>
    <automated>cd D:/workspace/temp/Auto_Minuting && grep -rn "contentDescription = null" app/src/main/java/com/autominuting/presentation/dashboard/DashboardScreen.kt app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt app/src/main/java/com/autominuting/presentation/minutes/MinutesScreen.kt app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt; echo "EXIT_CODE=$?"</automated>
  </verify>
  <acceptance_criteria>
    - 4개 파일에서 `contentDescription = null` 검색 결과가 0건이어야 한다
    - grep 결과가 빈 출력이고 EXIT_CODE=1 (매치 없음)
  </acceptance_criteria>
  <done>
    DashboardScreen, TranscriptsScreen, MinutesScreen, SettingsScreen의 모든 아이콘에 적절한 contentDescription이 설정되어 TalkBack으로 읽힌다.
  </done>
</task>

</tasks>

<verification>
1. `grep -rn "TopAppBar" app/src/main/java/com/autominuting/presentation/` — DashboardScreen, TranscriptsScreen, MinutesScreen, SettingsScreen 모두에 TopAppBar 존재 확인
2. `grep -rn "contentDescription = null" app/src/main/java/com/autominuting/presentation/` — 0건 확인
3. `grep -n "yyyy.MM.dd" app/src/main/java/com/autominuting/presentation/minutes/MinutesScreen.kt` — 통일된 날짜 포맷 확인
4. `grep -n "yyyy-MM-dd" app/src/main/java/com/autominuting/presentation/minutes/MinutesScreen.kt` — 0건 확인
5. `grep -A3 "빈 목록 안내\|빈 상태" app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt` — Icon 포함 확인
</verification>

<success_criteria>
- DashboardScreen과 TranscriptsScreen에 TopAppBar가 표시된다
- 4개 프레젠테이션 파일에 contentDescription = null이 0건이다
- 전사 목록 빈 상태에 아이콘+텍스트가 표시된다
- 전사 목록과 회의록 목록의 날짜 포맷이 yyyy.MM.dd HH:mm으로 동일하다
</success_criteria>

<output>
After completion, create `.planning/phases/33-/33-01-SUMMARY.md`
</output>
