---
phase: 36-minutes-data-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/com/autominuting/data/local/entity/MinutesEntity.kt
  - app/src/main/java/com/autominuting/domain/model/Minutes.kt
  - app/src/main/java/com/autominuting/data/local/dao/MinutesDao.kt
  - app/src/main/java/com/autominuting/data/local/AppDatabase.kt
  - app/src/main/java/com/autominuting/di/DatabaseModule.kt
  - app/src/main/java/com/autominuting/data/local/entity/MeetingEntity.kt
  - app/src/main/java/com/autominuting/data/local/dao/MeetingDao.kt
  - app/src/main/java/com/autominuting/domain/model/Meeting.kt
  - app/src/main/java/com/autominuting/domain/model/PipelineStatus.kt
autonomous: true
requirements: [DATA-01]

must_haves:
  truths:
    - "Room DB v5에 minutes 테이블이 존재하고 meetingId FK로 meetings 테이블과 연결된다"
    - "기존 minutesPath/minutesTitle 데이터가 minutes 테이블로 이관된다"
    - "Meeting 테이블에서 minutesPath/minutesTitle 컬럼이 제거된다"
    - "MINUTES_ONLY PipelineStatus가 COMPLETED로 변환된다"
  artifacts:
    - path: "app/src/main/java/com/autominuting/data/local/entity/MinutesEntity.kt"
      provides: "Minutes Room Entity (id, meetingId FK, minutesPath, minutesTitle, templateId, createdAt, updatedAt)"
    - path: "app/src/main/java/com/autominuting/domain/model/Minutes.kt"
      provides: "Minutes 도메인 모델"
    - path: "app/src/main/java/com/autominuting/data/local/dao/MinutesDao.kt"
      provides: "Minutes CRUD DAO"
    - path: "app/src/main/java/com/autominuting/data/local/AppDatabase.kt"
      provides: "version=5, MIGRATION_4_5, MinutesEntity 등록, minutesDao()"
  key_links:
    - from: "MinutesEntity"
      to: "MeetingEntity"
      via: "meetingId FK with onDelete=SET_NULL"
      pattern: "ForeignKey.*MeetingEntity.*SET_NULL"
    - from: "AppDatabase"
      to: "MIGRATION_4_5"
      via: "addMigrations in DatabaseModule"
      pattern: "MIGRATION_4_5"
---

<objective>
Minutes 독립 테이블의 데이터 레이어 기반을 구축한다.

Purpose: MinutesEntity/MinutesDao/Minutes 도메인 모델을 신설하고, Room DB v4->v5 마이그레이션으로 기존 데이터를 이관하며, MeetingEntity/MeetingDao에서 minutesPath/minutesTitle/MINUTES_ONLY를 제거한다.
Output: 컴파일 가능한 데이터 레이어 (신규 Entity/DAO/Migration + 기존 Entity/DAO 정리). 이 Plan에서는 Repository/Worker/ViewModel은 변경하지 않으므로 **컴파일 에러가 발생할 수 있다** — Plan 02/03에서 순차적으로 해결한다.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-minutes-data-model/36-RESEARCH.md

@app/src/main/java/com/autominuting/data/local/AppDatabase.kt
@app/src/main/java/com/autominuting/data/local/entity/MeetingEntity.kt
@app/src/main/java/com/autominuting/data/local/dao/MeetingDao.kt
@app/src/main/java/com/autominuting/domain/model/Meeting.kt
@app/src/main/java/com/autominuting/domain/model/PipelineStatus.kt
@app/src/main/java/com/autominuting/di/DatabaseModule.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: MinutesEntity + Minutes 도메인 모델 + MinutesDao 신설</name>
  <files>
    app/src/main/java/com/autominuting/data/local/entity/MinutesEntity.kt
    app/src/main/java/com/autominuting/domain/model/Minutes.kt
    app/src/main/java/com/autominuting/data/local/dao/MinutesDao.kt
  </files>
  <action>
1. **MinutesEntity.kt** 신규 생성 (`data/local/entity/`):
```kotlin
@Entity(
    tableName = "minutes",
    foreignKeys = [ForeignKey(
        entity = MeetingEntity::class,
        parentColumns = ["id"],
        childColumns = ["meetingId"],
        onDelete = ForeignKey.SET_NULL
    )],
    indices = [Index(value = ["meetingId"])]
)
data class MinutesEntity(
    @PrimaryKey(autoGenerate = true) val id: Long = 0,
    val meetingId: Long?,        // FK -> meetings.id, nullable (Meeting 삭제 시 SET_NULL)
    val minutesPath: String,     // NOT NULL - 회의록 파일 경로
    val minutesTitle: String?,   // 자동 추출 제목
    val templateId: Long?,       // 생성에 사용된 프롬프트 템플릿 ID
    val createdAt: Long,         // epoch millis
    val updatedAt: Long          // epoch millis
)
```
- `toDomain()` 함수 추가: MinutesEntity -> Minutes 변환 (createdAt/updatedAt는 Instant.ofEpochMilli)
- `companion object { fromDomain() }` 추가: Minutes -> MinutesEntity 변환

2. **Minutes.kt** 신규 생성 (`domain/model/`):
```kotlin
data class Minutes(
    val id: Long = 0,
    val meetingId: Long?,
    val minutesPath: String,
    val minutesTitle: String?,
    val templateId: Long?,
    val createdAt: Instant,
    val updatedAt: Instant
)
```

3. **MinutesDao.kt** 신규 생성 (`data/local/dao/`):
36-RESEARCH.md의 MinutesDao 설계를 그대로 구현한다. 메서드 목록:
- `getAllMinutes(): Flow<List<MinutesEntity>>` — createdAt DESC
- `getMinutesByMeetingId(meetingId: Long): Flow<List<MinutesEntity>>` — createdAt DESC
- `getMinutesById(id: Long): Flow<MinutesEntity?>`
- `getMinutesByIdOnce(id: Long): suspend MinutesEntity?`
- `getMinutesCountByMeetingId(meetingId: Long): Flow<Int>`
- `insert(entity: MinutesEntity): suspend Long`
- `updateTitle(id: Long, title: String, updatedAt: Long): suspend`
- `delete(id: Long): suspend`
- `deleteByMeetingId(meetingId: Long): suspend`
- 추가: `getMinutesByMeetingIdOnce(meetingId: Long): suspend List<MinutesEntity>` — 삭제 시 파일 경로 조회용
  </action>
  <verify>
    <automated>cd D:/workspace/temp/Auto_Minuting && cat app/src/main/java/com/autominuting/data/local/entity/MinutesEntity.kt app/src/main/java/com/autominuting/domain/model/Minutes.kt app/src/main/java/com/autominuting/data/local/dao/MinutesDao.kt | head -20</automated>
  </verify>
  <done>MinutesEntity, Minutes, MinutesDao 3개 파일이 존재하고, Entity에 ForeignKey(SET_NULL) + Index가 설정되어 있다</done>
</task>

<task type="auto">
  <name>Task 2: Room DB v5 마이그레이션 + MeetingEntity/MeetingDao/PipelineStatus 정리</name>
  <files>
    app/src/main/java/com/autominuting/data/local/AppDatabase.kt
    app/src/main/java/com/autominuting/di/DatabaseModule.kt
    app/src/main/java/com/autominuting/data/local/entity/MeetingEntity.kt
    app/src/main/java/com/autominuting/data/local/dao/MeetingDao.kt
    app/src/main/java/com/autominuting/domain/model/Meeting.kt
    app/src/main/java/com/autominuting/domain/model/PipelineStatus.kt
  </files>
  <action>
1. **AppDatabase.kt** 수정:
   - `entities`에 `MinutesEntity::class` 추가: `entities = [MeetingEntity::class, PromptTemplateEntity::class, MinutesEntity::class]`
   - `version = 5`로 변경
   - `abstract fun minutesDao(): MinutesDao` 추가
   - import 추가: `MinutesEntity`, `MinutesDao`
   - `MIGRATION_4_5` 추가 (companion object 안):
     36-RESEARCH.md의 MIGRATION_4_5 SQL을 정확히 그대로 사용한다. 5단계:
     (1) minutes 테이블 CREATE + INDEX
     (2) INSERT INTO minutes SELECT (minutesPath IS NOT NULL인 Meeting에서)
     (3) meetings_new 테이블 CREATE (minutesPath, minutesTitle 컬럼 없음)
     (4) INSERT INTO meetings_new SELECT (MINUTES_ONLY -> COMPLETED 변환)
     (5) DROP TABLE meetings + RENAME meetings_new -> meetings

2. **DatabaseModule.kt** 수정:
   - `.addMigrations(...)` 에 `AppDatabase.MIGRATION_4_5` 추가
   - `fun provideMinutesDao(db: AppDatabase): MinutesDao = db.minutesDao()` 추가 (`@Provides` 어노테이션)

3. **MeetingEntity.kt** 수정:
   - `minutesPath` 필드 제거
   - `minutesTitle` 필드 제거
   - `toDomain()` 에서 minutesPath, minutesTitle 매핑 제거
   - `fromDomain()` 에서 minutesPath, minutesTitle 매핑 제거

4. **Meeting.kt** (도메인 모델) 수정:
   - `minutesPath` 필드 제거
   - `minutesTitle` 필드 제거
   - KDoc에서 관련 @property 제거

5. **MeetingDao.kt** 수정:
   - `updateMinutes()` 메서드 제거
   - `updateMinutesTitle()` 메서드 제거
   - `clearMinutesPath()` 메서드 제거
   - `markMinutesOnly()` 메서드 제거
   - 나머지 메서드(`getAllMeetings`, `getMeetingById`, `insert`, `update`, `updatePipelineStatus`, `updateTranscript`, `getMeetingByAudioPath`, `searchMeetings`, `clearTranscriptPath`, `recoverStaleInProgressMeetings`, `delete`)는 그대로 유지

6. **PipelineStatus.kt** 수정:
   - `MINUTES_ONLY` 값 제거 (마이그레이션에서 COMPLETED로 변환 완료)
   - 관련 KDoc 제거

**주의:** 이 Task 완료 후 MeetingRepositoryImpl, MinutesGenerationWorker, ViewModel, Screen 파일에서 컴파일 에러가 발생한다. 이는 Plan 02와 Plan 03에서 순차적으로 해결한다.
  </action>
  <verify>
    <automated>cd D:/workspace/temp/Auto_Minuting && grep -c "minutesPath\|minutesTitle" app/src/main/java/com/autominuting/data/local/entity/MeetingEntity.kt app/src/main/java/com/autominuting/domain/model/Meeting.kt && grep "MINUTES_ONLY" app/src/main/java/com/autominuting/domain/model/PipelineStatus.kt; grep "version = 5" app/src/main/java/com/autominuting/data/local/AppDatabase.kt && grep "MIGRATION_4_5" app/src/main/java/com/autominuting/di/DatabaseModule.kt</automated>
  </verify>
  <done>MeetingEntity/Meeting에서 minutesPath/minutesTitle가 0개, PipelineStatus에 MINUTES_ONLY가 없고, AppDatabase version=5, DatabaseModule에 MIGRATION_4_5가 등록됨</done>
</task>

</tasks>

<verification>
- MinutesEntity에 @ForeignKey(onDelete = SET_NULL) + @Index(meetingId) 확인
- AppDatabase.version = 5, entities 배열에 MinutesEntity 포함 확인
- MIGRATION_4_5 SQL이 5단계(CREATE minutes, INSERT data, CREATE meetings_new, INSERT data, DROP+RENAME) 포함 확인
- MeetingEntity에서 minutesPath, minutesTitle 필드 완전 제거 확인
- MeetingDao에서 updateMinutes, updateMinutesTitle, clearMinutesPath, markMinutesOnly 메서드 완전 제거 확인
- PipelineStatus에서 MINUTES_ONLY 값 제거 확인
- DatabaseModule에 MIGRATION_4_5 추가 + minutesDao @Provides 추가 확인
</verification>

<success_criteria>
1. MinutesEntity, Minutes, MinutesDao 3개 신규 파일이 존재한다
2. AppDatabase가 version=5이고 MinutesEntity를 포함한다
3. MIGRATION_4_5가 AppDatabase에 정의되고 DatabaseModule에 등록되어 있다
4. MeetingEntity/Meeting에서 minutesPath/minutesTitle이 완전히 제거되었다
5. MeetingDao에서 minutes 관련 4개 메서드가 제거되었다
6. PipelineStatus에서 MINUTES_ONLY가 제거되었다
</success_criteria>

<output>
After completion, create `.planning/phases/36-minutes-data-model/36-01-SUMMARY.md`
</output>
