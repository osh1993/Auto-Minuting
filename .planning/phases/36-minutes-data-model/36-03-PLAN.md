---
phase: 36-minutes-data-model
plan: 03
type: execute
wave: 3
depends_on: [36-02]
files_modified:
  - app/src/main/java/com/autominuting/presentation/minutes/MinutesViewModel.kt
  - app/src/main/java/com/autominuting/presentation/minutes/MinutesDetailViewModel.kt
  - app/src/main/java/com/autominuting/presentation/minutes/MinutesScreen.kt
  - app/src/main/java/com/autominuting/presentation/minutes/MinutesDetailScreen.kt
  - app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt
  - app/src/main/java/com/autominuting/presentation/navigation/Screen.kt
  - app/src/main/java/com/autominuting/presentation/navigation/AppNavigation.kt
autonomous: true
requirements: [DATA-01, DATA-02]

must_haves:
  truths:
    - "회의록 목록 화면이 Minutes 테이블을 직접 조회하여 회의록을 표시한다"
    - "회의록 상세 화면이 minutesId 기반으로 Minutes 테이블에서 회의록을 로드한다"
    - "전사 카드의 회의록 상태 배지가 Minutes 테이블 기반으로 동작한다"
    - "앱이 빌드되고 정상 실행된다 (모든 컴파일 에러 해소)"
  artifacts:
    - path: "app/src/main/java/com/autominuting/presentation/minutes/MinutesViewModel.kt"
      provides: "Minutes 테이블 기반 회의록 목록 조회 + 삭제 + 제목 수정 + 공유"
    - path: "app/src/main/java/com/autominuting/presentation/minutes/MinutesDetailViewModel.kt"
      provides: "minutesId 기반 회의록 상세 조회"
  key_links:
    - from: "MinutesViewModel"
      to: "MinutesDataRepository"
      via: "Hilt 주입"
      pattern: "minutesDataRepository"
    - from: "MinutesDetailViewModel"
      to: "MinutesDataRepository.getMinutesById()"
      via: "minutesId navigation argument"
      pattern: "minutesDataRepository\\.getMinutesById"
    - from: "MinutesScreen"
      to: "Minutes 도메인 모델"
      via: "minutes.minutesPath, minutes.minutesTitle"
      pattern: "minutes\\.(minutesPath|minutesTitle)"
---

<objective>
Presentation 레이어(ViewModel + Screen)를 Minutes 테이블 기반으로 교체하고 빌드를 통과시킨다.

Purpose: MinutesViewModel/MinutesDetailViewModel의 데이터 소스를 Meeting.minutesPath에서 MinutesDataRepository로 변경하고, Screen 파일의 Meeting.minutesPath/minutesTitle 참조를 Minutes 도메인 모델 참조로 교체하며, Navigation 라우팅을 minutesId 기반으로 변경한다.
Output: 전체 프로젝트 빌드 성공. 기존과 동일한 기능이 Minutes 테이블 기반으로 동작.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/36-minutes-data-model/36-RESEARCH.md
@.planning/phases/36-minutes-data-model/36-01-SUMMARY.md
@.planning/phases/36-minutes-data-model/36-02-SUMMARY.md

@app/src/main/java/com/autominuting/presentation/minutes/MinutesViewModel.kt
@app/src/main/java/com/autominuting/presentation/minutes/MinutesDetailViewModel.kt
@app/src/main/java/com/autominuting/presentation/minutes/MinutesScreen.kt
@app/src/main/java/com/autominuting/presentation/minutes/MinutesDetailScreen.kt
@app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt
@app/src/main/java/com/autominuting/presentation/navigation/Screen.kt
@app/src/main/java/com/autominuting/presentation/navigation/AppNavigation.kt

<interfaces>
<!-- Plan 02에서 생성된 인터페이스 -->
From domain/repository/MinutesDataRepository.kt:
```kotlin
interface MinutesDataRepository {
    fun getAllMinutes(): Flow<List<Minutes>>
    fun getMinutesByMeetingId(meetingId: Long): Flow<List<Minutes>>
    fun getMinutesById(id: Long): Flow<Minutes?>
    suspend fun getMinutesByIdOnce(id: Long): Minutes?
    fun getMinutesCountByMeetingId(meetingId: Long): Flow<Int>
    suspend fun insertMinutes(minutes: Minutes): Long
    suspend fun updateMinutesTitle(id: Long, title: String)
    suspend fun deleteMinutes(id: Long)
    suspend fun deleteMinutesByMeetingId(meetingId: Long)
}
```

From domain/model/Minutes.kt:
```kotlin
data class Minutes(
    val id: Long = 0,
    val meetingId: Long?,
    val minutesPath: String,
    val minutesTitle: String?,
    val templateId: Long?,
    val createdAt: Instant,
    val updatedAt: Instant
)
```

Meeting 도메인 모델 (Plan 01에서 변경됨):
```kotlin
data class Meeting(
    val id: Long = 0,
    val title: String,
    val recordedAt: Instant,
    val audioFilePath: String,
    val transcriptPath: String? = null,
    // minutesPath, minutesTitle 제거됨
    val pipelineStatus: PipelineStatus,
    val errorMessage: String? = null,
    val createdAt: Instant,
    val updatedAt: Instant,
    val source: String = "PLAUD_BLE"
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MinutesViewModel + MinutesDetailViewModel을 Minutes 테이블 기반으로 교체</name>
  <files>
    app/src/main/java/com/autominuting/presentation/minutes/MinutesViewModel.kt
    app/src/main/java/com/autominuting/presentation/minutes/MinutesDetailViewModel.kt
  </files>
  <action>
1. **MinutesViewModel.kt** 전면 수정:
   - 생성자: `MeetingRepository` 대신 (또는 추가로) `MinutesDataRepository`를 주입
   - `import com.autominuting.domain.model.Minutes` 추가
   - `import com.autominuting.domain.repository.MinutesDataRepository` 추가

   - `meetings` StateFlow 교체 → `minutes` StateFlow:
     **기존:** meetingRepository.getMeetings() -> filter { minutesPath != null }
     **변경:** minutesDataRepository.getAllMinutes() (검색 기능은 Minutes 테이블에 없으므로 클라이언트 필터링)
     ```kotlin
     val minutes: StateFlow<List<Minutes>> = _searchQuery
         .debounce(300)
         .flatMapLatest { query ->
             minutesDataRepository.getAllMinutes().map { list ->
                 if (query.isBlank()) list
                 else list.filter { m ->
                     (m.minutesTitle ?: "").contains(query, ignoreCase = true)
                 }
             }
         }
         .stateIn(...)
     ```

   - `deleteMeeting(id)` → `deleteMinutes(id)`:
     **변경:** `minutesDataRepository.deleteMinutes(id)` 호출 (Minutes ID 기반)

   - `deleteSelectedMinutes(ids)`:
     **변경:** `ids.forEach { minutesDataRepository.deleteMinutes(it) }` (Minutes ID 기반)

   - `updateMinutesTitle(meetingId, newTitle)` → `updateMinutesTitle(minutesId, newTitle)`:
     **변경:** `minutesDataRepository.updateMinutesTitle(minutesId, newTitle)` (Minutes ID 기반)

   - `shareMinutes(meetingId, activityContext)` → `shareMinutes(minutesId, activityContext)`:
     **변경:** minutesDataRepository.getMinutesByIdOnce(minutesId)로 Minutes 엔티티 조회, minutes.minutesPath에서 파일 읽기, minutes.minutesTitle을 EXTRA_SUBJECT로 사용

   - `getMinutesContent(minutesPath)`: 변경 없음 (이미 경로 기반)

2. **MinutesDetailViewModel.kt** 전면 수정:
   - 생성자: `MeetingRepository` 대신 `MinutesDataRepository`를 주입
   - Navigation argument: `meetingId` → `minutesId` (savedStateHandle["minutesId"])
   - `meeting` StateFlow → `minutes` StateFlow:
     **변경:** `minutesDataRepository.getMinutesById(minutesId)` 반환
   - `minutesContent` StateFlow:
     **변경:** minutesDataRepository.getMinutesById(minutesId)에서 minutesPath를 감시하여 파일 내용 읽기
     ```kotlin
     val minutesContent: StateFlow<String> = minutesDataRepository.getMinutesById(minutesId)
         .flatMapLatest { minutes ->
             flow {
                 val path = minutes?.minutesPath
                 if (path != null) {
                     try {
                         val file = File(path)
                         if (file.exists()) emit(file.readText()) else emit("")
                     } catch (e: Exception) { emit("") }
                 } else { emit("") }
             }.flowOn(Dispatchers.IO)
         }
         .stateIn(...)
     ```
   - `MeetingRepository` import 제거, `Meeting` import 제거
  </action>
  <verify>
    <automated>cd D:/workspace/temp/Auto_Minuting && grep -c "Meeting\b" app/src/main/java/com/autominuting/presentation/minutes/MinutesDetailViewModel.kt && grep "minutesDataRepository" app/src/main/java/com/autominuting/presentation/minutes/MinutesViewModel.kt app/src/main/java/com/autominuting/presentation/minutes/MinutesDetailViewModel.kt</automated>
  </verify>
  <done>MinutesDetailViewModel에 Meeting 참조가 0건이고, 두 ViewModel 모두 minutesDataRepository를 사용한다</done>
</task>

<task type="auto">
  <name>Task 2: Screen 파일 + Navigation 라우팅을 minutesId 기반으로 교체 + 빌드 검증</name>
  <files>
    app/src/main/java/com/autominuting/presentation/minutes/MinutesScreen.kt
    app/src/main/java/com/autominuting/presentation/minutes/MinutesDetailScreen.kt
    app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt
    app/src/main/java/com/autominuting/presentation/navigation/Screen.kt
    app/src/main/java/com/autominuting/presentation/navigation/AppNavigation.kt
  </files>
  <action>
1. **Screen.kt** 수정:
   - `MinutesDetail` route: `"minutes/{meetingId}"` → `"minutes/{minutesId}"`
   - `createRoute` 파라미터명: meetingId → minutesId

2. **AppNavigation.kt** 수정:
   - MinutesScreen의 `onMinutesClick` 콜백: `meetingId` → `minutesId`
   - navArgument: `"meetingId"` → `"minutesId"`
   - 타입은 여전히 `NavType.LongType`

3. **MinutesScreen.kt** 수정:
   - `Meeting` import → `Minutes` import
   - `onMinutesClick: (Long) -> Unit` 파라미터: meetingId → minutesId (의미 변경)
   - ViewModel의 `meetings` → `minutes` (StateFlow 이름 변경에 맞춤)
   - 카드에서 `meeting.minutesPath`, `meeting.minutesTitle`, `meeting.title` 등 Meeting 속성 참조 → Minutes 속성으로 변경:
     - `meeting.minutesTitle ?: meeting.title` → `minutes.minutesTitle ?: "회의록"` (Minutes에는 title이 없으므로 minutesTitle 사용, null이면 기본값)
     - `meeting.minutesPath != null` 조건 → 제거 (Minutes Row가 있으면 항상 minutesPath가 NOT NULL)
     - `meeting.id` → `minutes.id` (Minutes의 ID)
   - `meeting.pipelineStatus` 기반 배지: Minutes 테이블에는 pipelineStatus가 없으므로 배지 로직을 제거하거나 단순화. Phase 36에서는 배지를 제거하고 Phase 38에서 새 디자인으로 추가.
   - `PipelineStatus.MINUTES_ONLY` 참조: 완전 제거
   - ViewModel 호출: `deleteMeeting(meeting.id)` → `deleteMinutes(minutes.id)`, `updateMinutesTitle(meeting.id, ...)` → `updateMinutesTitle(minutes.id, ...)`, `shareMinutes(meeting.id, ...)` → `shareMinutes(minutes.id, ...)`
   - 이름 편집 다이얼로그의 초기값: `meeting.minutesTitle ?: meeting.title` → `minutes.minutesTitle ?: ""`

4. **MinutesDetailScreen.kt** 수정:
   - ViewModel의 `meeting` → `minutes` (StateFlow 이름 변경에 맞춤)
   - `currentMeeting.minutesPath == null` 체크 → `currentMinutes == null` 체크 (Minutes Row 자체가 없으면)
   - Meeting 타입 참조 → Minutes 타입 참조
   - AppBar 제목: `currentMeeting.minutesTitle ?: currentMeeting.title` → `currentMinutes.minutesTitle ?: "회의록 상세"`

5. **TranscriptsScreen.kt** 수정:
   - `MinutesStatusBadge` 함수에서 `meeting.minutesPath != null` 조건 → `meeting.pipelineStatus == PipelineStatus.COMPLETED` 조건으로 변경 (Meeting에 minutesPath가 없으므로 pipelineStatus로 판단). 정확한 회의록 수 badge는 Phase 38 범위이므로, Phase 36에서는 COMPLETED 상태면 "회의록 완료" 배지를 표시한다.
   - `PipelineStatus.MINUTES_ONLY` 참조가 있으면 제거

6. **빌드 검증:**
   전체 프로젝트를 빌드하여 컴파일 에러가 없는지 확인한다.
   `./gradlew assembleDebug` 실행.
   에러가 있으면 해당 파일의 Meeting.minutesPath/minutesTitle 잔여 참조를 추가로 정리한다.

**주의 사항:**
- grep으로 전체 프로젝트에서 `\.minutesPath` `\.minutesTitle` `MINUTES_ONLY` `deleteMinutesOnly` `archiveAsMinutesOnly` `clearMinutesPath` `markMinutesOnly` `updateMinutesTitle.*meeting` 패턴을 검색하여 누락된 참조가 없는지 최종 확인할 것
- DashboardScreen에 minutesPath 참조가 있으면 함께 정리할 것
  </action>
  <verify>
    <automated>cd D:/workspace/temp/Auto_Minuting && ./gradlew assembleDebug 2>&1 | tail -5</automated>
  </verify>
  <done>./gradlew assembleDebug가 BUILD SUCCESSFUL로 완료되고, meeting.minutesPath/minutesTitle/MINUTES_ONLY 참조가 프로젝트 전체에서 0건이다</done>
</task>

</tasks>

<verification>
- `grep -r "\.minutesPath\|\.minutesTitle" app/src/main/java/com/autominuting/` → 0건 (MinutesEntity/Minutes의 자체 필드 제외)
- `grep -r "MINUTES_ONLY" app/src/main/java/com/autominuting/` → 0건
- `grep -r "deleteMinutesOnly\|archiveAsMinutesOnly\|clearMinutesPath\|markMinutesOnly" app/src/main/java/com/autominuting/` → 0건
- `./gradlew assembleDebug` → BUILD SUCCESSFUL
- MinutesScreen이 Minutes 모델 기반으로 회의록을 표시
- MinutesDetailScreen이 minutesId 기반으로 라우팅
- TranscriptsScreen이 pipelineStatus 기반으로 회의록 배지 표시
</verification>

<success_criteria>
1. MinutesViewModel이 MinutesDataRepository.getAllMinutes()로 회의록 목록을 조회한다
2. MinutesDetailViewModel이 minutesId 기반으로 MinutesDataRepository.getMinutesById()를 호출한다
3. Navigation 라우팅이 minutesId 기반으로 변경되었다
4. MinutesScreen/MinutesDetailScreen에서 Meeting.minutesPath/minutesTitle 참조가 없다
5. TranscriptsScreen에서 MINUTES_ONLY 참조가 없다
6. `./gradlew assembleDebug`가 성공한다
</success_criteria>

<output>
After completion, create `.planning/phases/36-minutes-data-model/36-03-SUMMARY.md`
</output>
