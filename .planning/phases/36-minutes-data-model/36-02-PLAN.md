---
phase: 36-minutes-data-model
plan: 02
type: execute
wave: 2
depends_on: [36-01]
files_modified:
  - app/src/main/java/com/autominuting/domain/repository/MeetingRepository.kt
  - app/src/main/java/com/autominuting/data/repository/MeetingRepositoryImpl.kt
  - app/src/main/java/com/autominuting/domain/repository/MinutesDataRepository.kt
  - app/src/main/java/com/autominuting/data/repository/MinutesDataRepositoryImpl.kt
  - app/src/main/java/com/autominuting/di/RepositoryModule.kt
  - app/src/main/java/com/autominuting/worker/MinutesGenerationWorker.kt
  - app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsViewModel.kt
autonomous: true
requirements: [DATA-01, DATA-02]

must_haves:
  truths:
    - "MinutesGenerationWorker가 회의록 생성 완료 시 Minutes 테이블에 새 Row를 INSERT한다"
    - "regenerateMinutes()가 기존 Meeting Row를 유지하고 새 Minutes Row만 추가한다 (중복 Meeting Row 생성 안 함)"
    - "MeetingRepository에서 minutes 관련 메서드(deleteMinutesOnly, archiveAsMinutesOnly, updateMinutesTitle)가 제거된다"
    - "MinutesDataRepository가 회의록 CRUD를 제공한다 (조회, 삽입, 삭제, 제목 수정)"
  artifacts:
    - path: "app/src/main/java/com/autominuting/domain/repository/MinutesDataRepository.kt"
      provides: "회의록 데이터 CRUD 인터페이스"
    - path: "app/src/main/java/com/autominuting/data/repository/MinutesDataRepositoryImpl.kt"
      provides: "MinutesDao 기반 CRUD 구현체"
    - path: "app/src/main/java/com/autominuting/worker/MinutesGenerationWorker.kt"
      provides: "Minutes 테이블 INSERT 기반 회의록 저장"
  key_links:
    - from: "MinutesGenerationWorker"
      to: "MinutesDao.insert()"
      via: "Hilt 주입된 MinutesDao"
      pattern: "minutesDao\\.insert"
    - from: "TranscriptsViewModel.regenerateMinutes()"
      to: "MinutesGenerationWorker"
      via: "WorkManager enqueue (기존 meetingId 사용, 새 Meeting Row 생성 안 함)"
      pattern: "enqueueMinutesWorker.*meetingId"
    - from: "MeetingRepositoryImpl.deleteMeeting()"
      to: "MinutesDao.getMinutesByMeetingIdOnce()"
      via: "Meeting 삭제 전 연관 Minutes 파일 삭제"
      pattern: "minutesDao.*getMinutesByMeetingIdOnce"
---

<objective>
Repository/Worker/ViewModel 로직을 Minutes 테이블 기반으로 교체한다.

Purpose: Plan 01에서 생성한 MinutesDao/MinutesEntity를 활용하여 MinutesDataRepository(CRUD)를 신설하고, MeetingRepository에서 minutes 관련 메서드를 제거하며, MinutesGenerationWorker와 TranscriptsViewModel.regenerateMinutes()의 워크어라운드를 정리한다.
Output: 데이터 레이어 + Worker + TranscriptsViewModel 컴파일 통과. Presentation 레이어(MinutesViewModel, MinutesDetailViewModel, Screen 파일)은 Plan 03에서 처리.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/36-minutes-data-model/36-RESEARCH.md
@.planning/phases/36-minutes-data-model/36-01-SUMMARY.md

@app/src/main/java/com/autominuting/domain/repository/MeetingRepository.kt
@app/src/main/java/com/autominuting/data/repository/MeetingRepositoryImpl.kt
@app/src/main/java/com/autominuting/worker/MinutesGenerationWorker.kt
@app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsViewModel.kt
@app/src/main/java/com/autominuting/domain/repository/MinutesRepository.kt
@app/src/main/java/com/autominuting/data/repository/MinutesRepositoryImpl.kt

<interfaces>
<!-- Plan 01에서 생성된 인터페이스 -->
From data/local/dao/MinutesDao.kt:
```kotlin
@Dao
interface MinutesDao {
    fun getAllMinutes(): Flow<List<MinutesEntity>>
    fun getMinutesByMeetingId(meetingId: Long): Flow<List<MinutesEntity>>
    fun getMinutesById(id: Long): Flow<MinutesEntity?>
    suspend fun getMinutesByIdOnce(id: Long): MinutesEntity?
    suspend fun getMinutesByMeetingIdOnce(meetingId: Long): List<MinutesEntity>
    fun getMinutesCountByMeetingId(meetingId: Long): Flow<Int>
    suspend fun insert(entity: MinutesEntity): Long
    suspend fun updateTitle(id: Long, title: String, updatedAt: Long)
    suspend fun delete(id: Long)
    suspend fun deleteByMeetingId(meetingId: Long)
}
```

From domain/model/Minutes.kt:
```kotlin
data class Minutes(
    val id: Long = 0,
    val meetingId: Long?,
    val minutesPath: String,
    val minutesTitle: String?,
    val templateId: Long?,
    val createdAt: Instant,
    val updatedAt: Instant
)
```

From data/local/entity/MinutesEntity.kt:
```kotlin
// toDomain() -> Minutes
// companion object { fromDomain(minutes: Minutes) -> MinutesEntity }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MinutesDataRepository 신설 + MeetingRepository 정리 + DI 바인딩</name>
  <files>
    app/src/main/java/com/autominuting/domain/repository/MinutesDataRepository.kt
    app/src/main/java/com/autominuting/data/repository/MinutesDataRepositoryImpl.kt
    app/src/main/java/com/autominuting/domain/repository/MeetingRepository.kt
    app/src/main/java/com/autominuting/data/repository/MeetingRepositoryImpl.kt
    app/src/main/java/com/autominuting/di/RepositoryModule.kt
  </files>
  <action>
**주의:** 기존 `MinutesRepository`/`MinutesRepositoryImpl`은 **회의록 생성(Gemini API 호출)** 전용이므로 건드리지 않는다. 새로 만드는 것은 **회의록 데이터 CRUD** 전용 `MinutesDataRepository`이다. 이름이 충돌하지 않도록 "Data"를 붙인다.

1. **MinutesDataRepository.kt** 신규 생성 (`domain/repository/`):
```kotlin
interface MinutesDataRepository {
    /** 모든 회의록을 생성 시각 역순으로 조회한다. */
    fun getAllMinutes(): Flow<List<Minutes>>
    /** 특정 Meeting에 연결된 회의록 목록을 조회한다. */
    fun getMinutesByMeetingId(meetingId: Long): Flow<List<Minutes>>
    /** 특정 회의록을 ID로 조회한다. */
    fun getMinutesById(id: Long): Flow<Minutes?>
    /** 특정 회의록을 ID로 일회성 조회한다. */
    suspend fun getMinutesByIdOnce(id: Long): Minutes?
    /** 특정 Meeting에 연결된 회의록 수를 조회한다. */
    fun getMinutesCountByMeetingId(meetingId: Long): Flow<Int>
    /** 회의록을 삽입하고 ID를 반환한다. */
    suspend fun insertMinutes(minutes: Minutes): Long
    /** 회의록 제목을 업데이트한다. */
    suspend fun updateMinutesTitle(id: Long, title: String)
    /** 회의록을 삭제한다 (파일도 삭제). */
    suspend fun deleteMinutes(id: Long)
    /** 특정 Meeting의 모든 회의록을 삭제한다 (파일도 삭제). */
    suspend fun deleteMinutesByMeetingId(meetingId: Long)
}
```

2. **MinutesDataRepositoryImpl.kt** 신규 생성 (`data/repository/`):
- `@Inject constructor(private val minutesDao: MinutesDao)`
- 각 메서드에서 MinutesDao를 호출하고 Entity<->Domain 변환 수행
- `deleteMinutes(id)`: minutesDao.getMinutesByIdOnce(id)로 파일 경로 조회 -> File(path).delete() -> minutesDao.delete(id)
- `deleteMinutesByMeetingId(meetingId)`: minutesDao.getMinutesByMeetingIdOnce(meetingId)로 모든 파일 경로 조회 -> 각 파일 삭제 -> minutesDao.deleteByMeetingId(meetingId)
- `updateMinutesTitle(id, title)`: minutesDao.updateTitle(id, title, Instant.now().toEpochMilli())
- `insertMinutes(minutes)`: minutesDao.insert(MinutesEntity.fromDomain(minutes))

3. **MeetingRepository.kt** 수정 (인터페이스):
   - `deleteMinutesOnly(id: Long)` 제거
   - `deleteTranscript(id: Long)` 제거
   - `archiveAsMinutesOnly(id: Long)` 제거
   - `updateMinutesTitle(id: Long, title: String)` 제거
   - 이 메서드들은 더 이상 필요 없거나 MinutesDataRepository로 이동됨

4. **MeetingRepositoryImpl.kt** 수정:
   - MinutesDao를 생성자에 주입: `@Inject constructor(private val meetingDao: MeetingDao, private val minutesDao: MinutesDao)`
   - `deleteMinutesOnly()` 구현 제거
   - `deleteTranscript()` 구현 제거
   - `archiveAsMinutesOnly()` 구현 제거
   - `updateMinutesTitle()` 구현 제거
   - `deleteMeeting(id)` 수정: entity.minutesPath 대신 `minutesDao.getMinutesByMeetingIdOnce(id)`로 연관 Minutes 파일 경로들을 조회하여 삭제. Meeting 삭제 시 FK SET_NULL로 Minutes Row는 보존되지만, 파일은 삭제해야 함 → **아니다, Meeting 삭제 시 Minutes Row는 SET_NULL로 보존되므로 파일도 보존해야 한다.** deleteMeeting()에서 minutes 파일 삭제 로직을 **제거**한다. 오디오/전사 파일만 삭제.

   deleteMeeting() 최종 로직:
   ```kotlin
   override suspend fun deleteMeeting(id: Long) {
       val entity = meetingDao.getMeetingByIdOnce(id) ?: return
       // 오디오/전사 파일만 삭제 (Minutes 파일은 FK SET_NULL로 Minutes Row가 고아로 보존되므로 유지)
       listOfNotNull(
           entity.audioFilePath.takeIf { it.isNotBlank() },
           entity.transcriptPath
       ).forEach { path ->
           try { File(path).delete() } catch (_: Exception) { }
       }
       meetingDao.delete(id)
   }
   ```

5. **RepositoryModule.kt** 수정 (또는 신규 생성):
   - `di/RepositoryModule.kt`가 존재하면 `MinutesDataRepository` 바인딩 추가
   - 존재하지 않으면 기존 DI 패턴을 확인하여 적절한 모듈에 추가
   - `@Binds abstract fun bindMinutesDataRepository(impl: MinutesDataRepositoryImpl): MinutesDataRepository`
   - **먼저** `di/` 디렉토리에서 기존 Repository 바인딩 패턴을 확인할 것 (RepositoryModule이 있는지, 또는 MeetingRepository가 어디서 바인딩되는지)
  </action>
  <verify>
    <automated>cd D:/workspace/temp/Auto_Minuting && grep -l "MinutesDataRepository" app/src/main/java/com/autominuting/domain/repository/ app/src/main/java/com/autominuting/data/repository/ && grep -c "deleteMinutesOnly\|archiveAsMinutesOnly\|deleteTranscript\b" app/src/main/java/com/autominuting/domain/repository/MeetingRepository.kt</automated>
  </verify>
  <done>MinutesDataRepository 인터페이스와 구현체가 존재하고, MeetingRepository에서 deleteMinutesOnly/archiveAsMinutesOnly/deleteTranscript/updateMinutesTitle가 제거됨</done>
</task>

<task type="auto">
  <name>Task 2: MinutesGenerationWorker + TranscriptsViewModel 로직 교체</name>
  <files>
    app/src/main/java/com/autominuting/worker/MinutesGenerationWorker.kt
    app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsViewModel.kt
  </files>
  <action>
1. **MinutesGenerationWorker.kt** 수정:
   - 생성자에 `MinutesDao` 주입 추가 (기존 meetingDao, minutesRepository, promptTemplateRepository 유지)
   - `import com.autominuting.data.local.dao.MinutesDao` 추가
   - `import com.autominuting.data.local.entity.MinutesEntity` 추가
   - doWork() 성공 시 DB 업데이트 로직 변경 (약 188-195행):
     **기존:**
     ```kotlin
     meetingDao.updateMinutes(id=meetingId, minutesPath=minutesPath, minutesTitle=minutesTitle, status=COMPLETED.name, updatedAt=completedAt)
     ```
     **변경:**
     ```kotlin
     val minutesEntity = MinutesEntity(
         meetingId = meetingId,
         minutesPath = minutesPath,
         minutesTitle = minutesTitle,
         templateId = templateId.takeIf { it > 0 },
         createdAt = completedAt,
         updatedAt = completedAt
     )
     minutesDao.insert(minutesEntity)
     meetingDao.updatePipelineStatus(meetingId, PipelineStatus.COMPLETED.name, null, completedAt)
     ```
   - 나머지 로직(Foreground 승격, 전사 파일 읽기, Gemini 호출, 에러 처리)은 그대로 유지

2. **TranscriptsViewModel.kt** 수정:
   - `regenerateMinutes(meetingId)` 워크어라운드 제거. 새 Meeting Row를 생성하지 않는다.
     **기존 (157-188행):** `original.copy(id = 0, ...)` → `meetingRepository.insertMeeting(newMeeting)` → Worker enqueue with `newMeetingId`
     **변경:** 기존 meetingId를 그대로 사용하여 Worker를 enqueue한다. Worker가 완료되면 새 Minutes Row가 INSERT된다.
     ```kotlin
     fun regenerateMinutes(meetingId: Long) {
         viewModelScope.launch {
             val original = meetingRepository.getMeetingById(meetingId).first() ?: return@launch
             if (original.transcriptPath == null) {
                 Log.w(TAG, "전사 파일 없음, 회의록 재생성 불가: meetingId=$meetingId")
                 return@launch
             }
             // 기존 Meeting Row를 유지하고, Worker가 새 Minutes Row를 INSERT한다
             enqueueMinutesWorker(meetingId)
         }
     }
     ```
   - `deleteTranscript(id)` 수정: `meetingRepository.deleteTranscript(id)` 대신 `meetingRepository.deleteMeeting(id)` 호출. (Phase 37에서 독립 삭제 로직을 정교하게 구현하므로, Phase 36에서는 단순히 Meeting 삭제로 변경. FK SET_NULL로 Minutes Row는 보존됨.)
   - `TRANSCRIPT_VISIBLE_STATUSES`에서 `PipelineStatus.MINUTES_ONLY` 제거 확인 (이미 없을 수 있지만 확인)
   - Meeting.minutesPath/minutesTitle 참조가 있으면 제거 (regenerateMinutes에서 `minutesPath = null, minutesTitle = null` 사용 부분)
  </action>
  <verify>
    <automated>cd D:/workspace/temp/Auto_Minuting && grep -c "insertMeeting\|copy(id = 0" app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsViewModel.kt && grep "minutesDao.insert" app/src/main/java/com/autominuting/worker/MinutesGenerationWorker.kt</automated>
  </verify>
  <done>TranscriptsViewModel.regenerateMinutes()에서 insertMeeting/copy(id=0) 호출이 0건이고, MinutesGenerationWorker에서 minutesDao.insert()가 사용된다</done>
</task>

</tasks>

<verification>
- MinutesDataRepository 인터페이스에 getAllMinutes, getMinutesByMeetingId, insertMinutes, deleteMinutes, updateMinutesTitle 메서드 존재
- MeetingRepository에서 deleteMinutesOnly, archiveAsMinutesOnly, deleteTranscript, updateMinutesTitle 완전 제거
- MeetingRepositoryImpl.deleteMeeting()에서 minutesPath 파일 삭제 로직이 제거되고 오디오/전사 파일만 삭제
- MinutesGenerationWorker.doWork() 성공 시 minutesDao.insert() + meetingDao.updatePipelineStatus() 호출
- TranscriptsViewModel.regenerateMinutes()가 새 Meeting Row를 생성하지 않고 기존 meetingId로 Worker enqueue
- TranscriptsViewModel.deleteTranscript()가 meetingRepository.deleteMeeting() 호출
</verification>

<success_criteria>
1. MinutesDataRepository/MinutesDataRepositoryImpl이 존재하고 DI에 바인딩됨
2. MeetingRepository에서 minutes 관련 4개 메서드가 제거됨
3. MinutesGenerationWorker가 Minutes 테이블에 INSERT하고 Meeting은 status만 업데이트
4. TranscriptsViewModel.regenerateMinutes()가 새 Meeting Row 생성 없이 Worker enqueue
5. TranscriptsViewModel.deleteTranscript()가 Meeting 삭제로 변경 (FK SET_NULL로 Minutes 보존)
</success_criteria>

<output>
After completion, create `.planning/phases/36-minutes-data-model/36-02-SUMMARY.md`
</output>
