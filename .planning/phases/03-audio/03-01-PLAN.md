---
phase: 03-audio
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gradle/libs.versions.toml
  - app/build.gradle.kts
  - app/src/main/java/com/autominuting/data/audio/PlaudSdkManager.kt
  - app/src/main/java/com/autominuting/data/audio/AudioFileManager.kt
  - app/src/main/java/com/autominuting/data/audio/PlaudCloudApiService.kt
  - app/src/main/java/com/autominuting/data/repository/AudioRepositoryImpl.kt
  - app/src/main/java/com/autominuting/di/AudioModule.kt
  - app/src/main/java/com/autominuting/di/RepositoryModule.kt
autonomous: true
requirements:
  - AUD-01
must_haves:
  truths:
    - "Plaud SDK 및 Retrofit 의존성이 빌드에 포함되어 컴파일된다"
    - "AudioRepository 인터페이스의 구현체가 data 레이어에 존재한다"
    - "SDK 1차 경로 실패 시 Cloud API 2차 폴백이 자동 전환된다"
    - "오디오 파일이 앱 내부 저장소에 저장되고 MeetingEntity로 DB에 기록된다"
  artifacts:
    - path: "app/src/main/java/com/autominuting/data/audio/PlaudSdkManager.kt"
      provides: "Plaud SDK BLE 연결/스캔/다운로드 래퍼"
      contains: "NiceBuildSdk"
    - path: "app/src/main/java/com/autominuting/data/audio/AudioFileManager.kt"
      provides: "오디오 파일 저장/검증/공간 확인"
      contains: "StatFs"
    - path: "app/src/main/java/com/autominuting/data/audio/PlaudCloudApiService.kt"
      provides: "Cloud API 폴백 Retrofit 인터페이스"
      contains: "PlaudCloudApi"
    - path: "app/src/main/java/com/autominuting/data/repository/AudioRepositoryImpl.kt"
      provides: "AudioRepository 구현체 (SDK + Cloud API 이중 경로)"
      exports: ["AudioRepositoryImpl"]
    - path: "app/src/main/java/com/autominuting/di/AudioModule.kt"
      provides: "Plaud SDK/Cloud API DI 바인딩"
      contains: "@Module"
  key_links:
    - from: "AudioRepositoryImpl"
      to: "PlaudSdkManager"
      via: "생성자 주입"
      pattern: "PlaudSdkManager"
    - from: "AudioRepositoryImpl"
      to: "PlaudCloudApiService"
      via: "폴백 경로"
      pattern: "PlaudCloudApiService"
    - from: "RepositoryModule"
      to: "AudioRepositoryImpl"
      via: "@Binds"
      pattern: "bindAudioRepository"
---

<objective>
Plaud SDK 의존성을 추가하고 AudioRepository 구현체를 완성한다. SDK(1차)와 Cloud API(2차) 이중 경로를 지원하며, 오디오 파일을 앱 내부 저장소에 저장하고 MeetingEntity로 DB에 기록한다.

Purpose: Phase 3의 핵심 데이터 레이어 -- Plaud 녹음기에서 오디오 파일을 가져오는 전체 경로를 구현한다.
Output: AudioRepositoryImpl + PlaudSdkManager + AudioFileManager + PlaudCloudApiService + DI 바인딩
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-audio/03-CONTEXT.md
@.planning/phases/03-audio/03-RESEARCH.md

<interfaces>
From app/src/main/java/com/autominuting/domain/repository/AudioRepository.kt:
```kotlin
interface AudioRepository {
    suspend fun startAudioCollection(): Flow<String>
    suspend fun stopAudioCollection()
    fun isCollecting(): Flow<Boolean>
}
```

From app/src/main/java/com/autominuting/domain/model/PipelineStatus.kt:
```kotlin
enum class PipelineStatus {
    AUDIO_RECEIVED, TRANSCRIBING, TRANSCRIBED, GENERATING_MINUTES, COMPLETED, FAILED
}
```

From app/src/main/java/com/autominuting/data/local/entity/MeetingEntity.kt:
```kotlin
@Entity(tableName = "meetings")
data class MeetingEntity(
    @PrimaryKey(autoGenerate = true) val id: Long = 0,
    val title: String, val recordedAt: Long, val audioFilePath: String,
    val transcriptPath: String? = null, val minutesPath: String? = null,
    val pipelineStatus: String, val errorMessage: String? = null,
    val createdAt: Long, val updatedAt: Long
)
```

From app/src/main/java/com/autominuting/data/local/dao/MeetingDao.kt:
```kotlin
@Dao
interface MeetingDao {
    @Insert(onConflict = OnConflictStrategy.REPLACE)
    suspend fun insert(entity: MeetingEntity): Long
    @Query("UPDATE meetings SET pipelineStatus = :status, errorMessage = :errorMessage, updatedAt = :updatedAt WHERE id = :id")
    suspend fun updatePipelineStatus(id: Long, status: String, errorMessage: String?, updatedAt: Long)
}
```

From app/src/main/java/com/autominuting/di/RepositoryModule.kt:
```kotlin
@Module @InstallIn(SingletonComponent::class)
abstract class RepositoryModule {
    @Binds abstract fun bindMeetingRepository(impl: MeetingRepositoryImpl): MeetingRepository
}
```

From app/src/main/java/com/autominuting/di/DatabaseModule.kt:
```kotlin
@Module @InstallIn(SingletonComponent::class)
object DatabaseModule {
    @Provides @Singleton fun provideDatabase(...): AppDatabase = ...
    @Provides fun provideMeetingDao(db: AppDatabase): MeetingDao = db.meetingDao()
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gradle 의존성 추가 및 BuildConfig 설정</name>
  <files>
    gradle/libs.versions.toml,
    app/build.gradle.kts,
    app/libs/README.txt
  </files>
  <read_first>
    gradle/libs.versions.toml,
    app/build.gradle.kts,
    .planning/phases/03-audio/03-RESEARCH.md
  </read_first>
  <action>
    1. `gradle/libs.versions.toml` [versions] 섹션에 추가:
       - `retrofit = "2.11.0"`
       - `okhttp = "4.12.0"`
       - `guava = "28.2-android"`

    2. `gradle/libs.versions.toml` [libraries] 섹션에 추가:
       - `retrofit = { group = "com.squareup.retrofit2", name = "retrofit", version.ref = "retrofit" }`
       - `retrofit-converter-gson = { group = "com.squareup.retrofit2", name = "converter-gson", version.ref = "retrofit" }`
       - `okhttp = { group = "com.squareup.okhttp3", name = "okhttp", version.ref = "okhttp" }`
       - `okhttp-logging = { group = "com.squareup.okhttp3", name = "logging-interceptor", version.ref = "okhttp" }`
       - `guava = { group = "com.google.guava", name = "guava", version.ref = "guava" }`

    3. `app/build.gradle.kts` android.buildFeatures에 `buildConfig = true` 추가 (기존 compose = true 유지)

    4. `app/build.gradle.kts` android.defaultConfig에 BuildConfig 필드 추가:
       ```kotlin
       val plaudAppKey: String = project.findProperty("PLAUD_APP_KEY")?.toString() ?: ""
       val plaudAppSecret: String = project.findProperty("PLAUD_APP_SECRET")?.toString() ?: ""
       buildConfigField("String", "PLAUD_APP_KEY", "\"$plaudAppKey\"")
       buildConfigField("String", "PLAUD_APP_SECRET", "\"$plaudAppSecret\"")
       ```

    5. dependencies에 추가 (per D-01, D-02):
       ```kotlin
       // Plaud SDK (AAR) -- D-01
       implementation(files("libs/plaud-sdk.aar"))
       implementation(libs.guava)
       // Retrofit + OkHttp -- D-02 Cloud API 폴백
       implementation(libs.retrofit)
       implementation(libs.retrofit.converter.gson)
       implementation(libs.okhttp)
       implementation(libs.okhttp.logging)
       ```

    6. Guava 충돌 방지: `configurations.all { resolutionStrategy.force("com.google.guava:guava:28.2-android") }`

    7. `app/libs/` 디렉토리 생성 + README.txt에 "plaud-sdk.aar 파일을 이 디렉토리에 배치하세요" 안내

    주의: appKey를 소스코드에 하드코딩하지 않는다 (Anti-Pattern 방지). local.properties에서만 읽는다.
  </action>
  <verify>
    <automated>grep "retrofit" gradle/libs.versions.toml && grep "guava" gradle/libs.versions.toml && grep "PLAUD_APP_KEY" app/build.gradle.kts && grep "buildConfig" app/build.gradle.kts</automated>
  </verify>
  <acceptance_criteria>
    - gradle/libs.versions.toml에 `retrofit = "2.11.0"` 존재
    - gradle/libs.versions.toml에 `guava = "28.2-android"` 존재
    - app/build.gradle.kts에 `buildConfig = true` 존재
    - app/build.gradle.kts에 `PLAUD_APP_KEY` BuildConfig 필드 존재
    - app/build.gradle.kts에 `implementation(libs.retrofit)` 존재
    - app/build.gradle.kts에 `resolutionStrategy.force` 존재
  </acceptance_criteria>
  <done>Plaud SDK, Retrofit, OkHttp, Guava 의존성이 선언되고 BuildConfig에 PLAUD_APP_KEY/PLAUD_APP_SECRET 필드가 생성된다</done>
</task>

<task type="auto">
  <name>Task 2: PlaudSdkManager + AudioFileManager + PlaudCloudApiService + AudioRepositoryImpl + DI 바인딩</name>
  <files>
    app/src/main/java/com/autominuting/data/audio/PlaudSdkManager.kt,
    app/src/main/java/com/autominuting/data/audio/AudioFileManager.kt,
    app/src/main/java/com/autominuting/data/audio/PlaudCloudApiService.kt,
    app/src/main/java/com/autominuting/data/repository/AudioRepositoryImpl.kt,
    app/src/main/java/com/autominuting/di/AudioModule.kt,
    app/src/main/java/com/autominuting/di/RepositoryModule.kt
  </files>
  <read_first>
    app/src/main/java/com/autominuting/domain/repository/AudioRepository.kt,
    app/src/main/java/com/autominuting/data/local/dao/MeetingDao.kt,
    app/src/main/java/com/autominuting/data/local/entity/MeetingEntity.kt,
    app/src/main/java/com/autominuting/domain/model/PipelineStatus.kt,
    app/src/main/java/com/autominuting/di/RepositoryModule.kt,
    app/src/main/java/com/autominuting/di/DatabaseModule.kt,
    app/src/main/java/com/autominuting/data/repository/MeetingRepositoryImpl.kt,
    .planning/phases/03-audio/03-RESEARCH.md
  </read_first>
  <action>
    **PlaudSdkManager.kt** (패키지: `com.autominuting.data.audio`):
    - 클래스: `class PlaudSdkManager @Inject constructor(@ApplicationContext private val context: Context)`
    - `@Singleton` 어노테이션
    - 필드: `private var isInitialized = false`
    - `fun initialize(appKey: String, appSecret: String)`: appKey 빈 문자열이면 return. `NiceBuildSdk.initSdk(context, appKey, appSecret, PlaudBleListener(), "AutoMinuting")` 호출 후 `isInitialized = true`
    - `suspend fun scanAndConnect()`: `suspendCancellableCoroutine`으로 `TntAgent.getInstant().bleAgent.scanBle(true)` 콜백 래핑. 에러 시 `PlaudSdkException` throw
    - `suspend fun exportAudio(sessionId: String, outputDir: File, format: String = "MP3"): String`: `suspendCancellableCoroutine`으로 `NiceBuildSdk.exportAudio()` 호출. `ExportCallback.onComplete(filePath)` 시 resume, `onError` 시 PlaudSdkException throw
    - `fun disconnect()`: SDK 연결 해제
    - `fun isAvailable(): Boolean = isInitialized`
    - 내부 클래스: `private class PlaudBleListener : BleAgentListener` (빈 구현)
    - 예외: `class PlaudSdkException(message: String, cause: Throwable? = null) : Exception(message, cause)`
    - 진행률 콜백용: `var onProgressUpdate: ((Float) -> Unit)? = null`

    **AudioFileManager.kt** (패키지: `com.autominuting.data.audio`):
    - 클래스: `class AudioFileManager @Inject constructor(@ApplicationContext private val context: Context)`
    - `@Singleton` 어노테이션
    - `fun getAudioDirectory(): File`: `File(context.filesDir, "audio").also { if (!it.exists()) it.mkdirs() }` 반환
    - `fun hasEnoughSpace(requiredBytes: Long = 100L * 1024 * 1024): Boolean`: `StatFs(context.filesDir.path).availableBytes >= requiredBytes`
    - `fun validateAudioFile(file: File): Boolean`: `file.exists() && file.length() > 0 && file.extension.lowercase() in listOf("mp3", "wav")`
    - `fun generateFileName(prefix: String = "meeting"): String`: `SimpleDateFormat("yyyyMMdd_HHmmss", Locale.getDefault())` 사용, `"${prefix}_${timestamp}.mp3"` 반환

    **PlaudCloudApiService.kt** (패키지: `com.autominuting.data.audio`):
    - Retrofit 인터페이스:
      ```kotlin
      interface PlaudCloudApi {
          @GET("recordings")
          suspend fun getRecordings(@Header("Authorization") token: String): List<RecordingResponse>
          @GET("recordings/{id}/download")
          @Streaming
          suspend fun downloadRecording(@Path("id") id: String, @Header("Authorization") token: String): ResponseBody
      }
      data class RecordingResponse(val id: String, val name: String, val createdAt: String, val size: Long)
      ```
    - 서비스 클래스: `class PlaudCloudApiService @Inject constructor(private val api: PlaudCloudApi, private val audioFileManager: AudioFileManager)`
    - `suspend fun downloadLatestRecordings(jwtToken: String): Flow<String>`: flow builder 안에서 getRecordings 호출 후 각 녹음 파일을 downloadRecording으로 다운로드, audioFileManager.getAudioDirectory()에 저장, 파일 경로 emit

    **AudioRepositoryImpl.kt** (패키지: `com.autominuting.data.repository`):
    - 클래스: `class AudioRepositoryImpl @Inject constructor(private val plaudSdkManager: PlaudSdkManager, private val cloudApiService: PlaudCloudApiService, private val audioFileManager: AudioFileManager, private val meetingDao: MeetingDao) : AudioRepository`
    - `private val _isCollecting = MutableStateFlow(false)`
    - `override suspend fun startAudioCollection(): Flow<String>`:
      - `channelFlow` 사용
      - `_isCollecting.value = true`
      - 저장 공간 확인: `audioFileManager.hasEnoughSpace()` 실패 시 throw
      - try: `plaudSdkManager.scanAndConnect()` 후 `plaudSdkManager.exportAudio(...)` 호출
      - 파일 경로 받으면 MeetingEntity 생성 (title="회의 {날짜}", pipelineStatus=PipelineStatus.AUDIO_RECEIVED.name) 후 `meetingDao.insert()`, 파일 경로 send
      - catch PlaudSdkException: Cloud API 폴백 (`cloudApiService.downloadLatestRecordings()`)으로 동일 처리
      - finally: `_isCollecting.value = false`
    - `override suspend fun stopAudioCollection()`: `_isCollecting.value = false` + `plaudSdkManager.disconnect()`
    - `override fun isCollecting(): Flow<Boolean> = _isCollecting.asStateFlow()`
    - per D-05: domain AudioRepository 인터페이스 구현, per D-06: MeetingEntity/MeetingDao 사용, per D-07: AUDIO_RECEIVED 상태 설정

    **AudioModule.kt** (패키지: `com.autominuting.di`):
    - `@Module @InstallIn(SingletonComponent::class) object AudioModule`
    - `@Provides @Singleton fun providePlaudCloudApi()`: Retrofit.Builder로 PlaudCloudApi 생성 (baseUrl = "https://api.plaud.ai/", OkHttpClient에 logging interceptor 추가)
    - per D-08: 기존 DatabaseModule 패턴 따름

    **RepositoryModule.kt** 수정:
    - `@Binds abstract fun bindAudioRepository(impl: AudioRepositoryImpl): AudioRepository` 추가
    - 기존 bindMeetingRepository 유지
    - import 추가: `AudioRepositoryImpl`, `AudioRepository`
  </action>
  <verify>
    <automated>grep -r "class AudioRepositoryImpl" app/src/main/java/ && grep -r "class PlaudSdkManager" app/src/main/java/ && grep -r "class AudioFileManager" app/src/main/java/ && grep -r "bindAudioRepository" app/src/main/java/ && grep -r "PlaudCloudApi" app/src/main/java/</automated>
  </verify>
  <acceptance_criteria>
    - AudioRepositoryImpl이 AudioRepository를 implements하고 `@Inject constructor` 존재
    - PlaudSdkManager에 `NiceBuildSdk` 참조 존재
    - AudioFileManager에 `StatFs` 참조 존재
    - PlaudCloudApiService에 `PlaudCloudApi` Retrofit 인터페이스 존재
    - RepositoryModule에 `bindAudioRepository` 메서드 존재
    - AudioModule에 `@Module` + `@InstallIn` + `providePlaudCloudApi` 존재
    - AudioRepositoryImpl에 `PipelineStatus.AUDIO_RECEIVED` 참조 존재 (per D-07)
    - AudioRepositoryImpl에 `MeetingEntity` 생성 및 `meetingDao.insert` 호출 존재 (per D-06)
  </acceptance_criteria>
  <done>AudioRepository 구현체가 SDK 1차/Cloud API 2차 이중 경로를 지원하고, 오디오 파일 저장 시 MeetingEntity를 AUDIO_RECEIVED 상태로 DB에 기록하며, DI 바인딩이 완성된다</done>
</task>

</tasks>

<verification>
- AudioRepositoryImpl이 AudioRepository 인터페이스의 모든 메서드(startAudioCollection, stopAudioCollection, isCollecting)를 구현한다
- PlaudSdkManager에서 SDK 실패 시 PlaudSdkException이 throw되어 AudioRepositoryImpl에서 catch 후 Cloud API 폴백으로 전환된다
- RepositoryModule에 AudioRepository 바인딩이 추가되어 Hilt DI 그래프가 완성된다
- BuildConfig.PLAUD_APP_KEY / PLAUD_APP_SECRET이 local.properties에서 안전하게 읽힌다
</verification>

<success_criteria>
- AudioRepository 구현체가 data 레이어에 존재하고 SDK + Cloud API 이중 경로를 지원한다
- Gradle 의존성(Plaud SDK, Retrofit, OkHttp, Guava)이 모두 선언된다
- DI 바인딩이 완성되어 AudioRepository를 주입받을 수 있다
- 오디오 파일 저장 시 MeetingEntity가 AUDIO_RECEIVED 상태로 DB에 기록된다
</success_criteria>

<output>
After completion, create `.planning/phases/03-audio/03-01-SUMMARY.md`
</output>
