---
phase: 16-file-delete
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/com/autominuting/domain/repository/MeetingRepository.kt
  - app/src/main/java/com/autominuting/data/repository/MeetingRepositoryImpl.kt
  - app/src/main/java/com/autominuting/data/local/dao/MeetingDao.kt
  - app/src/main/java/com/autominuting/presentation/minutes/MinutesViewModel.kt
  - app/src/main/java/com/autominuting/presentation/minutes/MinutesScreen.kt
  - app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsViewModel.kt
  - app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt
autonomous: true
requirements:
  - FILE-02
  - FILE-03

must_haves:
  truths:
    - "사용자가 회의록 목록에서 long-press로 다중 선택 모드에 진입한다"
    - "다중 선택 후 삭제 버튼으로 선택된 회의록을 일괄 삭제한다"
    - "회의록을 삭제해도 연결된 전사 파일(transcriptPath)은 보존된다"
    - "사용자가 전사 목록에서 long-press로 전사 파일을 삭제할 수 있다"
  artifacts:
    - path: "app/src/main/java/com/autominuting/data/repository/MeetingRepositoryImpl.kt"
      provides: "deleteMinutesOnly, deleteTranscript 메서드"
      contains: "deleteMinutesOnly"
    - path: "app/src/main/java/com/autominuting/presentation/minutes/MinutesScreen.kt"
      provides: "다중 선택 UI + 일괄 삭제"
      contains: "selectedIds"
    - path: "app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt"
      provides: "전사 삭제 기능"
      contains: "onDeleteRequest"
  key_links:
    - from: "MinutesScreen.kt"
      to: "MinutesViewModel.kt"
      via: "deleteSelectedMinutes(ids)"
      pattern: "deleteSelectedMinutes"
    - from: "MinutesViewModel.kt"
      to: "MeetingRepository.kt"
      via: "deleteMinutesOnly(id)"
      pattern: "deleteMinutesOnly"
    - from: "TranscriptsScreen.kt"
      to: "TranscriptsViewModel.kt"
      via: "deleteTranscript(id)"
      pattern: "deleteTranscript"
---

<objective>
회의록 다중 선택 삭제(전사 파일 보존)와 전사 파일 별도 삭제 기능을 구현한다.

Purpose: 사용자가 회의록과 전사 파일을 독립적으로 관리(삭제)할 수 있도록 UX를 개선한다.
Output: 회의록 다중 삭제 UI + 전사 삭제 UI가 동작하는 앱
</objective>

<execution_context>
@D:\Users\ADMIN/.claude/get-shit-done/workflows/execute-plan.md
@D:\Users\ADMIN/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- 현재 코드베이스의 핵심 인터페이스. 이 계약을 기반으로 수정한다. -->

From app/src/main/java/com/autominuting/domain/repository/MeetingRepository.kt:
```kotlin
interface MeetingRepository {
    fun getMeetings(): Flow<List<Meeting>>
    fun getMeetingById(id: Long): Flow<Meeting?>
    suspend fun insertMeeting(meeting: Meeting): Long
    suspend fun updateMeeting(meeting: Meeting)
    suspend fun updatePipelineStatus(id: Long, status: PipelineStatus, errorMessage: String? = null)
    fun searchMeetings(query: String): Flow<List<Meeting>>
    suspend fun deleteMeeting(id: Long)  // 현재: 오디오+전사+회의록 모두 삭제
}
```

From app/src/main/java/com/autominuting/data/local/dao/MeetingDao.kt:
```kotlin
@Dao
interface MeetingDao {
    suspend fun getMeetingByIdOnce(id: Long): MeetingEntity?
    @Query("DELETE FROM meetings WHERE id = :id")
    suspend fun delete(id: Long)
}
```

From app/src/main/java/com/autominuting/domain/model/Meeting.kt:
```kotlin
data class Meeting(
    val id: Long = 0,
    val title: String,
    val recordedAt: Instant,
    val audioFilePath: String,
    val transcriptPath: String? = null,
    val minutesPath: String? = null,
    val pipelineStatus: PipelineStatus,
    val errorMessage: String? = null,
    val createdAt: Instant,
    val updatedAt: Instant,
    val source: String = "PLAUD_BLE"
)
```

From app/src/main/java/com/autominuting/presentation/minutes/MinutesViewModel.kt:
```kotlin
@HiltViewModel
class MinutesViewModel @Inject constructor(
    private val meetingRepository: MeetingRepository
) : ViewModel() {
    val meetings: StateFlow<List<Meeting>>
    val searchQuery: StateFlow<String>
    fun deleteMeeting(id: Long)  // 현재: 단건 삭제
}
```

From app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsViewModel.kt:
```kotlin
@HiltViewModel
class TranscriptsViewModel @Inject constructor(
    private val meetingRepository: MeetingRepository,
    private val promptTemplateRepository: PromptTemplateRepository,
    private val minutesRepository: MinutesRepository,
    private val meetingDao: MeetingDao,
    @ApplicationContext private val context: Context
) : ViewModel() {
    val meetings: StateFlow<List<Meeting>>
    // 전사 삭제 기능 없음 — 추가 필요
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 회의록 다중 선택 삭제 (전사 보존) — FILE-02</name>
  <files>
    app/src/main/java/com/autominuting/domain/repository/MeetingRepository.kt
    app/src/main/java/com/autominuting/data/repository/MeetingRepositoryImpl.kt
    app/src/main/java/com/autominuting/data/local/dao/MeetingDao.kt
    app/src/main/java/com/autominuting/presentation/minutes/MinutesViewModel.kt
    app/src/main/java/com/autominuting/presentation/minutes/MinutesScreen.kt
  </files>
  <read_first>
    app/src/main/java/com/autominuting/domain/repository/MeetingRepository.kt
    app/src/main/java/com/autominuting/data/repository/MeetingRepositoryImpl.kt
    app/src/main/java/com/autominuting/data/local/dao/MeetingDao.kt
    app/src/main/java/com/autominuting/presentation/minutes/MinutesViewModel.kt
    app/src/main/java/com/autominuting/presentation/minutes/MinutesScreen.kt
  </read_first>
  <action>
    **1. MeetingDao에 회의록 전용 삭제 쿼리 추가:**
    - `clearMinutesPath(id: Long)`: UPDATE meetings SET minutesPath = null, pipelineStatus = 'TRANSCRIBED', updatedAt = :updatedAt WHERE id = :id
      (회의록 경로만 null로 설정하고 상태를 TRANSCRIBED로 되돌림. DB 레코드와 전사 파일은 보존)

    **2. MeetingRepository 인터페이스에 메서드 추가:**
    - `suspend fun deleteMinutesOnly(id: Long)`: 회의록 파일만 삭제하고 전사 파일을 보존하는 메서드

    **3. MeetingRepositoryImpl에 구현 추가:**
    - `deleteMinutesOnly(id: Long)`:
      1. `meetingDao.getMeetingByIdOnce(id)` 로 entity 조회
      2. `entity.minutesPath` 가 있으면 File(minutesPath).delete() (실패해도 진행 — 고아 파일 > 고아 레코드 원칙 per Phase 08 결정)
      3. `meetingDao.clearMinutesPath(id, Instant.now().toEpochMilli())` 호출
    - 기존 `deleteMeeting(id: Long)` 은 그대로 유지 (전사 화면에서의 전체 삭제에 사용)

    **4. MinutesViewModel 수정:**
    - 기존 `deleteMeeting(id)` 을 `deleteMinutesOnly(id)` 호출로 변경 (회의록 화면에서의 삭제는 회의록만 삭제)
    - `deleteSelectedMinutes(ids: Set<Long>)` 메서드 추가: ids.forEach { deleteMinutesOnly(it) } 으로 다중 삭제 수행

    **5. MinutesScreen 다중 선택 UI 구현:**
    - `selectedIds: MutableState<Set<Long>>` 상태 추가 (빈 Set이면 일반 모드, 비어있지 않으면 선택 모드)
    - Long-press 시: 해당 meeting.id를 selectedIds에 추가하여 선택 모드 진입
    - 선택 모드에서 일반 클릭: selectedIds에 toggle (추가/제거)
    - 선택 모드에서 카드: Checkbox 또는 선택 표시(containerColor 변경)로 선택 상태 시각화
    - 상단에 선택 모드 TopBar 표시: "N개 선택됨" 텍스트 + 삭제 아이콘(Icons.Default.Delete) + 취소(X) 아이콘
    - 삭제 아이콘 클릭 시 확인 다이얼로그 표시: "N개의 회의록을 삭제할까요?\n전사 파일은 보존됩니다." (기존 DeleteConfirmationDialog 메시지도 수정: "전사 파일은 보존됩니다"로 변경)
    - 확인 시 `viewModel.deleteSelectedMinutes(selectedIds)` 호출 후 selectedIds를 빈 Set으로 초기화
    - BackHandler: 선택 모드에서 뒤로가기 시 selectedIds 초기화 (선택 모드 해제)
  </action>
  <acceptance_criteria>
    - grep -r "deleteMinutesOnly" app/src/main/java/ 결과에 MeetingRepository.kt, MeetingRepositoryImpl.kt, MinutesViewModel.kt 포함
    - grep -r "deleteSelectedMinutes" app/src/main/java/ 결과에 MinutesViewModel.kt, MinutesScreen.kt 포함
    - grep -r "selectedIds" app/src/main/java/com/autominuting/presentation/minutes/MinutesScreen.kt 결과 존재
    - grep -r "clearMinutesPath" app/src/main/java/ 결과에 MeetingDao.kt, MeetingRepositoryImpl.kt 포함
    - grep "전사 파일은 보존" app/src/main/java/com/autominuting/presentation/minutes/MinutesScreen.kt 결과 존재
  </acceptance_criteria>
  <verify>
    <automated>cd "D:\workspace\temp\Auto_Minuting" && ./gradlew compileDebugKotlin 2>&1 | tail -5</automated>
  </verify>
  <done>
    - MeetingRepository에 deleteMinutesOnly 메서드가 선언되고 MeetingRepositoryImpl에 구현됨
    - MinutesViewModel에 deleteSelectedMinutes(ids) 메서드가 존재
    - MinutesScreen에서 long-press로 다중 선택 모드 진입, 선택 삭제 가능
    - 삭제 시 minutesPath만 null로 변경되고 transcriptPath는 보존
    - 삭제 확인 다이얼로그에 "전사 파일은 보존됩니다" 안내 표시
  </done>
</task>

<task type="auto">
  <name>Task 2: 전사 파일 별도 삭제 — FILE-03</name>
  <files>
    app/src/main/java/com/autominuting/domain/repository/MeetingRepository.kt
    app/src/main/java/com/autominuting/data/repository/MeetingRepositoryImpl.kt
    app/src/main/java/com/autominuting/data/local/dao/MeetingDao.kt
    app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsViewModel.kt
    app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt
  </files>
  <read_first>
    app/src/main/java/com/autominuting/domain/repository/MeetingRepository.kt
    app/src/main/java/com/autominuting/data/repository/MeetingRepositoryImpl.kt
    app/src/main/java/com/autominuting/data/local/dao/MeetingDao.kt
    app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsViewModel.kt
    app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt
  </read_first>
  <action>
    **1. MeetingDao에 전사 삭제 쿼리 추가:**
    - `clearTranscriptPath(id: Long, updatedAt: Long)`: UPDATE meetings SET transcriptPath = null, pipelineStatus = 'AUDIO_SAVED', updatedAt = :updatedAt WHERE id = :id
      (전사 경로를 null로 설정하고 상태를 AUDIO_SAVED로 되돌림)
    - 주의: minutesPath가 있는 경우도 함께 null로 설정해야 함 (전사가 없으면 회의록도 무효): UPDATE meetings SET transcriptPath = null, minutesPath = null, pipelineStatus = 'AUDIO_SAVED', updatedAt = :updatedAt WHERE id = :id

    **2. MeetingRepository 인터페이스에 추가:**
    - `suspend fun deleteTranscript(id: Long)`: 전사 파일(+ 연관 회의록 파일)을 삭제

    **3. MeetingRepositoryImpl에 구현 추가:**
    - `deleteTranscript(id: Long)`:
      1. `meetingDao.getMeetingByIdOnce(id)` 조회
      2. `entity.transcriptPath` 파일 삭제 (실패해도 진행)
      3. `entity.minutesPath` 파일도 삭제 (전사 없으면 회의록도 무효하므로. 실패해도 진행)
      4. `meetingDao.clearTranscriptPath(id, Instant.now().toEpochMilli())` 호출

    **4. TranscriptsViewModel에 삭제 메서드 추가:**
    - `fun deleteTranscript(id: Long)`: viewModelScope.launch { meetingRepository.deleteTranscript(id) }

    **5. TranscriptsScreen에 삭제 UI 추가:**
    - `meetingToDelete: MutableState<Meeting?>` 상태 추가
    - TranscriptMeetingCard에 `onDeleteRequest: (Long) -> Unit` 파라미터 추가
    - Card를 `combinedClickable`로 변경 (기존 clickable 대체): onClick은 기존 동작, onLongClick에서 `onDeleteRequest(meeting.id)` 호출
    - import 추가: `androidx.compose.foundation.ExperimentalFoundationApi`, `androidx.compose.foundation.combinedClickable`
    - 삭제 확인 다이얼로그 추가 (MinutesScreen의 DeleteConfirmationDialog와 유사):
      - 제목: "전사 파일 삭제"
      - 본문: "\"$title\"의 전사 파일을 삭제할까요?\n연관된 회의록도 함께 삭제됩니다."
      - 확인 시 `viewModel.deleteTranscript(meeting.id)` 호출 후 meetingToDelete = null
    - AlertDialog import 추가: `androidx.compose.material3.AlertDialog`, `androidx.compose.material3.TextButton`
  </action>
  <acceptance_criteria>
    - grep -r "deleteTranscript" app/src/main/java/ 결과에 MeetingRepository.kt, MeetingRepositoryImpl.kt, TranscriptsViewModel.kt 포함
    - grep -r "clearTranscriptPath" app/src/main/java/ 결과에 MeetingDao.kt, MeetingRepositoryImpl.kt 포함
    - grep -r "meetingToDelete" app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt 결과 존재
    - grep "전사 파일 삭제" app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt 결과 존재
    - grep "combinedClickable" app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsScreen.kt 결과 존재
  </acceptance_criteria>
  <verify>
    <automated>cd "D:\workspace\temp\Auto_Minuting" && ./gradlew compileDebugKotlin 2>&1 | tail -5</automated>
  </verify>
  <done>
    - MeetingRepository에 deleteTranscript 메서드가 선언되고 구현됨
    - TranscriptsViewModel에 deleteTranscript(id) 메서드 존재
    - TranscriptsScreen에서 long-press로 전사 삭제 가능
    - 전사 삭제 시 연관 회의록도 함께 삭제되고 상태가 AUDIO_SAVED로 되돌아감
    - 삭제 확인 다이얼로그에 "연관된 회의록도 함께 삭제됩니다" 안내 표시
  </done>
</task>

</tasks>

<verification>
1. `./gradlew compileDebugKotlin` 이 성공한다
2. grep으로 모든 acceptance_criteria가 충족된다
3. MeetingRepository에 deleteMinutesOnly, deleteTranscript 메서드가 존재한다
4. MinutesScreen에 다중 선택 UI (selectedIds, deleteSelectedMinutes) 가 존재한다
5. TranscriptsScreen에 전사 삭제 UI (meetingToDelete, combinedClickable) 가 존재한다
</verification>

<success_criteria>
- 회의록 목록에서 long-press로 다중 선택 모드 진입 후 일괄 삭제 가능
- 회의록 삭제 시 transcriptPath 보존 (minutesPath만 null로 변경)
- 전사 목록에서 long-press로 전사 파일 삭제 가능
- 전사 삭제 시 연관 회의록도 함께 삭제
- compileDebugKotlin 빌드 성공
</success_criteria>

<output>
완료 후 `.planning/phases/16-file-delete/16-01-SUMMARY.md` 생성
</output>
