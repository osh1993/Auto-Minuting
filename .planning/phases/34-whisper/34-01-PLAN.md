---
phase: 34-whisper
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/cpp/whisper_jni.cpp
  - app/src/main/java/com/autominuting/data/stt/SttEngine.kt
  - app/src/main/java/com/autominuting/data/stt/WhisperEngine.kt
  - app/src/main/java/com/autominuting/data/stt/GeminiSttEngine.kt
  - app/src/main/java/com/autominuting/data/stt/MlKitEngine.kt
  - app/src/main/java/com/autominuting/domain/repository/TranscriptionRepository.kt
  - app/src/main/java/com/autominuting/data/repository/TranscriptionRepositoryImpl.kt
autonomous: true
requirements:
  - PROG-01
must_haves:
  truths:
    - "WhisperEngine 전사 시 whisper.cpp가 0~100 진행률 콜백을 Kotlin으로 전달한다"
    - "TranscriptionRepository.transcribe() 호출자가 onProgress 콜백으로 진행률을 수신한다"
    - "GeminiSttEngine과 MlKitEngine은 기존과 동일하게 동작한다 (onProgress 기본값 사용)"
  artifacts:
    - path: "app/src/main/cpp/whisper_jni.cpp"
      provides: "whisper progress_callback JNI 연결"
      contains: "whisper_progress_cb"
    - path: "app/src/main/java/com/autominuting/data/stt/SttEngine.kt"
      provides: "onProgress 파라미터가 추가된 SttEngine 인터페이스"
      contains: "onProgress"
    - path: "app/src/main/java/com/autominuting/data/stt/WhisperEngine.kt"
      provides: "JNI 콜백 수신 + onProgress 전달"
      contains: "onNativeProgress"
  key_links:
    - from: "app/src/main/cpp/whisper_jni.cpp"
      to: "WhisperEngine.onNativeProgress"
      via: "JNI CallVoidMethod"
      pattern: "CallVoidMethod.*onNativeProgress"
    - from: "app/src/main/java/com/autominuting/data/stt/WhisperEngine.kt"
      to: "SttEngine.transcribe onProgress"
      via: "콜백 호출"
      pattern: "onProgress"
    - from: "app/src/main/java/com/autominuting/data/repository/TranscriptionRepositoryImpl.kt"
      to: "SttEngine.transcribe"
      via: "onProgress 파라미터 전달"
      pattern: "engine\\.transcribe.*onProgress"
---

<objective>
whisper.cpp의 progress_callback을 JNI를 통해 Kotlin으로 연결하고, SttEngine 인터페이스에 onProgress 콜백을 추가하여 전사 진행률 데이터가 Engine -> Repository -> 호출자까지 흐르도록 한다.

Purpose: Whisper 전사가 장시간(수 분) 소요되므로 사용자에게 진행률을 제공하기 위한 데이터 파이프라인 구축
Output: 진행률 콜백이 동작하는 SttEngine/TranscriptionRepository 레이어
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-whisper/34-RESEARCH.md

<interfaces>
<!-- 현재 SttEngine 인터페이스 — onProgress 파라미터 추가 대상 -->
From app/src/main/java/com/autominuting/data/stt/SttEngine.kt:
```kotlin
interface SttEngine {
    suspend fun transcribe(audioFilePath: String): Result<String>
    suspend fun isAvailable(): Boolean
    fun engineName(): String
}
```

From app/src/main/java/com/autominuting/domain/repository/TranscriptionRepository.kt:
```kotlin
interface TranscriptionRepository {
    suspend fun transcribe(audioFilePath: String): Result<String>
    fun isTranscribing(): Flow<Boolean>
}
```

<!-- 현재 nativeTranscribe JNI 시그니처 -->
From WhisperEngine.kt:
```kotlin
private external fun nativeTranscribe(
    modelPath: String, audioPath: String,
    language: String, temperature: Float
): String?
```

<!-- 현재 whisper_jni.cpp JNI 함수 시그니처 -->
```cpp
extern "C" JNIEXPORT jstring JNICALL
Java_com_autominuting_data_stt_WhisperEngine_nativeTranscribe(
    JNIEnv *env, jobject /* this */,
    jstring modelPath, jstring audioPath,
    jstring language, jfloat temperature)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: JNI progress_callback + WhisperEngine onNativeProgress 연결</name>
  <files>
    app/src/main/cpp/whisper_jni.cpp
    app/src/main/java/com/autominuting/data/stt/WhisperEngine.kt
  </files>
  <read_first>
    app/src/main/cpp/whisper_jni.cpp
    app/src/main/java/com/autominuting/data/stt/WhisperEngine.kt
    app/src/main/cpp/whisper.cpp/include/whisper.h
  </read_first>
  <action>
**whisper_jni.cpp 변경:**

1. ProgressCallbackData 구조체를 추가한다:
```cpp
struct ProgressCallbackData {
    JNIEnv *env;
    jobject callback_obj;   // WhisperEngine 인스턴스 (Global Ref 아님 — 같은 스레드)
    jmethodID method_id;    // onNativeProgress(int) 메서드 ID
    int last_progress;      // 이전 진행률 (1% 이상 변경 시만 콜백)
};
```

2. whisper_progress_cb 정적 함수를 추가한다:
```cpp
static void whisper_progress_cb(
    struct whisper_context *ctx,
    struct whisper_state *state,
    int progress,
    void *user_data
) {
    auto *data = (ProgressCallbackData *)user_data;
    if (progress - data->last_progress >= 1) {
        data->env->CallVoidMethod(data->callback_obj, data->method_id, (jint)progress);
        data->last_progress = progress;
    }
}
```

3. nativeTranscribe JNI 함수 시그니처를 변경한다 — `jobject progressListener` 파라미터 추가:
```cpp
extern "C" JNIEXPORT jstring JNICALL
Java_com_autominuting_data_stt_WhisperEngine_nativeTranscribe(
    JNIEnv *env, jobject thiz,
    jstring modelPath, jstring audioPath,
    jstring language, jfloat temperature,
    jobject progressListener)
```

4. whisper_full 호출 전에 progressListener가 null이 아니면 콜백을 설정한다:
```cpp
ProgressCallbackData cb_data = {nullptr, nullptr, nullptr, 0};
if (progressListener != nullptr) {
    jclass listenerClass = env->GetObjectClass(progressListener);
    jmethodID onProgress = env->GetMethodID(listenerClass, "onNativeProgress", "(I)V");
    if (onProgress != nullptr) {
        cb_data.env = env;
        cb_data.callback_obj = progressListener;
        cb_data.method_id = onProgress;
        params.progress_callback = whisper_progress_cb;
        params.progress_callback_user_data = &cb_data;
    }
}
```
주의: whisper_full()은 같은 스레드에서 콜백을 호출하므로 GlobalRef 불필요. env도 그대로 사용 가능.

**WhisperEngine.kt 변경:**

1. `onProgress` 콜백을 저장할 프로퍼티를 추가한다:
```kotlin
@Volatile
private var currentProgressCallback: ((Float) -> Unit)? = null
```

2. `onNativeProgress(progress: Int)` 메서드를 추가한다 (JNI에서 호출됨):
```kotlin
// JNI에서 직접 호출하는 메서드 — @Keep 또는 proguard 예외 필요
fun onNativeProgress(progress: Int) {
    currentProgressCallback?.invoke(progress / 100f)
}
```

3. `nativeTranscribe` 시그니처에 `progressListener: Any?` 파라미터를 추가한다:
```kotlin
private external fun nativeTranscribe(
    modelPath: String,
    audioPath: String,
    language: String,
    temperature: Float,
    progressListener: Any?
): String?
```

4. `transcribe()` 메서드 시그니처를 `onProgress: (Float) -> Unit = {}` 파라미터를 받도록 변경하고, nativeTranscribe 호출 시 `this`를 progressListener로 전달한다:
```kotlin
override suspend fun transcribe(
    audioFilePath: String,
    onProgress: (Float) -> Unit
): Result<String> = withContext(Dispatchers.IO) {
    try {
        currentProgressCallback = onProgress
        // ... 기존 로직 ...
        val result = nativeTranscribe(
            modelPath = modelPath,
            audioPath = wavPath,
            language = LANGUAGE,
            temperature = TEMPERATURE,
            progressListener = this@WhisperEngine
        )
        // ... 기존 결과 처리 ...
    } finally {
        currentProgressCallback = null
    }
}
```
  </action>
  <verify>
    <automated>cd D:/workspace/temp/Auto_Minuting && grep -c "whisper_progress_cb" app/src/main/cpp/whisper_jni.cpp && grep -c "onNativeProgress" app/src/main/java/com/autominuting/data/stt/WhisperEngine.kt && grep -c "progressListener" app/src/main/cpp/whisper_jni.cpp</automated>
  </verify>
  <acceptance_criteria>
    - whisper_jni.cpp에 `ProgressCallbackData` 구조체가 정의되어 있다
    - whisper_jni.cpp에 `whisper_progress_cb` 정적 함수가 정의되어 있다
    - whisper_jni.cpp의 nativeTranscribe 시그니처에 `jobject progressListener` 파라미터가 있다
    - whisper_jni.cpp에서 `params.progress_callback = whisper_progress_cb` 설정이 있다
    - WhisperEngine.kt에 `onNativeProgress(progress: Int)` public 메서드가 있다
    - WhisperEngine.kt의 nativeTranscribe에 `progressListener: Any?` 파라미터가 있다
    - WhisperEngine.kt의 transcribe()에 `onProgress: (Float) -> Unit` 파라미터가 있다
    - nativeTranscribe 호출 시 `progressListener = this@WhisperEngine`을 전달한다
  </acceptance_criteria>
  <done>whisper.cpp progress_callback이 JNI를 통해 WhisperEngine.onNativeProgress()를 호출하고, WhisperEngine.transcribe()의 onProgress 콜백이 0.0~1.0 범위로 진행률을 전달한다</done>
</task>

<task type="auto">
  <name>Task 2: SttEngine 인터페이스 + Repository + 다른 엔진 시그니처 정합</name>
  <files>
    app/src/main/java/com/autominuting/data/stt/SttEngine.kt
    app/src/main/java/com/autominuting/data/stt/GeminiSttEngine.kt
    app/src/main/java/com/autominuting/data/stt/MlKitEngine.kt
    app/src/main/java/com/autominuting/domain/repository/TranscriptionRepository.kt
    app/src/main/java/com/autominuting/data/repository/TranscriptionRepositoryImpl.kt
  </files>
  <read_first>
    app/src/main/java/com/autominuting/data/stt/SttEngine.kt
    app/src/main/java/com/autominuting/data/stt/GeminiSttEngine.kt
    app/src/main/java/com/autominuting/data/stt/MlKitEngine.kt
    app/src/main/java/com/autominuting/domain/repository/TranscriptionRepository.kt
    app/src/main/java/com/autominuting/data/repository/TranscriptionRepositoryImpl.kt
  </read_first>
  <action>
**SttEngine.kt 변경:**

`transcribe` 메서드에 `onProgress` 기본 파라미터를 추가한다:
```kotlin
interface SttEngine {
    suspend fun transcribe(
        audioFilePath: String,
        onProgress: (Float) -> Unit = {}
    ): Result<String>
    suspend fun isAvailable(): Boolean
    fun engineName(): String
}
```

**GeminiSttEngine.kt 변경:**

`transcribe` 시그니처에 `onProgress: (Float) -> Unit` 파라미터를 추가한다. 본문 변경 없음 (Gemini는 진행률 미지원):
```kotlin
override suspend fun transcribe(
    audioFilePath: String,
    onProgress: (Float) -> Unit
): Result<String> = withContext(Dispatchers.IO) {
    // ... 기존 로직 동일 ...
}
```

**MlKitEngine.kt 변경:**

동일하게 `onProgress: (Float) -> Unit` 파라미터 추가. 본문 변경 없음:
```kotlin
override suspend fun transcribe(
    audioFilePath: String,
    onProgress: (Float) -> Unit
): Result<String> {
    // ... 기존 로직 동일 ...
}
```

**TranscriptionRepository.kt 변경:**

`transcribe` 메서드에 `onProgress` 파라미터를 추가한다:
```kotlin
interface TranscriptionRepository {
    suspend fun transcribe(
        audioFilePath: String,
        onProgress: (Float) -> Unit = {}
    ): Result<String>
    fun isTranscribing(): Flow<Boolean>
}
```

**TranscriptionRepositoryImpl.kt 변경:**

1. `transcribe` 메서드에 `onProgress` 파라미터를 추가한다:
```kotlin
override suspend fun transcribe(
    audioFilePath: String,
    onProgress: (Float) -> Unit
): Result<String> = withContext(Dispatchers.IO) {
    // ... 기존 로직 ...
}
```

2. `tryEngine` 메서드에도 `onProgress` 파라미터를 전달한다:
```kotlin
private suspend fun tryEngine(
    engine: SttEngine,
    audioFilePath: String,
    onProgress: (Float) -> Unit
): Result<String> {
    return try {
        Log.d(TAG, "${engine.engineName()} 시도")
        val result = engine.transcribe(audioFilePath, onProgress)
        // ... 기존 로직 ...
    }
}
```

3. `transcribe()` 내의 `tryEngine()` 호출에 `onProgress`를 전달한다:
```kotlin
val result = tryEngine(engine, audioFilePath, onProgress)
```
  </action>
  <verify>
    <automated>cd D:/workspace/temp/Auto_Minuting && grep "onProgress" app/src/main/java/com/autominuting/data/stt/SttEngine.kt && grep "onProgress" app/src/main/java/com/autominuting/data/stt/GeminiSttEngine.kt && grep "onProgress" app/src/main/java/com/autominuting/domain/repository/TranscriptionRepository.kt && grep "onProgress" app/src/main/java/com/autominuting/data/repository/TranscriptionRepositoryImpl.kt</automated>
  </verify>
  <acceptance_criteria>
    - SttEngine.kt의 transcribe 시그니처에 `onProgress: (Float) -> Unit = {}` 파라미터가 있다
    - GeminiSttEngine.kt의 transcribe 시그니처에 `onProgress: (Float) -> Unit` 파라미터가 있다
    - MlKitEngine.kt의 transcribe 시그니처에 `onProgress: (Float) -> Unit` 파라미터가 있다
    - TranscriptionRepository.kt의 transcribe 시그니처에 `onProgress: (Float) -> Unit = {}` 파라미터가 있다
    - TranscriptionRepositoryImpl.kt의 transcribe에서 tryEngine 호출 시 onProgress를 전달한다
    - TranscriptionRepositoryImpl.kt의 tryEngine에서 engine.transcribe 호출 시 onProgress를 전달한다
  </acceptance_criteria>
  <done>SttEngine 인터페이스부터 TranscriptionRepository까지 onProgress 콜백 체인이 완성되어, Whisper 전사 시 호출자가 진행률을 수신할 수 있다</done>
</task>

</tasks>

<verification>
- whisper_jni.cpp에 progress_callback 등록 코드가 존재한다
- WhisperEngine에 onNativeProgress 메서드가 존재한다
- SttEngine, GeminiSttEngine, MlKitEngine, TranscriptionRepository, TranscriptionRepositoryImpl 모두 onProgress 파라미터가 있다
- 기존 코드에서 transcribe()를 호출하는 곳은 기본 파라미터 `= {}`로 인해 컴파일 오류가 발생하지 않는다
</verification>

<success_criteria>
whisper.cpp progress_callback -> JNI -> WhisperEngine.onNativeProgress -> onProgress 콜백 -> TranscriptionRepository -> 호출자까지 진행률 데이터 흐름이 완성된다. GeminiSttEngine과 MlKitEngine은 기존과 동일하게 동작한다.
</success_criteria>

<output>
After completion, create `.planning/phases/34-whisper/34-01-SUMMARY.md`
</output>
