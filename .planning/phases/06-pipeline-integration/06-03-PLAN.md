---
phase: 06-pipeline-integration
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - app/src/main/java/com/autominuting/presentation/settings/SettingsViewModel.kt
  - app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt
  - app/src/main/java/com/autominuting/presentation/minutes/MinutesDetailScreen.kt
  - app/src/main/java/com/autominuting/presentation/minutes/MinutesDetailViewModel.kt
  - app/src/main/java/com/autominuting/presentation/dashboard/DashboardScreen.kt
  - app/src/main/java/com/autominuting/presentation/dashboard/DashboardViewModel.kt
autonomous: true
requirements:
  - MIN-05
  - MIN-06
  - UI-04

must_haves:
  truths:
    - "설정 화면에서 회의록 형식 3종을 드롭다운으로 선택할 수 있다"
    - "설정 화면에서 완전 자동/하이브리드 모드를 토글로 전환할 수 있다"
    - "회의록 상세 화면 AppBar에 공유 아이콘이 있고 눌러서 Share Intent가 실행된다"
    - "대시보드에 현재 진행 중인 파이프라인이 있으면 상단 배너로 상태가 표시된다"
  artifacts:
    - path: "app/src/main/java/com/autominuting/presentation/settings/SettingsViewModel.kt"
      provides: "설정 상태 관리 ViewModel"
      contains: "class SettingsViewModel"
    - path: "app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt"
      provides: "형식 선택 + 모드 토글 UI"
      contains: "ExposedDropdownMenuBox"
    - path: "app/src/main/java/com/autominuting/presentation/minutes/MinutesDetailScreen.kt"
      provides: "공유 아이콘"
      contains: "Icons.Default.Share"
    - path: "app/src/main/java/com/autominuting/presentation/dashboard/DashboardViewModel.kt"
      provides: "진행 중 파이프라인 상태"
      contains: "class DashboardViewModel"
  key_links:
    - from: "SettingsViewModel.kt"
      to: "UserPreferencesRepository.kt"
      via: "Hilt 주입"
      pattern: "userPreferencesRepository"
    - from: "MinutesDetailScreen.kt"
      to: "Intent.ACTION_SEND"
      via: "Share Intent"
      pattern: "ACTION_SEND"
    - from: "DashboardViewModel.kt"
      to: "MeetingRepository"
      via: "진행 중 회의 조회"
      pattern: "meetingRepository"
---

<objective>
설정 화면(형식 선택/모드 토글), 회의록 상세 화면(공유), 대시보드(진행 배너) UI를 구현한다.

Purpose: 사용자가 파이프라인 설정을 제어하고, 회의록을 공유하며, 진행 상태를 앱 내에서 확인할 수 있도록 한다.
Output: SettingsViewModel + 확장된 SettingsScreen, 공유 기능이 추가된 MinutesDetailScreen, 배너가 있는 DashboardScreen
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-pipeline-integration/06-CONTEXT.md
@.planning/phases/06-pipeline-integration/06-RESEARCH.md
@.planning/phases/06-pipeline-integration/06-01-SUMMARY.md

<interfaces>
<!-- Plan 01에서 생성된 타입들 -->

From domain/model/MinutesFormat.kt:
```kotlin
enum class MinutesFormat { STRUCTURED, SUMMARY, ACTION_ITEMS }
```

From domain/model/AutomationMode.kt:
```kotlin
enum class AutomationMode { FULL_AUTO, HYBRID }
```

From data/preferences/UserPreferencesRepository.kt:
```kotlin
@Singleton
class UserPreferencesRepository @Inject constructor(dataStore: DataStore<Preferences>) {
    val minutesFormat: Flow<MinutesFormat>
    val automationMode: Flow<AutomationMode>
    suspend fun setMinutesFormat(format: MinutesFormat)
    suspend fun setAutomationMode(mode: AutomationMode)
}
```

From presentation/minutes/MinutesDetailViewModel.kt:
```kotlin
@HiltViewModel
class MinutesDetailViewModel @Inject constructor(
    savedStateHandle: SavedStateHandle,
    private val meetingRepository: MeetingRepository
) : ViewModel() {
    val meeting: StateFlow<Meeting?>
    val minutesContent: StateFlow<String>
}
```

From domain/model/PipelineStatus.kt:
```kotlin
enum class PipelineStatus {
    AUDIO_RECEIVED, TRANSCRIBING, TRANSCRIBED, GENERATING_MINUTES, COMPLETED, FAILED
}
```

From domain/repository/MeetingRepository.kt (주요 메서드):
```kotlin
interface MeetingRepository {
    fun getMeetingById(id: Long): Flow<Meeting?>
    fun getAllMeetings(): Flow<List<Meeting>>
}
```

기존 UI 패턴 참조:
- TranscriptsScreen: hiltViewModel(), collectAsStateWithLifecycle()
- MinutesDetailScreen: Scaffold + TopAppBar + IconButton
- SuggestionChip 상태 칩: SuggestionChipDefaults.suggestionChipColors
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SettingsViewModel + SettingsScreen 확장 (형식 선택 + 모드 토글)</name>
  <files>
    app/src/main/java/com/autominuting/presentation/settings/SettingsViewModel.kt,
    app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt
  </files>
  <read_first>
    app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt,
    app/src/main/java/com/autominuting/data/preferences/UserPreferencesRepository.kt,
    app/src/main/java/com/autominuting/domain/model/MinutesFormat.kt,
    app/src/main/java/com/autominuting/domain/model/AutomationMode.kt,
    app/src/main/java/com/autominuting/presentation/transcripts/TranscriptsViewModel.kt
  </read_first>
  <action>
1. SettingsViewModel.kt 생성:
   - package com.autominuting.presentation.settings
   - @HiltViewModel class SettingsViewModel @Inject constructor(private val userPreferencesRepository: UserPreferencesRepository) : ViewModel()
   - val minutesFormat: StateFlow<MinutesFormat> = userPreferencesRepository.minutesFormat.stateIn(viewModelScope, SharingStarted.WhileSubscribed(5_000), MinutesFormat.STRUCTURED)
   - val automationMode: StateFlow<AutomationMode> = userPreferencesRepository.automationMode.stateIn(viewModelScope, SharingStarted.WhileSubscribed(5_000), AutomationMode.FULL_AUTO)
   - fun setMinutesFormat(format: MinutesFormat) { viewModelScope.launch { userPreferencesRepository.setMinutesFormat(format) } }
   - fun setAutomationMode(mode: AutomationMode) { viewModelScope.launch { userPreferencesRepository.setAutomationMode(mode) } }
   - import: androidx.lifecycle.ViewModel, androidx.lifecycle.viewModelScope, com.autominuting.data.preferences.UserPreferencesRepository, com.autominuting.domain.model.MinutesFormat, com.autominuting.domain.model.AutomationMode, dagger.hilt.android.lifecycle.HiltViewModel, kotlinx.coroutines.flow.SharingStarted, kotlinx.coroutines.flow.StateFlow, kotlinx.coroutines.flow.stateIn, kotlinx.coroutines.launch, javax.inject.Inject

2. SettingsScreen.kt 전체 재작성 (per D-02, D-06):
   - @Composable fun SettingsScreen(viewModel: SettingsViewModel = hiltViewModel())
   - val selectedFormat by viewModel.minutesFormat.collectAsStateWithLifecycle()
   - val automationMode by viewModel.automationMode.collectAsStateWithLifecycle()
   - Scaffold + TopAppBar(title = "설정") 구조
   - Column(Modifier.fillMaxSize().verticalScroll(rememberScrollState()).padding(16.dp)) 내부:

   **회의록 형식 섹션** (per D-01, D-02):
   - Text("회의록 형식", style = MaterialTheme.typography.titleMedium)
   - Text("기본 회의록 생성 형식을 선택합니다", style = bodySmall, color = onSurfaceVariant)
   - Spacer(8.dp)
   - ExposedDropdownMenuBox 사용:
     - var expanded by remember { mutableStateOf(false) }
     - val formatLabels = mapOf(MinutesFormat.STRUCTURED to "구조화된 회의록", MinutesFormat.SUMMARY to "요약", MinutesFormat.ACTION_ITEMS to "액션 아이템")
     - OutlinedTextField(value = formatLabels[selectedFormat] ?: "", readOnly = true, onValueChange = {}, trailingIcon = ExposedDropdownMenuDefaults.TrailingIcon(expanded), modifier = Modifier.menuAnchor().fillMaxWidth())
     - ExposedDropdownMenu에서 MinutesFormat.entries.forEach { format -> DropdownMenuItem(text = { Text(formatLabels[format] ?: format.name) }, onClick = { viewModel.setMinutesFormat(format); expanded = false }) }

   **자동화 모드 섹션** (per D-04, D-05, D-06):
   - Spacer(24.dp)
   - Text("자동화 모드", style = titleMedium)
   - Spacer(8.dp)
   - Row(verticalAlignment = CenterVertically, modifier = Modifier.fillMaxWidth()):
     - Column(Modifier.weight(1f)):
       - Text("완전 자동 모드", style = bodyLarge)
       - Text("오디오 감지부터 회의록 생성까지 자동 진행", style = bodySmall, color = onSurfaceVariant)
     - Switch(checked = automationMode == AutomationMode.FULL_AUTO, onCheckedChange = { isAuto -> viewModel.setAutomationMode(if (isAuto) AutomationMode.FULL_AUTO else AutomationMode.HYBRID) })
   - if (automationMode == AutomationMode.HYBRID):
     - Spacer(4.dp)
     - Text("하이브리드 모드: 전사 완료 후 확인을 거쳐 회의록 생성", style = bodySmall, color = tertiary)

   - TestWorker 버튼 제거 (Phase 2 스캐폴딩 용도 완료)
   - 기존 import 정리 + 신규 import 추가: hiltViewModel, collectAsStateWithLifecycle, ExposedDropdownMenuBox, ExposedDropdownMenu, ExposedDropdownMenuDefaults, OutlinedTextField, DropdownMenuItem, Switch, Row, rememberScrollState, verticalScroll, mutableStateOf, remember, Spacer, Scaffold, TopAppBar 등
  </action>
  <verify>
    <automated>cd D:/workspace/temp/Auto_Minuting && ./gradlew compileDebugKotlin 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - SettingsViewModel.kt에 `@HiltViewModel` `class SettingsViewModel @Inject constructor` 포함
    - SettingsViewModel.kt에 `val minutesFormat: StateFlow<MinutesFormat>` 포함
    - SettingsViewModel.kt에 `val automationMode: StateFlow<AutomationMode>` 포함
    - SettingsViewModel.kt에 `fun setMinutesFormat(format: MinutesFormat)` 포함
    - SettingsScreen.kt에 `ExposedDropdownMenuBox` 포함
    - SettingsScreen.kt에 `"구조화된 회의록"` `"요약"` `"액션 아이템"` 문자열 포함
    - SettingsScreen.kt에 `Switch(` 포함
    - SettingsScreen.kt에 `AutomationMode.FULL_AUTO` 포함
    - SettingsScreen.kt에 `hiltViewModel()` 포함
    - SettingsScreen.kt에 TestWorker 참조가 없음
    - compileDebugKotlin이 BUILD SUCCESSFUL로 종료
  </acceptance_criteria>
  <done>설정 화면에서 3종 회의록 형식 드롭다운 선택과 완전 자동/하이브리드 모드 토글이 동작하며 DataStore에 저장된다</done>
</task>

<task type="auto">
  <name>Task 2: MinutesDetailScreen 공유 + DashboardScreen 진행 배너</name>
  <files>
    app/src/main/java/com/autominuting/presentation/minutes/MinutesDetailScreen.kt,
    app/src/main/java/com/autominuting/presentation/minutes/MinutesDetailViewModel.kt,
    app/src/main/java/com/autominuting/presentation/dashboard/DashboardScreen.kt,
    app/src/main/java/com/autominuting/presentation/dashboard/DashboardViewModel.kt
  </files>
  <read_first>
    app/src/main/java/com/autominuting/presentation/minutes/MinutesDetailScreen.kt,
    app/src/main/java/com/autominuting/presentation/minutes/MinutesDetailViewModel.kt,
    app/src/main/java/com/autominuting/presentation/dashboard/DashboardScreen.kt,
    app/src/main/java/com/autominuting/domain/model/PipelineStatus.kt,
    app/src/main/java/com/autominuting/domain/model/Meeting.kt,
    app/src/main/java/com/autominuting/domain/repository/MeetingRepository.kt,
    app/src/main/java/com/autominuting/presentation/minutes/MinutesScreen.kt
  </read_first>
  <action>
1. MinutesDetailScreen.kt 수정 (per D-10, D-11):
   - import 추가: Icons.Default.Share, android.content.Intent, androidx.compose.ui.platform.LocalContext
   - TopAppBar의 actions 파라미터 추가:
     ```kotlin
     actions = {
         // 공유 버튼: 회의록이 있을 때만 표시
         if (minutesContent.isNotBlank()) {
             val context = LocalContext.current
             IconButton(onClick = {
                 val sendIntent = Intent().apply {
                     action = Intent.ACTION_SEND
                     putExtra(Intent.EXTRA_SUBJECT, meeting?.title ?: "회의록")
                     putExtra(Intent.EXTRA_TEXT, minutesContent)
                     type = "text/plain"
                 }
                 val shareIntent = Intent.createChooser(sendIntent, null)
                 context.startActivity(shareIntent)
             }) {
                 Icon(
                     imageVector = Icons.Default.Share,
                     contentDescription = "공유"
                 )
             }
         }
     }
     ```
   - 주의: `val context = LocalContext.current`를 Scaffold 밖(Composable 최상위)에 이미 있으므로 actions 내에서는 별도 변수 불필요 — 실제로 현재 코드에는 context가 없으므로 TopAppBar 위에 `val context = LocalContext.current` 추가

2. DashboardViewModel.kt 생성 (per D-09):
   - package com.autominuting.presentation.dashboard
   - @HiltViewModel class DashboardViewModel @Inject constructor(private val meetingRepository: MeetingRepository) : ViewModel()
   - 진행 중인 파이프라인 조회: val activePipeline: StateFlow<Meeting?> = meetingRepository.getAllMeetings().map { meetings -> meetings.firstOrNull { it.pipelineStatus in listOf(PipelineStatus.AUDIO_RECEIVED, PipelineStatus.TRANSCRIBING, PipelineStatus.TRANSCRIBED, PipelineStatus.GENERATING_MINUTES) } }.stateIn(viewModelScope, SharingStarted.WhileSubscribed(5_000), null)
   - import: ViewModel, viewModelScope, MeetingRepository, Meeting, PipelineStatus, HiltViewModel, StateFlow, SharingStarted, stateIn, map, Inject

3. DashboardScreen.kt 수정 (per D-09):
   - @Composable fun DashboardScreen(viewModel: DashboardViewModel = hiltViewModel())
   - val activePipeline by viewModel.activePipeline.collectAsStateWithLifecycle()
   - 기존 Box를 Column으로 교체:
     ```kotlin
     Column(modifier = Modifier.fillMaxSize()) {
         // 진행 중인 파이프라인 배너
         activePipeline?.let { meeting ->
             val statusText = when (meeting.pipelineStatus) {
                 PipelineStatus.AUDIO_RECEIVED -> "오디오 수신됨"
                 PipelineStatus.TRANSCRIBING -> "전사 중..."
                 PipelineStatus.TRANSCRIBED -> "전사 완료 — 회의록 생성 대기"
                 PipelineStatus.GENERATING_MINUTES -> "회의록 생성 중..."
                 else -> null
             }
             if (statusText != null) {
                 Card(
                     modifier = Modifier
                         .fillMaxWidth()
                         .padding(16.dp),
                     colors = CardDefaults.cardColors(
                         containerColor = MaterialTheme.colorScheme.primaryContainer
                     )
                 ) {
                     Row(
                         modifier = Modifier.padding(16.dp),
                         verticalAlignment = Alignment.CenterVertically
                     ) {
                         CircularProgressIndicator(
                             modifier = Modifier.size(24.dp),
                             strokeWidth = 2.dp
                         )
                         Spacer(modifier = Modifier.width(12.dp))
                         Column {
                             Text(
                                 text = meeting.title,
                                 style = MaterialTheme.typography.titleSmall
                             )
                             Text(
                                 text = statusText,
                                 style = MaterialTheme.typography.bodySmall,
                                 color = MaterialTheme.colorScheme.onPrimaryContainer
                             )
                         }
                     }
                 }
             }
         }

         // 기본 대시보드 콘텐츠
         Box(
             modifier = Modifier.fillMaxSize(),
             contentAlignment = Alignment.Center
         ) {
             Text("대시보드")
         }
     }
     ```
   - import 추가: hiltViewModel, collectAsStateWithLifecycle, Column, Card, CardDefaults, Row, Spacer, CircularProgressIndicator, size, width, padding 등
  </action>
  <verify>
    <automated>cd D:/workspace/temp/Auto_Minuting && ./gradlew compileDebugKotlin 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - MinutesDetailScreen.kt에 `Icons.Default.Share` 포함
    - MinutesDetailScreen.kt에 `Intent.ACTION_SEND` 포함
    - MinutesDetailScreen.kt에 `Intent.EXTRA_TEXT` 포함
    - MinutesDetailScreen.kt에 `contentDescription = "공유"` 포함
    - DashboardViewModel.kt에 `@HiltViewModel` `class DashboardViewModel @Inject constructor` 포함
    - DashboardViewModel.kt에 `val activePipeline: StateFlow<Meeting?>` 포함
    - DashboardViewModel.kt에 `PipelineStatus.TRANSCRIBING` 포함
    - DashboardScreen.kt에 `hiltViewModel()` 포함
    - DashboardScreen.kt에 `activePipeline` 참조 포함
    - DashboardScreen.kt에 `"전사 중..."` 문자열 포함
    - DashboardScreen.kt에 `"회의록 생성 중..."` 문자열 포함
    - DashboardScreen.kt에 `Card(` 포함
    - DashboardScreen.kt에 `CircularProgressIndicator` 포함
    - compileDebugKotlin이 BUILD SUCCESSFUL로 종료
  </acceptance_criteria>
  <done>회의록 상세 화면에서 공유 아이콘으로 Share Intent 공유가 가능하고, 대시보드에 진행 중인 파이프라인 상태 배너가 표시된다</done>
</task>

</tasks>

<verification>
- compileDebugKotlin 성공
- SettingsScreen에 형식 드롭다운 + 모드 토글 존재
- MinutesDetailScreen AppBar에 공유 아이콘 존재
- DashboardScreen에 파이프라인 진행 배너 존재
</verification>

<success_criteria>
설정 화면에서 형식/모드 변경이 가능하고, 회의록 공유가 가능하며, 대시보드에 파이프라인 진행 상태가 표시된다
</success_criteria>

<output>
After completion, create `.planning/phases/06-pipeline-integration/06-03-SUMMARY.md`
</output>
