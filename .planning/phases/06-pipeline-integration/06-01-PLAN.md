---
phase: 06-pipeline-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/com/autominuting/domain/model/MinutesFormat.kt
  - app/src/main/java/com/autominuting/domain/model/AutomationMode.kt
  - app/src/main/java/com/autominuting/di/DataStoreModule.kt
  - app/src/main/java/com/autominuting/data/preferences/UserPreferencesRepository.kt
  - app/src/main/java/com/autominuting/data/minutes/GeminiEngine.kt
  - app/src/main/java/com/autominuting/domain/repository/MinutesRepository.kt
  - app/src/main/java/com/autominuting/data/repository/MinutesRepositoryImpl.kt
  - app/src/main/java/com/autominuting/service/PipelineNotificationHelper.kt
  - app/src/main/java/com/autominuting/AutoMinutingApplication.kt
autonomous: true
requirements:
  - MIN-05

must_haves:
  truths:
    - "MinutesFormat enum에 STRUCTURED, SUMMARY, ACTION_ITEMS 3종이 정의된다"
    - "AutomationMode enum에 FULL_AUTO, HYBRID 2종이 정의된다"
    - "GeminiEngine.generate()가 MinutesFormat 파라미터를 받아 형식별 프롬프트를 적용한다"
    - "UserPreferencesRepository가 DataStore로 형식/모드 설정을 저장하고 Flow로 읽는다"
    - "PipelineNotificationHelper가 파이프라인 진행 알림을 생성/업데이트할 수 있다"
  artifacts:
    - path: "app/src/main/java/com/autominuting/domain/model/MinutesFormat.kt"
      provides: "회의록 형식 enum"
      contains: "enum class MinutesFormat"
    - path: "app/src/main/java/com/autominuting/domain/model/AutomationMode.kt"
      provides: "자동화 모드 enum"
      contains: "enum class AutomationMode"
    - path: "app/src/main/java/com/autominuting/data/preferences/UserPreferencesRepository.kt"
      provides: "DataStore 설정 저장/읽기"
      contains: "class UserPreferencesRepository"
    - path: "app/src/main/java/com/autominuting/data/minutes/GeminiEngine.kt"
      provides: "형식별 프롬프트 분기"
      contains: "fun generate(transcriptText: String, format: MinutesFormat"
    - path: "app/src/main/java/com/autominuting/service/PipelineNotificationHelper.kt"
      provides: "파이프라인 알림 유틸리티"
      contains: "object PipelineNotificationHelper"
  key_links:
    - from: "GeminiEngine.kt"
      to: "MinutesFormat.kt"
      via: "when(format) 분기"
      pattern: "when\\s*\\(format\\)"
    - from: "UserPreferencesRepository.kt"
      to: "DataStoreModule.kt"
      via: "Hilt 주입"
      pattern: "@Inject constructor"
    - from: "MinutesRepositoryImpl.kt"
      to: "GeminiEngine.kt"
      via: "format 파라미터 전달"
      pattern: "geminiEngine\\.generate\\(transcriptText,\\s*format\\)"
---

<objective>
Phase 6 기반 레이어: 도메인 모델, DataStore 설정, GeminiEngine 프롬프트 확장, 알림 헬퍼를 구축한다.

Purpose: Plan 02(Worker 수정)와 Plan 03(UI)가 의존하는 모든 타입, 설정 인프라, 알림 유틸리티를 선행 구축하여 이후 플랜이 탐색 없이 바로 구현할 수 있도록 한다.
Output: MinutesFormat/AutomationMode enum, UserPreferencesRepository, 확장된 GeminiEngine, PipelineNotificationHelper
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-pipeline-integration/06-CONTEXT.md
@.planning/phases/06-pipeline-integration/06-RESEARCH.md

<interfaces>
<!-- 기존 코드 패턴. Executor는 이 패턴을 따라야 한다. -->

From app/src/main/java/com/autominuting/domain/model/PipelineStatus.kt:
```kotlin
package com.autominuting.domain.model
enum class PipelineStatus {
    AUDIO_RECEIVED, TRANSCRIBING, TRANSCRIBED, GENERATING_MINUTES, COMPLETED, FAILED
}
```

From app/src/main/java/com/autominuting/data/minutes/GeminiEngine.kt:
```kotlin
@Singleton
class GeminiEngine @Inject constructor() {
    suspend fun generate(transcriptText: String): Result<String>
    fun engineName(): String
}
```

From app/src/main/java/com/autominuting/domain/repository/MinutesRepository.kt:
```kotlin
interface MinutesRepository {
    suspend fun generateMinutes(transcriptText: String): Result<String>
    fun isGenerating(): Flow<Boolean>
}
```

From app/src/main/java/com/autominuting/data/repository/MinutesRepositoryImpl.kt:
```kotlin
@Singleton
class MinutesRepositoryImpl @Inject constructor(
    private val geminiEngine: GeminiEngine,
    @ApplicationContext private val context: Context
) : MinutesRepository {
    override suspend fun generateMinutes(transcriptText: String): Result<String>
    fun saveMinutesToFile(meetingId: Long, minutesText: String): String
}
```

From app/src/main/java/com/autominuting/service/AudioCollectionService.kt (알림 패턴):
```kotlin
val channel = NotificationChannel(CHANNEL_ID, "오디오 수집", NotificationManager.IMPORTANCE_LOW)
NotificationCompat.Builder(this, CHANNEL_ID)
    .setSmallIcon(R.drawable.ic_launcher_foreground)
    .setContentTitle("Auto Minuting")
    .setContentText(text)
    .setOngoing(true)
    .build()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 도메인 모델 + DataStore 설정 인프라</name>
  <files>
    app/src/main/java/com/autominuting/domain/model/MinutesFormat.kt,
    app/src/main/java/com/autominuting/domain/model/AutomationMode.kt,
    app/src/main/java/com/autominuting/di/DataStoreModule.kt,
    app/src/main/java/com/autominuting/data/preferences/UserPreferencesRepository.kt
  </files>
  <read_first>
    app/src/main/java/com/autominuting/domain/model/PipelineStatus.kt,
    app/src/main/java/com/autominuting/di/RepositoryModule.kt,
    app/src/main/java/com/autominuting/di/AppModule.kt,
    app/build.gradle.kts
  </read_first>
  <action>
1. MinutesFormat.kt 생성 (per D-01):
   - package com.autominuting.domain.model
   - enum class MinutesFormat { STRUCTURED, SUMMARY, ACTION_ITEMS }
   - PipelineStatus.kt와 동일한 스타일 (한국어 KDoc 주석, 각 값에 주석)
   - STRUCTURED: "구조화된 회의록 (기본값)", SUMMARY: "핵심 내용 요약", ACTION_ITEMS: "액션 아이템 중심"

2. AutomationMode.kt 생성 (per D-04, D-05):
   - package com.autominuting.domain.model
   - enum class AutomationMode { FULL_AUTO, HYBRID }
   - FULL_AUTO: "완전 자동 모드 — 파이프라인 전체 자동 진행 (기본값)"
   - HYBRID: "하이브리드 모드 — 전사 완료 후 사용자 확인을 거쳐 회의록 생성"

3. DataStoreModule.kt 생성:
   - package com.autominuting.di
   - 파일 최상위에 `private val Context.dataStore: DataStore<Preferences> by preferencesDataStore(name = "user_preferences")`
   - @Module @InstallIn(SingletonComponent::class) object DataStoreModule
   - @Provides @Singleton fun provideDataStore(@ApplicationContext context: Context): DataStore<Preferences> = context.dataStore
   - import: android.content.Context, androidx.datastore.core.DataStore, androidx.datastore.preferences.core.Preferences, androidx.datastore.preferences.preferencesDataStore, dagger.Module, dagger.Provides, dagger.hilt.InstallIn, dagger.hilt.android.qualifiers.ApplicationContext, dagger.hilt.components.SingletonComponent, javax.inject.Singleton

4. UserPreferencesRepository.kt 생성 (per D-02, D-06):
   - package com.autominuting.data.preferences
   - @Singleton class UserPreferencesRepository @Inject constructor(private val dataStore: DataStore<Preferences>)
   - companion object에 MINUTES_FORMAT_KEY = stringPreferencesKey("minutes_format"), AUTOMATION_MODE_KEY = stringPreferencesKey("automation_mode")
   - val minutesFormat: Flow<MinutesFormat> = dataStore.data.map { prefs -> val name = prefs[MINUTES_FORMAT_KEY] ?: MinutesFormat.STRUCTURED.name; MinutesFormat.valueOf(name) }
   - val automationMode: Flow<AutomationMode> = dataStore.data.map { prefs -> val name = prefs[AUTOMATION_MODE_KEY] ?: AutomationMode.FULL_AUTO.name; AutomationMode.valueOf(name) }
   - suspend fun setMinutesFormat(format: MinutesFormat) — dataStore.edit { it[MINUTES_FORMAT_KEY] = format.name }
   - suspend fun setAutomationMode(mode: AutomationMode) — dataStore.edit { it[AUTOMATION_MODE_KEY] = mode.name }
   - 동기 조회 헬퍼 추가 (Worker의 enqueue 시점 사용): suspend fun getMinutesFormatOnce(): MinutesFormat = dataStore.data.first().let { prefs -> MinutesFormat.valueOf(prefs[MINUTES_FORMAT_KEY] ?: MinutesFormat.STRUCTURED.name) }
   - suspend fun getAutomationModeOnce(): AutomationMode = dataStore.data.first().let { prefs -> AutomationMode.valueOf(prefs[AUTOMATION_MODE_KEY] ?: AutomationMode.FULL_AUTO.name) }
  </action>
  <verify>
    <automated>cd D:/workspace/temp/Auto_Minuting && ./gradlew compileDebugKotlin 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - MinutesFormat.kt 파일에 `enum class MinutesFormat` 포함, STRUCTURED/SUMMARY/ACTION_ITEMS 3개 값
    - AutomationMode.kt 파일에 `enum class AutomationMode` 포함, FULL_AUTO/HYBRID 2개 값
    - DataStoreModule.kt 파일에 `@Module` `@InstallIn(SingletonComponent::class)` `object DataStoreModule` 포함
    - DataStoreModule.kt에 `preferencesDataStore(name = "user_preferences")` 포함
    - UserPreferencesRepository.kt에 `class UserPreferencesRepository @Inject constructor` 포함
    - UserPreferencesRepository.kt에 `val minutesFormat: Flow<MinutesFormat>` 포함
    - UserPreferencesRepository.kt에 `val automationMode: Flow<AutomationMode>` 포함
    - UserPreferencesRepository.kt에 `suspend fun getMinutesFormatOnce()` 포함
    - compileDebugKotlin이 BUILD SUCCESSFUL로 종료
  </acceptance_criteria>
  <done>4개 파일이 생성되고 MinutesFormat/AutomationMode enum과 DataStore 기반 UserPreferencesRepository가 컴파일된다</done>
</task>

<task type="auto">
  <name>Task 2: GeminiEngine 형식별 프롬프트 확장 + MinutesRepository 시그니처 변경 + 알림 헬퍼</name>
  <files>
    app/src/main/java/com/autominuting/data/minutes/GeminiEngine.kt,
    app/src/main/java/com/autominuting/domain/repository/MinutesRepository.kt,
    app/src/main/java/com/autominuting/data/repository/MinutesRepositoryImpl.kt,
    app/src/main/java/com/autominuting/service/PipelineNotificationHelper.kt,
    app/src/main/java/com/autominuting/AutoMinutingApplication.kt
  </files>
  <read_first>
    app/src/main/java/com/autominuting/data/minutes/GeminiEngine.kt,
    app/src/main/java/com/autominuting/domain/repository/MinutesRepository.kt,
    app/src/main/java/com/autominuting/data/repository/MinutesRepositoryImpl.kt,
    app/src/main/java/com/autominuting/service/AudioCollectionService.kt,
    app/src/main/java/com/autominuting/AutoMinutingApplication.kt,
    app/src/main/java/com/autominuting/worker/MinutesGenerationWorker.kt
  </read_first>
  <action>
1. GeminiEngine.kt 수정 (per D-01):
   - import com.autominuting.domain.model.MinutesFormat 추가
   - generate() 시그니처를 `suspend fun generate(transcriptText: String, format: MinutesFormat = MinutesFormat.STRUCTURED): Result<String>` 으로 변경
   - companion object에 기존 MINUTES_PROMPT를 STRUCTURED_PROMPT로 이름 변경
   - SUMMARY_PROMPT 추가:
     ```
     |당신은 전문 회의록 작성자입니다. 아래 회의 전사 텍스트를 읽고, 핵심 내용을 3~5줄로 간결하게 요약해주세요.
     |
     |## 작성 지침
     |- 가장 중요한 결정 사항과 논의 결과를 중심으로 요약한다
     |- 불필요한 세부사항은 제외한다
     |- 한국어로 작성한다
     |
     |---
     |
     |## 회의 전사 텍스트
     |
     ```
   - ACTION_ITEMS_PROMPT 추가:
     ```
     |당신은 전문 회의록 작성자입니다. 아래 회의 전사 텍스트에서 결정 사항과 액션 아이템만 추출해주세요.
     |
     |## 출력 형식
     |
     |### 결정 사항
     |(번호를 매겨 명확하게 나열)
     |
     |### 액션 아이템
     || 담당자 | 할 일 | 기한 |
     ||--------|--------|------|
     |(테이블 형식으로 정리)
     |
     |## 작성 지침
     |- 결정 사항과 할 일만 포함한다. 논의 과정은 제외한다
     |- 담당자가 불분명하면 "미정"으로 표시한다
     |- 한국어로 작성한다
     |
     |---
     |
     |## 회의 전사 텍스트
     |
     ```
   - generate() 내부에서 `val prompt = when (format) { MinutesFormat.STRUCTURED -> STRUCTURED_PROMPT; MinutesFormat.SUMMARY -> SUMMARY_PROMPT; MinutesFormat.ACTION_ITEMS -> ACTION_ITEMS_PROMPT } + transcriptText` 로 변경

2. MinutesRepository.kt 수정:
   - import com.autominuting.domain.model.MinutesFormat 추가
   - generateMinutes 시그니처를 `suspend fun generateMinutes(transcriptText: String, format: MinutesFormat = MinutesFormat.STRUCTURED): Result<String>` 으로 변경

3. MinutesRepositoryImpl.kt 수정:
   - import com.autominuting.domain.model.MinutesFormat 추가
   - generateMinutes 오버라이드 시그니처에 format 파라미터 추가
   - geminiEngine.generate(transcriptText) 호출을 geminiEngine.generate(transcriptText, format) 으로 변경

4. PipelineNotificationHelper.kt 생성 (per D-07, D-08):
   - package com.autominuting.service
   - object PipelineNotificationHelper
   - const val PIPELINE_CHANNEL_ID = "pipeline_progress_channel"
   - const val PIPELINE_NOTIFICATION_ID = 2001
   - fun createChannel(context: Context): NotificationChannel 생성 — id=PIPELINE_CHANNEL_ID, name="파이프라인 진행", importance=IMPORTANCE_LOW, description="회의록 생성 파이프라인 진행 상태 알림"
   - fun updateProgress(context: Context, text: String, ongoing: Boolean = true): NotificationCompat.Builder로 알림 생성, setSmallIcon(R.drawable.ic_launcher_foreground), setContentTitle("Auto Minuting"), setContentText(text), setOngoing(ongoing), setSilent(true). NotificationManager.notify(PIPELINE_NOTIFICATION_ID, ...)
   - fun notifyComplete(context: Context, meetingId: Long, minutesText: String): 완료 알림. setAutoCancel(true), setOngoing(false). 공유 액션 추가: Intent(context, PipelineActionReceiver::class.java).apply { action = "com.autominuting.action.SHARE_MINUTES"; putExtra("meetingId", meetingId); putExtra("minutesText", minutesText) }. PendingIntent.getBroadcast(..., FLAG_UPDATE_CURRENT or FLAG_IMMUTABLE). addAction(R.drawable.ic_launcher_foreground, "공유", sharePendingIntent) (per D-11)
   - fun notifyTranscriptionComplete(context: Context, meetingId: Long, transcriptPath: String): 하이브리드 모드용 전사 완료 알림. "전사 완료" 제목, "전사 결과를 확인하고 회의록 생성을 시작하세요" 내용. "회의록 생성 시작" 액션 버튼: Intent action = "com.autominuting.action.GENERATE_MINUTES", putExtra meetingId/transcriptPath, PendingIntent.getBroadcast with FLAG_IMMUTABLE. setAutoCancel(true)
   - 주의: PipelineActionReceiver 클래스는 Plan 02에서 생성하므로 여기서는 문자열 클래스명으로 참조하지 말 것. 대신 Intent에 직접 action만 설정하고, receiver 클래스 참조는 Plan 02에서 완성. -> 실제로는 PipelineActionReceiver.kt를 이 Task에서 스텁으로 생성하여 컴파일 오류를 방지:
     - package com.autominuting.receiver
     - class PipelineActionReceiver : BroadcastReceiver() { override fun onReceive(context: Context, intent: Intent) { /* Plan 02에서 구현 */ } }

5. AutoMinutingApplication.kt 수정:
   - onCreate()에서 PipelineNotificationHelper.createChannel(this) 호출 추가 (SDK 초기화 후)
  </action>
  <verify>
    <automated>cd D:/workspace/temp/Auto_Minuting && ./gradlew compileDebugKotlin 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - GeminiEngine.kt에 `fun generate(transcriptText: String, format: MinutesFormat` 포함
    - GeminiEngine.kt에 `SUMMARY_PROMPT` 와 `ACTION_ITEMS_PROMPT` 문자열 포함
    - GeminiEngine.kt에 `when (format)` 분기 로직 포함
    - MinutesRepository.kt에 `fun generateMinutes(transcriptText: String, format: MinutesFormat` 포함
    - MinutesRepositoryImpl.kt에 `geminiEngine.generate(transcriptText, format)` 포함
    - PipelineNotificationHelper.kt에 `object PipelineNotificationHelper` 포함
    - PipelineNotificationHelper.kt에 `PIPELINE_CHANNEL_ID = "pipeline_progress_channel"` 포함
    - PipelineNotificationHelper.kt에 `fun updateProgress(` 포함
    - PipelineNotificationHelper.kt에 `fun notifyComplete(` 포함
    - PipelineNotificationHelper.kt에 `fun notifyTranscriptionComplete(` 포함
    - PipelineNotificationHelper.kt에 `FLAG_IMMUTABLE` 포함
    - AutoMinutingApplication.kt에 `PipelineNotificationHelper.createChannel(this)` 포함
    - PipelineActionReceiver.kt 스텁 파일 존재
    - compileDebugKotlin이 BUILD SUCCESSFUL로 종료
  </acceptance_criteria>
  <done>GeminiEngine이 3종 형식별 프롬프트를 지원하고, MinutesRepository 인터페이스/구현체가 format 파라미터를 전달하며, PipelineNotificationHelper가 진행/완료/전사완료 알림을 생성할 수 있다</done>
</task>

</tasks>

<verification>
- compileDebugKotlin 성공
- MinutesFormat enum에 3개 값, AutomationMode에 2개 값 존재
- GeminiEngine.generate()가 format 파라미터를 받아 when 분기
- UserPreferencesRepository가 DataStore로 설정 저장/읽기
- PipelineNotificationHelper가 알림 채널 생성 + 진행/완료 알림 업데이트
</verification>

<success_criteria>
Phase 6 기반 레이어가 컴파일되고, Plan 02/03에서 참조할 모든 타입과 인프라가 준비된다
</success_criteria>

<output>
After completion, create `.planning/phases/06-pipeline-integration/06-01-SUMMARY.md`
</output>
