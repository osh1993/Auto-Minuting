---
phase: 06-pipeline-integration
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - app/src/main/java/com/autominuting/worker/TranscriptionTriggerWorker.kt
  - app/src/main/java/com/autominuting/worker/MinutesGenerationWorker.kt
  - app/src/main/java/com/autominuting/receiver/PipelineActionReceiver.kt
  - app/src/main/java/com/autominuting/service/AudioCollectionService.kt
  - app/src/main/AndroidManifest.xml
autonomous: true
requirements:
  - UI-02
  - UI-04

must_haves:
  truths:
    - "완전 자동 모드에서 전사 완료 후 자동으로 회의록 생성 Worker가 시작된다"
    - "하이브리드 모드에서 전사 완료 시 알림만 표시되고, 사용자가 액션 버튼을 누르면 회의록 생성이 시작된다"
    - "각 Worker에서 파이프라인 단계 전환 시 알림이 업데이트된다 (전사 중/회의록 생성 중/완료)"
    - "회의록 생성 Worker가 형식 설정값을 inputData로 받아 GeminiEngine에 전달한다"
    - "PipelineActionReceiver가 알림 액션을 수신하여 MinutesGenerationWorker를 enqueue한다"
  artifacts:
    - path: "app/src/main/java/com/autominuting/worker/TranscriptionTriggerWorker.kt"
      provides: "하이브리드 분기 + 알림 업데이트 + 형식 전달"
      contains: "automationMode"
    - path: "app/src/main/java/com/autominuting/worker/MinutesGenerationWorker.kt"
      provides: "형식별 회의록 생성 + 알림 업데이트"
      contains: "KEY_MINUTES_FORMAT"
    - path: "app/src/main/java/com/autominuting/receiver/PipelineActionReceiver.kt"
      provides: "알림 액션 수신 BroadcastReceiver"
      contains: "class PipelineActionReceiver"
    - path: "app/src/main/AndroidManifest.xml"
      provides: "BroadcastReceiver 등록"
      contains: "PipelineActionReceiver"
  key_links:
    - from: "TranscriptionTriggerWorker.kt"
      to: "PipelineNotificationHelper.kt"
      via: "알림 업데이트"
      pattern: "PipelineNotificationHelper\\.updateProgress"
    - from: "TranscriptionTriggerWorker.kt"
      to: "MinutesGenerationWorker.kt"
      via: "automationMode 분기 후 enqueue"
      pattern: "AutomationMode\\.FULL_AUTO"
    - from: "PipelineActionReceiver.kt"
      to: "MinutesGenerationWorker.kt"
      via: "알림 액션 -> WorkManager enqueue"
      pattern: "OneTimeWorkRequestBuilder<MinutesGenerationWorker>"
    - from: "AudioCollectionService.kt"
      to: "UserPreferencesRepository.kt"
      via: "enqueue 시점 설정값 읽기"
      pattern: "getAutomationModeOnce\\(\\)|getMinutesFormatOnce\\(\\)"
---

<objective>
Worker 파이프라인에 하이브리드 모드 분기, 형식 전달, 단계별 알림 업데이트를 구현하고, 알림 액션을 처리하는 BroadcastReceiver를 완성한다.

Purpose: 파이프라인 자동 체이닝에 자동화 모드(완전 자동/하이브리드) 분기와 형식 선택을 반영하여 Core Value("수동 작업 없이")를 완성하고, 사용자에게 진행 상태를 알림으로 제공한다.
Output: 수정된 Worker 2개, PipelineActionReceiver 구현, 업데이트된 Manifest
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-pipeline-integration/06-CONTEXT.md
@.planning/phases/06-pipeline-integration/06-RESEARCH.md
@.planning/phases/06-pipeline-integration/06-01-SUMMARY.md

<interfaces>
<!-- Plan 01에서 생성되는 타입들 -->

From domain/model/MinutesFormat.kt:
```kotlin
enum class MinutesFormat { STRUCTURED, SUMMARY, ACTION_ITEMS }
```

From domain/model/AutomationMode.kt:
```kotlin
enum class AutomationMode { FULL_AUTO, HYBRID }
```

From data/preferences/UserPreferencesRepository.kt:
```kotlin
@Singleton
class UserPreferencesRepository @Inject constructor(dataStore: DataStore<Preferences>) {
    val minutesFormat: Flow<MinutesFormat>
    val automationMode: Flow<AutomationMode>
    suspend fun setMinutesFormat(format: MinutesFormat)
    suspend fun setAutomationMode(mode: AutomationMode)
    suspend fun getMinutesFormatOnce(): MinutesFormat
    suspend fun getAutomationModeOnce(): AutomationMode
}
```

From service/PipelineNotificationHelper.kt:
```kotlin
object PipelineNotificationHelper {
    const val PIPELINE_CHANNEL_ID = "pipeline_progress_channel"
    const val PIPELINE_NOTIFICATION_ID = 2001
    fun createChannel(context: Context)
    fun updateProgress(context: Context, text: String, ongoing: Boolean = true)
    fun notifyComplete(context: Context, meetingId: Long, minutesText: String)
    fun notifyTranscriptionComplete(context: Context, meetingId: Long, transcriptPath: String)
}
```

From data/minutes/GeminiEngine.kt (수정 후):
```kotlin
suspend fun generate(transcriptText: String, format: MinutesFormat = MinutesFormat.STRUCTURED): Result<String>
```

From domain/repository/MinutesRepository.kt (수정 후):
```kotlin
suspend fun generateMinutes(transcriptText: String, format: MinutesFormat = MinutesFormat.STRUCTURED): Result<String>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Worker 파이프라인 수정 (하이브리드 분기 + 형식 전달 + 알림)</name>
  <files>
    app/src/main/java/com/autominuting/worker/TranscriptionTriggerWorker.kt,
    app/src/main/java/com/autominuting/worker/MinutesGenerationWorker.kt,
    app/src/main/java/com/autominuting/service/AudioCollectionService.kt
  </files>
  <read_first>
    app/src/main/java/com/autominuting/worker/TranscriptionTriggerWorker.kt,
    app/src/main/java/com/autominuting/worker/MinutesGenerationWorker.kt,
    app/src/main/java/com/autominuting/service/AudioCollectionService.kt,
    app/src/main/java/com/autominuting/service/PipelineNotificationHelper.kt,
    app/src/main/java/com/autominuting/domain/model/MinutesFormat.kt,
    app/src/main/java/com/autominuting/domain/model/AutomationMode.kt,
    app/src/main/java/com/autominuting/data/preferences/UserPreferencesRepository.kt
  </read_first>
  <action>
1. TranscriptionTriggerWorker.kt 수정:
   - 생성자에 UserPreferencesRepository 주입 제거 — Worker에서 DataStore 직접 사용하지 않음 (anti-pattern per RESEARCH.md)
   - 대신, inputData에서 설정값 읽기: KEY_AUTOMATION_MODE = "automationMode", KEY_MINUTES_FORMAT = "minutesFormat" companion에 추가
   - doWork() 시작 시: `val automationMode = inputData.getString(KEY_AUTOMATION_MODE) ?: AutomationMode.FULL_AUTO.name`
   - doWork() 시작 시: `val minutesFormat = inputData.getString(KEY_MINUTES_FORMAT) ?: MinutesFormat.STRUCTURED.name`
   - 전사 시작 전 알림: `PipelineNotificationHelper.updateProgress(applicationContext, "전사 중...")` (per D-08)
   - 전사 성공 후 분기 (per D-04, D-05):
     ```kotlin
     if (automationMode == AutomationMode.FULL_AUTO.name) {
         // 완전 자동: 기존처럼 MinutesGenerationWorker enqueue
         val minutesWorkRequest = OneTimeWorkRequestBuilder<MinutesGenerationWorker>()
             .setInputData(workDataOf(
                 MinutesGenerationWorker.KEY_MEETING_ID to meetingId,
                 MinutesGenerationWorker.KEY_TRANSCRIPT_PATH to transcriptPath,
                 MinutesGenerationWorker.KEY_MINUTES_FORMAT to minutesFormat
             ))
             .build()
         WorkManager.getInstance(applicationContext).enqueue(minutesWorkRequest)
         Log.d(TAG, "완전 자동 모드: 회의록 생성 Worker 체이닝")
     } else {
         // 하이브리드: 알림만 표시, 사용자 확인 대기
         PipelineNotificationHelper.notifyTranscriptionComplete(
             applicationContext, meetingId, transcriptPath
         )
         Log.d(TAG, "하이브리드 모드: 전사 완료 알림 표시, 사용자 확인 대기")
     }
     ```

2. MinutesGenerationWorker.kt 수정:
   - companion에 `const val KEY_MINUTES_FORMAT = "minutesFormat"` 추가
   - doWork() 시작 시: `val minutesFormatName = inputData.getString(KEY_MINUTES_FORMAT) ?: MinutesFormat.STRUCTURED.name`
   - `val minutesFormat = try { MinutesFormat.valueOf(minutesFormatName) } catch (e: Exception) { MinutesFormat.STRUCTURED }`
   - GENERATING_MINUTES 상태 전환 후 알림: `PipelineNotificationHelper.updateProgress(applicationContext, "회의록 생성 중...")` (per D-08)
   - generateMinutes 호출을 `minutesRepository.generateMinutes(transcriptText, minutesFormat)` 으로 변경
   - 성공 후: `PipelineNotificationHelper.notifyComplete(applicationContext, meetingId, minutesText)` (per D-08, D-11)
   - 실패 후: `PipelineNotificationHelper.updateProgress(applicationContext, "회의록 생성 실패", ongoing = false)`

3. AudioCollectionService.kt 수정:
   - 생성자에 `@Inject lateinit var userPreferencesRepository: UserPreferencesRepository` 추가 (import 추가)
   - triggerTranscriptionPipeline 메서드를 suspend fun으로 변경
   - collectAudio() 내에서 triggerTranscriptionPipeline 호출 전: 설정값 읽기
     ```kotlin
     val automationMode = userPreferencesRepository.getAutomationModeOnce()
     val minutesFormat = userPreferencesRepository.getMinutesFormatOnce()
     ```
   - inputData에 automationMode와 minutesFormat 추가:
     ```kotlin
     val inputData = workDataOf(
         "audioFilePath" to filePath,
         "automationMode" to automationMode.name,
         "minutesFormat" to minutesFormat.name
     )
     ```
  </action>
  <verify>
    <automated>cd D:/workspace/temp/Auto_Minuting && ./gradlew compileDebugKotlin 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - TranscriptionTriggerWorker.kt에 `KEY_AUTOMATION_MODE = "automationMode"` 포함
    - TranscriptionTriggerWorker.kt에 `KEY_MINUTES_FORMAT = "minutesFormat"` 포함
    - TranscriptionTriggerWorker.kt에 `AutomationMode.FULL_AUTO.name` 분기 로직 포함
    - TranscriptionTriggerWorker.kt에 `PipelineNotificationHelper.updateProgress(applicationContext, "전사 중..."` 포함
    - TranscriptionTriggerWorker.kt에 `PipelineNotificationHelper.notifyTranscriptionComplete` 포함
    - MinutesGenerationWorker.kt에 `KEY_MINUTES_FORMAT = "minutesFormat"` 포함
    - MinutesGenerationWorker.kt에 `minutesRepository.generateMinutes(transcriptText, minutesFormat)` 포함
    - MinutesGenerationWorker.kt에 `PipelineNotificationHelper.updateProgress(applicationContext, "회의록 생성 중..."` 포함
    - MinutesGenerationWorker.kt에 `PipelineNotificationHelper.notifyComplete` 포함
    - AudioCollectionService.kt에 `userPreferencesRepository` 필드 포함
    - AudioCollectionService.kt에 `getAutomationModeOnce()` 호출 포함
    - compileDebugKotlin이 BUILD SUCCESSFUL로 종료
  </acceptance_criteria>
  <done>Worker 파이프라인이 자동화 모드에 따라 분기하고, 각 단계에서 알림을 업데이트하며, 형식 설정이 MinutesGenerationWorker까지 전달된다</done>
</task>

<task type="auto">
  <name>Task 2: PipelineActionReceiver 구현 + AndroidManifest 등록</name>
  <files>
    app/src/main/java/com/autominuting/receiver/PipelineActionReceiver.kt,
    app/src/main/AndroidManifest.xml
  </files>
  <read_first>
    app/src/main/java/com/autominuting/receiver/PipelineActionReceiver.kt,
    app/src/main/java/com/autominuting/worker/MinutesGenerationWorker.kt,
    app/src/main/java/com/autominuting/service/PipelineNotificationHelper.kt,
    app/src/main/AndroidManifest.xml
  </read_first>
  <action>
1. PipelineActionReceiver.kt 완성 (Plan 01에서 스텁 생성됨):
   - package com.autominuting.receiver
   - import: android.content.BroadcastReceiver, android.content.Context, android.content.Intent, androidx.work.OneTimeWorkRequestBuilder, androidx.work.WorkManager, androidx.work.workDataOf, com.autominuting.worker.MinutesGenerationWorker, com.autominuting.domain.model.MinutesFormat, android.util.Log
   - class PipelineActionReceiver : BroadcastReceiver()
   - override fun onReceive(context: Context, intent: Intent):
     ```kotlin
     when (intent.action) {
         ACTION_GENERATE_MINUTES -> {
             val meetingId = intent.getLongExtra("meetingId", -1L)
             val transcriptPath = intent.getStringExtra("transcriptPath") ?: return
             val minutesFormat = intent.getStringExtra("minutesFormat")
                 ?: MinutesFormat.STRUCTURED.name

             Log.d(TAG, "회의록 생성 요청: meetingId=$meetingId")

             val workRequest = OneTimeWorkRequestBuilder<MinutesGenerationWorker>()
                 .setInputData(workDataOf(
                     MinutesGenerationWorker.KEY_MEETING_ID to meetingId,
                     MinutesGenerationWorker.KEY_TRANSCRIPT_PATH to transcriptPath,
                     MinutesGenerationWorker.KEY_MINUTES_FORMAT to minutesFormat
                 ))
                 .build()
             WorkManager.getInstance(context).enqueue(workRequest)
         }
         ACTION_SHARE_MINUTES -> {
             val minutesText = intent.getStringExtra("minutesText") ?: return
             val shareIntent = Intent().apply {
                 action = Intent.ACTION_SEND
                 putExtra(Intent.EXTRA_TEXT, minutesText)
                 type = "text/plain"
                 addFlags(Intent.FLAG_ACTIVITY_NEW_TASK)
             }
             val chooser = Intent.createChooser(shareIntent, null).apply {
                 addFlags(Intent.FLAG_ACTIVITY_NEW_TASK)
             }
             context.startActivity(chooser)
         }
     }
     ```
   - companion object:
     ```kotlin
     const val ACTION_GENERATE_MINUTES = "com.autominuting.action.GENERATE_MINUTES"
     const val ACTION_SHARE_MINUTES = "com.autominuting.action.SHARE_MINUTES"
     private const val TAG = "PipelineAction"
     ```

2. PipelineNotificationHelper.kt 업데이트:
   - notifyTranscriptionComplete()에서 Intent 생성 시 PipelineActionReceiver::class.java 사용 (스텁이 아닌 실제 참조)
   - notifyTranscriptionComplete의 generateIntent에 minutesFormat extra도 추가: UserPreferencesRepository에서 읽은 값을 전달받도록 파라미터에 `minutesFormat: String = MinutesFormat.STRUCTURED.name` 추가
   - notifyComplete()에서도 PipelineActionReceiver::class.java 사용

3. AndroidManifest.xml 수정:
   - <application> 내에 <receiver> 추가:
     ```xml
     <!-- 파이프라인 알림 액션 처리 (회의록 생성/공유) -->
     <receiver
         android:name=".receiver.PipelineActionReceiver"
         android:exported="false" />
     ```
  </action>
  <verify>
    <automated>cd D:/workspace/temp/Auto_Minuting && ./gradlew compileDebugKotlin 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - PipelineActionReceiver.kt에 `ACTION_GENERATE_MINUTES = "com.autominuting.action.GENERATE_MINUTES"` 포함
    - PipelineActionReceiver.kt에 `ACTION_SHARE_MINUTES = "com.autominuting.action.SHARE_MINUTES"` 포함
    - PipelineActionReceiver.kt에 `OneTimeWorkRequestBuilder<MinutesGenerationWorker>()` 포함
    - PipelineActionReceiver.kt에 `Intent.ACTION_SEND` 포함
    - PipelineActionReceiver.kt에 `FLAG_ACTIVITY_NEW_TASK` 포함
    - AndroidManifest.xml에 `android:name=".receiver.PipelineActionReceiver"` 포함
    - AndroidManifest.xml에 `android:exported="false"` (PipelineActionReceiver 관련) 포함
    - compileDebugKotlin이 BUILD SUCCESSFUL로 종료
  </acceptance_criteria>
  <done>PipelineActionReceiver가 GENERATE_MINUTES/SHARE_MINUTES 알림 액션을 수신 처리하고, AndroidManifest에 등록되어 컴파일 성공한다</done>
</task>

</tasks>

<verification>
- compileDebugKotlin 성공
- TranscriptionTriggerWorker가 automationMode에 따라 FULL_AUTO/HYBRID 분기
- MinutesGenerationWorker가 minutesFormat을 받아 generateMinutes에 전달
- 각 Worker에서 PipelineNotificationHelper 알림 업데이트 호출
- PipelineActionReceiver가 두 가지 액션을 처리
- AndroidManifest에 receiver 등록
</verification>

<success_criteria>
파이프라인이 설정에 따라 완전 자동/하이브리드 모드로 동작하고, 각 단계에서 알림이 업데이트되며, 알림 액션으로 회의록 생성/공유가 가능하다
</success_criteria>

<output>
After completion, create `.planning/phases/06-pipeline-integration/06-02-SUMMARY.md`
</output>
