---
phase: 51-gemini-api-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/com/autominuting/data/security/GeminiApiKeyEntry.kt
  - app/src/main/java/com/autominuting/data/security/SecureApiKeyRepository.kt
  - app/src/main/java/com/autominuting/data/preferences/UserPreferencesRepository.kt
  - app/src/main/java/com/autominuting/presentation/settings/SettingsViewModel.kt
  - app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt
autonomous: false
requirements:
  - GEMINI-01
  - GEMINI-04

must_haves:
  truths:
    - "설정 화면 Gemini API 섹션에 등록된 키 목록이 카드 행으로 표시된다"
    - "사용자가 별명 + 키 값을 입력하고 '검증 후 추가'를 탭하면 검증 성공 시 목록에 추가된다"
    - "사용자가 🗑 아이콘을 탭하면 확인 다이얼로그가 뜨고 삭제 후 목록이 갱신된다"
    - "등록된 키가 없을 때 빈 상태 컴포넌트가 표시된다"
    - "기존 단일 gemini_api_key 값이 있으면 앱 시작 시 index 0으로 마이그레이션된다"
    - "모든 키 값은 EncryptedSharedPreferences에 암호화 저장된다"
  artifacts:
    - path: "app/src/main/java/com/autominuting/data/security/GeminiApiKeyEntry.kt"
      provides: "GeminiApiKeyEntry data class (label, maskedKey, index)"
    - path: "app/src/main/java/com/autominuting/data/security/SecureApiKeyRepository.kt"
      provides: "getGeminiApiKeys, addGeminiApiKey, removeGeminiApiKey, getAllGeminiApiKeyValues, migrateIfNeeded"
    - path: "app/src/main/java/com/autominuting/presentation/settings/SettingsViewModel.kt"
      provides: "_geminiApiKeys StateFlow, validateAndAddApiKey, removeGeminiApiKey"
    - path: "app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt"
      provides: "GeminiApiKeySection composable (목록 + 추가 폼 + 삭제 다이얼로그)"
  key_links:
    - from: "SettingsScreen.kt GeminiApiKeySection"
      to: "SettingsViewModel.geminiApiKeys"
      via: "collectAsStateWithLifecycle()"
      pattern: "geminiApiKeys.*collectAsStateWithLifecycle"
    - from: "SettingsViewModel.validateAndAddApiKey"
      to: "SecureApiKeyRepository.addGeminiApiKey"
      via: "검증 성공 후 호출"
      pattern: "addGeminiApiKey\\(label"
    - from: "SecureApiKeyRepository.addGeminiApiKey"
      to: "encryptedPrefs.edit().putString"
      via: "gemini_api_key_{n}_value 키로 저장"
      pattern: "gemini_api_key_.*_value"
---

# Phase 51 — Plan 01: Gemini 다중 API 키 설정 UI

## Goal

설정 화면의 Gemini API 키 섹션을 단일 키 입력에서 다중 키 목록 관리(추가/삭제)로 교체하고, Phase 52 라운드로빈을 위한 저장 구조를 확정한다.

## Wave Structure

Wave 1 (순차 실행 — 레이어 간 의존):
1. Task 1: GeminiApiKeyEntry 타입 정의 + SecureApiKeyRepository 다중 키 CRUD 추가
2. Task 2: SettingsViewModel 교체 (_hasApiKey → _geminiApiKeys, validateAndSaveApiKey → validateAndAddApiKey)
3. Task 3: SettingsScreen ApiKeySection → GeminiApiKeySection 교체 (목록 UI + 추가 폼 + 삭제 다이얼로그)
4. Task 4: 빌드 검증 + 수동 기능 확인 체크포인트

---

<objective>
Gemini API 키 설정 UI를 단일 입력에서 다중 키 목록 관리로 전환한다.

Purpose: Phase 52 라운드로빈 로직이 읽을 저장 구조(gemini_api_key_count + gemini_api_key_{n}_label/value)를 이 Phase에서 확정한다. 기존 단일 키 사용자의 데이터는 마이그레이션으로 보존한다.
Output: GeminiApiKeyEntry.kt(신규), SecureApiKeyRepository.kt(확장), SettingsViewModel.kt(교체), SettingsScreen.kt(교체)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/51-gemini-api-ui/51-CONTEXT.md
@.planning/phases/51-gemini-api-ui/51-UI-SPEC.md

<interfaces>
<!-- 기존 코드에서 추출한 핵심 인터페이스. Executor는 이것을 기준으로 구현한다. -->

From SecureApiKeyRepository.kt (현재 단일 키 메서드 — 교체/추가 대상):
```kotlin
// companion object 내 기존 상수 (보존)
private const val KEY_GEMINI_API = "gemini_api_key"  // 마이그레이션 후 삭제될 레거시 키

// 기존 메서드 (하위 호환 유지 또는 deprecated 처리)
fun getGeminiApiKey(): String?
fun saveGeminiApiKey(apiKey: String)
fun clearGeminiApiKey()
```

From SettingsViewModel.kt (교체 대상 상태/메서드):
```kotlin
// 교체 전
private val _hasApiKey = MutableStateFlow(false)
val hasApiKey: StateFlow<Boolean>
fun validateAndSaveApiKey(apiKey: String)
fun clearApiKey()

// init 블록에서 교체:
_hasApiKey.value = secureApiKeyRepository.getGeminiApiKey() != null
```

From SettingsScreen.kt (교체 대상 composable):
```kotlin
// 교체 전 — ApiKeySection composable (line 1397~1500)
private fun ApiKeySection(viewModel: SettingsViewModel)
// 호출부 (line 546): ApiKeySection(viewModel = viewModel)
```

From UserPreferencesRepository.kt companion object (상수 추가 위치):
```kotlin
companion object {
    val AUTOMATION_MODE_KEY = stringPreferencesKey("automation_mode")
    // ... 기존 키들 ...
    val DRIVE_AUTO_UPLOAD_ENABLED_KEY = booleanPreferencesKey("drive_auto_upload_enabled")
    // Phase 51: 아래 상수 추가 (실제 사용은 Phase 52)
    // val GEMINI_ROUNDROBIN_INDEX_KEY = intPreferencesKey("gemini_roundrobin_index")
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GeminiApiKeyEntry 타입 정의 + SecureApiKeyRepository 다중 키 CRUD 추가</name>
  <files>
    app/src/main/java/com/autominuting/data/security/GeminiApiKeyEntry.kt
    app/src/main/java/com/autominuting/data/security/SecureApiKeyRepository.kt
    app/src/main/java/com/autominuting/data/preferences/UserPreferencesRepository.kt
  </files>
  <action>
**1. GeminiApiKeyEntry.kt 신규 생성**

패키지: `com.autominuting.data.security`

```kotlin
package com.autominuting.data.security

/**
 * Gemini API 키 목록의 단일 항목. UI 표시용 (복호화 키 값 미포함).
 *
 * @param label 사용자가 입력한 별명 (예: 회사용, 개인용)
 * @param maskedKey 마스킹된 키 표시 문자열. 형식: AIza****WXYZ (앞 4자 + **** + 뒤 4자)
 * @param index EncryptedSharedPreferences 내 저장 인덱스 (0-based)
 */
data class GeminiApiKeyEntry(
    val label: String,
    val maskedKey: String,
    val index: Int
)
```

**2. SecureApiKeyRepository.kt 수정**

companion object에 다중 키 관련 상수 추가 (기존 상수 아래):
```kotlin
// 다중 Gemini API 키 (GEMINI-01, GEMINI-04)
private const val KEY_GEMINI_KEY_COUNT = "gemini_api_key_count"
private fun keyGeminiLabel(i: Int) = "gemini_api_key_${i}_label"
private fun keyGeminiValue(i: Int) = "gemini_api_key_${i}_value"
```

마이그레이션 메서드 추가 (init 블록이 없으므로 public 메서드로 추가, ViewModel의 init에서 호출):
```kotlin
/**
 * 기존 단일 gemini_api_key 값을 다중 키 구조의 index 0으로 마이그레이션한다.
 * 이미 마이그레이션되었거나 레거시 키가 없으면 아무것도 하지 않는다.
 * SettingsViewModel.init에서 호출한다.
 */
fun migrateGeminiApiKeyIfNeeded() {
    val prefs = encryptedPrefs ?: return
    val legacyKey = prefs.getString(KEY_GEMINI_API, null) ?: return
    // 이미 다중 키 구조가 존재하면 마이그레이션 건너뜀
    val count = prefs.getString(KEY_GEMINI_KEY_COUNT, "0")?.toIntOrNull() ?: 0
    if (count > 0) {
        prefs.edit().remove(KEY_GEMINI_API).apply()
        return
    }
    // index 0으로 이동
    prefs.edit()
        .putString(keyGeminiLabel(0), "기본 키")
        .putString(keyGeminiValue(0), legacyKey)
        .putString(KEY_GEMINI_KEY_COUNT, "1")
        .remove(KEY_GEMINI_API)
        .apply()
    Log.i(TAG, "레거시 gemini_api_key → 다중 키 index 0으로 마이그레이션 완료")
}
```

다중 키 CRUD 메서드 추가:
```kotlin
/**
 * 등록된 Gemini API 키 목록을 반환한다. 키 값은 마스킹 처리된다.
 * 마스킹 형식: AIza****WXYZ (앞 4자 + **** + 뒤 4자; 총 길이 < 8이면 전체 마스킹)
 */
fun getGeminiApiKeys(): List<GeminiApiKeyEntry> {
    val prefs = encryptedPrefs ?: return emptyList()
    val count = prefs.getString(KEY_GEMINI_KEY_COUNT, "0")?.toIntOrNull() ?: 0
    return (0 until count).mapNotNull { i ->
        val label = prefs.getString(keyGeminiLabel(i), null) ?: return@mapNotNull null
        val value = prefs.getString(keyGeminiValue(i), null) ?: return@mapNotNull null
        val masked = maskApiKey(value)
        GeminiApiKeyEntry(label = label, maskedKey = masked, index = i)
    }
}

/**
 * 새 Gemini API 키를 목록 끝에 추가한다.
 * 중복 키 값 검사: 이미 동일한 키 값이 존재하면 IllegalArgumentException 발생.
 */
fun addGeminiApiKey(label: String, key: String) {
    val prefs = encryptedPrefs ?: run {
        Log.w(TAG, "addGeminiApiKey 실패: EncryptedSharedPreferences 사용 불가")
        return
    }
    val count = prefs.getString(KEY_GEMINI_KEY_COUNT, "0")?.toIntOrNull() ?: 0
    // 중복 검사
    for (i in 0 until count) {
        if (prefs.getString(keyGeminiValue(i), null) == key) {
            throw IllegalArgumentException("이미 등록된 API 키입니다.")
        }
    }
    prefs.edit()
        .putString(keyGeminiLabel(count), label)
        .putString(keyGeminiValue(count), key)
        .putString(KEY_GEMINI_KEY_COUNT, (count + 1).toString())
        .apply()
    Log.i(TAG, "Gemini API 키 추가: index=$count, label=$label")
}

/**
 * 지정 인덱스의 Gemini API 키를 삭제하고 인덱스를 재정렬한다.
 * @param index 삭제할 키의 인덱스 (0-based)
 */
fun removeGeminiApiKey(index: Int) {
    val prefs = encryptedPrefs ?: run {
        Log.w(TAG, "removeGeminiApiKey 실패: EncryptedSharedPreferences 사용 불가")
        return
    }
    val count = prefs.getString(KEY_GEMINI_KEY_COUNT, "0")?.toIntOrNull() ?: 0
    if (index < 0 || index >= count) return
    // 삭제 후 이후 항목을 앞으로 당김 (인덱스 재정렬)
    val editor = prefs.edit()
    for (i in index until count - 1) {
        val nextLabel = prefs.getString(keyGeminiLabel(i + 1), "")!!
        val nextValue = prefs.getString(keyGeminiValue(i + 1), "")!!
        editor.putString(keyGeminiLabel(i), nextLabel)
        editor.putString(keyGeminiValue(i), nextValue)
    }
    // 마지막 항목 삭제
    editor.remove(keyGeminiLabel(count - 1))
    editor.remove(keyGeminiValue(count - 1))
    editor.putString(KEY_GEMINI_KEY_COUNT, (count - 1).toString())
    editor.apply()
    Log.i(TAG, "Gemini API 키 삭제: index=$index, 재정렬 후 count=${count - 1}")
}

/**
 * 등록된 모든 Gemini API 키의 복호화된 실제 값 목록을 반환한다.
 * Phase 52 라운드로빈 로직에서 사용한다.
 */
fun getAllGeminiApiKeyValues(): List<String> {
    val prefs = encryptedPrefs ?: return emptyList()
    val count = prefs.getString(KEY_GEMINI_KEY_COUNT, "0")?.toIntOrNull() ?: 0
    return (0 until count).mapNotNull { i ->
        prefs.getString(keyGeminiValue(i), null)
    }
}

/** API 키를 마스킹한다. 형식: AIza****WXYZ (앞 4자 + **** + 뒤 4자). */
private fun maskApiKey(key: String): String {
    return if (key.length >= 8) {
        "${key.take(4)}****${key.takeLast(4)}"
    } else {
        "****"
    }
}
```

**3. UserPreferencesRepository.kt 수정**

companion object 마지막에 Phase 52용 상수 추가 (주석 포함):
```kotlin
/** GEMINI-02: 라운드로빈 현재 인덱스 설정 키. 실제 사용은 Phase 52. */
val GEMINI_ROUNDROBIN_INDEX_KEY = androidx.datastore.preferences.core.intPreferencesKey("gemini_roundrobin_index")
```

import 추가 필요: `androidx.datastore.preferences.core.intPreferencesKey`
  </action>
  <verify>
    <automated>cd /d/workspace/temp/Auto_Minuting && ./gradlew :app:compileDebugKotlin 2>&1 | tail -20</automated>
  </verify>
  <done>
    - GeminiApiKeyEntry.kt 파일이 존재하고 data class 3개 필드(label, maskedKey, index) 포함
    - SecureApiKeyRepository에 migrateGeminiApiKeyIfNeeded, getGeminiApiKeys, addGeminiApiKey(중복 검사 포함), removeGeminiApiKey(인덱스 재정렬 포함), getAllGeminiApiKeyValues, maskApiKey 메서드 존재
    - UserPreferencesRepository에 GEMINI_ROUNDROBIN_INDEX_KEY 상수 존재
    - 컴파일 오류 없음
  </done>
</task>

<task type="auto">
  <name>Task 2: SettingsViewModel 교체 — _hasApiKey → _geminiApiKeys, 다중 키 메서드 추가</name>
  <files>
    app/src/main/java/com/autominuting/presentation/settings/SettingsViewModel.kt
  </files>
  <action>
**SettingsViewModel.kt 수정 사항 (기존 코드 변경/추가):**

1. import 추가:
```kotlin
import com.autominuting.data.security.GeminiApiKeyEntry
```

2. `_hasApiKey` 및 `hasApiKey` StateFlow 교체:
```kotlin
// 제거:
private val _hasApiKey = MutableStateFlow(false)
val hasApiKey: StateFlow<Boolean> = _hasApiKey.asStateFlow()

// 추가:
/** 등록된 Gemini API 키 목록 */
private val _geminiApiKeys = MutableStateFlow<List<GeminiApiKeyEntry>>(emptyList())
val geminiApiKeys: StateFlow<List<GeminiApiKeyEntry>> = _geminiApiKeys.asStateFlow()
```

3. `init` 블록 수정 — 마이그레이션 호출 + geminiApiKeys 초기화:
```kotlin
init {
    // 레거시 단일 키 → 다중 키 구조 마이그레이션 (GEMINI-04)
    secureApiKeyRepository.migrateGeminiApiKeyIfNeeded()
    // 초기 로드
    _geminiApiKeys.value = secureApiKeyRepository.getGeminiApiKeys()
    // 나머지 기존 초기화 코드 유지 (hasOAuthClientId, hasGroqApiKey 등)
    _hasOAuthClientId.value = !secureApiKeyRepository.getGoogleOAuthClientId().isNullOrBlank()
    _hasGroqApiKey.value = !secureApiKeyRepository.getGroqApiKey().isNullOrBlank()
    _hasDeepgramApiKey.value = !secureApiKeyRepository.getDeepgramApiKey().isNullOrBlank()
    _hasClovaInvokeUrl.value = !secureApiKeyRepository.getClovaInvokeUrl().isNullOrBlank()
    _hasClovaSecretKey.value = !secureApiKeyRepository.getClovaSecretKey().isNullOrBlank()
    _hasClovaSummaryClientId.value = !secureApiKeyRepository.getClovaSummaryClientId().isNullOrBlank()
    _hasClovaSummaryClientSecret.value = !secureApiKeyRepository.getClovaSummaryClientSecret().isNullOrBlank()
    viewModelScope.launch {
        googleAuthRepository.restoreAuthState()
    }
}
```
주의: 기존 `_hasApiKey.value = secureApiKeyRepository.getGeminiApiKey() != null` 라인 제거.

4. `validateAndSaveApiKey` 메서드를 `validateAndAddApiKey`로 교체:
```kotlin
/**
 * 새 Gemini API 키를 검증한 후 다중 키 목록에 추가한다 (GEMINI-01, GEMINI-04).
 * 검증: generateContent("Hello") 호출 성공 여부로 판단.
 * 중복 키는 addGeminiApiKey에서 IllegalArgumentException으로 거부된다.
 *
 * @param label 사용자 입력 별명
 * @param apiKey 추가할 API 키 값
 */
fun validateAndAddApiKey(label: String, apiKey: String) {
    viewModelScope.launch {
        _apiKeyValidationState.value = ApiKeyValidationState.Validating
        try {
            val model = GenerativeModel(
                modelName = "gemini-2.5-flash",
                apiKey = apiKey
            )
            val response = withTimeout(10_000) {
                model.generateContent("Hello")
            }
            if (response.text != null) {
                secureApiKeyRepository.addGeminiApiKey(label, apiKey)
                _geminiApiKeys.value = secureApiKeyRepository.getGeminiApiKeys()
                _apiKeyValidationState.value = ApiKeyValidationState.Success
            } else {
                _apiKeyValidationState.value = ApiKeyValidationState.Error(
                    "빈 응답이 반환되었습니다. 키 값을 다시 확인해주세요."
                )
            }
        } catch (e: IllegalArgumentException) {
            // 중복 키
            _apiKeyValidationState.value = ApiKeyValidationState.Error(
                e.message ?: "이미 등록된 API 키입니다."
            )
        } catch (e: Exception) {
            val msg = e.message ?: ""
            _apiKeyValidationState.value = ApiKeyValidationState.Error(
                when {
                    msg.contains("Unable to resolve host", ignoreCase = true) ||
                    msg.contains("timeout", ignoreCase = true) ->
                        "네트워크 오류로 검증하지 못했습니다. 연결 상태를 확인 후 다시 시도하세요."
                    else -> "API 키 검증에 실패했습니다. 키 값을 다시 확인해주세요."
                }
            )
        }
    }
}
```

5. `clearApiKey()` 메서드 제거, `removeGeminiApiKey(index: Int)` 추가:
```kotlin
/**
 * 지정 인덱스의 Gemini API 키를 삭제하고 목록을 갱신한다.
 * @param index 삭제할 키의 인덱스 (0-based, GeminiApiKeyEntry.index 값)
 */
fun removeGeminiApiKey(index: Int) {
    secureApiKeyRepository.removeGeminiApiKey(index)
    _geminiApiKeys.value = secureApiKeyRepository.getGeminiApiKeys()
}
```

6. `ApiKeyValidationState.Success` data class에 label 필드 추가 (성공 메시지에 별명 표시용):
```kotlin
// 기존:
data object Success : ApiKeyValidationState

// 변경:
data class Success(val addedLabel: String = "") : ApiKeyValidationState
```
validateAndAddApiKey에서 `ApiKeyValidationState.Success(addedLabel = label)` 로 교체.

주의: 기존 `validateAndSaveApiKey` 메서드와 `clearApiKey()` 메서드는 완전히 제거한다. SettingsScreen에서 참조하는 코드도 Task 3에서 함께 교체된다.
  </action>
  <verify>
    <automated>cd /d/workspace/temp/Auto_Minuting && ./gradlew :app:compileDebugKotlin 2>&1 | tail -30</automated>
  </verify>
  <done>
    - SettingsViewModel에 geminiApiKeys: StateFlow&lt;List&lt;GeminiApiKeyEntry&gt;&gt; 존재
    - validateAndAddApiKey(label, apiKey) 메서드 존재
    - removeGeminiApiKey(index) 메서드 존재
    - clearApiKey() 및 validateAndSaveApiKey() 메서드 제거됨
    - init 블록에서 migrateGeminiApiKeyIfNeeded() 호출 확인
    - ApiKeyValidationState.Success가 addedLabel 필드를 가진 data class로 변경됨
    - 컴파일 오류 없음
  </done>
</task>

<task type="auto">
  <name>Task 3: SettingsScreen ApiKeySection → GeminiApiKeySection 교체</name>
  <files>
    app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt
  </files>
  <action>
**SettingsScreen.kt 수정 사항:**

**1. import 추가** (필요한 것만, 중복 방지):
```kotlin
import androidx.compose.foundation.lazy.LazyColumn
import androidx.compose.foundation.lazy.items
import androidx.compose.material.icons.filled.Add
import androidx.compose.material.icons.filled.Delete
import androidx.compose.material3.AlertDialog
import androidx.compose.material3.Card
import androidx.compose.material3.CardDefaults
import androidx.compose.material3.OutlinedCard
import androidx.compose.ui.text.font.FontFamily
import com.autominuting.data.security.GeminiApiKeyEntry
```

**2. SettingsScreen 함수 내 State 수집 교체** (line ~1399 근처):
```kotlin
// 제거:
val hasApiKey by viewModel.hasApiKey.collectAsStateWithLifecycle()

// 추가:
val geminiApiKeys by viewModel.geminiApiKeys.collectAsStateWithLifecycle()
```

**3. 호출부 교체** (line ~546, `ApiKeySection(viewModel = viewModel)` → `GeminiApiKeySection(viewModel = viewModel)`):
기존 `ApiKeySection(viewModel = viewModel)` 를 `GeminiApiKeySection(viewModel = viewModel)` 으로 교체.

**4. 기존 `ApiKeySection` composable(line 1397~1500) 전체를 아래 새 composable로 교체:**

```kotlin
/**
 * Gemini API 키 다중 관리 섹션 (GEMINI-01, GEMINI-04).
 * 목록 뷰 + 인라인 추가 폼 + 삭제 확인 다이얼로그를 포함한다.
 */
@Composable
private fun GeminiApiKeySection(viewModel: SettingsViewModel) {
    val apiKeyValidationState by viewModel.apiKeyValidationState.collectAsStateWithLifecycle()
    val geminiApiKeys by viewModel.geminiApiKeys.collectAsStateWithLifecycle()

    // 추가 폼 표시 여부 (로컬 UI 상태)
    var showAddForm by remember { mutableStateOf(false) }
    // 추가 폼 입력 값
    var labelInput by remember { mutableStateOf("") }
    var keyInput by remember { mutableStateOf("") }
    var isKeyVisible by remember { mutableStateOf(false) }
    // 삭제 확인 다이얼로그 대상
    var deleteTarget by remember { mutableStateOf<GeminiApiKeyEntry?>(null) }

    // 검증 성공 시 폼 닫기 + 입력 초기화
    LaunchedEffect(apiKeyValidationState) {
        if (apiKeyValidationState is ApiKeyValidationState.Success) {
            showAddForm = false
            labelInput = ""
            keyInput = ""
            isKeyVisible = false
        }
    }

    // 섹션 타이틀
    Text(
        text = "Gemini API 키",
        style = MaterialTheme.typography.titleMedium
    )
    Spacer(modifier = Modifier.height(4.dp))
    // 헬퍼 텍스트
    Text(
        text = "등록된 키는 회의록 생성 시 순환 사용됩니다",
        style = MaterialTheme.typography.bodySmall,
        color = MaterialTheme.colorScheme.onSurfaceVariant
    )
    Spacer(modifier = Modifier.height(16.dp))

    // 키 목록 또는 빈 상태
    if (geminiApiKeys.isEmpty()) {
        // 빈 상태 컴포넌트
        Column(
            modifier = Modifier.fillMaxWidth(),
            horizontalAlignment = Alignment.CenterHorizontally
        ) {
            Text(
                text = "등록된 Gemini API 키가 없습니다",
                style = MaterialTheme.typography.bodyMedium,
                color = MaterialTheme.colorScheme.onSurfaceVariant
            )
            Spacer(modifier = Modifier.height(4.dp))
            Text(
                text = "아래 '새 키 추가' 버튼으로 첫 API 키를 등록하세요.\naistudio.google.com에서 발급받을 수 있습니다.",
                style = MaterialTheme.typography.bodySmall,
                color = MaterialTheme.colorScheme.onSurfaceVariant,
                textAlign = androidx.compose.ui.text.style.TextAlign.Center
            )
        }
    } else {
        // 키 목록 — LazyColumn 대신 Column + forEach 사용
        // (SettingsScreen 자체가 verticalScroll Column 안에 있어 중첩 스크롤 금지)
        geminiApiKeys.forEach { entry ->
            OutlinedCard(
                modifier = Modifier.fillMaxWidth()
            ) {
                Row(
                    modifier = Modifier
                        .fillMaxWidth()
                        .padding(16.dp),
                    verticalAlignment = Alignment.CenterVertically
                ) {
                    Column(modifier = Modifier.weight(1f)) {
                        Text(
                            text = entry.label,
                            style = MaterialTheme.typography.bodyLarge
                        )
                        Spacer(modifier = Modifier.height(4.dp))
                        Text(
                            text = entry.maskedKey,
                            style = MaterialTheme.typography.bodyMedium.copy(
                                fontFamily = FontFamily.Monospace
                            ),
                            color = MaterialTheme.colorScheme.onSurfaceVariant
                        )
                    }
                    IconButton(
                        onClick = { deleteTarget = entry },
                        modifier = Modifier.size(48.dp)
                    ) {
                        Icon(
                            imageVector = Icons.Default.Delete,
                            contentDescription = "'${entry.label}' 키 삭제",
                            tint = MaterialTheme.colorScheme.error
                        )
                    }
                }
            }
            Spacer(modifier = Modifier.height(8.dp))
        }
    }

    Spacer(modifier = Modifier.height(16.dp))

    // 추가 폼 or "새 키 추가" 버튼
    if (!showAddForm) {
        FilledTonalButton(
            onClick = {
                showAddForm = true
                viewModel.resetApiKeyValidationState()
            },
            modifier = Modifier.fillMaxWidth()
        ) {
            Icon(
                imageVector = Icons.Default.Add,
                contentDescription = null,
                modifier = Modifier.size(18.dp)
            )
            Spacer(modifier = Modifier.width(8.dp))
            Text("새 키 추가")
        }
    } else {
        // 인라인 추가 폼
        val isValidating = apiKeyValidationState is ApiKeyValidationState.Validating

        // 별명 입력
        OutlinedTextField(
            value = labelInput,
            onValueChange = {
                labelInput = it
                viewModel.resetApiKeyValidationState()
            },
            label = { Text("별명") },
            placeholder = { Text("별명 (예: 회사용, 개인용)") },
            singleLine = true,
            enabled = !isValidating,
            modifier = Modifier.fillMaxWidth()
        )

        Spacer(modifier = Modifier.height(8.dp))

        // API 키 입력
        OutlinedTextField(
            value = keyInput,
            onValueChange = {
                keyInput = it
                viewModel.resetApiKeyValidationState()
            },
            label = { Text("Gemini API 키") },
            visualTransformation = if (isKeyVisible)
                VisualTransformation.None
            else
                PasswordVisualTransformation(),
            trailingIcon = {
                IconButton(onClick = { isKeyVisible = !isKeyVisible }) {
                    Icon(
                        imageVector = if (isKeyVisible)
                            Icons.Default.VisibilityOff
                        else
                            Icons.Default.Visibility,
                        contentDescription = if (isKeyVisible) "키 숨기기" else "키 보기"
                    )
                }
            },
            singleLine = true,
            readOnly = isValidating,
            modifier = Modifier.fillMaxWidth()
        )

        Spacer(modifier = Modifier.height(8.dp))

        // 검증 오류 메시지 (필드 아래, 버튼 위)
        if (apiKeyValidationState is ApiKeyValidationState.Error) {
            Text(
                text = (apiKeyValidationState as ApiKeyValidationState.Error).message,
                style = MaterialTheme.typography.bodySmall,
                color = MaterialTheme.colorScheme.error
            )
            Spacer(modifier = Modifier.height(4.dp))
        }

        // 검증 후 추가 + 취소 버튼
        Row(
            horizontalArrangement = Arrangement.spacedBy(8.dp)
        ) {
            Button(
                onClick = {
                    viewModel.validateAndAddApiKey(labelInput.trim(), keyInput.trim())
                },
                enabled = labelInput.isNotBlank() && keyInput.isNotBlank() && !isValidating
            ) {
                if (isValidating) {
                    CircularProgressIndicator(
                        modifier = Modifier.size(16.dp),
                        strokeWidth = 2.dp,
                        color = MaterialTheme.colorScheme.onPrimary
                    )
                    Spacer(modifier = Modifier.width(8.dp))
                    Text("검증 중...")
                } else {
                    Text("검증 후 추가")
                }
            }
            TextButton(
                onClick = {
                    showAddForm = false
                    labelInput = ""
                    keyInput = ""
                    isKeyVisible = false
                    viewModel.resetApiKeyValidationState()
                },
                enabled = !isValidating
            ) {
                Text("취소")
            }
        }

        // 검증 성공 메시지 (Success 상태는 LaunchedEffect에서 폼 닫힘 처리 — 이 분기는 도달 안 함)
        if (apiKeyValidationState is ApiKeyValidationState.Success) {
            val addedLabel = (apiKeyValidationState as ApiKeyValidationState.Success).addedLabel
            Text(
                text = "'$addedLabel' 키가 추가되었습니다",
                style = MaterialTheme.typography.bodySmall,
                color = MaterialTheme.colorScheme.primary
            )
        }
    }

    // 삭제 확인 다이얼로그
    deleteTarget?.let { target ->
        AlertDialog(
            onDismissRequest = { deleteTarget = null },
            title = { Text("API 키 삭제") },
            text = { Text("\"${target.label}\" 키를 삭제할까요? 이 작업은 되돌릴 수 없습니다.") },
            confirmButton = {
                TextButton(
                    onClick = {
                        viewModel.removeGeminiApiKey(target.index)
                        deleteTarget = null
                    }
                ) {
                    Text(
                        text = "삭제",
                        color = MaterialTheme.colorScheme.error
                    )
                }
            },
            dismissButton = {
                TextButton(onClick = { deleteTarget = null }) {
                    Text("취소")
                }
            }
        )
    }
}
```

**5. 주의사항:**
- 기존 `ApiKeySection` 함수(line 1397~1500)를 `GeminiApiKeySection`으로 완전 교체. 기존 함수 시그니처(`ApiKeySection`)는 제거.
- SettingsScreen 함수 내에서 `hasApiKey` 로컬 변수 참조가 있다면 제거 (Task 2에서 `geminiApiKeys`로 교체됨).
- `LazyColumn`이 아닌 `Column + forEach` 패턴 사용: SettingsScreen이 이미 `verticalScroll`을 사용하는 Column이라 `LazyColumn` 중첩 불가.
- 기존 SettingsScreen 내 `ApiKeySection` 관련 imports(`hasApiKey` 참조 등)는 정리한다.
- `textAlign` 사용을 위해 `import androidx.compose.ui.text.style.TextAlign` 추가.
  </action>
  <verify>
    <automated>cd /d/workspace/temp/Auto_Minuting && ./gradlew :app:assembleDebug 2>&1 | grep -E "error:|warning:|BUILD" | tail -30</automated>
  </verify>
  <done>
    - SettingsScreen에 GeminiApiKeySection composable 존재 (기존 ApiKeySection 제거됨)
    - 호출부(line ~546)에서 GeminiApiKeySection(viewModel) 참조
    - geminiApiKeys collectAsStateWithLifecycle 호출 존재
    - hasApiKey 로컬 변수 참조 제거됨
    - assembleDebug 빌드 성공 (BUILD SUCCESSFUL)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: 수동 기능 확인 체크포인트</name>
  <what-built>
    Gemini 다중 API 키 설정 UI 전체:
    - GeminiApiKeyEntry data class (신규)
    - SecureApiKeyRepository 다중 키 CRUD + 마이그레이션
    - SettingsViewModel _geminiApiKeys StateFlow + validateAndAddApiKey + removeGeminiApiKey
    - SettingsScreen GeminiApiKeySection (목록 + 추가 폼 + 삭제 다이얼로그)
  </what-built>
  <how-to-verify>
    1. 앱을 실행하고 설정(Settings) 화면으로 이동
    2. Gemini 인증 모드가 "API 키"인지 확인. API 키 섹션으로 이동.
    3. **빈 상태 확인**: 기존 저장된 키가 없으면 "등록된 Gemini API 키가 없습니다" 메시지 표시 확인
    4. **마이그레이션 확인** (기존 키가 있는 경우): "기본 키" 라벨로 index 0에 기존 키가 마이그레이션되어 목록에 표시되는지 확인
    5. **키 추가**: "새 키 추가" 탭 → 인라인 폼 확장 확인 → 별명("테스트") + 유효한 Gemini API 키 입력 → "검증 후 추가" 탭 → CircularProgressIndicator 표시 → 검증 완료 후 목록에 카드 행 추가 확인
    6. **마스킹 확인**: 카드 행의 키 표시가 AIza****WXYZ 형식인지 확인 (Monospace 폰트)
    7. **취소**: 추가 폼에서 "취소" 탭 → 폼이 닫히고 "새 키 추가" 버튼으로 복귀 확인
    8. **삭제**: 카드 행 우측 🗑 아이콘 탭 → AlertDialog "API 키 삭제" 표시 확인 → "삭제" 탭 → 목록에서 제거 확인
    9. **중복 키 거부**: 동일한 API 키 값으로 추가 시도 → "이미 등록된 API 키입니다." 오류 메시지 표시 확인
    10. **앱 재시작**: 재시작 후 목록이 유지되는지 확인 (암호화 저장 검증)
  </how-to-verify>
  <action>Task 1~3 완료 후 기기에서 수동으로 기능을 검증한다. 빌드 후 기기 설치, how-to-verify 항목 순서대로 확인.</action>
  <verify><automated>MISSING — 수동 검증 태스크 (checkpoint)</automated></verify>
  <done>10개 검증 항목 모두 통과 확인</done>
  <resume-signal>기능이 정상 동작하면 "approved" 입력. 문제가 있으면 증상을 설명해 주세요.</resume-signal>
</task>

</tasks>

<verification>
빌드 검증:
```bash
cd /d/workspace/temp/Auto_Minuting && ./gradlew :app:assembleDebug
```
BUILD SUCCESSFUL 확인.

코드 구조 검증:
```bash
grep -n "geminiApiKeys\|validateAndAddApiKey\|removeGeminiApiKey\|GeminiApiKeySection" \
  app/src/main/java/com/autominuting/presentation/settings/SettingsViewModel.kt \
  app/src/main/java/com/autominuting/presentation/settings/SettingsScreen.kt
```
</verification>

<success_criteria>
- [x] GeminiApiKeyEntry.kt 존재: label, maskedKey, index 필드
- [x] SecureApiKeyRepository: getGeminiApiKeys, addGeminiApiKey(중복 검사), removeGeminiApiKey(인덱스 재정렬), getAllGeminiApiKeyValues, migrateGeminiApiKeyIfNeeded 존재
- [x] SettingsViewModel: geminiApiKeys StateFlow, validateAndAddApiKey, removeGeminiApiKey 존재 / hasApiKey, validateAndSaveApiKey, clearApiKey 제거
- [x] SettingsScreen: GeminiApiKeySection composable 존재, 빈 상태 / 카드 목록 / 인라인 추가 폼 / 삭제 다이얼로그 구현
- [x] 마이그레이션: init에서 migrateGeminiApiKeyIfNeeded() 호출
- [x] UI-SPEC 준수: Material 3 colorScheme/typography 토큰만 사용, Hex 직접 참조 없음, 마스킹 포맷 AIza****WXYZ, 삭제 아이콘 tint=error, 추가 버튼 내부 CircularProgressIndicator 16.dp strokeWidth 2.dp
- [x] DataStore: GEMINI_ROUNDROBIN_INDEX_KEY 상수 선언 (Phase 52 준비)
- [x] assembleDebug BUILD SUCCESSFUL
</success_criteria>

<output>
완료 후 `.planning/phases/51-gemini-api-ui/51-01-SUMMARY.md` 생성.

REQUIREMENTS.md Traceability 표 업데이트:
- GEMINI-01: Phase 51, Plan 01, Status: Done
- GEMINI-04: Phase 51, Plan 01, Status: Done
</output>
