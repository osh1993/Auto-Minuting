---
phase: quick
plan: 260325-b8p
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/com/autominuting/presentation/dashboard/DashboardViewModel.kt
  - app/src/main/java/com/autominuting/presentation/dashboard/DashboardScreen.kt
autonomous: true
requirements: []

must_haves:
  truths:
    - "대시보드에 녹음기 연결/해제 토글 버튼이 표시된다"
    - "버튼 클릭 시 AudioCollectionService가 시작/중지된다"
    - "서비스 실행 상태에 따라 버튼 텍스트와 스타일이 변경된다"
  artifacts:
    - path: "app/src/main/java/com/autominuting/presentation/dashboard/DashboardViewModel.kt"
      provides: "서비스 시작/중지 로직 및 실행 상태 StateFlow"
    - path: "app/src/main/java/com/autominuting/presentation/dashboard/DashboardScreen.kt"
      provides: "녹음기 연결 토글 버튼 UI"
  key_links:
    - from: "DashboardScreen.kt"
      to: "DashboardViewModel.kt"
      via: "isCollecting StateFlow 구독 및 toggleCollection() 호출"
    - from: "DashboardViewModel.kt"
      to: "AudioCollectionService"
      via: "Intent(ACTION_START/ACTION_STOP) + context.startForegroundService()"
---

<objective>
대시보드 화면에 "녹음기 연결" 토글 버튼을 추가하여 AudioCollectionService를 시작/중지할 수 있게 한다.

Purpose: 사용자가 대시보드에서 한 번의 탭으로 BLE 오디오 수집을 시작/중지할 수 있어야 한다.
Output: 토글 버튼이 포함된 대시보드 UI + ViewModel 서비스 제어 로직
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@app/src/main/java/com/autominuting/service/AudioCollectionService.kt
@app/src/main/java/com/autominuting/presentation/dashboard/DashboardViewModel.kt
@app/src/main/java/com/autominuting/presentation/dashboard/DashboardScreen.kt

<interfaces>
<!-- AudioCollectionService companion object 상수 -->
From AudioCollectionService.kt:
```kotlin
companion object {
    const val NOTIFICATION_ID = 1001
    const val CHANNEL_ID = "audio_collection_channel"
    const val ACTION_START = "com.autominuting.action.START_COLLECTION"
    const val ACTION_STOP = "com.autominuting.action.STOP_COLLECTION"
}
```

<!-- DashboardViewModel 기존 주입 -->
From DashboardViewModel.kt:
```kotlin
@HiltViewModel
class DashboardViewModel @Inject constructor(
    private val meetingRepository: MeetingRepository,
    private val minutesRepository: MinutesRepository,
    @ApplicationContext private val context: Context
) : ViewModel()
```

<!-- 기존 StateFlow 패턴 -->
```kotlin
val activePipeline: StateFlow<Meeting?>
val testStatus: StateFlow<String>
val isTestingGemini: StateFlow<Boolean>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ViewModel에 서비스 토글 로직 추가</name>
  <files>app/src/main/java/com/autominuting/presentation/dashboard/DashboardViewModel.kt</files>
  <action>
DashboardViewModel에 다음을 추가한다:

1. 서비스 실행 상태를 추적하는 StateFlow 추가:
   - `private val _isCollecting = MutableStateFlow(false)`
   - `val isCollecting: StateFlow<Boolean> = _isCollecting.asStateFlow()`

2. 토글 함수 추가:
   ```kotlin
   fun toggleCollection() {
       val intent = Intent(context, AudioCollectionService::class.java)
       if (_isCollecting.value) {
           intent.action = AudioCollectionService.ACTION_STOP
           context.startService(intent)
       } else {
           intent.action = AudioCollectionService.ACTION_START
           context.startForegroundService(intent)
       }
       _isCollecting.value = !_isCollecting.value
   }
   ```

3. import 추가: `android.content.Intent`, `com.autominuting.service.AudioCollectionService`

참고: 서비스 실행 상태는 로컬 Boolean으로 관리한다. 서비스가 시스템에 의해 강제 종료되는 경우의 상태 동기화는 이후 개선 사항이다.
  </action>
  <verify>
    <automated>cd D:/workspace/temp/Auto_Minuting && ./gradlew compileDebugKotlin 2>&1 | tail -5</automated>
  </verify>
  <done>DashboardViewModel에 isCollecting StateFlow와 toggleCollection() 함수가 존재하고, 컴파일 성공</done>
</task>

<task type="auto">
  <name>Task 2: 대시보드 UI에 녹음기 연결 토글 버튼 추가</name>
  <files>app/src/main/java/com/autominuting/presentation/dashboard/DashboardScreen.kt</files>
  <action>
DashboardScreen에 녹음기 연결 버튼을 추가한다:

1. ViewModel에서 상태 수집 추가:
   ```kotlin
   val isCollecting by viewModel.isCollecting.collectAsStateWithLifecycle()
   ```

2. "녹음에서 회의록까지, 자동으로." 텍스트와 Spacer(32.dp) 사이에 녹음기 연결 카드를 추가:
   ```
   Spacer(height = 24.dp)
   Card (fillMaxWidth, padding 16.dp):
     - containerColor: 수집 중이면 primaryContainer, 아니면 secondaryContainer
     Column (padding 16.dp):
       Text "녹음기 연결" (titleMedium)
       Spacer(8.dp)
       Text: 수집 중이면 "Plaud 녹음기에서 오디오를 수집하고 있습니다." 아니면 "녹음기를 연결하여 오디오 수집을 시작합니다."
         (bodySmall, onSecondaryContainer/onPrimaryContainer)
       Spacer(12.dp)
       Button 또는 OutlinedButton (수집 상태에 따라 토글):
         - 수집 중: OutlinedButton, 텍스트 "수집 중지"
         - 미수집: Button (filled), 텍스트 "녹음기 연결 시작"
         - onClick: viewModel.toggleCollection()
         - fillMaxWidth
   ```

3. 수집 중일 때 버튼 옆에 작은 CircularProgressIndicator(size=16.dp)를 표시하여 활성 상태를 시각적으로 전달한다.

4. import 추가 필요 시: 기존 import로 충분할 가능성이 높으나, 누락된 것이 있으면 추가한다.
  </action>
  <verify>
    <automated>cd D:/workspace/temp/Auto_Minuting && ./gradlew compileDebugKotlin 2>&1 | tail -5</automated>
  </verify>
  <done>대시보드에 녹음기 연결 카드가 표시되고, 버튼 클릭 시 수집 중/미수집 상태가 토글되며, 컴파일 성공</done>
</task>

</tasks>

<verification>
1. `./gradlew compileDebugKotlin` 컴파일 성공
2. DashboardViewModel에 isCollecting, toggleCollection() 존재
3. DashboardScreen에 녹음기 연결 카드와 토글 버튼 존재
</verification>

<success_criteria>
- 대시보드 화면에 녹음기 연결 카드가 표시된다
- 버튼 클릭 시 AudioCollectionService에 START/STOP intent가 전달된다
- 버튼 텍스트와 카드 색상이 수집 상태에 따라 변경된다
- 컴파일 오류 없음
</success_criteria>

<output>
After completion, create `.planning/quick/260325-b8p-dashboard-ble-button/260325-b8p-SUMMARY.md`
</output>
